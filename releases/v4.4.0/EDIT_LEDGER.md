# Nova_A 4.4.0 edit ledger

| Exact files | Edit and consequence | Removed or compatibility impact | Evidence |
| --- | --- | --- | --- |
| `src/assets/types.ts` | Added Asset DB v2 records, content groups, collections, path assets, platform overrides, provenance, sprite/SVG/font settings. | No persisted user data removed; loaders normalize older records. | asset audit, migration/core tests |
| `src/assets/assetProduction.ts` | Added deterministic graph/cycles/duplicates/closure, stable hashes, Compare/Revert, bulk metadata, transactional moves, atlas packing, slicing, glyph/security checks, thumbnail cache. | Silent invalid fallback is intentionally removed. | 15 runtime checks, graphs, golden/security reports |
| `src/assets/importPipeline.ts`, `importProfiles.ts`, `AssetDatabase.ts`, `TextureAtlas.ts` | Upgraded importer/cache to v3, validated decoded artifacts, atomic cache writes, explicit profiles, `.nova-path`, metadata normalization, deterministic atlas ordering. | Importer cache v2 is invalidated; last verified artifacts remain available. | importer comparisons, fault injection |
| `src/components/EditorBottomPanel.vue` | Added five importer tabs, copyable full provenance, diagnostics, Compare/Revert, bulk/batch tools, collections/tags/search/favorites, grid/list and bounded 20k browsing, sprite/SVG/font/atlas controls, closure/report views; moved Export into `•••`. | Export removed only from the primary row; action remains in overflow. | browser matrix, manual, asset audit |
| `src/components/ProjectHealthPanel.vue` | Added production dependency, cycle, duplicate, content-group, missing-reference and cache-health visibility. | None. | browser Project Health capture |
| `src/runtime/tilemap.ts`, `src/components/TilemapPanel.vue` | Added deterministic RLE storage, world coordinates, streaming boundaries, bounded history, search and production TileSet/TileMap tools. | None. | large TileMap benchmark and golden hash |
| `src/world/Entity.ts`, `src/editor/authoring2d.ts`, `src/store/physics.ts` | Added normalized parallax mirror/depth and reusable path/tangent/follower state. | None; previous parallax/path fields retain defaults. | type/core tests and reference projects |
| `src/renderer/sceneRenderer.ts`, `src/world/World.ts`, `src/components/ConfigPanel.vue` | Added mirrored/repeated parallax rendering, smoothed tangent paths, fixed-step path followers, reusable path save/load and composition controls. | None. | renderer audit and camera/parallax/path reference |
| `src/assets/main.css`, `src/main.ts`, component scoped styles | Added rounded bundled Nunito/Noto/JetBrains stacks, semantic type/control sizes, quieter surfaces, material depth, spring/ease tokens, GPU-friendly panel/control motion and reduced-motion cancellation. | No existing animation deleted; reduced-motion remains user controlled. | typography audit and 576-state browser matrix |
| `src/i18n.ts`, `manual/index.html`, `manual/MANUAL.en.md`, `MANUAL.de.md`, `MANUAL.zh-CN.md` | Translated all v4.4 controls and documented complete production workflows in three languages with bookmarks/search. | None. | manual audit and three-locale browser run |
| `README.md`, `README.zh-CN.md`, `docs/ASSET_PIPELINE_4_4.md`, `SPRITE_TILEMAP_FONT_4_4.md`, `KNOWN_ISSUES_4_4.md` | Added setup, migration, workflow, limitations and acceptance documentation. | None. | documentation audit |
| `package.json`, `Cargo.toml`, lockfiles, `crates/nova_format/src/lib.rs`, `src/projects/projectFormat.ts`, `src-tauri/Cargo.toml`, `tauri.conf.json`, `src-tauri/src/lib.rs`, runtime release-metadata files | Synchronized 4.4.0 identity; retained schema 29/API contracts; restored maximized-but-resizable startup. | Project Format 2, Runtime API 1, Plugin API 2, Package Manifest 1 and physics ABI unchanged. | type/core/native build and product-version check |
| `scripts/audit-v4.4.mjs`, `verify-v4.4.mjs`, `qualify-layout-v4.4.mjs`, `verify-v4.4-windows.mjs`, `generate-v4.4-release-evidence.mjs`, audit/CLI/reference/package scripts | Added static/runtime/security/performance/fault/layout/native/package qualification and exact release assembly. | None. | release evidence and independent package verifier |
| `reference-projects/**`, `tests/fixtures/migrations/public-schema-expected.json` | Version-pinned 62 executable references, added four v4.4 content references, explicit inclusion rules, expected outputs/control IDs, and updated the engine-version golden fixture. | No reference workflow removed. | CLI validation and packaged reference audit |

No existing feature or animation was deleted. The only deliberate behavior removal is the unsafe silent importer fallback; invalid inputs now produce actionable diagnostics while the last verified artifact remains active. External results and approvals are not fabricated.
