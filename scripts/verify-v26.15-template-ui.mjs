/** 功能回归脚本：执行 verify-v26.15-template-ui.mjs 对应场景，保留断言和证据输出。 */
import assert from 'node:assert/strict'
import {build} from 'vite'
import {existsSync} from 'node:fs'
import {mkdir,readFile,writeFile} from 'node:fs/promises'
import {dirname,join,resolve} from 'node:path'
import {fileURLToPath,pathToFileURL} from 'node:url'
import {assetAuditMetadata} from './lib/assetAudit15.mjs'
const option=/* 调用 process.argv.find(value=>value.startsWith('--'+name+'='))?.slice(name.length+3) 并返回调用结果。 */ name=>process.argv.find(/* 调用 value.startsWith('--'+name+'=') 并返回调用结果。 */ value=>value.startsWith('--'+name+'='))?.slice(name.length+3),home=dirname(dirname(fileURLToPath(import.meta.url))),root=existsSync(join(home,'package.json'))?home:resolve(home,'../..'),sourceRoot=resolve(option('source-root')??(home===root?root:join(root,'.cache/development-v26.15-render-user-check'))),out=join(root,'.cache/verify-v26.15-template-ui'),reportPath=resolve(option('report')??join(home,home===root?'release-audits':'reports','v26.15-template-ui.json'))
const checks=[],observations=[]
await build({configFile:false,root:sourceRoot,logLevel:'error',ssr:{noExternal:true},build:{ssr:true,outDir:out,emptyOutDir:true,rollupOptions:{input:Object.fromEntries(Object.entries({templates:'projects/templates',physics:'store/physics',assets:'assets/AssetDatabase',pak:'runtime/novaPak',localization:'runtime/localization'}).map(/* 返回按声明顺序构造的数组 [name,join(sourceRoot,'src',path+'.ts')]。 */ ([name,path])=>[name,join(sourceRoot,'src',path+'.ts')])),output:{entryFileNames:'[name].mjs'}}}})
const load=/* 调用 import(pathToFileURL(join(out,name+'.mjs'))) 并返回调用结果。 */ name=>import(pathToFileURL(join(out,name+'.mjs'))),[templates,physics,assets,pak,localization]=await Promise.all(['templates','physics','assets','pak','localization'].map(load))
globalThis.window={setTimeout:/* 返回固定值 0。 */ ()=>0,clearTimeout:/** 提供不执行额外操作的空回调，用于测试接口占位。 */ ()=>{}}
const wasm=await import(pathToFileURL(join(sourceRoot,'nova_core/pkg/nova_core.js')));wasm.initSync({module:await readFile(join(sourceRoot,'nova_core/pkg/nova_core_bg.wasm'))})
const check=/** 结构说明（自动提取）：check；输入 name、fn；直接调用 fn、checks.push、console.log、String、console.error；等待异步结果。 */ async(name,fn)=>{try{await fn();checks.push({name,status:'passed'});console.log('PASS '+name)}catch(error){checks.push({name,status:'failed',error:error.stack??String(error)});console.error('FAIL '+name+': '+error.message)}}
/** 结构说明（自动提取）：current；无显式参数；直接调用 find、getComponent、button.getComponent、localization.localize、Object.hasOwn 等。 */ function current(){
 const find=/** 结构说明（自动提取）：find；输入 name；直接调用 physics.physicsState.world.entities.find、assert.ok；返回路径包含 entity。 */ name=>{const entity=physics.physicsState.world.entities.find(/* 比较 entity.name 与 name，返回严格相等的判断结果。 */ entity=>entity.name===name);assert.ok(entity,name);return entity},button=find('Play Button'),progress=find('Loading Progress').getComponent('ProgressBar'),panel=find('Menu Panel').getComponent('Panel'),text=button.getComponent('Text')
 return{caption:text?localization.localize(text.localizationKey,text.localizationVariables,text.text):null,localizationKey:text?.localizationKey,buttonHasFakeText:Object.hasOwn(button.getComponent('Button'),'text'),placeholder:find('Player Name').getComponent('TextInput').placeholder,checkbox:{label:localization.localize(find('Options Toggle').getComponent('Checkbox').localizationKey,{},find('Options Toggle').getComponent('Checkbox').label),localizationKey:find('Options Toggle').getComponent('Checkbox').localizationKey},progress:{min:progress.min,max:progress.max,value:progress.value},panelLayout:panel.layout,positions:Object.fromEntries(['Localized Title','Player Name','Play Button','Options Toggle','Loading Progress','News Scroll View'].map(/* 返回按声明顺序构造的数组 [name,{...find(name).getComponent('RectTransform').position}]。 */ name=>[name,{...find(name).getComponent('RectTransform').position}]))}
}
const expected={caption:'Play',localizationKey:'menu.play',buttonHasFakeText:false,placeholder:'Player name',checkbox:{label:'Sound enabled',localizationKey:'menu.sound'},progress:{min:0,max:100,value:68},panelLayout:'None',positions:{'Localized Title':{x:0,y:-220},'Player Name':{x:0,y:-110},'Play Button':{x:0,y:-20},'Options Toggle':{x:0,y:70},'Loading Progress':{x:0,y:160},'News Scroll View':{x:0,y:245}}}
for(const id of['ui-showcase','responsive-ui','audio-lab']){
 let authored,hydrated
 await check(id+': actual factory and WASM migration hydrate real localized Text,68/100 progress and authored positions',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 templates.createTemplateProject、authored.scenes.flatMap、raw.find、assert.ok、button.components.some 等；写入 authored、hydrated。 */ ()=>{
  authored=templates.createTemplateProject(id,'UI Semantics '+id);const raw=authored.scenes.flatMap(/* 返回 scene.entities 的当前值。 */ scene=>scene.entities),button=raw.find(/* 比较 entity.name 与 'Play Button'，返回严格相等的判断结果。 */ entity=>entity.name==='Play Button');assert.ok(button.components.some(/* 先计算 component.kind==='Text'；仅当其为真值时求右侧 component.data.localizationKey==='menu.play'，返回短路求值结果。 */ component=>component.kind==='Text'&&component.data.localizationKey==='menu.play'));assert.equal(Object.hasOwn(button.components.find(/* 比较 component.kind 与 'Button'，返回严格相等的判断结果。 */ component=>component.kind==='Button').data,'text'),false)
  assert.equal(physics.loadProject(wasm.migrate_project_json(JSON.stringify(authored))),true);hydrated=current();assert.deepEqual(hydrated,expected);observations.push({id,phase:'factory-WASM-hydration',values:hydrated})
 })
 await check(id+': actual NovaPak encode/decode/player-project reopening preserves the same semantics',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 physics.getSceneJSON、JSON.parse、pak.createNovaPak、pak.parseNovaPak、assert.ok 等；等待异步结果。 */ async()=>{
  const snapshot=physics.getSceneJSON(),document=JSON.parse(snapshot),bytes=await pak.createNovaPak(snapshot,assets.assetState.records,document.activeSceneUuid,{deterministic:true,compression:'balanced'}),decoded=await pak.parseNovaPak(bytes);assert.ok(decoded.files.has('project.nova'));const reopened=await pak.projectJsonFromNovaPak(bytes);assert.equal(physics.loadProject(wasm.migrate_project_json(reopened)),true);assert.deepEqual(current(),expected);observations.push({id,phase:'NovaPak-reopen',packageBytes:bytes.length,entries:decoded.index.entries.length,engineVersion:decoded.index.engineVersion})
 })
}
await check('old unsupported Button.text and implicit max1 reproduce missing caption and saturated progress under real hydration',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 templates.createTemplateProject、entities.find、components.find、button.components.filter、button.components.find 等；写入 button.components、data.text。 */ ()=>{
 const broken=templates.createTemplateProject('ui-showcase','Prior shape'),entities=broken.scenes[0].entities,button=entities.find(/* 比较 entity.name 与 'Play Button'，返回严格相等的判断结果。 */ entity=>entity.name==='Play Button'),progress=entities.find(/* 比较 entity.name 与 'Loading Progress'，返回严格相等的判断结果。 */ entity=>entity.name==='Loading Progress').components.find(/* 比较 component.kind 与 'ProgressBar'，返回严格相等的判断结果。 */ component=>component.kind==='ProgressBar')
 button.components=button.components.filter(/* 比较 component.kind 与 'Text'，返回严格不等的判断结果。 */ component=>component.kind!=='Text');button.components.find(/* 比较 component.kind 与 'Button'，返回严格相等的判断结果。 */ component=>component.kind==='Button').data.text='{menu.play}';delete progress.data.min;delete progress.data.max
 assert.equal(physics.loadProject(wasm.migrate_project_json(JSON.stringify(broken))),true);const observed=current();assert.equal(observed.caption,null);assert.equal(observed.progress.max,1);assert.equal(observed.progress.value,1);observations.push({phase:'negative-legacy-reproduction',observed})
})
const metadata=await assetAuditMetadata(root,sourceRoot),status=checks.every(/* 比较 check.status 与 'passed'，返回严格相等的判断结果。 */ check=>check.status==='passed')?'passed':'failed';await mkdir(dirname(reportPath),{recursive:true});await writeFile(reportPath,JSON.stringify({format:'nova-v26.15-template-ui',version:1,...metadata,status,checks,observations,scope:'Actual template factory, native WASM format migration, production component hydration/localization and NovaPak/player-project reopening. Separate browser audit proves visible pixels and Inspector layout; no descriptor-only or user-click claim here.'},null,2)+'\n');if(status!=='passed')process.exitCode=1
