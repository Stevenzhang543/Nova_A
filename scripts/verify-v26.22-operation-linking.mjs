/** 功能回归脚本：执行 verify-v26.22-operation-linking.mjs 对应场景，保留断言和证据输出。 */
import assert from 'node:assert/strict'
import ts from 'typescript'
import {constructorFactoryReference22} from './lib/constructorFactoryEvidence22.mjs'
import {propertyAudit22} from './lib/propertyAudit22.mjs'
const audit=await propertyAudit22('operation-linking'),checks=[],scope=new Map([['World',{source:'src/world/World.ts',path:'World'}]])
const resolve=/* 根据 ts.isIdentifier(node) 的真假，分别返回 bindings.get(node.text) 或 null。 */ (node,bindings)=>ts.isIdentifier(node)?bindings.get(node.text):null
/** 结构说明（自动提取）：infer；输入 text；直接调用 ts.createSourceFile、constructorFactoryReference22。 */ function infer(text){const file=ts.createSourceFile('fixture.js','const factory='+text,ts.ScriptTarget.Latest,true,ts.ScriptKind.JS);return constructorFactoryReference22(file.statements[0].declarationList.declarations[0].initializer,scope,resolve)}
/** 结构说明（自动提取）：test；输入 name、fn；直接调用 fn、checks.push、console.log。 */ function test(name,fn){fn();checks.push({name,status:'passed'});console.log('PASS '+name)}
test('Resolve direct constructors and initialized constant instances',/** 结构说明（自动提取）：test 回调；无显式参数；直接调用 assert.deepEqual、infer、scope.get；包含循环处理。 */ ()=>{for(const source of ['()=>new World()','entities=>{const world=new World();world.entities=entities;world.setNextId(10);return world}','function(){const world=new World();Object.defineProperty(world,"flag",{value:true});return world}'])assert.deepEqual(infer(source),scope.get('World'))})
test('Reject parameter and local aliases that hide the constructor',/** 结构说明（自动提取）：test 回调；无显式参数；直接调用 assert.equal、infer；包含循环处理。 */ ()=>{for(const source of ['World=>new World()','({World})=>new World()','()=>{const World=other;const world=new World();return world}'])assert.equal(infer(source),null)})
test('Reject branches, reassignment and computed or mutable returns',/** 结构说明（自动提取）：test 回调；无显式参数；直接调用 assert.equal、infer；包含循环处理。 */ ()=>{for(const source of ['()=>{const world=new World();if(flag)return other;return world}','()=>{let world=new World();return world}','()=>{const world=new World();world=other;return world}','()=>{const world=new World();return flag?world:other}','()=>{const world=new World();return wrap(world)}','()=>{const world=new World();while(flag){}return world}'])assert.equal(infer(source),null)})
test('Unknown constructors and missing return values receive no evidence',/** 结构说明（自动提取）：test 回调；无显式参数；直接调用 assert.equal、infer；包含循环处理。 */ ()=>{for(const source of ['async()=>new World()','function*(){const world=new World();return world}','()=>new Unknown()','()=>{const world=new Unknown();return world}','()=>{const world=new World();}','()=>({world:new World()})'])assert.equal(infer(source),null)})
await audit.write(checks,'Conservative static audit-link inference only. Checks prevent ambiguous test factories from being presented as executed source-operation evidence; no production behavior is changed.')
