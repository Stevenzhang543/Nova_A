import assert from 'node:assert/strict'
import {mkdir,readFile,writeFile,mkdtemp} from 'node:fs/promises'
import {existsSync} from 'node:fs'
import {createHash} from 'node:crypto'
import {dirname,join,resolve} from 'node:path'
import {fileURLToPath} from 'node:url'
import {writeAuditBundle} from './lib/milestoneAuditBundle.mjs'
const home=dirname(dirname(fileURLToPath(import.meta.url))),integrated=existsSync(join(home,'package.json')),root=integrated?home:resolve(home,'../..'),reports=join(home,integrated?'release-audits':'reports')
await mkdir(join(root,'.cache'),{recursive:true});await mkdir(reports,{recursive:true})
const fixture=await mkdtemp(join(root,'.cache/audit-attachments-')),evidence=join(fixture,'release-audits'),checks=[]
await mkdir(evidence);await writeFile(join(fixture,'package.json'),JSON.stringify({version:'26.15.0'}))
const archive=Buffer.from('PK\x03\x04 bounded actual-byte fixture','binary'),file=join(evidence,'player.zip');await writeFile(file,archive)
const startedAt=Date.now(),reportPath='release-audits/user.json',execution={startedAt,completedAt:Date.now(),reports:[reportPath]}
async function report(artifact,extra={}){await writeFile(join(fixture,reportPath),JSON.stringify({status:'passed',generatedAt:new Date().toISOString(),observations:[{artifact}],...extra}))}
async function check(name,run){try{await run();checks.push({name,status:'passed'});console.log('PASS '+name)}catch(error){checks.push({name,status:'failed',error:String(error.stack??error)});console.error('FAIL '+name)}}
await check('fresh in-root ZIP evidence is embedded exactly once with original bytes and SHA256',async()=>{await report(file,{observations:[{artifact:file},{file}]});const bundle=await writeAuditBundle(fixture,'26.15','attachments-test',[execution]);assert.equal(bundle.attachments.length,1);assert.equal(bundle.attachments[0].path,'player.zip');assert.deepEqual(Buffer.from(bundle.attachments[0].base64,'base64'),archive);assert.equal(bundle.attachments[0].sha256,createHash('sha256').update(archive).digest('hex'))})
await check('string and typed pixel captures plus the overview are retained with unchanged byte hashes',async()=>{const pixel=Buffer.from('real pixel bytes fixture'),overview=Buffer.from('overview fixture');await writeFile(join(evidence,'pixel.png'),pixel);await writeFile(join(evidence,'overview.png'),overview);await report(file,{captures:['pixel.png',{name:'same-pixel',file:'pixel.png'}],overviewCapture:'overview.png'});const bundle=await writeAuditBundle(fixture,'26.15','attachments-test',[execution]);assert.equal(bundle.attachments.length,3);assert.equal(bundle.attachments.find(item=>item.path==='pixel.png').sha256,createHash('sha256').update(pixel).digest('hex'));assert.deepEqual(Buffer.from(bundle.attachments.find(item=>item.path==='overview.png').base64,'base64'),overview);await report(file,{captures:[{name:'unsafe',file:'../outside.png'}]});await assert.rejects(()=>writeAuditBundle(fixture,'26.15','attachments-test',[execution]),/stays inside/);await report(file,{captures:[{name:'missing-file'}]});await assert.rejects(()=>writeAuditBundle(fixture,'26.15','attachments-test',[execution]),/must name/)})
await check('ZIP attachment outside release-audits is rejected',async()=>{await report(join(fixture,'outside.zip'));await assert.rejects(()=>writeAuditBundle(fixture,'26.15','attachments-test',[execution]),/stays inside/)})
await check('ZIP retains existing32 MiB individual attachment bound',async()=>{const oversized=join(evidence,'oversized.zip');await writeFile(oversized,Buffer.alloc(32*1024*1024+1));await report(oversized);await assert.rejects(()=>writeAuditBundle(fixture,'26.15','attachments-test',[execution]),/Bounded/)})
await check('development and stale reports cannot be relabelled as qualified ZIP evidence',async()=>{await report(file,{development:true,qualifiedRelease:null,expectedRelease:'26.13'});await assert.rejects(()=>writeAuditBundle(fixture,'26.15','attachments-test',[execution]));await report(file,{generatedAt:'2000-01-01T00:00:00Z'});await assert.rejects(()=>writeAuditBundle(fixture,'26.15','attachments-test',[execution]),/executed command/)})
const status=checks.every(check=>check.status==='passed')?'passed':'failed';await writeFile(join(reports,'v26.15-audit-attachments.json'),JSON.stringify({release:'26.15',generatedAt:new Date().toISOString(),status,checks,scope:'Isolated evidence-bundle rejection and byte-identity fixtures; no product user or native execution claim.'},null,2)+'\n');if(status!=='passed')process.exitCode=1
