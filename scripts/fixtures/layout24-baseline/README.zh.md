# 26.23 布局历史基线

此目录保存提交 `0bfc303` 的 `src/visual/graphLayoutEngine.ts` 原始字节压缩档，仅供 26.24 同进程布局比较。它属于不可变历史测试数据，不是当前引擎实现；保持原始字节才能检验已记录的路由缺陷和测量结果。当前工作实现及中文函数注释位于 `src/visual/graphLayoutEngine.ts`。

`manifest.json` 记录原路径、长度及 SHA-256。审计解压后先核对这些值，再在临时目录编译执行。这样从源码 ZIP 解压后无需 `.git` 也可复现实验。请勿用当前修复覆盖旧基线，也不得删除旧版本的失败路由以制造性能或正确性提升。
