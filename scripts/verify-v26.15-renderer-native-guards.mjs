/** 功能回归脚本：执行 verify-v26.15-renderer-native-guards.mjs 对应场景，保留断言和证据输出。 */
import assert from 'node:assert/strict'
import {mkdir,writeFile} from 'node:fs/promises'
import {dirname,join} from 'node:path'
import {fileURLToPath} from 'node:url'
import {qualifyNativeRendererInput,validateNativeRendererVersion,validateNativeRendererReceipt,validateRendererBrowserComparison} from './lib/rendererNativeAudit.mjs'
const checks=[],check=/** 结构说明（自动提取）：check；输入 name、fn；直接调用 fn、checks.push、console.log；等待异步结果。 */ async(name,fn)=>{await fn();checks.push({name,status:'passed'});console.log('PASS '+name)}
await check('native qualification refuses a development overlay before touching an executable',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 assert.rejects；返回表达式求值结果。 */ ()=>assert.rejects(/* 调用 qualifyNativeRendererInput(process.cwd(),join(process.cwd(),'.cache/development-v26.15'),'never-launch.exe') 并返回调用结果。 */ ()=>qualifyNativeRendererInput(process.cwd(),join(process.cwd(),'.cache/development-v26.15'),'never-launch.exe'),/development overlay|requires Windows/))
await check('native qualification requires source, browser and actual build receipt evidence',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 assert.rejects；返回表达式求值结果。 */ ()=>assert.rejects(/* 调用 qualifyNativeRendererInput(process.cwd(),process.cwd(),'never-launch.exe') 并返回调用结果。 */ ()=>qualifyNativeRendererInput(process.cwd(),process.cwd(),'never-launch.exe'),/requires --snapshot|requires Windows/))
await check('PE product and file versions both reject stale or malformed versions',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 assert.throws、validateNativeRendererVersion；包含循环处理。 */ ()=>{for(const version of ['26.13.0','26.14.0','26.15.1','26.15.0bad','','26.15.0.1'])for(const field of['ProductVersion','FileVersion'])assert.throws(/* 调用 validateNativeRendererVersion({ProductVersion:'26.15.0',FileVersion:'26.15.0',[field]:version}) 并返回调用结果。 */ ()=>validateNativeRendererVersion({ProductVersion:'26.15.0',FileVersion:'26.15.0',[field]:version}),/stale native/);validateNativeRendererVersion({ProductVersion:'26.15.0',FileVersion:'26.15.0.0'})})
const receipt={format:'nova-release-executed-gates',version:1,release:'26.15',machineVersion:'26.15.0',sourceInputDigest:'source',gates:[{id:'native-build',status:'passed',exitCode:0,sourceInputDigest:'source',artifacts:[{name:'windows-editor',status:'passed',sha256:'binary',bytes:42}]}]}
await check('native receipt permits an ongoing qualification only after its exact build passed',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 validateNativeRendererReceipt、structuredClone、mutate、assert.throws；包含循环处理。 */ ()=>{validateNativeRendererReceipt({...receipt,status:'running'},'source','binary',42);for(const mutate of[/** 结构说明（自动提取）：匿名回调；输入 r；写入 r.sourceInputDigest。 */ r=>{r.sourceInputDigest='other'},/** 结构说明（自动提取）：匿名回调；输入 r；写入 r.release。 */ r=>{r.release='26.13'},/** 结构说明（自动提取）：匿名回调；输入 r；写入 r.gates[…].status。 */ r=>{r.gates[0].status='running'},/** 结构说明（自动提取）：匿名回调；输入 r；写入 r.gates[…].exitCode。 */ r=>{r.gates[0].exitCode=1},/** 结构说明（自动提取）：匿名回调；输入 r；写入 r.gates[…].sourceInputDigest。 */ r=>{r.gates[0].sourceInputDigest='other'},/** 结构说明（自动提取）：匿名回调；输入 r；写入 r.gates[…].artifacts[…].sha256。 */ r=>{r.gates[0].artifacts[0].sha256='old-binary'},/** 结构说明（自动提取）：匿名回调；输入 r；写入 r.gates[…].artifacts[…].bytes。 */ r=>{r.gates[0].artifacts[0].bytes=41},/** 结构说明（自动提取）：匿名回调；输入 r；直接调用 r.gates.push、structuredClone。 */ r=>{r.gates.push(structuredClone(r.gates[0]))}]){const clone=structuredClone(receipt);mutate(clone);assert.throws(/* 调用 validateNativeRendererReceipt(clone,'source','binary',42) 并返回调用结果。 */ ()=>validateNativeRendererReceipt(clone,'source','binary',42))}})
await check('browser comparison rejects missing, failed, duplicate or differently sourced evidence',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 validateRendererBrowserComparison、structuredClone、mutate、assert.throws；包含循环处理。 */ ()=>{const browser={sourceMode:'staged',sourceInputDigest:'source',checks:[{status:'passed'}],captures:[{name:'pixels'}]};validateRendererBrowserComparison(browser,'source');for(const mutate of[/** 结构说明（自动提取）：匿名回调；输入 b；写入 b.sourceInputDigest。 */ b=>{b.sourceInputDigest='other'},/** 结构说明（自动提取）：匿名回调；输入 b；写入 b.sourceMode。 */ b=>{b.sourceMode='native'},/** 结构说明（自动提取）：匿名回调；输入 b；写入 b.sourceMode。 */ b=>{b.sourceMode='baseline'},/** 结构说明（自动提取）：匿名回调；输入 b；写入 b.checks。 */ b=>{b.checks=[]},/** 结构说明（自动提取）：匿名回调；输入 b；写入 b.checks[…].status。 */ b=>{b.checks[0].status='failed'},/** 结构说明（自动提取）：匿名回调；输入 b；写入 b.captures。 */ b=>{b.captures=[]},/** 结构说明（自动提取）：匿名回调；输入 b；直接调用 b.captures.push。 */ b=>{b.captures.push({name:'pixels'})}]){const clone=structuredClone(browser);mutate(clone);assert.throws(/* 调用 validateRendererBrowserComparison(clone,'source') 并返回调用结果。 */ ()=>validateRendererBrowserComparison(clone,'source'))}})
const home=dirname(dirname(fileURLToPath(import.meta.url))),output=join(home,home===process.cwd()?'release-audits':'reports');await mkdir(output,{recursive:true});await writeFile(join(output,'renderer-native-guards.json'),JSON.stringify({status:'passed',generatedAt:new Date().toISOString(),scope:'Receipt/schema/version rejection unit fixtures only; no native application was launched and no native pixel comparison is claimed.',checks},null,2)+'\n')
