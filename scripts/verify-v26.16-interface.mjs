import {registerNodeBundle22,captureNodeBundle22} from './lib/nodeOperationTrace22.mjs'
import { resolveMilestoneAuditContext } from './lib/milestoneAuditContext.mjs'
import assert from 'node:assert/strict'
import { build } from 'vite'
import { existsSync } from 'node:fs'
import { mkdir, mkdtemp, rm, writeFile, readFile } from 'node:fs/promises'
import { resolve, dirname, join, sep } from 'node:path'
import { pathToFileURL } from 'node:url'
const auditContext=resolveMilestoneAuditContext(import.meta.url,{release:'26.16',reportName:'interface'}),root=auditContext.repository,stage=auditContext.sourceRoot,bases=auditContext.bases,checks=[]
await mkdir(join(root,'.cache'),{recursive:true})
const check=async(name,run)=>{try{await run();checks.push({name,status:'passed'})}catch(error){checks.push({name,status:'failed',error:error.stack??String(error)})}}
Object.assign(globalThis,{window:globalThis,location:{href:'https://nova.local/'},localStorage:{getItem:()=>null,setItem(){},removeItem(){}},addEventListener(){},removeEventListener(){},document:{documentElement:{dataset:{},style:{setProperty(){}}},fonts:{add(){},delete(){return true}},createElement:()=>({getContext:()=>null})}})
const madeAudio=[]
globalThis.Audio=class{currentTime=0;duration=10;volume=1;playbackRate=1;loop=false;paused=true;ended=false;playCount=0;listeners=new Map();constructor(source=''){this.src=source;madeAudio.push(this)}play(){this.playCount++;this.paused=false;this.ended=false;return Promise.resolve()}pause(){this.paused=true}load(){}removeAttribute(){this.src=''}addEventListener(name,handler){this.listeners.set(name,[...(this.listeners.get(name)??[]),handler])}removeEventListener(name,handler){this.listeners.set(name,(this.listeners.get(name)??[]).filter(value=>value!==handler))}}
const temporary=await mkdtemp(join(root,'.cache','nova-v2616-interface-'))
const locate=path=>bases.map(base=>join(base,'src',path+'.ts')).find(existsSync)
const overlay={name:'media-overlay',enforce:'pre',resolveId(source,importer){if(!importer||!source.startsWith('.'))return null;const raw=resolve(dirname(importer.split('?')[0]),source),prefix=bases.map(base=>join(base,'src')).find(prefix=>raw.startsWith(prefix+sep));if(!prefix)return null;for(const base of bases)for(const ext of ['','.ts','.json','.js','/index.ts']){const file=join(base,'src',raw.slice(prefix.length+1)+ext);if(existsSync(file))return file.replaceAll('\\','/')}return null}}
try{
const entries={input:'runtime/input',tint:'runtime/uiImageTint',physics:'store/physics',pak:'runtime/novaPak',assetProduction:'assets/assetProduction',native:'runtime/uiNativeInput',drafts:'editor/studioDraftRetention',layout:'runtime/uiLayout',text:'runtime/uiTextLayout',textView:'runtime/uiTextPresentation',ui:'runtime/gameUi',validation:'runtime/uiProduction',locale:'runtime/localization',theme:'runtime/uiTheme',presentation:'runtime/presentation',database:'assets/AssetDatabase',types:'assets/types',components:'world/components',box:'world/BoxEntity'}
await build({configFile:false,root,plugins:[overlay],logLevel:'error',build:{ssr:true,sourcemap:process.env.NOVA_AUDIT_NODE_OPERATION_COVERAGE==='1'?'hidden':false,outDir:temporary,emptyOutDir:false,rollupOptions:{input:Object.fromEntries(Object.entries(entries).map(([name,path])=>[name,locate(path)])),output:{entryFileNames:'[name].mjs'}}}})
registerNodeBundle22(temporary)
const modules=Object.fromEntries(await Promise.all(Object.keys(entries).map(async name=>[name,await import(pathToFileURL(join(temporary,name+'.mjs')).href)])))
const {layout,text,textView,ui,validation,locale,theme,database:db,types,components,box}=modules
const uuid=n=>n.toString(16).padStart(8,'0')+'-0000-4000-8000-000000000000'
let sequence=100
const entity=(parent=null)=>{const value=new box.BoxEntity(sequence++,{x:0,y:0},{x:1,y:1},uuid(sequence));value.parentUuid=parent?.uuid??null;value.addComponent(new components.RectTransform());return value}
const rootCanvas=()=>{const root=entity(),canvas=root.addComponent(new components.Canvas());canvas.referenceSize={x:1000,y:500};return root}
const rect=entity=>entity.getComponent('RectTransform')
const item=(result,entity)=>result.items.find(item=>item.entity.uuid===entity.uuid)
const context=()=>new Proxy({font:'20px sans-serif',canvas:{width:1000,height:500},measureText(value){return {width:[...value].length*(Number(this.font.match(/([0-9.]+)px/)?.[1])||20)*.62}},calls:[],fillText(...args){this.calls.push(args)}},{get(target,key){return key in target?target[key]:()=>{}}})
const asset=(n,type,value)=>({uuid:uuid(n),name:`Asset${n}`,path:`Assets/Asset${n}`,assetType:type,mimeType:'application/json',source:typeof value==='string'?value:JSON.stringify(value),byteLength:1,sourceModified:1,importedAt:1,width:0,height:0,duration:0,fontFamily:'',settings:types.defaultImportSettings()})
const render=entities=>{const runtime=new ui.GameUiRuntime();runtime.render(context(),1000,500,entities,{preview:true});return runtime}
await check('Centered reference-sized controls scale without false Fixed-mode advice; ignored Fixed breakpoints are diagnosed',()=>{const root=rootCanvas(),child=entity(root),entities=[root,child],preset={id:'half',label:'Half size',width:500,height:250,dpi:1,safeArea:{left:0,right:0,top:0,bottom:0}};rect(child).size={x:100,y:40};const responsive=validation.validateUiForPreset(entities,preset);assert.equal(responsive.rects.find(v=>v.entityUuid===child.uuid).width,50);assert.equal(responsive.issues.some(v=>v.code==='NOVA-UI-FIXED-LAYOUT'),false);rect(child).layoutMode='Fixed';rect(child).breakpoints=[{minWidth:0,maxWidth:1000,position:{x:0,y:0},size:{x:300,y:40},visible:true}];const fixed=validation.validateUiForPreset(entities,preset);assert.equal(fixed.rects.find(v=>v.entityUuid===child.uuid).width,100);assert.ok(fixed.issues.some(v=>v.entityUuid===child.uuid&&v.code==='NOVA-UI-FIXED-LAYOUT'&&v.message.includes('ignores responsive breakpoints')))})
await check('Partial stretch anchors independently resolve both axes and inset offsets',()=>{const root=rootCanvas(),child=entity(root);Object.assign(rect(child),{anchorMin:{x:.25,y:.5},anchorMax:{x:.75,y:.5},size:{x:10,y:40},offsets:{left:10,right:20,top:0,bottom:0}});const actual=item(layout.resolveUiLayout(1000,500,[root,child]),child).rect;assert.deepEqual(actual,{x:260,y:230,width:470,height:40})})
await check('Anchor presets remain compatible with default .5 metadata and custom anchors override presets',()=>{const root=rootCanvas(),child=entity(root);Object.assign(rect(child),{anchorPreset:'top-left',pivot:{x:0,y:0},position:{x:20,y:30},size:{x:100,y:40}});assert.deepEqual(item(layout.resolveUiLayout(1000,500,[root,child]),child).rect,{x:20,y:30,width:100,height:40});rect(child).anchorMin={x:.2,y:.3};rect(child).anchorMax={x:.2,y:.3};assert.equal(item(layout.resolveUiLayout(1000,500,[root,child]),child).rect.x,220)})
await check('Hidden/disabled ancestors suppress render, hit testing and accessibility descendants',()=>{for(const mode of ['panel','entity','breakpoint']){const root=rootCanvas(),child=entity(root);child.addComponent(new components.Button());rect(child).focusable=true;rect(child).skipNavigation=false;if(mode==='panel')root.addComponent(new components.Panel()).visible=false;if(mode==='entity')root.enabled=false;if(mode==='breakpoint')rect(root).breakpoints=[{minWidth:0,maxWidth:2000,visible:false,position:{x:0,y:0},size:{x:10,y:10}}];const runtime=render([root,child]);assert.equal(runtime.layoutSnapshot().length,0);assert.equal(runtime.entityAt({x:500,y:250},true),null);assert.equal(runtime.accessibilityNodes().length,0)}})
await check('Cyclic and over-depth hierarchy fails visibly without hanging or partial descendants',()=>{const first=entity(),second=entity(first);first.parentUuid=second.uuid;const result=layout.resolveUiLayout(1000,500,[first,second]);assert.equal(result.items.length,0);assert.ok(result.issues.some(issue=>issue.code==='NOVA-UI-HIERARCHY'))})
await check('Fixed pixels, responsive scale, breakpoints and safe area use the same geometry',()=>{const root=rootCanvas(),child=entity(root);root.getComponent('Canvas').safeArea=true;root.getComponent('Canvas').safeAreaInsets={left:10,right:10,top:10,bottom:10};const result=layout.resolveUiLayout(500,250,[root,child],{safeArea:{left:0,right:0,top:30,bottom:20}});assert.deepEqual(item(result,root).rect,{x:5,y:30,width:490,height:200});assert.equal(item(result,child).rect.width,120);rect(child).layoutMode='Fixed';rect(child).breakpoints=[{minWidth:0,maxWidth:1000,visible:false,position:{x:0,y:0},size:{x:0,y:0}}];const fixed=item(layout.resolveUiLayout(500,250,[root,child]),child);assert.equal(fixed.rect.width,240);assert.equal(fixed.visible,true)})
await check('Container fixed preferred sizes, Fill allocation and alignment/justification are applied',()=>{const root=rootCanvas(),panel=root.addComponent(new components.Panel());panel.layout='Row';panel.gap=10;panel.align='Center';panel.justify='End';const a=entity(root),b=entity(root);rect(a).preferredSize={x:100,y:50};rect(b).preferredSize={x:200,y:80};let result=layout.resolveUiLayout(1000,500,[root,a,b]);assert.deepEqual(item(result,a).rect,{x:690,y:225,width:100,height:50});rect(b).horizontalPolicy='Fill';result=layout.resolveUiLayout(1000,500,[root,a,b]);assert.equal(item(result,b).rect.width,890);assert.equal(item(result,a).rect.x,0)})
await check('Wrapped Flow uses authored widths, line breaks and spacing deterministically',()=>{const root=rootCanvas(),panel=root.addComponent(new components.Panel());panel.layout='Flow';panel.gap=10;const children=Array.from({length:3},()=>entity(root));children.forEach(child=>{rect(child).preferredSize={x:450,y:50}});const result=layout.resolveUiLayout(1000,500,[root,...children]);assert.equal(item(result,children[1]).rect.x,460);assert.equal(item(result,children[2]).rect.y,60)})
await check('RTL mirrors asymmetric child geometry once, preserving dimensions',()=>{const root=rootCanvas(),child=entity(root);Object.assign(rect(child),{anchorPreset:'top-left',pivot:{x:0,y:0},position:{x:20,y:30},size:{x:100,y:40}});assert.equal(item(layout.resolveUiLayout(1000,500,[root,child],{direction:'rtl'}),child).rect.x,880)})
await check('Clipping and scrolling share hit-test and visible-area bounds',()=>{const root=rootCanvas(),panel=root.addComponent(new components.Panel());panel.scrollHorizontal=true;panel.scrollOffset.x=2000;const child=entity(root);child.addComponent(new components.Button());rect(child).focusable=true;rect(child).skipNavigation=false;const runtime=render([root,child]);assert.equal(runtime.focusByUuid(child.uuid),false);assert.equal(runtime.accessibilityNodes().some(node=>node.uuid===child.uuid),false)})
await check('Runtime and responsive matrix expose exactly identical actual rectangles',()=>{const root=rootCanvas(),child=entity(root);root.addComponent(new components.Panel()).layout='Center';rect(child).preferredSize={x:170,y:65};const entities=[root,child],runtime=render(entities),matrix=validation.validateUiForPreset(entities,{id:'test',label:'Test',width:1000,height:500,dpi:1,safeArea:{left:0,right:0,top:0,bottom:0}});assert.deepEqual(runtime.layoutSnapshot().map(value=>({uuid:value.entityUuid,...value.rect})),matrix.rects.map(value=>({uuid:value.entityUuid,x:value.x,y:value.y,width:value.width,height:value.height})))})
await check('Measured text wraps without squeeze and ellipsis respects width and height',()=>{const options={width:50,height:40,lineHeight:20,wrap:'Word',overflow:'Clip',measure:value=>[...value].length*10};assert.deepEqual(text.layoutUiText('one two three',options).lines,['one','two','three']);assert.equal(text.layoutUiText('one two three',options).clipped,true);const ellipsis=text.layoutUiText('one two three',{...options,overflow:'Ellipsis'});assert.deepEqual(ellipsis.lines,['one','two…']);assert.deepEqual(text.layoutUiText('🙂🙂🙂',{...options,width:20,wrap:'Character',overflow:'Visible'}).lines,['🙂🙂','🙂'])})
await check('Unicode limits do not create broken surrogate pairs and segmentation preserves composition glyphs',()=>{assert.equal(text.truncateUiText('a🙂b',2),'a');assert.equal(text.truncateUiText('a🙂b',3),'a🙂');assert.deepEqual(text.uiTextGraphemes('e\u0301👩‍💻'),['é','👩‍💻']);assert.equal(text.layoutUiText('x'.repeat(40000),{width:10,height:10,lineHeight:10,wrap:'None',overflow:'Clip',measure:value=>value.length}).limited,true)})
await check('Actual renderer never supplies Canvas fillText maxWidth for authored Text',()=>{const root=rootCanvas(),child=entity(root),label=child.addComponent(new components.Text());label.text='one two three';label.fontSize=20;rect(child).size={x:60,y:100};const ctx=context();new ui.GameUiRuntime().render(ctx,1000,500,[root,child],{preview:true});assert.ok(ctx.calls.length>1);assert.ok(ctx.calls.every(call=>call.length===3))})
await check('Checkbox requires pointer release inside; cancellation and outside release remain inert',()=>{const root=rootCanvas(),child=entity(root),checkbox=child.addComponent(new components.Checkbox());rect(child).focusable=true;rect(child).skipNavigation=false;const runtime=render([root,child]);runtime.pointerDown({x:500,y:250});assert.equal(checkbox.checked,false);runtime.pointerCancel();assert.equal(checkbox.checked,false);runtime.pointerDown({x:500,y:250});runtime.pointerUp({x:1,y:1});assert.equal(checkbox.checked,false);runtime.pointerDown({x:500,y:250});runtime.pointerUp({x:500,y:250});assert.equal(checkbox.checked,true)})
await check('Repeated focus and repeat keydown cannot repeat activation callbacks',()=>{const root=rootCanvas(),child=entity(root);child.addComponent(new components.Button());rect(child).focusable=true;rect(child).skipNavigation=false;const runtime=render([root,child]),calls=[];runtime.setCallback((_,name)=>calls.push(name));runtime.focusByUuid(child.uuid);runtime.focusByUuid(child.uuid);runtime.keyDown({key:'Enter',repeat:false});runtime.keyDown({key:'Enter',repeat:true});assert.equal(calls.filter(name=>name==='on_focus_enter').length,1);assert.equal(calls.filter(name=>name==='on_pressed').length,1)})
await check('Modal focus excludes background controls while disabled controls retain semantics',()=>{const root=rootCanvas(),background=entity(root);background.addComponent(new components.Button());rect(background).focusable=true;rect(background).skipNavigation=false;const modal=entity(root);modal.addComponent(new components.Panel()).behavior='Modal';const disabled=entity(modal);disabled.addComponent(new components.Button()).interactable=false;rect(disabled).focusable=true;rect(disabled).skipNavigation=false;const runtime=render([root,background,modal,disabled]);assert.equal(runtime.focusByUuid(background.uuid),false);const node=runtime.accessibilityNodes().find(node=>node.uuid===disabled.uuid);assert.ok(node);assert.equal(node.disabled,true);assert.equal(node.tabIndex,-1)})
await check('Keyboard text focus creates native-input ownership and commits values atomically',()=>{const root=rootCanvas(),child=entity(root),input=child.addComponent(new components.TextInput());rect(child).focusable=true;rect(child).skipNavigation=false;input.maxLength=3;const runtime=render([root,child]);runtime.keyDown({key:'Tab',shiftKey:false});assert.equal(runtime.focusedTextInput()?.entity.uuid,child.uuid);runtime.commitTextInput(child.uuid,'a🙂z');assert.equal(input.value,'a🙂');assert.equal(runtime.keyDown({key:'Enter',isComposing:true}),false);assert.ok(runtime.focusedTextInput());runtime.keyDown({key:'Enter'});assert.equal(runtime.focusedTextInput(),null)})
await check('Child theme omission preserves parent tokens, variables and every state',()=>{const parent=theme.defaultUiTheme();parent.variables.accent='#123456';parent.tokens.colors.accent='#abcdef';parent.classes.button.pressed={foreground:'#102030'};const child=theme.mergeUiTheme(parent,{name:'Child',variables:{radius:4},classes:{button:{normal:{border:'#111111'}}}});assert.equal(child.variables.accent,'#123456');assert.equal(child.tokens.colors.accent,'#abcdef');assert.equal(child.classes.button.pressed.foreground,'#102030');assert.equal(theme.themeStyle(child,'button').background,'#123456');assert.equal(theme.themeStyle(child,'button').cornerRadius,4)})
await check('Grouped and chained tokens resolve; cycles and invalid numbers are not applied',()=>{const value=theme.defaultUiTheme();value.variables.alias='$colors.accent';value.variables.loop='$loop';value.classes.button.normal={background:'$alias',foreground:'$loop',opacity:4,cornerRadius:-10};const style=theme.themeStyle(value,'button');assert.equal(style.background,value.tokens.colors.accent);assert.equal(style.foreground,undefined);assert.equal(style.opacity,1);assert.equal(style.cornerRadius,0)})
await check('Multi-hop locale fallback and Canvas locale override resolve actual text and direction',()=>{const ar=locale.defaultLocalizationTable('ar');ar.entries={};ar.fallbackLocale='de';const de=locale.defaultLocalizationTable('de');de.entries={};de.fallbackLocale='fr';const fr=locale.defaultLocalizationTable('fr');fr.entries={greeting:'Bonjour'};const assets=[asset(1,'localization',ar),asset(2,'localization',de),asset(3,'localization',fr)];for(let i=0;i<assets.length;i++)assets[i].settings.localizationSettings.locale=[ar,de,fr][i].locale;db.loadAssets(assets);assert.equal(locale.localize('greeting',{},'Fallback','ar'),'Bonjour');assert.equal(locale.activeTextDirection('ar'),'rtl');locale.localizationSettings.buildLocales=['ar'];assert.ok(locale.selectedLocalizationAssetUuids().has(uuid(3)));const root=rootCanvas();root.getComponent('Canvas').localePreview='ar';assert.equal(item(layout.resolveUiLayout(1000,500,[root],{localeDirection:locale.activeTextDirection}),root).direction,'rtl')})
await check('Pseudolocalization also audits literal fallback text and locale cache invalidates with assets',()=>{locale.localizationSettings.pseudolocalization=true;assert.notEqual(locale.localize('',{},'literal'),'literal');locale.localizationSettings.pseudolocalization=false;const table=locale.defaultLocalizationTable('en');table.entries={hello:'first'};db.loadAssets([asset(1,'localization',table)]);assert.equal(locale.localize('hello',{},'','en'),'first');table.entries.hello='second';db.updateTextAsset(uuid(1),JSON.stringify(table));assert.equal(locale.localize('hello',{},'','en'),'second')})
await check('Font fallback references follow all locale fallback tables',()=>{const ar=locale.defaultLocalizationTable('ar'),en=locale.defaultLocalizationTable('en');ar.fontFallbacks=[`asset://${uuid(3)}`];en.fontFallbacks=[`asset://${uuid(4)}`];const first=asset(3,'font','data:font/ttf;base64,AAAA'),second=asset(4,'font','data:font/ttf;base64,AAAA');first.fontFamily='Arabic Probe';second.fontFamily='Latin Probe';db.loadAssets([asset(1,'localization',ar),asset(2,'localization',en),first,second]);assert.deepEqual(locale.activeFontFallbackFamilies('ar'),['Arabic Probe','Latin Probe']);const label=new components.Text();assert.match(textView.uiTextPresentation(label,1,'ar').font,/Arabic Probe.*Latin Probe/)})
await check('Native bridge defers composition commits and preserves selection during synchronization',()=>{const commits=[],bridge=new modules.native.UiNativeInputBridge((uuid,value)=>{commits.push({uuid,value});return true}),input={value:'',selectionStart:0,selectionEnd:0,selectionDirection:'none',setSelectionRange(start,end){this.selectionStart=start;this.selectionEnd=end}};bridge.bind(input,'owner','a');input.selectionStart=input.selectionEnd=1;bridge.compositionStart();input.value='a中';assert.equal(bridge.input(input,10,true),false);bridge.bind(input,'owner','a');assert.equal(input.value,'a中');assert.equal(bridge.ownsCompositionKey({key:'Enter',keyCode:229,isComposing:true}),true);bridge.compositionEnd(input,10);assert.deepEqual(commits,[{uuid:'owner',value:'a中'}]);bridge.bind(input,'owner','a中文');assert.equal(input.selectionStart,1);bridge.reset();assert.equal(bridge.flush(input,10),false)})
await check('Shared captions pause, expire at the half-open boundary and clear on disposal',()=>{const api=modules.presentation;api.setRuntimeCaptionTime(0);api.publishRuntimeCaption({id:'probe',text:'Caption',category:'Dialogue',speaker:'Narrator',durationMs:1000});assert.equal(api.activeRuntimeCaptions().length,1);api.setRuntimeCaptionTime(.5);assert.equal(api.activeRuntimeCaptions().length,1);assert.equal(api.activeRuntimeCaptions().length,1);api.setRuntimeCaptionTime(1);assert.equal(api.activeRuntimeCaptions().length,0);api.setRuntimeCaptionTime(null);assert.equal(api.runtimeCaptions.length,0)})
await check('CSV retains multiline strings, quote escapes and all variants; malformed imports refuse',()=>{const table=locale.defaultLocalizationTable('en');table.entries={multiline:'first\r\n"second",third',plural:{one:'one',other:'many','=0':'none'},select:{female:'she',other:'they'}};const imported=locale.importLocalizationCsv(locale.exportLocalizationCsv(table),'en');assert.deepEqual(JSON.parse(JSON.stringify(imported.entries)),{...locale.defaultLocalizationTable('en').entries,...table.entries});assert.throws(()=>locale.importLocalizationCsv('key,value\nx,"unterminated'));assert.throws(()=>locale.importLocalizationCsv('key,value\nx,"text"junk'));assert.throws(()=>locale.importLocalizationCsv('key,value\nx,"{""one"":2}"'))})
await check('PO preserves multiline strings and Nova variant metadata; unsupported indexed imports refuse',()=>{const table=locale.defaultLocalizationTable('en');table.entries={multiline:'first\nsecond',plural:{one:'one',other:'many','=0':'none'}};const imported=locale.importLocalizationPo(locale.exportLocalizationPo(table),'en');assert.deepEqual(JSON.parse(JSON.stringify(imported.entries)),{...locale.defaultLocalizationTable('en').entries,...table.entries});assert.equal(locale.importLocalizationPo('msgid "hello"\nmsgstr ""\n"first\\n"\n"second"\n','en').entries.hello,'first\nsecond');assert.throws(()=>locale.importLocalizationPo('msgid "x"\nmsgstr[0] "one"'));assert.throws(()=>locale.importLocalizationPo('msgid "x"\nmsgstr "bad\\q"'))})
await check('Invalid locale/theme saves retain the exact previous asset and settings',()=>{const value=locale.defaultLocalizationTable('en'),record=asset(1,'localization',value);db.loadAssets([record]);const live=db.resolveAsset(uuid(1)),before=JSON.stringify(live);value.locale='not a locale';assert.throws(()=>locale.writeLocalizationTable(uuid(1),value));assert.equal(JSON.stringify(live),before);const skin=theme.defaultUiTheme();db.loadAssets([asset(2,'uiTheme',skin)]);const saved=db.readTextAsset(uuid(2));skin.classes.button.normal.opacity=NaN;assert.throws(()=>theme.writeUiTheme(uuid(2),skin));assert.equal(db.readTextAsset(uuid(2)),saved)})
await check('Localization extraction reads live component Maps and ignores commented script calls',()=>{const child=entity(),label=child.addComponent(new components.Text());label.localizationKey='ui.live';const result=locale.extractLocalizationKeys([child],[{path:'main.rhai',source:'// tr("ghost")\nfn start(){ print("text"); tr("real.key"); }'}]);assert.deepEqual(result.map(item=>item.key),['real.key','ui.live'])})
await check('Theme/localization session drafts participate in project departure and explicit discard',()=>{const api=modules.drafts,record={uuid:'theme',name:'Theme',assetType:'uiTheme'},candidate={record,projectId:'project',kind:'theme',source:'draft',baseSource:'saved'};api.retainStudioDraft(candidate);assert.equal(api.listPendingAuthoringDrafts('project',[record])[0].kind,'theme');assert.equal(api.readStudioDraft(record,'other','theme','saved'),null);api.discardAuthoringDrafts('project',[record]);assert.equal(api.listPendingAuthoringDrafts('project',[record]).length,0)})
await check('Slider pointer and keyboard emit changed-value callbacks once and clamp repeat input',()=>{const root=rootCanvas(),child=entity(root),slider=child.addComponent(new components.Slider());rect(child).focusable=true;rect(child).skipNavigation=false;const runtime=render([root,child]),calls=[];runtime.setCallback((_,name)=>{if(name==='on_value_changed')calls.push(slider.value)});runtime.pointerDown({x:600,y:250});runtime.pointerMove({x:600,y:250});runtime.pointerUp({x:600,y:250});assert.equal(calls.length,1);runtime.keyDown({key:'ArrowRight'});assert.equal(calls.length,2);slider.value=slider.max;runtime.keyDown({key:'ArrowRight'});assert.equal(calls.length,2)})
await check('Explicit RTL preview, actual Content width and z-order remain effective for crowded scenes',()=>{const root=rootCanvas(),child=entity(root),label=child.addComponent(new components.Text());rect(child).size={x:50,y:20};rect(child).verticalPolicy='Content';label.text='one two three';const result=layout.resolveUiLayout(1000,500,[root,child],{locale:'en',direction:'rtl',localeDirection:locale.activeTextDirection,measureText:(entity,text,scale,language,width)=>textView.measureUiText(text,scale,language,width,context())});assert.equal(item(result,child).direction,'rtl');assert.ok(item(result,child).rect.height>50);const others=Array.from({length:20},()=>entity(root));rect(child).zOrder=1;const crowded=layout.resolveUiLayout(1000,500,[root,child,...others]);assert.equal(crowded.items.at(-1).entity.uuid,child.uuid)})
await check('Duplicate and invalid locale creation refuses before changing asset identity or count',()=>{const table=locale.defaultLocalizationTable('en');db.loadAssets([asset(1,'localization',table)]);const before=JSON.stringify(db.assetState.records);assert.throws(()=>locale.createLocalizationTable('en'),/already/);assert.throws(()=>locale.createLocalizationTable('not a locale'),/valid/);assert.equal(JSON.stringify(db.assetState.records),before)})
await check('Bounded key extraction reports incomplete coverage rather than a clean empty audit',()=>{const source=Array.from({length:513},(_,index)=>({path:`${index}.rhai`,source:'fn start(){}'}));assert.ok(locale.localizationDiagnostics(locale.extractLocalizationKeys([],source),[]).some(issue=>issue.code==='NOVA-LOC-EXTRACTION-LIMIT'))})
await check('Native input retains authored 100k limit while preserving astral composition boundaries',()=>{const commits=[],bridge=new modules.native.UiNativeInputBridge((id,value)=>{commits.push(value);return true}),input={value:'',selectionStart:0,selectionEnd:0,setSelectionRange(){}};bridge.bind(input,'owner','');input.value='x'.repeat(40000)+'🙂';bridge.flush(input,100000);assert.equal(commits[0],input.value);assert.equal(commits[0].length,40002)})
await check('Saving an edited sparse child theme preserves future parent typography/tokens/states',()=>{const parent=theme.defaultUiTheme();parent.variables.accent='#123456';parent.variables.radius=17;parent.classes.button.hovered={background:'#ff0000'};const child={version:3,name:'Child',parentTheme:`asset://${uuid(1)}`,variables:{},tokens:{},classes:{button:{normal:{border:'#010203'}}}};db.loadAssets([asset(1,'uiTheme',parent),asset(2,'uiTheme',child)]);const draft=theme.readUiThemeSource(uuid(2));assert.deepEqual(draft.variables,{});draft.name='Renamed Child';assert.equal(theme.writeUiTheme(uuid(2),draft),true);const saved=JSON.parse(db.readTextAsset(uuid(2)));assert.deepEqual(saved.variables,{});assert.deepEqual(saved.tokens.colors,{});assert.equal(saved.classes.button.normal.background,undefined);parent.variables.accent='#abcdef';parent.variables.radius=23;parent.classes.button.hovered.background='#00ff00';db.updateTextAsset(uuid(1),JSON.stringify(parent));const resolved=theme.readUiTheme(uuid(2));assert.equal(theme.themeStyle(resolved,'button').background,'#abcdef');assert.equal(theme.themeStyle(resolved,'button').cornerRadius,23);assert.equal(theme.themeStyle(resolved,'button','hovered').background,'#00ff00');assert.equal(theme.themeStyle(resolved,'button').border,'#010203')})
await check('Actual input collector ignores editor controls/composition and releases held keys on focus transfer',()=>{const prior=globalThis.HTMLElement;class Element{constructor(control=false){this.control=control;this.isContentEditable=false}closest(){return this.control?this:null}}globalThis.HTMLElement=Element;try{const api=modules.input,manager=new api.InputManager(),actions=[api.createInputAction('Activate','button',[api.createInputBinding('keyboard','Enter')])],event={code:'Enter',key:'Enter',keyCode:13,repeat:false,target:new Element(false)};for(const patch of [{target:new Element(true)},{isComposing:true},{keyCode:229},{defaultPrevented:true}]){manager.onKeyDown({...event,...patch});assert.equal(manager.sample(actions).down.Activate,false)}manager.onKeyDown(event);assert.equal(manager.sample(actions).down.Activate,true);manager.onFocusIn({target:new Element(true)});assert.equal(manager.sample(actions).down.Activate,false);manager.onKeyDown(event);manager.onKeyUp(event);assert.equal(manager.sample(actions).down.Activate,false)}finally{globalThis.HTMLElement=prior}})
await check('Negative tab index permits deliberate focus but skips sequential traversal; positive order precedes automatic',()=>{const root=rootCanvas(),automatic=entity(root),negative=entity(root),first=entity(root);for(const host of [automatic,negative,first]){host.addComponent(new components.Button());rect(host).focusable=true;rect(host).skipNavigation=false}rect(negative).tabIndex=-1;rect(first).tabIndex=2;const runtime=render([root,automatic,negative,first]);runtime.keyDown({key:'Tab'});assert.equal(runtime.accessibilityNodes().find(node=>node.focused).uuid,first.uuid);runtime.keyDown({key:'Tab'});assert.equal(runtime.accessibilityNodes().find(node=>node.focused).uuid,automatic.uuid);assert.equal(runtime.focusByUuid(negative.uuid),true);assert.equal(runtime.accessibilityNodes().find(node=>node.uuid===negative.uuid).tabIndex,-1)})
await check('Reading order, state description and ProgressBar range reach the actual accessibility mirror',()=>{const root=rootCanvas(),a=entity(root),b=entity(root);rect(a).readingOrder=8;rect(b).readingOrder=3;rect(b).accessibilityDescription='Loading';rect(b).accessibilityState='busy';const progress=b.addComponent(new components.ProgressBar());progress.min=0;progress.max=100;progress.value=68;const nodes=render([root,a,b]).accessibilityNodes(),node=nodes.find(node=>node.uuid===b.uuid);assert.ok(nodes.findIndex(node=>node.uuid===b.uuid)<nodes.findIndex(node=>node.uuid===a.uuid));assert.equal(node.role,'progressbar');assert.equal(node.valueNow,68);assert.equal(node.valueMax,100);assert.equal(node.description,'Loading; busy')})
await check('Rounded mask corners cannot hit clipped child controls',()=>{const root=rootCanvas(),parent=entity(root),panel=parent.addComponent(new components.Panel());rect(parent).size={x:100,y:100};panel.maskChildren=true;panel.cornerRadius=40;const child=entity(parent);rect(child).size={x:100,y:100};child.addComponent(new components.Button());const runtime=render([root,parent,child]);assert.equal(runtime.entityAt({x:451,y:201},true),null);assert.equal(runtime.entityAt({x:500,y:250},true)?.uuid,child.uuid)})
const fixture=JSON.parse(await readFile(join(auditContext.scriptRoot,'scripts/fixtures/v26.16-interface-components.json'),'utf8')),serialized=[]
for(const [kind,fields] of Object.entries(fixture))await check(`${kind}: every declared field survives scene JSON and hydration with documented transient-state reset`,()=>{
  const component=new components[kind](),declared=Object.keys(component).filter(key=>!['uuid','kind','enabled','removed'].includes(key)).sort()
  assert.deepEqual(Object.keys(fields).sort(),declared,`${kind} fixture must explicitly enumerate every actual field`)
  Object.assign(component,structuredClone(fields));const host=entity();host.componentMap.set(kind,component)
  const saved=JSON.parse(JSON.stringify(modules.physics.serializeEntity(host))),stored=saved.components.find(item=>item.kind===kind)
  for(const [key,value] of Object.entries(fields))assert.deepEqual(stored.data[key],kind==='Button'&&key==='state'?undefined:value,`${kind}.${key} serialized`)
  const restored=modules.physics.createEntityFromData(saved),actual=restored.componentMap.get(kind)
  assert.ok(actual,kind)
  for(const [key,value] of Object.entries(fields))assert.deepEqual(actual[key],kind==='Button'&&key==='state'?'Disabled':value,`${kind}.${key} hydrated`)
  for(const enabled of [false,true])for(const removed of [false,true]){stored.enabled=enabled;stored.removed=removed;const copy=modules.physics.createEntityFromData(saved).componentMap.get(kind);assert.equal(copy.enabled,removed?false:enabled,`${kind}.enabled`);assert.equal(copy.removed,removed,`${kind}.removed`)}
  stored.enabled=true;stored.removed=false;serialized.push(saved)
})
await check('Every UI component field is retained through deterministic player package files and rehydration',async()=>{
  const project={format:'nova-project',version:2,engineVersion:'26.16.0',assets:[],scenes:[{uuid:uuid(500),name:'Interface Fields',entities:serialized}],projectSettings:{}}
  assert.equal(serialized.length,Object.keys(fixture).length)
  const options={compression:'store',deterministic:true,authoritativeAssets:true},bytes=await modules.pak.createNovaPak(JSON.stringify(project),[],uuid(500),options)
  assert.deepEqual(bytes,await modules.pak.createNovaPak(JSON.stringify(project),[],uuid(500),options))
  const unpacked=JSON.parse(await modules.pak.projectJsonFromNovaPak(bytes));assert.deepEqual(unpacked.scenes,project.scenes)
  for(const [recordIndex,record] of unpacked.scenes[0].entities.entries()){const expectedKind=Object.keys(fixture)[recordIndex];const restored=modules.physics.createEntityFromData(record);for(const component of record.components){if(component.kind!==expectedKind)continue;for(const [key,value]of Object.entries(component.data)){if(key==='state'&&component.kind==='Button')continue;assert.deepEqual(restored.componentMap.get(component.kind)[key],value,`${component.kind}.${key} package rehydration`)}}}
})
await check('Astral text survives hydration limits at both maximum input and authored-source boundaries',()=>{
  for(const [value,maxLength,expected]of [['a🙂b',2,'a'],['a🙂b',3,'a🙂'],['x'.repeat(99999)+'🙂',100000,'x'.repeat(99999)]]){
    const host=entity(),input=host.addComponent(new components.TextInput());input.value=value;input.maxLength=maxLength
    assert.equal(modules.physics.createEntityFromData(JSON.parse(JSON.stringify(modules.physics.serializeEntity(host)))).getComponent('TextInput').value,expected)
  }
})
await check('Password accessibility never publishes the stored password or custom value override',()=>{const root=rootCanvas(),child=entity(root),input=child.addComponent(new components.TextInput());input.password=true;input.value='secret🙂';rect(child).accessibilityValue='also secret';const value=render([root,child]).accessibilityNodes().find(node=>node.uuid===child.uuid).value;assert.equal(value,'•••••••');assert.ok(!value.includes('secret'))})
await check('Tint uses only an isolated bounded canvas and invalidates by source identity',()=>{
  const original=document.createElement,contexts=[];document.createElement=()=>{const ctx=context();contexts.push(ctx);return{width:0,height:0,getContext:()=>ctx}};
  try{const cache=new modules.tint.UiImageTintCache(64),source={},region={x:0,y:0,width:2,height:2},color={r:50,g:100,b:150};const first=cache.resolve('a',source,region,color);assert.equal(cache.resolve('a',source,region,color),first);assert.equal(contexts.length,1);assert.equal(contexts[0].globalCompositeOperation,'source-over');assert.notEqual(cache.resolve('a',{},region,color),first);for(let index=0;index<20;index++)cache.resolve(String(index),{},region,color);assert.ok(cache.inspect().bytes<=64);assert.throws(()=>cache.resolve('large',{}, {...region,width:100},color),/budget/);cache.clear();assert.deepEqual(cache.inspect(),{entries:0,bytes:0})}finally{document.createElement=original}
})
await check('Stripped player builds include recursive locale/font dependencies and reject excluded fallback resources',async()=>{
  const ar=locale.defaultLocalizationTable('ar'),de=locale.defaultLocalizationTable('de'),en=locale.defaultLocalizationTable('en');ar.fallbackLocale='de';de.fallbackLocale='en';en.fallbackLocale='';en.fontFallbacks=[`asset://${uuid(14)}`];const records=[asset(11,'localization',ar),asset(12,'localization',de),asset(13,'localization',en),asset(14,'font','data:font/ttf;base64,AAAA'),asset(15,'localization',locale.defaultLocalizationTable('ja'))]
  const project={assets:records,scenes:[],projectSettings:{presentation:{localization:{sourceLocale:'ar',buildLocales:['ar'],fallbackChain:[]}}}},groups=[{id:'main',name:'Main',mode:'included',root:'Assets'}],policy={include:[],exclude:[],stripUnusedAssets:true}
  const selected=modules.assetProduction.selectBuildAssets(records,groups,project,policy);assert.deepEqual(selected.diagnostics.filter(item=>item.severity==='error'),[]);assert.deepEqual(selected.assets.map(asset=>asset.uuid).sort(),[11,12,13,14].map(uuid).sort())
  const packed=JSON.parse(await modules.pak.projectJsonFromNovaPak(await modules.pak.createNovaPak(JSON.stringify(project),selected.assets,'',{compression:'store',deterministic:true,authoritativeAssets:true})));assert.deepEqual(packed.assets.map(asset=>asset.uuid).sort(),selected.assets.map(asset=>asset.uuid).sort())
  const blocked=modules.assetProduction.selectBuildAssets(records,groups,project,{...policy,exclude:[records[2].path]});assert.ok(blocked.diagnostics.some(item=>item.severity==='error'&&item.uuid===uuid(13)))
})
db.loadAssets([])
const report={...auditContext.metadata(),scope:'Programmer behavior tests using actual staged modules and controlled canvas/media I/O; not a browser/user or physical font/audio test.',status:checks.every(check=>check.status==='passed')?'passed':'failed',checks};const output=auditContext.reportPath;await mkdir(dirname(output),{recursive:true});await writeFile(output,JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify({status:report.status,checks:checks.length,failed:checks.filter(check=>check.status==='failed').map(check=>({name:check.name,error:check.error.split('\n').slice(0,6).join('\n')})),output}));if(report.status==='failed')process.exitCode=1
}finally{if(process.env.NOVA_AUDIT_NODE_OPERATION_COVERAGE==='1')await captureNodeBundle22(temporary);await rm(temporary,{recursive:true,force:true})}
