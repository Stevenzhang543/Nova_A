/** 功能回归脚本：执行 verify-v26.22-resource-actions.mjs 对应场景，保留断言和证据输出。 */
import assert from 'node:assert/strict'
import {propertyAudit22} from './lib/propertyAudit22.mjs'
import {openMediaAuditModules} from './lib/mediaAudit16.mjs'
const audit=await propertyAudit22('resource-actions'),opened=await openMediaAuditModules({repository:process.cwd(),bases:[process.cwd()]},{p:'store/physics',assets:'assets/AssetDatabase',templates:'projects/templates',animation:'runtime/animation',timeline:'runtime/timeline',rigging:'runtime/rigging',resources:'runtime/resources',data:'runtime/dataResources',tilemap:'runtime/tilemap',profiles:'assets/importProfiles',production:'assets/assetProduction',render:'renderer/renderSettings',build:'runtime/buildSettings',assetTypes:'assets/types',packages:'runtime/packages',input:'runtime/input',device:'runtime/deviceInput',trash:'runtime/projectTrash',team:'runtime/teamWorkflow'}),checks=[]
/** 结构说明（自动提取）：check；输入 name、run；直接调用 run、checks.push、console.log；等待异步结果。 */ async function check(name,run){await run();checks.push({name,status:'passed'});console.log('PASS '+name)}
try{
 const {p,assets,templates,animation,timeline,rigging,resources,data,tilemap,profiles,production,render,build,trash,team,input,device,assetTypes,packages}=opened.modules,wasm=await opened.wasm();assert.ok(p.loadProject(wasm.module.migrate_project_json(templates.createTemplateProjectJson('empty','Resource action audit'))));const baseline=p.getSceneJSON();
 /** 结构说明（自动提取）：transaction；输入 name、action、verify；直接调用 assert.ok、p.loadProject、p.clearEditorHistory、p.getSceneJSON、assert.equal 等；等待异步结果。 */ async function transaction(name,action,verify){assert.ok(p.loadProject(baseline));p.clearEditorHistory(name);const before=p.getSceneJSON();assert.equal(p.beginHistoryTransaction(name),true);const result=await action();verify(result);p.commitHistoryTransaction();const after=p.getSceneJSON();if(after===before){assert.equal(p.historyState.length,0,'No-op preset creates no history')}else{p.undo();assert.equal(p.getSceneJSON(),before);p.redo();assert.equal(p.getSceneJSON(),after)}assert.ok(p.loadProject(wasm.module.migrate_project_json(after)));assert.equal(p.getSceneJSON(),after,'Native reopen '+name)}
 const creations=[['animation',/* 调用 animation.createAnimationClipAsset('Clip') 并返回调用结果。 */ ()=>animation.createAnimationClipAsset('Clip')],['controller',/* 调用 animation.createAnimatorControllerAsset('Controller') 并返回调用结果。 */ ()=>animation.createAnimatorControllerAsset('Controller')],['animationMask',/* 调用 animation.createAnimationMaskAsset('Mask') 并返回调用结果。 */ ()=>animation.createAnimationMaskAsset('Mask')],['timeline',/* 调用 timeline.createTimelineAsset('Timeline') 并返回调用结果。 */ ()=>timeline.createTimelineAsset('Timeline')],['rig',/* 调用 rigging.createRigAsset('Rig') 并返回调用结果。 */ ()=>rigging.createRigAsset('Rig')],['skin',/* 调用 rigging.createSkinAsset('Skin') 并返回调用结果。 */ ()=>rigging.createSkinAsset('Skin')],['brushPreset',/* 调用 tilemap.createBrushPreset() 并返回调用结果。 */ ()=>tilemap.createBrushPreset()],['terrainRules',/* 调用 tilemap.createTerrainRules() 并返回调用结果。 */ ()=>tilemap.createTerrainRules()],['tilePalette',/* 调用 tilemap.createTilePalette(null,[0,1,2]) 并返回调用结果。 */ ()=>tilemap.createTilePalette(null,[0,1,2])],['dataSchema',/* 调用 data.createDataSchemaAsset('Schema') 并返回调用结果。 */ ()=>data.createDataSchemaAsset('Schema')],['dataTable',/* 调用 data.createDataTableAsset('Table') 并返回调用结果。 */ ()=>data.createDataTableAsset('Table')],...['Material','AnimationLibrary','InputMap','PhysicsMaterial','Theme','DataTable'].map(/* 返回按声明顺序构造的数组 ['resource '+kind,()=>resources.createResourceAsset(kind,'Resource '+kind)]。 */ kind=>['resource '+kind,/* 调用 resources.createResourceAsset(kind,'Resource '+kind) 并返回调用结果。 */ ()=>resources.createResourceAsset(kind,'Resource '+kind)])];
 for(const [name,create] of creations)await check('Create '+name+' asset preserves exact source and identity through transaction Undo Redo native reopen',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Create '+name,create,/** 结构说明（自动提取）：transaction 回调；输入 record；直接调用 assert.ok、assets.assetState.records.some、assert.doesNotThrow。 */ record=>{assert.ok(record.uuid);assert.ok(assets.assetState.records.some(/* 比较 x.uuid 与 record.uuid，返回严格相等的判断结果。 */ x=>x.uuid===record.uuid));assert.doesNotThrow(/* 调用 JSON.parse(assets.readTextAsset(record.uuid)) 并返回调用结果。 */ ()=>JSON.parse(assets.readTextAsset(record.uuid)))}));
 await check('Data schema and table Save retain the schema reference and typed row values',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Data rows',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 data.createDataSchemaAsset、data.readDataSchema、schema.fields.push、assert.equal、data.saveDataSchema 等；写入 table.rows。 */ ()=>{const schemaAsset=data.createDataSchemaAsset('Items'),schema=data.readDataSchema(schemaAsset.uuid);schema.fields.push({name:'count',type:'integer',required:true,default:0});assert.equal(data.saveDataSchema(schemaAsset.uuid,schema),true);const tableAsset=data.createDataTableAsset('Inventory','asset://'+schemaAsset.uuid),table=data.readDataTable(tableAsset.uuid);table.rows=[{id:'apple',count:7}];assert.equal(data.saveDataTable(tableAsset.uuid,table),true);return {schemaAsset,tableAsset}},/** 结构说明（自动提取）：transaction 回调；输入 {schemaAsset,tableAsset}；直接调用 assert.equal、data.readDataSchema、assert.deepEqual、data.readDataTable。 */ ({schemaAsset,tableAsset})=>{assert.equal(data.readDataSchema(schemaAsset.uuid).fields[1].name,'count');assert.deepEqual(data.readDataTable(tableAsset.uuid).rows,[{id:'apple',count:7}]);assert.equal(data.readDataTable(tableAsset.uuid).schemaAsset,'asset://'+schemaAsset.uuid)}));
 await check('Data row validation identifies missing keys duplicate identities and invalid numeric values',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 data.normalizeDataSchema、data.validateDataRows、assert.deepEqual、assert.equal、assert.ok；包含循环处理。 */ ()=>{const schema=data.normalizeDataSchema({fields:[{name:'id',type:'string',required:true,default:''},{name:'count',type:'number',required:true,default:0}]});const good=data.validateDataRows([{id:'one',count:'7.5'}],schema);assert.deepEqual(good.issues,[]);assert.equal(good.rows[0].count,7.5);for(const rows of [[{count:1}],[{id:'same',count:1},{id:'same',count:2}],[{id:'bad',count:'invalid'}]])assert.ok(data.validateDataRows(rows,schema).issues.length)});
 for(const profile of ['General','PixelArt','UI','NormalMap'])await check('Texture profile '+profile+' applies explicit import fields and survives history',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Texture '+profile,/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assets.createTextAsset、assets.assetState.records.find、profiles.applyTextureImportProfile；返回路径包含 live。 */ ()=>{const record=assets.createTextAsset('Profile fixture','script','fn start(){}'),live=assets.assetState.records.find(/* 比较 x.uuid 与 record.uuid，返回严格相等的判断结果。 */ x=>x.uuid===record.uuid);profiles.applyTextureImportProfile(live.settings,profile);return live},/** 结构说明（自动提取）：transaction 回调；输入 record；直接调用 assert.equal。 */ record=>{assert.equal(record.settings.textureProfile,profile);assert.equal(record.settings.filterMode,profile==='PixelArt'?'Nearest':'Linear')}));
 for(const profile of ['SoundEffect','Music','Voice','Streaming'])await check('Audio profile '+profile+' preserves import intent without claiming transcoding',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Audio '+profile,/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assets.createTextAsset、assets.assetState.records.find、profiles.applyAudioImportProfile；返回路径包含 live。 */ ()=>{const record=assets.createTextAsset('Audio profile fixture','script','fn start(){}'),live=assets.assetState.records.find(/* 比较 x.uuid 与 record.uuid，返回严格相等的判断结果。 */ x=>x.uuid===record.uuid);profiles.applyAudioImportProfile(live.settings,profile);return live},/** 结构说明（自动提取）：transaction 回调；输入 record；直接调用 assert.equal。 */ record=>{assert.equal(record.settings.audioSettings.profile,profile);assert.equal(record.settings.audioSettings.streaming,profile==='Music'||profile==='Streaming')}));
 await check('Bulk metadata deduplicates tags and preserves independent asset identity',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Bulk metadata',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assets.createTextAsset、assets.assetState.records.filter、assert.equal、production.applyBulkAssetSettings；返回路径包含 records。 */ ()=>{const a=assets.createTextAsset('A','script','fn start(){}'),b=assets.createTextAsset('B','script','fn start(){}'),records=assets.assetState.records.filter(/* 调用 [a.uuid,b.uuid].includes(x.uuid) 并返回调用结果。 */ x=>[a.uuid,b.uuid].includes(x.uuid));assert.equal(production.applyBulkAssetSettings(records,{tags:[' b ','a','a'],editorOnly:true}),2);return records},/** 结构说明（自动提取）：transaction 回调；输入 records；直接调用 assert.equal、assert.deepEqual；包含循环处理。 */ records=>{assert.equal(records.length,2);for(const r of records){assert.equal(r.editorOnly,true);assert.deepEqual(r.tags,['a','b'])}}));
 for(const preset of ['Performance','Balanced','High','Ultra','PixelArt'])await check('Render preset '+preset+' applies declared settings with reversible history',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Render '+preset,/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 render.applyQualityPreset。 */ ()=>{render.applyQualityPreset(preset)},/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assert.equal。 */ ()=>{assert.equal(render.renderingSettings.qualityPreset,preset);assert.equal(render.renderingSettings.pixelSnap,preset==='PixelArt')}));
 for(const profile of ['debug','release'])await check('Build profile '+profile+' synchronizes development flag and retains history',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Build '+profile,/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 build.setBuildProfile。 */ ()=>{build.setBuildProfile(profile)},/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assert.equal。 */ ()=>{assert.equal(build.buildSettings.profile,profile);assert.equal(build.buildSettings.developmentBuild,profile==='debug')}));
 for(const preset of build.BUILTIN_BUILD_PRESETS)await check('Build preset '+preset.id+' applies matching target profile and cache policy',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Build preset '+preset.id,/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assert.equal、build.applyBuildPreset。 */ ()=>{assert.equal(build.applyBuildPreset(preset.id),true)},/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assert.equal。 */ ()=>{assert.equal(build.buildSettings.target,preset.target);assert.equal(build.buildSettings.profile,preset.profile);assert.equal(build.buildSettings.delivery.incremental,preset.cacheMode!=='clean')}));
 await check('Unknown build preset leaves all authored settings unchanged',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 p.getSceneJSON、assert.equal、build.applyBuildPreset。 */ ()=>{const before=p.getSceneJSON();assert.equal(build.applyBuildPreset('missing-audit-preset'),false);assert.equal(p.getSceneJSON(),before)});
 await check('Trash restore preserves asset identity and source; purge removes only the recovery entry',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Trash restore',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assets.createTextAsset、trash.moveAssetToProjectTrash、assert.ok、assets.assetState.records.some、assert.equal 等；返回路径包含 asset。 */ ()=>{const asset=assets.createTextAsset('Recover','script','fn start(){}');const item=trash.moveAssetToProjectTrash(asset.uuid,3);assert.ok(item);assert.ok(!assets.assetState.records.some(/* 比较 x.uuid 与 asset.uuid，返回严格相等的判断结果。 */ x=>x.uuid===asset.uuid));assert.equal(trash.restoreProjectTrashItem(item.id),true);assert.equal(assets.readTextAsset(asset.uuid),'fn start(){}');const removed=trash.moveAssetToProjectTrash(asset.uuid);assert.equal(trash.purgeProjectTrashItem(removed.id),true);assert.equal(trash.restoreProjectTrashItem(removed.id),false);return asset},/** 结构说明（自动提取）：transaction 回调；输入 asset；直接调用 assert.ok、assets.assetState.records.some；返回表达式求值结果。 */ asset=>assert.ok(!assets.assetState.records.some(/* 比较 x.uuid 与 asset.uuid，返回严格相等的判断结果。 */ x=>x.uuid===asset.uuid))));
 await check('Team ownership task notes and build presets retain independent records through document history',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Team metadata',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assert.equal、team.addOwnershipRule、team.addTeamTaskLink、team.addTeamChangeNote、assert.ok 等。 */ ()=>{assert.equal(team.addOwnershipRule('Assets/*','@alice,bob,alice'),true);assert.equal(team.addTeamTaskLink('TASK-22','https://example.invalid/TASK-22','Audit fixture'),true);assert.equal(team.addTeamChangeNote('alice','Check asset history'),true);assert.ok(team.shareTeamBuildPreset('Audit','Web','release',{quality:'High'}));assert.deepEqual(team.ownershipForPath('Assets/test.rhai'),['alice','bob'])},/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assert.match、team.codeOwnersFile、assert.ok、team.teamWorkflowState.sharedBuildPresets.some。 */ ()=>{assert.match(team.codeOwnersFile(),/alice/);assert.ok(team.teamWorkflowState.sharedBuildPresets.some(/* 比较 x.name 与 'Audit'，返回严格相等的判断结果。 */ x=>x.name==='Audit'))}));
 await check('Binary asset locks reject another owner and release only the matching owner',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Asset lock',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 team.acquireBinaryAssetLock、assert.ok、assert.equal、team.releaseBinaryAssetLock。 */ ()=>{const lock=team.acquireBinaryAssetLock('Assets/locked.png','alice');assert.ok(lock);assert.equal(team.acquireBinaryAssetLock(lock.path,'bob'),null);assert.equal(team.releaseBinaryAssetLock(lock.path,'bob'),false);assert.equal(team.releaseBinaryAssetLock(lock.path,'alice'),true);return team.acquireBinaryAssetLock(lock.path,'bob')},/* 调用 assert.equal(lock.owner,'bob') 并返回调用结果。 */ lock=>assert.equal(lock.owner,'bob')));
 await check('Tile layer duplicate isolates cell arrays and removal retains the original layer',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Tile layers',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 p.createTileMapEntity、entity.getComponent、tilemap.normalizeTileMap、tilemap.duplicateTileLayer、assert.notEqual 等；写入 component.tiles[…]；返回路径包含 component。 */ ()=>{const entity=p.createTileMapEntity(),component=entity.getComponent('TileMap2D');tilemap.normalizeTileMap(component);component.tiles[0]=4;const original=component.layers[0].id;tilemap.duplicateTileLayer(component);assert.notEqual(component.layers[1].id,original);component.tiles[0]=7;assert.equal(component.layers[0].tiles[0],4);assert.equal(tilemap.removeTileLayer(component),true);assert.equal(tilemap.removeTileLayer(component),false);return component},/** 结构说明（自动提取）：transaction 回调；输入 component；直接调用 assert.equal。 */ component=>{assert.equal(component.layers.length,1);assert.equal(component.tiles[0],4)}));
 await check('Tile clipboard and transforms apply to selected cells and survive document history',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Tile clipboard',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 p.createTileMapEntity、entity.getComponent、tilemap.normalizeTileMap、assert.equal、tilemap.copyTileSelection 等；写入 component.tiles[…]、tilemap.tilemapEditorState.selection；返回路径包含 component。 */ ()=>{const entity=p.createTileMapEntity(),component=entity.getComponent('TileMap2D');tilemap.normalizeTileMap(component);component.tiles[0]=2;component.tiles[1]=3;tilemap.tilemapEditorState.selection={start:{x:0,y:0},end:{x:1,y:0}};assert.equal(tilemap.copyTileSelection(component),true);assert.equal(tilemap.pasteTileClipboard(component,{x:0,y:1}),true);assert.equal(component.tiles[component.width],2);assert.equal(component.tiles[component.width+1],3);assert.equal(tilemap.transformTileSelection(component,'mirrorX'),true);return component},/** 结构说明（自动提取）：transaction 回调；输入 component；直接调用 assert.equal。 */ component=>{assert.equal(component.tiles[0],3);assert.equal(component.tiles[1],2)}));
 await check('Virtual control creation persists explicit action labels and independent identities',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Virtual controls',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 device.createVirtualControl、device.deviceInputSettings.virtualControls.push。 */ ()=>{const a=device.createVirtualControl('Jump','button',0),b=device.createVirtualControl('Move','joystick',1);device.deviceInputSettings.virtualControls.push(a,b);return [a,b]},/** 结构说明（自动提取）：transaction 回调；输入 controls；直接调用 assert.notEqual、assert.equal。 */ controls=>{assert.notEqual(controls[0].id,controls[1].id);assert.equal(controls[0].accessibleLabel,'Jump')}));
 await check('Input rebinding rejects noninteger and nonfinite indices without changing bindings',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 input.createInputBinding、JSON.stringify、assert.equal、input.rebindInputAction；包含循环处理。 */ ()=>{const actions=[{name:'Jump',bindings:[input.createInputBinding('keyboard','Space')]}];for(const index of [-1,.5,NaN,Infinity,32]){const before=JSON.stringify(actions);assert.equal(input.rebindInputAction(actions,'Jump',index,{device:'keyboard',code:'KeyJ'}),false);assert.equal(JSON.stringify(actions),before)}assert.equal(input.rebindInputAction(actions,'Jump',0,{device:'keyboard',code:'KeyJ'}),true);assert.equal(actions[0].bindings[0].code,'KeyJ')});
 await check('Embedded image deduplication and tileset creation retain the texture reference',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Embedded tileset',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 toString、Buffer.from、assets.registerEmbeddedImage、assert.equal、tilemap.createTileSet；写入 image.width、image.height。 */ ()=>{const source='data:image/svg+xml;base64,'+Buffer.from('<svg xmlns="http://www.w3.org/2000/svg" width="64" height="64"><rect width="64" height="64" fill="red"/></svg>').toString('base64'),image=assets.registerEmbeddedImage(source,'Tiles');assert.equal(assets.registerEmbeddedImage(source,'Same').uuid,image.uuid);image.width=64;image.height=64;const set=tilemap.createTileSet(image,16,16);return {image,set}},/** 结构说明（自动提取）：transaction 回调；输入 {image,set}；直接调用 JSON.parse、assets.readTextAsset、assert.equal。 */ ({image,set})=>{const doc=JSON.parse(assets.readTextAsset(set.uuid));assert.equal(doc.textureAsset,'asset://'+image.uuid);assert.equal(doc.columns,4);assert.equal(doc.rows,4)}));
 await check('Verified-source recovery retains original text through transaction history',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Recover source',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assets.createTextAsset、assets.updateTextAsset、assert.equal、production.revertToVerifiedArtifact；写入 record.pipeline.lastValidSource；返回路径包含 record。 */ ()=>{const record=assets.createTextAsset('Verified source','script','fn start(){}'),source=record.source;assets.updateTextAsset(record.uuid,'fn start(){print(1);}');record.pipeline.lastValidSource=source;assert.equal(production.revertToVerifiedArtifact(record),true);return record},/* 调用 assert.equal(assets.readTextAsset(record.uuid),'fn start(){}') 并返回调用结果。 */ record=>assert.equal(assets.readTextAsset(record.uuid),'fn start(){}')));
 await check('Animation reimport retains the source reference and chosen sample rate',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Reimport animation',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 animation.createAnimationClipAsset、assetTypes.defaultAnimationImportMetadata、animation.reimportAnimationClip、assert.ok；写入 target.animationImport；返回路径包含 target。 */ ()=>{const source=animation.createAnimationClipAsset('Source clip'),target=animation.createAnimationClipAsset('Imported clip');target.animationImport={...assetTypes.defaultAnimationImportMetadata(),sourceAsset:'asset://'+source.uuid,sampleRate:30};const result=animation.reimportAnimationClip(target);assert.ok(result);return target},/** 结构说明（自动提取）：transaction 回调；输入 target；直接调用 assert.equal、animation.readAnimationClip。 */ target=>{assert.equal(animation.readAnimationClip(target.uuid).frameRate,30);assert.equal(target.animationImport.sampleRate,30)}));
 await check('Skin auto-weighting stores finite normalized bone weights through history',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Skin weights',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 rigging.defaultRig、rigging.defaultSkin、rigging.autoWeightSkin、assert.equal、assert.ok 等；写入 skin.vertices；返回路径包含 asset。 */ ()=>{const rig=rigging.defaultRig(),skin=rigging.defaultSkin();skin.vertices=[{position:{x:.5,y:0},uv:{x:0,y:0},weights:[]}];const result=rigging.autoWeightSkin(rig,skin);assert.equal(result.vertices,1);assert.ok(skin.vertices[0].weights.length);assert.ok(Math.abs(skin.vertices[0].weights.reduce(/* 计算表达式 sum+w.weight 并返回结果，沿用操作数的原有类型规则。 */ (sum,w)=>sum+w.weight,0)-1)<1e-9);const asset=rigging.createSkinAsset('Weighted');assert.equal(assets.updateTextAsset(asset.uuid,JSON.stringify(skin)),true);return asset},/* 调用 assert.ok(rigging.readSkin(asset.uuid).vertices[0].weights.every(w=>Number.isFinite(w.weight))) 并返回调用结果。 */ asset=>assert.ok(rigging.readSkin(asset.uuid).vertices[0].weights.every(/* 调用 Number.isFinite(w.weight) 并返回调用结果。 */ w=>Number.isFinite(w.weight)))));
 await check('Continuous tile stroke paints intermediate cells inside one document transaction',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Tile stroke',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 p.createTileMapEntity、entity.getComponent、tilemap.normalizeTileMap、tilemap.beginTileStroke、tilemap.continueTileStroke 等；写入 tilemap.tilemapEditorState.tool、tilemap.tilemapEditorState.tileIndex；返回路径包含 component。 */ ()=>{const entity=p.createTileMapEntity(),component=entity.getComponent('TileMap2D');tilemap.normalizeTileMap(component);tilemap.tilemapEditorState.tool='brush';tilemap.tilemapEditorState.tileIndex=5;const stroke=tilemap.beginTileStroke(component,{x:0,y:0});tilemap.continueTileStroke(component,stroke,{x:3,y:0});assert.equal(tilemap.endTileStroke(component,stroke,{x:3,y:0}),true);return component},/* 调用 assert.deepEqual(component.tiles.slice(0,4),[5,5,5,5]) 并返回调用结果。 */ component=>assert.deepEqual(component.tiles.slice(0,4),[5,5,5,5])));
 await check('Package quarantine is persisted and missing package update leaves document unchanged',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Quarantine package',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assert.ok、packages.quarantinePackage、p.getSceneJSON、assert.equal、packages.applyPackageUpdate；返回路径包含 manifest。 */ ()=>{const manifest=packages.packageState.registryCatalog[0];assert.ok(manifest);packages.quarantinePackage(manifest,'Audit quarantine');const before=p.getSceneJSON();assert.equal(packages.applyPackageUpdate('org.audit.missing'),false);assert.equal(p.getSceneJSON(),before);return manifest},/** 结构说明（自动提取）：transaction 回调；输入 manifest；直接调用 assert.ok、packages.packageState.quarantine.some；返回表达式求值结果。 */ manifest=>assert.ok(packages.packageState.quarantine.some(/* 比较 x.id 与 manifest.id，返回严格相等的判断结果。 */ x=>x.id===manifest.id))));
 await check('Batch file import preserves script bytes and cancelled import cannot create an asset',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Batch file import',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 File、assets.importAssetFiles、assert.equal、assets.readTextAsset、assert.ok 等；写入 globalThis.FileReader、assets.assetState.currentFolder、assets.assetState.search；返回路径包含 records[…]；等待异步结果。 */ async()=>{const previous=globalThis.FileReader;globalThis.FileReader=class{/** 结构说明（自动提取）：readAsDataURL；输入 blob；直接调用 then、blob.arrayBuffer。 */ readAsDataURL(blob){blob.arrayBuffer().then(/** 结构说明（自动提取）：then 回调；输入 bytes；直接调用 toString、Buffer.from、onload；写入 result。 */ bytes=>{this.result='data:'+blob.type+';base64,'+Buffer.from(bytes).toString('base64');this.onload?.()},/** 结构说明（自动提取）：then 回调；输入 error；直接调用 onerror；写入 error。 */ error=>{this.error=error;this.onerror?.()})}};try{const file=new File(['fn start(){print(22);}'],'batch-audit.rhai',{type:'text/plain'}),records=await assets.importAssetFiles([file],'Assets');assert.equal(records.length,1);assert.equal(assets.readTextAsset(records[0].uuid),'fn start(){print(22);}');assets.assetState.currentFolder='Assets';assets.assetState.search='batch-audit';assert.ok(assets.filteredAssets().some(/* 比较 x.uuid 与 records[0].uuid，返回严格相等的判断结果。 */ x=>x.uuid===records[0].uuid));const controller=new AbortController();controller.abort();const before=p.getSceneJSON();await assert.rejects(/* 调用 assets.importAssetFiles([file],'Assets',controller.signal) 并返回调用结果。 */ ()=>assets.importAssetFiles([file],'Assets',controller.signal),/cancelled/);assert.equal(p.getSceneJSON(),before);return records[0]}finally{globalThis.FileReader=previous}},/* 调用 assert.equal(assets.readTextAsset(record.uuid),'fn start(){print(22);}') 并返回调用结果。 */ record=>assert.equal(assets.readTextAsset(record.uuid),'fn start(){print(22);}')));
}finally{await opened.close()}
await audit.write(checks,'Actual resource creation, metadata/profile and data Save APIs with named history and native reopen. Script records isolate import-setting persistence without claiming decoding/transcoding; output rendering and arbitrary schema rejection domains have separate evidence.')

