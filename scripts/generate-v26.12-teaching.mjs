/** 版本26.12：组织教学步骤与示例说明，生成版本教程和用户操作文档。 */
import { readFile, writeFile } from 'node:fs/promises'
import { dirname, join } from 'node:path'
import { fileURLToPath } from 'node:url'
const root=dirname(dirname(fileURLToPath(import.meta.url)))
const sample='// Keep this comment while switching modes.\nfn doubled(value) { value * 2 }\nfn start() {\n  let values = [1, 2, 3];\n  let state = #{score: 0};\n  for value in values { state.score += doubled(value); }\n  print(state.score);\n}'
const lessons={
  en:{title:'26.12 — edit one program in Code and Visual',intro:'New Rhai companions use typed syntax nodes. Existing execution graphs keep their compiler and explicit source-backed blocks. The conversion preview reports the representation of the current graph and points to exact source regions; it does not certify the behavior of an untested game.',steps:[
    'Create a Clear Scene project. Open the Script workspace, create a Rhai script, and replace its source with the example below. Save. Attach that asset to a Script2D component on an enabled scene object. Play and inspect the Console: it prints 12.',
    'Stop and choose Visual for the same asset. Review the conversion panel. Functions, parameters, local declarations, array elements, map entries, the for loop, member assignment and calls each have connected typed nodes. Select a source region to focus its code or corresponding node.',
    'Select the Literal node containing 2 in doubled. In Literal (Rhai spelling), enter 3 and save. Return to Code: the function now multiplies by 3. Play again: the Console prints18. Undo the field change, save, and verify12 again.',
    'Select the Array node. Add an elements child, create a Literal containing4, and connect its output to that input. Reorder children with the up/down controls. Disconnecting a required scalar child is an error; removing an ordered child removes its wire and leaves the former child available as a disconnected node.',
    'Select a Block to add, remove or reorder statements. Use the optional-child controls for an initializer, else branch, return value, loop counter, catch parameter or typed-method receiver. A blank visual module can be built from Function Declaration, Block, Expression Statement, Call and Literal nodes. Declaration/function nodes own language variables and arguments; the old execution-graph tables do not apply to this representation.',
    'Rename a parsed declaration in its node field. References with the same lexical binding follow the rename; a separate shadowed declaration keeps its own name. Return to Code after edits and inspect the actual generated source. Correct invalid identifiers or scope conflicts before saving.',
    'Create Assets/Scripts/Shared.rhai containing fn bonus(value) { value + 1 }. In the main module add a standalone use "Assets/Scripts/Shared.rhai"; line. Call bonus from start, save both assets and replay. Shared dependencies initialize once in dependency order. Strings and comments that resemble use lines are inert. Missing paths and dependency cycles are actionable errors.',
    'Add a missing closing bracket in Code and attempt to switch. The draft and diagnostic stay visible. Cancel the transition, repair the source and switch again. For a changed asset or failed save, keep the editor open and resolve the conflict; accepting a conversion preview is tied to that exact source revision.',
    'Open creator-v2612-code-game, creator-v2612-blocks-game and creator-v2612-mixed-game from reference-projects/projects. In each, collect six checkpoints with WASD/arrows and press R to restart. The blocks variant attaches the graph directly; the others attach Rhai. Save/reopen and export the same scene to repeat the route.'
  ],expected:'Expected checks:12 before the multiplier edit,18 after it,12 after undo; no comment or variable loss over three switches; six Coin Trail points, completion, and a working restart. Syntax errors block saving/conversion without clearing the draft. Compare actual commands, logs, state and runtime errors when extending the example.',limits:'The shared source projection is limited to64,000 UTF16 characters,10,000 nodes and128 pins per node; ordered lists are grouped when necessary. Limits reject rather than truncate. Rhai integers and floats are distinct overloads: write0.0 where a float is required. The API overload palette identifies actual registered signatures. Native import aliases/namespaces, eval, await and blocking sleep are unavailable; project-local use is dependency concatenation, and timers/tasks provide supported delays. Local closures and function pointers execute, but closure identities cannot be serialized into saved properties. Syntax coverage does not replace runtime, export or user testing.',migration:'Project Format2/schema29 and Graph Format1 remain unchanged. The additive language metadata identifies the new representation. Preserve a backup before opening a typed graph in an older editor. Existing legacy graphs remain usable and report any source-backed regions. For the exact syntax, API signatures and audit scope see VERSION_26_12_LANGUAGE.md, SCRIPT_SUPPORT_MATRIX_26_12.md and the separate release evidence.'},
  de:{title:'26.12 — ein Programm in Code und Visual bearbeiten',intro:'Neue Rhai-Begleitgraphen bestehen aus typisierten Syntaxknoten. Bestehende Ablaufgraphen behalten ihren Compiler und ausdrücklich gekennzeichnete Quelltextblöcke. Die Konvertierungsvorschau beschreibt den aktuellen Graphen und verweist auf genaue Quelltextbereiche; sie bestätigt nicht automatisch das Verhalten eines ungetesteten Spiels.',steps:[
    'Erstelle ein Projekt mit Clear Scene. Öffne den Skriptarbeitsbereich, erstelle ein Rhai-Skript und ersetze den Inhalt durch das folgende Beispiel. Speichere es und verknüpfe das Asset über Script2D mit einem aktiven Szenenobjekt. Starte Play: Die Konsole zeigt12.',
    'Stoppe und wechsle für dasselbe Asset zu Visual. Prüfe die Konvertierungsvorschau. Funktionen, Parameter, Deklarationen, Array-Elemente, Map-Einträge, for-Schleife, Zuweisung und Aufrufe besitzen verbundene Syntaxknoten. Ein ausgewählter Quellbereich führt zum Code oder zu seinem Knoten.',
    'Wähle den Literal-Knoten mit2 in doubled. Trage unter Literal (Rhai spelling) die3 ein und speichere. Im Code multipliziert die Funktion nun mit3. Play zeigt18. Mache die Feldänderung rückgängig, speichere und prüfe erneut12.',
    'Wähle das Array, füge ein elements-Kind hinzu und verbinde einen neuen Literal-Knoten mit4. Ändere die Reihenfolge mit den Pfeiltasten der Liste. Ein fehlendes erforderliches Einzelkind ist ein Fehler. Das Entfernen eines Listenkinds löst dessen Verbindung; der bisherige Knoten bleibt unverbunden erhalten.',
    'Füge im Block Anweisungen hinzu, entferne oder sortiere sie. Optionale Kinder steuern Initialisierung, else, Rückgabewert, Schleifenzähler, catch-Parameter und Methodenempfänger. Aus Function Declaration, Block, Expression Statement, Call und Literal entsteht ein leeres visuelles Programm. Sprachdeklarationen besitzen ihre Variablen und Argumente; die alten Tabellen für Ablaufgraphen gelten hier nicht.',
    'Benenne eine eingelesene Deklaration im Knoten um. Referenzen derselben lexikalischen Bindung folgen ihr; eine verdeckende Deklaration behält ihren eigenen Namen. Kontrolliere danach den erzeugten Code und behebe ungültige Bezeichner oder Namenskonflikte vor dem Speichern.',
    'Erstelle Assets/Scripts/Shared.rhai mit fn bonus(value) { value + 1 }. Ergänze im Hauptmodul eine eigene Zeile use "Assets/Scripts/Shared.rhai";. Rufe bonus in start auf, speichere beide Assets und starte erneut. Abhängigkeiten initialisieren einmal in ihrer Reihenfolge. Ähnliche Zeichenketten und Kommentare sind wirkungslos; fehlende Pfade und Zyklen ergeben konkrete Fehler.',
    'Entferne im Code eine schließende Klammer und versuche zu wechseln. Entwurf und Diagnose bleiben sichtbar. Brich den Wechsel ab, repariere die Syntax und versuche es erneut. Löse Asset-Konflikte oder Speicherfehler im offenen Editor; eine bestätigte Vorschau gilt nur für genau diese Quelltextrevision.',
    'Öffne die Referenzen creator-v2612-code-game, creator-v2612-blocks-game und creator-v2612-mixed-game unter reference-projects/projects. Sammle jeweils sechs Kontrollpunkte mit WASD/Pfeilen und starte mit R neu. Die Blockvariante bindet den Graphen direkt, die anderen Rhai. Speichere, öffne erneut und exportiere dieselbe Szene.'
  ],expected:'Prüfe12 vor der Änderung,18 danach und12 nach Rückgängig; drei Wechsel ohne verlorene Kommentare oder Variablen; sechs Punkte, Abschluss und Neustart in Coin Trail. Syntaxfehler blockieren Speichern/Konvertierung und erhalten den Entwurf. Vergleiche bei Erweiterungen wirkliche Befehle, Logs, Zustand und Laufzeitfehler.',limits:'Die Projektion begrenzt Module auf64.000 UTF16-Zeichen,10.000 Knoten und128 Pins je Knoten. Lange Listen werden gruppiert; Überschreitungen werden abgelehnt und nicht abgeschnitten. Rhai unterscheidet Integer und Float: Für Float-Parameter ist beispielsweise0.0 nötig. Die API-Palette nennt tatsächlich registrierte Überladungen. Native import-Aliase/Namensräume, eval, await und blockierendes sleep stehen nicht bereit. Lokales use verbindet Abhängigkeiten; Timer und Tasks liefern unterstützte Verzögerungen. Lokale Closures/Funktionszeiger laufen, ihre Identitäten sind aber nicht als Projekteigenschaften speicherbar. Syntaxabdeckung ersetzt keinen Laufzeit-, Export- oder Benutzertest.',migration:'Project Format2/Schema29 und Graph Format1 bleiben erhalten. Zusätzliche Sprachmetadaten kennzeichnen die neue Darstellung. Sichere Projekte vor dem Öffnen typisierter Graphen in alten Editoren. Bestehende Ablaufgraphen bleiben nutzbar und zeigen Quelltextbereiche an. Syntax, Signaturen und Testgrenzen stehen in VERSION_26_12_LANGUAGE.md, SCRIPT_SUPPORT_MATRIX_26_12.md und den getrennten Release-Nachweisen.'},
  'zh-CN':{title:'26.12 — 在代码与可视化中编辑同一个程序',intro:'新建的 Rhai 关联图使用具有明确结构的语法节点。旧执行图保留原编译器，并明确标出依赖原始代码的块。转换预览说明当前图的表示方式，能定位准确源码范围；它不会自动证明未经测试的游戏行为正确。',steps:[
    '使用 Clear Scene 创建项目。在脚本工作区新建 Rhai 脚本，输入下方示例并保存。通过 Script2D 将该资源绑定到已启用的场景对象。点击 Play，控制台应输出12。',
    '停止播放，切换同一资源到 Visual，检查转换预览。函数、参数、局部声明、数组元素、映射项、for循环、成员赋值与调用都有互相连接的语法节点。选择源码区域可以定位代码或对应节点。',
    '选中 doubled 中值为2的 Literal 节点，在 Literal (Rhai spelling) 中改为3并保存。切回代码，应看到乘数变为3。再次播放，输出应为18。撤销字段修改并保存，输出应恢复12。',
    '选中 Array，增加一个 elements 子项，新建值为4的 Literal，将其输出连接到新输入。使用列表的上移/下移按钮调整顺序。缺少必需的单个子节点会报错；删除列表子项会移除连线，但保留原来的节点为未连接节点。',
    '在 Block 中添加、删除和排序语句。可选子项控制初始化表达式、else分支、返回值、循环计数器、catch参数与类型方法接收者。从 Function Declaration、Block、Expression Statement、Call 和 Literal 可以搭建空白可视化程序。语言声明和函数节点拥有变量与参数，旧执行图的独立表格不适用于这种结构。',
    '在节点字段中重命名已解析的声明。同一词法绑定的引用会同步修改，遮蔽它的独立声明仍保留自己的名字。切回代码检查真实生成结果，保存前修复无效标识符或作用域冲突。',
    '创建 Assets/Scripts/Shared.rhai，写入 fn bonus(value) { value + 1 }。在主模块独立一行写 use "Assets/Scripts/Shared.rhai";，从 start 调用 bonus，保存两个资源后再次播放。共享依赖按依赖顺序仅初始化一次。字符串或注释中看似 use 的内容不会加载模块；路径缺失和依赖环会明确报错。',
    '在代码中删掉一个闭合括号并尝试切换。草稿和诊断应保留。取消切换、修复语法，再次切换。资源冲突或保存失败时保持编辑器打开并解决问题；转换预览的确认仅对当前准确源码版本有效。',
    '从 reference-projects/projects 打开 creator-v2612-code-game、creator-v2612-blocks-game 和 creator-v2612-mixed-game。分别用 WASD/方向键按顺序收集六个检查点，用 R 重新开始。块版本直接绑定图资源，其他版本绑定 Rhai。保存、重新打开并导出同一场景，再次检查游戏流程。'
  ],expected:'核对修改前12、修改后18、撤销后12；连续切换三次不丢失注释或变量；Coin Trail获得六分、完成并能重新开始。语法错误应阻止保存/转换，同时保留草稿。扩展示例时比较实际命令、日志、状态以及运行错误。',limits:'源码图每模块最多64,000个UTF16字符、10,000个节点、每节点128个引脚。较长有序列表会分组；超限明确拒绝，不截断。Rhai整数与浮点数属于不同重载，浮点参数应写为0.0等形式。API面板说明真正注册的重载。原生import别名/命名空间、eval、await以及阻塞sleep不可用；项目内use按依赖拼接源码，延迟使用已支持的计时器和任务API。局部闭包与函数指针可以执行，但其身份不能序列化为已保存属性。语法覆盖率不能代替运行、导出和用户测试。',migration:'继续使用Project Format2/schema29与Graph Format1；附加语言元数据标识新的图结构。在旧编辑器打开新图前应保留备份。旧执行图仍可使用，并显示依赖原始代码的范围。完整语法、实际签名和审计范围见 VERSION_26_12_LANGUAGE.md、SCRIPT_SUPPORT_MATRIX_26_12.md 以及独立版本的发布证据。'}
}
const escape=/* 调用 value.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;') 并返回调用结果。 */ value=>value.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;')
let html=await readFile(join(root,'manual/index.html'),'utf8')
let supplement='<!-- NOVA_V2612_START -->\n<div class="release-supplement">'
for(const [language,lesson]of Object.entries(lessons)){
  const path=join(root,'manual','MANUAL.'+language+'.md'),before=await readFile(path,'utf8'),chapter='<!-- NOVA_V2612_START -->\n## '+lesson.title+'\n\nEngine: **26.12.0** · Project Format 2/schema 29 · Graph Format 1.\n\n'+lesson.intro+'\n\n'+lesson.steps.map(/* 计算表达式 (index+1)+'. '+step 并返回结果，沿用操作数的原有类型规则。 */ (step,index)=>(index+1)+'. '+step).join('\n\n')+'\n\n```rhai\n'+sample+'\n```\n\n'+lesson.expected+'\n\n'+lesson.limits+'\n\n'+lesson.migration+'\n<!-- NOVA_V2612_END -->\n'
  const updated=before.replace(/^# Nova_A 26\.\d+/, '# Nova_A 26.12').replace(/<!-- NOVA_V2612_START -->[\s\S]*?<!-- NOVA_V2612_END -->\r?\n?/,'')
  const split=updated.indexOf('\n');await writeFile(path,updated.slice(0,split)+'\n\n'+chapter+'\n'+updated.slice(split+1).replace(/^\s*\n/,''))
  supplement+='<article data-lang="'+language+'"><section id="'+language+'-v2612-language"><h2>'+escape(lesson.title)+'</h2><p>Engine 26.12.0 · Project Format 2/schema 29 · Graph Format 1.</p><p>'+escape(lesson.intro)+'</p><ol>'+lesson.steps.map(/* 计算表达式 '<li>'+escape(step)+'</li>' 并返回结果，沿用操作数的原有类型规则。 */ step=>'<li>'+escape(step)+'</li>').join('')+'</ol><pre><code>'+escape(sample)+'</code></pre><p>'+escape(lesson.expected)+'</p><p>'+escape(lesson.limits)+'</p><p>'+escape(lesson.migration)+'</p></section></article>'
}
supplement+='</div>\n<!-- NOVA_V2612_END -->\n'
html=html.replace(/<!-- NOVA_V2612_START -->[\s\S]*?<!-- NOVA_V2612_END -->\r?\n?/,'').replace(/<title>Nova_A 26\.\d+ Manual<\/title>/,'<title>Nova_A26.12 Manual</title>').replace('Nova_A26.12 Manual','Nova_A 26.12 Manual')
html=html.replace(/26\.\d+ Offline Teaching Manual/g,'26.12 Offline Teaching Manual').replace(/(<meta name="description" content="Complete Nova_A )26\.\d+( \(engine )26\.\d+\.0/, '$126.12$226.12.0')
const at=html.indexOf('<script>');if(at<0)throw new Error('Manual language controller missing; no HTML was replaced.')
await writeFile(join(root,'manual/index.html'),html.slice(0,at)+supplement+html.slice(at))
console.log('Added complete EN/DE/ZH26.12 task chapter to all four offline manual files.')
