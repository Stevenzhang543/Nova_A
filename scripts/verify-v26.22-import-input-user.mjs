import {userFixture22} from './lib/userFixtures22.mjs'
import assert from 'node:assert/strict'
import {readFile} from 'node:fs/promises'
import {join} from 'node:path'
import {withBrowserAudit,wait} from './lib/browserUserAudit.mjs'
import {worldUserControls17} from './lib/worldAudit17.mjs'
const fixture=await userFixture22('import-input-user','imports')
await withBrowserAudit({release:'26.22',name:'import-input-user',development:process.argv.includes('--development'),expectedRelease:JSON.parse(await readFile('package.json','utf8')).version.split('.').slice(0,2).join('.'),width:1366,height:900},async a=>{
 const u=worldUserControls17(a);await u.open(fixture)
 async function selectAsset(name){await a.clickText('.bottom-panel .panel-tab','Assets',true);if(await a.evaluate("document.querySelector('[data-panel-maximize=bottom]').getAttribute('aria-pressed')!=='true'"))await a.click('[data-panel-maximize=bottom]');while(await a.evaluate("!!document.querySelector('.toast-stack article button:last-child')"))await a.click('.toast-stack article button:last-child');await a.clickText('.asset-grid article',name);if(await a.evaluate("(()=>{const e=document.querySelector('.asset-detail-toggle');return !!e&&!!e.getBoundingClientRect().width})()"))await a.click('.asset-detail-toggle');await a.clickText('.importer-tabs button','Import',true)}
 const field=name=>'.asset-inspector input[data-resource-key$=":settings.'+name+'"]',value=selector=>a.evaluate(`Number(document.querySelector(${JSON.stringify(selector)}).value)`)
 await selectAsset('Import image')
 await a.check('Import numeric drafts reject malformed input without changing the asset, Escape restores the value',async()=>{const selector=field('pixelsPerUnit'),before=await value(selector);await u.field(selector,'1/0');assert.equal(await a.evaluate(`document.querySelector(${JSON.stringify(selector)}).getAttribute('aria-invalid')`),'true');await a.evaluate(`document.querySelector(${JSON.stringify(selector)}).focus()`);await a.press('Escape');assert.equal(await value(selector),before);await a.capture('invalid-draft-restored')})
 await a.check('Two asset fields produce two independent Undo actions and persist on immediate save',async()=>{const pixels=field('pixelsPerUnit'),pivot=field('pivot.x'),before=[await value(pixels),await value(pivot)];await u.field(pixels,'120+3');await u.field(pivot,'.25');await a.evaluate('document.activeElement.blur()');await a.press('z',2);await wait(250);assert.equal(await value(pivot),before[1]);assert.equal(await value(pixels),123);await a.press('z',2);await wait(250);assert.equal(await value(pixels),before[0]);await u.field(pixels,'123');const saved=await u.save('import-numeric-history-22');assert.equal(saved.document.assets.find(item=>item.assetType==='image').settings.pixelsPerUnit,123);await u.open(saved.file);await selectAsset('Import image');assert.equal(await value(pixels),123)})
 await a.check('Narrow asset inspector exposes six-digit numeric capacity and wrapping paired fields',async()=>{await a.viewport(1024,768);const selector=field('pivot.x');await a.evaluate(`document.querySelector(${JSON.stringify(selector)}).scrollIntoView({block:'center'})`);await wait(200);const metrics=await a.evaluate(`(()=>{const e=document.querySelector(${JSON.stringify(selector)}),s=getComputedStyle(e),r=e.getBoundingClientRect();return{width:r.width,padding:parseFloat(s.paddingLeft)+parseFloat(s.paddingRight),font:parseFloat(s.fontSize),right:r.right,viewport:innerWidth}})()`);assert.ok(metrics.width-metrics.padding>=metrics.font*3.6,JSON.stringify(metrics));assert.ok(metrics.right<=metrics.viewport,JSON.stringify(metrics));await a.capture('narrow-import-fields')})
})
