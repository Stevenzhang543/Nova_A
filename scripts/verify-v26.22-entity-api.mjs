/** 功能回归脚本：执行 verify-v26.22-entity-api.mjs 对应场景，保留断言和证据输出。 */
import assert from 'node:assert/strict'
import {propertyAudit22} from './lib/propertyAudit22.mjs'
import {openMediaAuditModules} from './lib/mediaAudit16.mjs'
const audit=await propertyAudit22('entity-api'),opened=await openMediaAuditModules({repository:process.cwd(),bases:[process.cwd()]},{triangle:'world/TriangleEntity',box:'world/BoxEntity',components:'world/components',physics:'store/physics'})
try{const {box,components:c,physics:p}=opened.modules,e=new box.BoxEntity(101,{x:0,y:0},{x:2,y:2}),checks=[];const test=/** 结构说明（自动提取）：test；输入 name、covers、fn；直接调用 fn、checks.push、covers.map。 */ (name,covers,fn)=>{fn();checks.push({name,status:'passed',covers:covers.map(/* 计算表达式 'src/world/Entity.ts#Entity.'+operation 并返回结果，沿用操作数的原有类型规则。 */ operation=>'src/world/Entity.ts#Entity.'+operation)})},get=/** 结构说明（自动提取）：get；输入 v、path；直接调用 reduce、path.split；返回表达式求值结果。 */ (v,path)=>path.split('.').reduce(/* 返回 v[k] 的当前值。 */ (v,k)=>v[k],v)
 const aliases=[['color',{r:20,g:30,b:40},'renderer.color'],['transparency',42,'renderer.opacity'],['texture','asset://00000001-0000-4000-8000-000000000000','renderer.texture'],['velocity',{x:2,y:-3},'rigidBody.velocity'],['angularVelocity',.2,'rigidBody.angularVelocity'],['linearDamping',.3,'rigidBody.linearDamping'],['angularDamping',.4,'rigidBody.angularDamping'],['density',2,'rigidBody.density'],['mass',3,'rigidBody.mass'],['autoInertia',false,'rigidBody.autoInertia'],['inertia',4,'rigidBody.inertia'],['gravityScale',-.5,'rigidBody.gravityScale'],['force',{x:3,y:4},'rigidBody.force'],['torque',5,'rigidBody.torque'],['gravity',6,'rigidBody.localGravity'],['acceleration',{x:4,y:5},'rigidBody.acceleration'],['restitution',.7,'collider.material.restitution'],['restitutionThreshold',.8,'collider.material.restitutionThreshold'],['staticFriction',.9,'collider.material.staticFriction'],['dynamicFriction',.6,'collider.material.dynamicFriction'],['isSensor',true,'collider.sensor']]
 test('Entity authored aliases update the canonical component and survive serialization/hydration',aliases.map(/* 返回 x[0] 的当前值。 */ x=>x[0]),/** 结构说明（自动提取）：test 回调；无显式参数；直接调用 structuredClone、assert.deepEqual、get、p.createEntityFromData、p.serializeEntity；写入 e[…]；包含循环处理。 */ ()=>{for(const [field,value,path]of aliases){e[field]=structuredClone(value);assert.deepEqual(get(e,path),value,field+' owner');assert.deepEqual(e[field],value,field+' getter');const copy=p.createEntityFromData(p.serializeEntity(e));assert.deepEqual(copy[field],value,field+' hydration')}})
 test('Nonfinite entity alias writes reject before changing their canonical owner',aliases.filter(/* 先计算 typeof x[1]==='number'；仅当其为假值时求右侧 typeof x[1]==='object'，返回短路求值结果。 */ x=>typeof x[1]==='number'||typeof x[1]==='object').map(/* 返回 x[0] 的当前值。 */ x=>x[0]),/** 结构说明（自动提取）：test 回调；无显式参数；直接调用 structuredClone、get、Object.keys、assert.throws、assert.deepEqual；包含循环处理。 */ ()=>{for(const [field,value,path]of aliases){if(typeof value!=='number'&&typeof value!=='object')continue;const before=structuredClone(get(e,path));for(const invalid of typeof value==='number'?[NaN,Infinity,-Infinity]:[{...value,[Object.keys(value)[0]]:NaN},{...value,[Object.keys(value)[0]]:Infinity}]){assert.throws(/** 结构说明（自动提取）：assert.throws 回调；无显式参数；写入 e[…]。 */ ()=>{e[field]=invalid},/finite/i,field);assert.deepEqual(get(e,path),before,field+' preserved')}}})
 test('Body-mode aliases preserve unrelated modes and layer writes update all retained renderers',['isStatic','isKinematic','layer','spriteRenderer','textRenderer'],/** 结构说明（自动提取）：test 回调；无显式参数；直接调用 assert.equal、e.addComponent、c.SpriteRenderer2D、c.TextRenderer2D、e.removeComponent 等；写入 e.isStatic、e.isKinematic、e.layer。 */ ()=>{e.isStatic=true;assert.equal(e.rigidBody.bodyType,'Static');assert.equal(e.isStatic,true);e.isKinematic=false;assert.equal(e.isStatic,true);e.isStatic=false;assert.equal(e.rigidBody.bodyType,'Dynamic');e.isKinematic=true;assert.equal(e.isKinematic,true);e.isStatic=false;assert.equal(e.isKinematic,true);e.isKinematic=false;assert.equal(e.rigidBody.bodyType,'Dynamic');const sprite=e.addComponent(new c.SpriteRenderer2D()),text=e.addComponent(new c.TextRenderer2D());e.removeComponent('SpriteRenderer2D');e.layer=7;assert.equal(e.renderer.sortingLayer,7);assert.equal(sprite.sortingLayer,7);assert.equal(text.sortingLayer,7);assert.equal(e.layer,7);assert.throws(/** 结构说明（自动提取）：assert.throws 回调；无显式参数；写入 e.layer。 */ ()=>{e.layer=NaN},/finite/);assert.equal(e.renderer.sortingLayer,7);assert.equal(sprite.sortingLayer,7);assert.equal(text.sortingLayer,7)})
 test('Removed component records keep identity while live collider replacement and optional accessors stay correct',['components','addComponent','getComponent','getCollider','hasComponent','removeComponent','collider','transform','renderer','rigidBody','camera2D','script2D'],/** 结构说明（自动提取）：test 回调；无显式参数；直接调用 assert.equal、e.removeComponent、assert.ok、e.hasComponent、e.getComponent 等。 */ ()=>{const transform=e.transform;assert.equal(e.removeComponent('Transform2D'),false);assert.equal(e.transform,transform);const old=e.collider;assert.ok(e.removeComponent('BoxCollider2D'));assert.equal(e.hasComponent('BoxCollider2D'),false);assert.equal(e.getComponent('BoxCollider2D'),null);assert.equal(e.getComponent('BoxCollider2D',true),old);const ellipse=e.addComponent(new c.Collider2D('EllipseCollider2D'));assert.equal(e.getCollider(),ellipse);assert.equal(e.getCollider(true),ellipse);assert.equal(e.collider,ellipse);assert.ok(!e.components.includes(old));const revived=e.addComponent(new c.Collider2D('BoxCollider2D'));assert.equal(revived,old);assert.equal(revived.removed,false);assert.equal(revived.enabled,true);assert.equal(e.camera2D,null);assert.equal(e.script2D,null);const camera=e.addComponent(new c.Camera2D()),script=e.addComponent(new c.Script2D());assert.equal(e.camera2D,camera);assert.equal(e.script2D,script)})
 test('Boolean Entity aliases reject nonboolean values before modifying any component',['autoInertia','isSensor','isStatic','isKinematic'],/** 结构说明（自动提取）：test 回调；无显式参数；直接调用 p.serializeEntity、assert.throws、String、assert.deepEqual；包含循环处理。 */ ()=>{for(const field of ['autoInertia','isSensor','isStatic','isKinematic'])for(const invalid of [0,1,'false',null,undefined,{},[]]){const before=p.serializeEntity(e);assert.throws(/** 结构说明（自动提取）：assert.throws 回调；无显式参数；写入 e[…]。 */ ()=>{e[field]=invalid},/boolean/i,field+':'+String(invalid));assert.deepEqual(p.serializeEntity(e),before,field+' whole entity preserved')}})
 const triangle=new opened.modules.triangle.TriangleEntity(102,{x:0,y:0},{x:2,y:2});
 for(const invalid of [null,{},[null],[{x:NaN,y:1}],[{x:1,y:Infinity}],[{x:'2',y:1}],Array(3)]){const before=p.serializeEntity(triangle);assert.throws(/** 结构说明（自动提取）：assert.throws 回调；无显式参数；写入 triangle.vertices。 */ ()=>{triangle.vertices=invalid},/finite|vertices/i);assert.deepEqual(p.serializeEntity(triangle),before)}
 checks.push({name:'Triangle vertices reject malformed and nonfinite coordinates before either component changes',status:'passed',covers:['src/world/TriangleEntity.ts#TriangleEntity.vertices']});
 await audit.write(checks,'Direct public Entity forwarding, rejection and identity semantics. This does not qualify every raw component assignment or transient solver state.',{aliases:aliases.map(/** 结构说明（自动提取）：aliases.map 回调；输入 [field,,path]；直接调用 path.split、join、slice、map、checks.filter；返回表达式求值结果。 */ ([field,,path])=>({field,path,canonicalDeclaration:({renderer:'ShapeRenderer2D',rigidBody:'RigidBody2D',collider:'Collider2D'})[path.split('.')[0]],canonicalField:path.split('.').slice(1).join('.'),checks:checks.filter(/* 调用 check.covers.includes('src/world/Entity.ts#Entity.'+field) 并返回调用结果。 */ check=>check.covers.includes('src/world/Entity.ts#Entity.'+field)).map(/* 返回 check.name 的当前值。 */ check=>check.name)}))});console.log('PASS '+checks.length+' entity API cases')
}finally{await opened.close()}
