import { readFile, writeFile } from 'node:fs/promises'
import { dirname, join } from 'node:path'
import { fileURLToPath } from 'node:url'
const root=dirname(dirname(fileURLToPath(import.meta.url)))
const sample='// Layout changes must preserve this comment and the result.\nfn multiply(value) { value * 2 }\nfn start() {\n  let total = 0;\n  for row in 0..3 {\n    for column in 0..2 { total += multiply(row + column); }\n  }\n  print(total);\n}\n'
const lessons={
  en:{title:'26.13 — organize a graph and keep a readable workspace',intro:'Arrange the same nested-loop program in Code and Visual, then tailor the editor to the task. Graph layout changes positions and wire geometry; they must preserve generated program behavior. Panel sizing changes editor presentation and does not change game properties.',steps:[
    'Create a blank project and a script with the example below. Save, attach it through Script2D to an enabled scene object, and Play. The Console should print 18. Stop, switch to Visual and select the function and nested loops.',
    'Apply layout to the whole graph. Inspect the measured cards, pins and control regions. Select a subset and apply selected layout: unselected nodes must keep their positions. Pan and zoom, apply layout again, then Undo and Redo. Verify selection and viewport are retained and no node identity disappears.',
    'Search for total or multiply to navigate to a declaration or function. Use the graph keyboard controls and pin actions to make and cancel a connection. Select a wire, add a reroute point, move it and remove it. Save and reopen the graph; retained waypoints must keep their order. Code still prints18.',
    'Expand a long source-backed node when its text needs inspection, then compact it. Source-backed status remains visible; compacting does not turn text into typed syntax. For large graphs, cancel an arrangement in progress and verify the draft remains usable. A blocked wire route is a diagnostic to repair, not a silently missing connection.',
    'Drag a visible panel separator. Arrow keys resize it; Shift uses a larger step, Home/End use its bounds, double-click resets and Escape cancels a drag. Try a short window with a large bottom-dock preference: the first movement must start from its visible size. Collapse hierarchy and use its visible expansion rail.',
    'Maximize hierarchy, Inspector or bottom tools, then restore. Their mounted fields, saved sizes and docking choices remain available. Tab through the focused panel and ensure covered panels receive no focus. Repeat after floating a panel through Layout → Manage workspaces.',
    'Resize Design, switch to Script, then return: each preset remembers its own dimensions. In Manage workspaces, save a named custom layout. Change a size, select the saved row, and choose Apply workspace. Export/import layouts to test portability. User and project storage scopes stay separate; the custom-layout capacity is24.',
    'Repeat the task in English, German and Chinese, light/dark themes and100/150/200% scale. Fields must remain readable and reachable; use panel focus or scrolling where necessary. In Animation, typing or deleting in a text field must edit that field. In Event Sheets, cancel a document switch and verify the unsaved draft remains.'
  ],expected:'Expected result:18 before and after arrangement, selected arrangement, rerouting, Undo/Redo, save/reopen and export. Manual node positions outside a selected arrangement stay unchanged. Panel restore returns the previous dimensions; applying a named layout restores that layout. Record actual failures rather than assuming that a parseable graph or a contained page is usable.',limits:'Large graph measurements depend on hardware and graph shape. The release evidence records actual100/1,000/10,000-node timings and worker/fallback/cancellation results. A graph with arbitrary fixed obstacles or authored waypoints may have no valid route; the editor must report that case. Local browser accessibility-tree and focus checks do not certify physical assistive technology. The same Project Format2/schema29 and Graph Format1 remain in use; optional bounded reroute points are additive.'},
  de:{title:'26.13 — Graphen ordnen und einen lesbaren Arbeitsbereich behalten',intro:'Ordne dasselbe Programm mit verschachtelten Schleifen in Code und Visual und passe den Editor an die Aufgabe an. Graphlayout verändert Positionen und Leitungen, aber nicht das Programmverhalten. Bereichsgrößen betreffen die Darstellung des Editors, nicht die Spieleigenschaften.',steps:[
    'Erstelle ein leeres Projekt und ein Skript mit dem folgenden Beispiel. Speichere es, verknüpfe es über Script2D mit einem aktiven Objekt und starte Play. Die Konsole soll18 anzeigen. Stoppe und wechsle zu Visual; wähle Funktion und verschachtelte Schleifen.',
    'Ordne den gesamten Graphen an und prüfe gemessene Karten, Pins und Kontrollbereiche. Ordne danach nur ausgewählte Knoten an: alle anderen Positionen bleiben erhalten. Verschiebe und zoome die Ansicht, ordne erneut an und prüfe Rückgängig/Wiederholen. Auswahl, Ansicht und Knotenidentitäten bleiben erhalten.',
    'Suche total oder multiply und navigiere zur Deklaration/Funktion. Erstelle und annulliere eine Verbindung mit Tastatur und Pin-Aktionen. Füge einer gewählten Leitung einen Umlenkpunkt hinzu, verschiebe und entferne ihn. Speichere und öffne den Graphen erneut; verbliebene Punkte behalten ihre Reihenfolge. Das Programm zeigt weiterhin18.',
    'Erweitere einen langen Quelltextknoten zum Lesen und klappe ihn wieder kompakt. Die Kennzeichnung als Quelltext bleibt sichtbar; Kompaktansicht erzeugt keine typisierte Struktur. Brich eine laufende Anordnung eines großen Graphen ab und prüfe den Entwurf. Eine blockierte Leitungsroute muss eine reparierbare Diagnose bleiben.',
    'Ziehe einen sichtbaren Trenner. Pfeiltasten ändern die Größe; Umschalt vergrößert den Schritt, Pos1/Ende wählen die Grenzen, Doppelklick setzt zurück und Escape bricht Ziehen ab. Prüfe ein kurzes Fenster mit großer gespeicherter Werkzeughöhe: die Bewegung beginnt bei der sichtbaren Größe. Klappe die Hierarchie ein und nutze ihre sichtbare Öffnungsleiste.',
    'Maximiere Hierarchie, Inspector oder untere Werkzeuge und stelle sie wieder her. Felder, gespeicherte Größen und Dockposition bleiben erhalten. Tab darf keine verdeckten Bereiche fokussieren. Wiederhole dies nach dem schwebenden Andocken im Arbeitsbereichsmanager.',
    'Ändere Design, wechsle zu Skript und zurück: jede Vorlage merkt sich eigene Maße. Speichere einen benannten Arbeitsbereich, ändere die Größe und wähle danach die gespeicherte Zeile sowie Arbeitsbereich anwenden. Prüfe Export/Import. Benutzer- und Projektbereiche bleiben getrennt; höchstens24 eigene Layouts werden gespeichert.',
    'Wiederhole die Aufgabe auf Englisch, Deutsch und Chinesisch, hell/dunkel und bei100/150/200% Skalierung. Felder müssen lesbar und erreichbar sein. Tippen/Löschen in Animations-Textfeldern bearbeitet deren Inhalt. Brich einen Event-Sheet-Wechsel ab und prüfe, dass der ungespeicherte Entwurf erhalten bleibt.'
  ],expected:'Erwartet:18 vor und nach Anordnung, ausgewählter Anordnung, Umlenkpunkten, Rückgängig/Wiederholen, erneutem Öffnen und Export. Nicht ausgewählte Positionen bleiben erhalten. Wiederherstellen bringt die vorherigen Maße zurück; ein angewendetes benanntes Layout seine gespeicherten Maße. Erfasse echte Fehler statt aus gültiger Syntax auf gute Bedienbarkeit zu schließen.',limits:'Große Graphen hängen von Hardware und Struktur ab. Release-Nachweise enthalten gemessene100/1.000/10.000-Knoten-Werte sowie Worker-, Rückfall- und Abbruchtests. Feste Hindernisse oder vorgegebene Punkte können eine gültige Route verhindern; dies muss sichtbar gemeldet werden. Browser-Fokus und Accessibility-Baum bestätigen keine physischen Hilfstechnologien. Project Format2/Schema29 und Graph Format1 bleiben bestehen; begrenzte Umlenkpunkte sind eine optionale Ergänzung.'},
  'zh-CN':{title:'26.13 — 整理图结构并保持工作区可读',intro:'用代码和可视化编辑同一个嵌套循环程序，再根据任务调整编辑器。自动布局改变节点和连线的几何位置，不能改变程序行为。面板大小只影响编辑器显示，不改变游戏属性。',steps:[
    '创建空白项目，输入下方脚本并保存，通过 Script2D 绑定到启用的场景对象。播放后控制台应输出18。停止并切换到 Visual，选中函数与嵌套循环。',
    '先对全图自动布局，检查按实际尺寸排列的节点、引脚和控制区域。再只布局部分选中节点：未选中节点的位置必须保持。平移、缩放、再次布局，然后撤销和重做；选择、视口与节点身份应保留。',
    '搜索 total 或 multiply，定位声明或函数。使用键盘与引脚操作创建和取消连线。选中一条线，添加、移动和删除转折点。保存并重新打开图，保留的转折点顺序不应变化。程序仍输出18。',
    '展开较长的源码块阅读内容，再切换为紧凑显示。依赖源码的标记应保留，紧凑显示不等于完整语法结构。对大图运行布局并取消，确认草稿仍可用。无法通过障碍的线路应给出可修复的诊断，不能无声丢失连接。',
    '拖动可见分隔条。方向键调整大小，Shift 加大步长，Home/End 到达边界，双击重置，Escape 取消拖动。在较矮窗口中测试已保存的大底部面板：第一次拖动必须从当前可见大小开始。折叠层级面板，再使用可见展开条恢复。',
    '最大化层级、检查器或底部工具，再恢复。面板中的字段、原尺寸和停靠位置应保留。使用 Tab 检查焦点不能进入被遮挡面板。在布局管理器中将面板浮动后重复测试。',
    '调整设计工作区大小，切到脚本再返回；每个预设记住自己的尺寸。在管理工作区中保存命名布局，改变尺寸，再选中保存的行并点击应用工作区。测试布局导出与导入。用户和项目存储范围相互隔离；自定义布局上限为24个。',
    '在英语、德语、中文，浅色/深色和100/150/200%缩放下重复。字段应可读且可访问，需要时使用面板最大化或滚动。在动画文本框中输入或删除只能编辑文本。在事件表切换时选择取消，确认未保存草稿保留。'
  ],expected:'布局、选区布局、修改转折点、撤销/重做、保存重开与导出前后均应输出18。选区外手动位置不变。恢复面板返回原尺寸，应用命名布局返回该布局的保存尺寸。记录真正的故障，不能用语法正确或页面没有溢出来代替可用性测试。',limits:'大图性能取决于硬件与图形结构；发布证据记录100/1,000/10,000节点实测以及工作线程、回退和取消测试。固定障碍或指定转折点可能使线路无解，编辑器必须明确说明。浏览器焦点和无障碍树测试不能证明物理辅助技术兼容。继续使用Project Format2/schema29与Graph Format1；有数量边界的转折点属于可选附加数据。'}
}
const escape=value=>value.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;')
let html=await readFile(join(root,'manual/index.html'),'utf8'),supplement='<!-- NOVA_V2613_START -->\n<div class="release-supplement">'
for(const [language,lesson]of Object.entries(lessons)){
  const path=join(root,'manual','MANUAL.'+language+'.md'),before=await readFile(path,'utf8')
  const chapter='<!-- NOVA_V2613_START -->\n## '+lesson.title+'\n\nEngine: **26.13.0** · Project Format2/schema29 · Graph Format1.\n\n'+lesson.intro+'\n\n'+lesson.steps.map((step,index)=>(index+1)+'. '+step).join('\n\n')+'\n\n```rhai\n'+sample+'\n```\n\n'+lesson.expected+'\n\n'+lesson.limits+'\n<!-- NOVA_V2613_END -->\n'
  const updated=before.replace(/^# Nova_A 26\.\d+/,'# Nova_A 26.13').replace(/<!-- NOVA_V2613_START -->[\s\S]*?<!-- NOVA_V2613_END -->\r?\n?/,'')
  const split=updated.indexOf('\n');await writeFile(path,updated.slice(0,split)+'\n\n'+chapter+'\n'+updated.slice(split+1).replace(/^\s*\n/,''))
  supplement+='<article data-lang="'+language+'"><section id="'+language+'-v2613-layout"><h2>'+escape(lesson.title)+'</h2><p>Engine 26.13.0 · Project Format 2/schema 29 · Graph Format 1.</p><p>'+escape(lesson.intro)+'</p><ol>'+lesson.steps.map(step=>'<li>'+escape(step)+'</li>').join('')+'</ol><pre><code>'+escape(sample)+'</code></pre><p>'+escape(lesson.expected)+'</p><p>'+escape(lesson.limits)+'</p></section></article>'
}
supplement+='</div>\n<!-- NOVA_V2613_END -->\n'
html=html.replace(/<!-- NOVA_V2613_START -->[\s\S]*?<!-- NOVA_V2613_END -->\r?\n?/,'').replace(/<title>Nova_A 26\.\d+ Manual<\/title>/,'<title>Nova_A 26.13 Manual</title>').replace(/26\.\d+ Offline Teaching Manual/g,'26.13 Offline Teaching Manual').replace(/(<meta name="description" content="Complete Nova_A )26\.\d+( \(engine )26\.\d+\.0/,'$126.13$226.13.0')
const at=html.indexOf('<script>');if(at<0)throw Error('Manual controller not found')
await writeFile(join(root,'manual/index.html'),html.slice(0,at)+supplement+html.slice(at))
console.log('Added the 26.13 layout task to all four localized offline manuals; actual UI and runtime checks remain separate.')
