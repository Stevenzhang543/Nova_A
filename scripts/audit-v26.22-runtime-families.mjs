import assert from 'node:assert/strict'
import {readFile,writeFile} from 'node:fs/promises'
import {createHash} from 'node:crypto'
const specs=[
 ['rhai-dynamic-values','reports/v26.22-traced-api-signatures.json',['isolated actual WASM: reflection and Fn/curry/call intrinsics','isolated actual WASM: string, map, array and iterator packages','isolated actual WASM: blob and JSON standard packages'],'Actual shipped WASM dynamic-value/package execution; no GDScript/C# compatibility claim.'],
 ['rhai-closures-and-mutation','reports/v26.22-traced-language.json',['arrays, maps and compound writes: independent structural emission preserves WASM observable result','captured closure: independent structural emission preserves WASM observable result'],'Original and structurally emitted scripts produce the asserted observable WASM results.'],
 ['structural-graph-editing','reports/v26.22-traced-syntax-slots.json',['Function parameter reordering changes execution order while retaining stable slot and pin IDs','Array slot reorder/removal changes values and removes only the removed pin connections','Block statement order and removal change the executed sequence; serialization preserves undo snapshots'],'Actual slot mutations, serialized graph restoration and VM execution. Browser wiring geometry remains separate.'],
 ['script-modules-and-reload','reports/v26.22-traced-script-modules.json',['production text runtime resolves and executes project helper','production visual runtime uses identical module resolution and VM behavior without changing debug state','failed override preserves the prior stored playable source'],'Production resolver and runtime with real WASM, graph compiler and AssetDatabase; unrelated subsystem fixtures remain explicit.'],
 ['resource-inheritance','reports/v26.22-traced-assets.json',['Inherited active variants survive save/reload and unresolved selections cannot mutate saved source'],'Actual inherited variant resolution merges parent and child token values; invalid selection preserves stored source.'],
 ['blueprint-instantiation','release-audits/v26.22-script-assets-corpus.json',['Actual blueprint instantiation applies required components, tags, groups, position and event binding'],'Actual instantiated entity components/metadata/position and event reference. This is not complete event-dispatch or inheritance coverage.'],
 ['physics-live-values','reports/v26.22-bindings.json',['Every scalar body/material/primary collider ABI binding accepts edits after body creation with the same native handle','Joint and rope property edits reach retained connection records without stale rope data overwriting joint limits'],'Native body, primary collider and connection updates after creation.'],
 ['physics-queries','reports/v26.22-queries.json',['Live transform, layer, sensor and enabled edits reach existing native handles without invalidation','Real TileMap collision bodies return their scene owner and support exclusions'],'Live facade queries, tile collision identity and filters.'],
 ['navigation','reports/v26.22-navigation.json',['Every grid algorithm routes around real obstacles; polygon visibility uses the same geometry','Agent radius, authored radius, transforms, masks and disabled regions invalidate cached paths','Finished and unreachable navigation agents stop the actual simulation velocity'],'Path algorithms, live authored cache invalidation and agent stopping.'],
 ['ai','reports/v26.22-bindings.json',['AI blackboard writes persist across ticks and authored revision/retirement resets Wait and overrides','Perception uses world units, parent rotation, tags, deterministic distance ties and bounded results'],'Blackboard, task revision/lifetime and perception.'],
 ['world-streaming','reports/v26.22-bindings.json',['Reactive scene hosts preserve shared leases, persistent descendants and lifetime generations across unload/reload','Scene transition aligns new entities with shifted persistent objects and preserves origin-relative connections'],'Live scene leases, persistence and transition placement.'],
 ['gameplay-controller','reports/v26.22-bindings.json',['Top-down component input accelerates once per fixed tick, stops after release and respects collisions'],'Top-down controller only; other gameplay component semantics require their own cases.'],
 ['animation','reports/v26.22-animation-audio.json',['Direct clip seek evaluates actual numeric pose without crossed callbacks','Step keys switch at interior boundaries and Auto tangents use adjacent slopes','Explicit root-motion extraction covers multiple and reverse loops without absolute teleport','Looping animator reaches its exit-time-one transition'],'Pose sampling, interpolation, root motion and controller transitions.'],
 ['rigging','reports/v26.22-animation-audio.json',['Rotation limits remain authoritative after inverse kinematics','Retargeting an unchanged rest pose preserves the target rest transform','Rig skin deformation preserves mirrored bind geometry and follows translated root pose'],'Rig constraints, retargeting and skin deformation.'],
 ['timeline','reports/v26.22-animation-audio.json',['Timeline emits every crossed occurrence over multiple loops','Timeline samples assigned animation asset with local offset and rate','Reverse timeline dispatch order follows traversal and explicit seek dispatches nothing'],'Crossed callback ordering, nested sampling and traversal.'],
 ['timeline-ui-actions','reports/v26.22-timeline-actions.json',['Explicit play evaluates initial subtitle, pause freezes real timeline, and play resumes','Skip and resume reconstruct real captions and marker time'],'Actual UI-triggered timeline action behavior.'],
 ['audio','reports/v26.22-animation-audio.json',['Regular source and polyphonic clocks follow scaled transport, manual pause, seek and trim end','Solo nested bus keeps its ancestors audible and unrelated sibling silent','Bus automation holds its end unless an explicit loop duration is set'],'Executed audio runtime with instrumented media elements; physical sound/device acceptance is not implied.'],
 ['ui-layout','reports/v26.22-interface.json',['Partial stretch anchors independently resolve both axes and inset offsets','Fixed pixels, responsive scale, breakpoints and safe area use the same geometry','Wrapped Flow uses authored widths, line breaks and spacing deterministically'],'Resolved UI layout geometry; separate browser tests inspect actual editor panels.'],
 ['ui-input','reports/v26.22-interface.json',['Checkbox requires pointer release inside; cancellation and outside release remain inert','Slider pointer and keyboard emit changed-value callbacks once and clamp repeat input','Keyboard text focus creates native-input ownership and commits values atomically'],'Game UI activation, slider callbacks and native text ownership.'],
 ['localization-theme','reports/v26.22-interface.json',['Child theme omission preserves parent tokens, variables and every state','Multi-hop locale fallback and Canvas locale override resolve actual text and direction'],'Theme inheritance and resolved localized text.'],
 ['network','reports/v26.22-networking.json',['Incoming snapshots obey the authored property allowlist','Explicit live owner edits reach ownership while unrelated property edits preserve transfers','RPC contracts added during a live session reach gameplay listeners'],'Authored replication policy, ownership and live RPC contract binding.'],
 ['renderer-material-particle','release-audits/v26.22-renderer-development/renderer-staged.json',['actual GLSL material changes pixels and invalid shader uses visible base fallback','actual particle simulation produces matching visible pixels in both renderer backends','normal-map lighting follows sprite flips and local rotation'],'Actual pixel assertions on the recorded browser/software graphics context; no native pixel equivalence or universal FPS claim.']
]
const families=[];for(const [id,path,names,scope]of specs){const bytes=await readFile(path),report=JSON.parse(bytes);assert.equal(report.status,'passed',path);const checks=names.map(name=>{const check=report.checks.find(row=>(row.name??row.id)===name);assert.equal(check?.status,'passed',name);return name});families.push({id,scope,evidence:{path,sha256:createHash('sha256').update(bytes).digest('hex'),generatedAt:report.generatedAt,checks}})}
await writeFile('reports/v26.22-runtime-family-evidence.json',JSON.stringify({release:'26.22',development:true,status:'partial',scope:'Links executed effect assertions, independently of serialized-field retention. This is not a per-field completeness pass. Unlisted executable families and additional input domains remain pending.',families},null,2)+'\n');console.log('Linked actual effect evidence for '+families.length+' named families')
