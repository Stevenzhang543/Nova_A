/** 26.27 同设备测量：隔离保存版本，记录实际帧回调 CPU/间隔、页面生命周期和资源计数。 */
import assert from 'node:assert/strict'
import {readFile,writeFile,mkdir,readdir,cp,copyFile,access} from 'node:fs/promises'
import {resolve,join,relative} from 'node:path'
import {createHash} from 'node:crypto'
import {cpus,platform,arch,totalmem} from 'node:os'
import {execFile} from 'node:child_process'
import {promisify} from 'node:util'
import {withBrowserAudit,wait} from './lib/browserUserAudit.mjs'
import {worldUserControls17} from './lib/worldAudit17.mjs'
const quiet=process.argv.includes('--baseline-quiet'),smoke=process.argv.includes('--smoke'),phase=quiet?'baseline-quiet':process.argv.includes('--baseline')?'baseline':'after',release=phase.startsWith('baseline')?'26.26':'26.27'
const runLabel=process.argv.find(/** 读取不可变结果运行标签，避免开发测量覆盖正式证据。 */ value=>value.startsWith('--run-label='))?.split('=')[1]??'final';assert.match(runLabel,/^[a-z0-9-]+$/)
const suffix=phase==='after'?phase+'-'+runLabel:phase,base=resolve(`.cache/performance-26.27/${phase.startsWith('baseline')?'baseline-26.26':suffix+'-'+release}`),result=resolve(`release-audits/v26.27-performance-${suffix}.json`)
/** 明确拒绝覆盖已完成测量，失败尝试由浏览器审计另外保留。 */ async function absent(path){try{await access(path);throw Error('Refusing to overwrite existing measurement: '+path)}catch(error){if(error.code!=='ENOENT')throw error}}
await absent(result)
if(phase==='after'){
 await absent(base);await mkdir(base,{recursive:true});await cp('dist',join(base,'dist'),{recursive:true});await copyFile('package.json',join(base,'package.json'));await copyFile('.cache/performance-26.27/baseline-26.26/scene.nova',join(base,'scene.nova'))
 await promisify(execFile)(process.execPath,['scripts/nova-export.mjs','--project',join(base,'scene.nova'),'--target','web','--output',join(base,'dist/game'),'--profile','release','--architecture','x86_64','--runtime','game','--compression','store','--template','web-es2022-v1','--no-patch','--dist',join(base,'dist')],{windowsHide:true,maxBuffer:4*1024*1024})
}
assert.equal(JSON.parse(await readFile(join(base,'package.json'),'utf8')).version,release+'.0')
/** 对隔离构建的所有实际发布字节计算稳定清单，不依赖工作树版本标签。 */ async function hashes(directory){const out=[];for(const entry of await readdir(directory,{withFileTypes:true})){const path=join(directory,entry.name);if(entry.isDirectory())out.push(...await hashes(path));else out.push({path:relative(base,path).replaceAll('\\','/'),sha256:createHash('sha256').update(await readFile(path)).digest('hex')})}return out.sort(/** 按路径稳定排序，便于两次测量追溯。 */ (a,b)=>a.path.localeCompare(b.path))}
const build=await hashes(join(base,'dist')),sceneHash=createHash('sha256').update(await readFile(join(base,'scene.nova'))).digest('hex'),observations=[],checks=[]
const baselineBytes=phase==='after'?await readFile('release-audits/v26.27-performance-baseline-quiet.json'):null,baselineData=baselineBytes?JSON.parse(baselineBytes.toString()):null
// 此注入只观察浏览器调度边界，不访问或改变项目数据。所有测量窗口采用同一探针开销。
const probe=`(()=>{
 const p={frames:[],last:null,objects:0,workers:0,terminated:0};window.__novaPerf27=p;
 const raf=window.requestAnimationFrame;window.requestAnimationFrame=function(callback){return raf.call(this,function(time){const start=performance.now();try{return callback(time)}finally{let frame=p.frames[p.frames.length-1];if(!frame||frame.time!==time){frame={time,cpuMs:0,intervalMs:p.last===null?null:time-p.last};p.last=time;p.frames.push(frame);if(p.frames.length>12000)p.frames.shift()}frame.cpuMs+=performance.now()-start}})};
 const create=URL.createObjectURL,revoke=URL.revokeObjectURL,urls=new Set();URL.createObjectURL=function(...args){const url=create.apply(this,args);urls.add(url);p.objects=urls.size;return url};URL.revokeObjectURL=function(url){urls.delete(url);p.objects=urls.size;return revoke.call(this,url)};
 const WorkerBase=window.Worker;if(WorkerBase){window.Worker=class extends WorkerBase{constructor(...args){super(...args);p.workers++}terminate(){if(!this.__novaTerminated){this.__novaTerminated=true;p.terminated++}return super.terminate()}}}
})()`
// 仅新版增加定时器所有权计数；旧版未采该指标，比较报告明确说明这部分探针差异。
const timerProbe=phase==='after'?`(()=>{const p=window.__novaPerf27;p.timeouts=new Set();p.intervals=new Set();const timeout=window.setTimeout.bind(window),interval=window.setInterval.bind(window),clearT=window.clearTimeout.bind(window),clearI=window.clearInterval.bind(window);window.setTimeout=function(fn,delay,...args){if(typeof fn!=='function')return timeout(fn,delay,...args);let id;id=timeout(function(){p.timeouts.delete(id);return fn.apply(window,args)},delay);p.timeouts.add(id);return id};window.setInterval=function(fn,delay,...args){const id=interval(fn,delay,...args);p.intervals.add(id);return id};window.clearTimeout=function(id){p.timeouts.delete(id);p.intervals.delete(id);return clearT(id)};window.clearInterval=function(id){p.timeouts.delete(id);p.intervals.delete(id);return clearI(id)}})()`:''
await withBrowserAudit({root:base,release,name:'performance-'+suffix,width:1400,height:900,commandTimeoutMs:60000},/** 同一浏览器实例按固定顺序测编辑器、生命周期和导出游戏。 */ async a=>{
 await a.client.send('Page.addScriptToEvaluateOnNewDocument',{source:probe+';'+timerProbe});const origin=await a.evaluate('location.origin');await a.client.send('Page.navigate',{url:origin+'/'});await a.until("!!document.querySelector('.project-manager')")
 const u=worldUserControls17(a);await a.client.send('Performance.enable')
 /** 读取浏览器真实累计 CPU 和堆/节点/监听器计数。 */ async function metrics(){const response=await a.client.send('Performance.getMetrics');return Object.fromEntries(response.metrics.filter(/** 只保留可比较的调度及内存计数器。 */ item=>['JSHeapUsedSize','JSHeapTotalSize','Nodes','JSEventListeners','Documents','TaskDuration','ScriptDuration','LayoutDuration'].includes(item.name)).map(/** 将协议指标转为稳定键值映射。 */ item=>[item.name,item.value]))}
 /** 仅观察实际渲染画布的分辨率、API 与抗锯齿状态。 */ async function canvas(){return a.evaluate(`(()=>{const c=document.querySelector('.render-canvas');if(!c)return null;const gl=c.getContext('webgl2'),ext=gl?.getExtension('WEBGL_debug_renderer_info');return{width:c.width,height:c.height,cssWidth:c.getBoundingClientRect().width,cssHeight:c.getBoundingClientRect().height,dpr:devicePixelRatio,backend:gl?'WebGL2':'Canvas2D',antialias:gl?.getContextAttributes()?.antialias??null,defaultFramebufferSamples:gl?.getParameter(gl.SAMPLES)??null,renderer:ext?gl.getParameter(ext.UNMASKED_RENDERER_WEBGL):null,gpuTiming:'not measured; no disjoint-query results collected'}})()`)}
 /** 在真实墙钟窗口内汇总实际应用 rAF 回调成本和交付间隔，不把它们冒充 GPU 或呈现耗时。 */ async function measure(name,duration){
  await a.evaluate('window.__novaPerf27.frames=[];window.__novaPerf27.last=null');const begin=await metrics(),started=Date.now();await wait(duration);const end=await metrics(),frames=await a.evaluate('window.__novaPerf27.frames'),resources=await a.evaluate('({objectURLs:window.__novaPerf27.objects,workers:window.__novaPerf27.workers,terminated:window.__novaPerf27.terminated,visibility:document.visibilityState,timeouts:window.__novaPerf27.timeouts?.size??null,intervals:window.__novaPerf27.intervals?.size??null,liveDomNodes:document.getElementsByTagName("*").length})')
  /** 对实际有限样本计算分位数；隐藏零回调窗口明确返回空。 */ function stats(values){const sorted=values.filter(/** 排除首次间隔空值和非有限读数。 */ value=>typeof value==='number'&&Number.isFinite(value)).sort(/** 数值升序用于分位。 */ (a,b)=>a-b);return {samples:sorted.length,median:sorted.length?sorted[Math.floor((sorted.length-1)*.5)]:null,p95:sorted.length?sorted[Math.floor((sorted.length-1)*.95)]:null,p99:sorted.length?sorted[Math.floor((sorted.length-1)*.99)]:null}}
  const item={name,durationMs:Date.now()-started,callbackCpuMs:stats(frames.map(/** 读取一帧所有应用回调的累计 CPU 时间。 */ frame=>frame.cpuMs)),deliveredFrameIntervalMs:stats(frames.map(/** 读取实际相邻浏览器帧时间戳差。 */ frame=>frame.intervalMs)),taskCpuMs:(end.TaskDuration-begin.TaskDuration)*1000,scriptCpuMs:(end.ScriptDuration-begin.ScriptDuration)*1000,layoutCpuMs:(end.LayoutDuration-begin.LayoutDuration)*1000,begin,end,resources,canvas:await canvas()};if(phase==='after'&&name.startsWith('foreground-design-idle-')){await a.client.send('HeapProfiler.collectGarbage');item.retainedAfterGC=await metrics()}observations.push(item);console.log(JSON.stringify({name,durationMs:item.durationMs,callbackCpuMs:item.callbackCpuMs,interval:item.deliveredFrameIntervalMs,taskCpuMs:item.taskCpuMs,heap:end.JSHeapUsedSize,listeners:end.JSEventListeners}));return item
 }
 /** 通过实际 File/Open 在同一页面内切换项目，以观察资源释放；不使用会重置所有状态的刷新。 */ async function openInPlace(file){
  const expected=JSON.parse(await readFile(file,'utf8')).projectMetadata.name,identity=await a.evaluate('performance.timeOrigin');let chooser
  const off=a.client.on('Page.fileChooserOpened',/** 捕获真实文件选择器对应输入。 */ value=>chooser=value)
  await a.client.send('Page.setInterceptFileChooserDialog',{enabled:true});await a.click('.menu-item>button',0);await a.until("!!document.querySelector('.menu-item .dropdown button')");await a.click('.menu-item .dropdown button',2)
  for(let count=0;!chooser&&count<100;count++)await wait(50)
  assert.ok(chooser,'File/Open chooser');await a.client.send('DOM.setFileInputFiles',{files:[file],backendNodeId:chooser.backendNodeId});off();await wait(200)
  if(await a.evaluate("!!document.querySelector('.confirm-card')"))await a.click('.confirm-actions button',1)
  if(await a.evaluate("!!document.querySelector('.upgrade-dialog')"))await a.click('.upgrade-dialog footer button.primary')
  await a.until(`document.querySelector('.context-title')?.textContent.includes(${JSON.stringify(expected)})`,30000);await wait(350);assert.equal(await a.evaluate('performance.timeOrigin'),identity,'Project switch must not reload browser')
 }
 const cold=Date.now();await u.open(join(base,'scene.nova'));await a.until("!!document.querySelector('.render-canvas')");observations.push({name:'cold-project-design-open',wallMs:Date.now()-cold,canvas:await canvas()});await wait(3000)
 await a.click('.actionbar button',0);await wait(3000);await measure('editor-playing',smoke?2000:15000);await a.capture('editor-playing');await a.click('.actionbar button',3)
 for(let pass=0;pass<3;pass++){await u.workspace('Script');const started=Date.now();await u.workspace('Design');await a.until("document.querySelector('.render-canvas').getBoundingClientRect().width>0");observations.push({name:pass===0?'first-design-return':'warm-design-return',pass,wallMs:Date.now()-started,canvas:await canvas()})}
 await u.workspace('Script');await measure('script-stopped-idle',smoke?2000:30000);await u.workspace('Design')
 // 新版空闲窗口开始前清理先前导航已失去引用的对象，避免把GC下降误判为空闲泄漏。
 if(phase==='after')await a.client.send('HeapProfiler.collectGarbage')
 if(!quiet)for(let pass=0;pass<10;pass++)await measure('foreground-design-idle-'+pass,smoke?500:30000)
 if(!quiet){const {targetInfo}=await a.client.send('Target.getTargetInfo'),other=await a.client.send('Target.createTarget',{url:'about:blank',newWindow:false});await a.client.send('Target.activateTarget',{targetId:other.targetId});await a.until('document.visibilityState==="hidden"');await measure('background-idle',smoke?1000:30000);await a.client.send('Target.activateTarget',{targetId:targetInfo.targetId});await a.until('document.visibilityState==="visible"');await a.client.send('Target.closeTarget',{targetId:other.targetId});const resumed=Date.now();await u.workspace('Script');await u.workspace('Design');observations.push({name:'resume-input-navigation',wallMs:Date.now()-resumed});await measure('resumed-editor',smoke?1000:5000)}
 for(let pass=0;pass<3;pass++){await openInPlace(resolve('reference-projects/projects/creator-v2624-code-game/project.nova'));await openInPlace(join(base,'scene.nova'));await wait(1000);if(phase==='after')await a.client.send('HeapProfiler.collectGarbage');observations.push({name:'repeated-project-switch',pass,metrics:await metrics(),resources:await a.evaluate('({objects:window.__novaPerf27.objects,workers:window.__novaPerf27.workers,terminated:window.__novaPerf27.terminated,timeouts:window.__novaPerf27.timeouts?.size??null,intervals:window.__novaPerf27.intervals?.size??null,liveDomNodes:document.getElementsByTagName("*").length})')})}
 if(phase==='after')await a.check('Same-page switch resources remain bounded and resume responds',/** 基线决定 URL/Worker/监听器容差；堆允许垃圾回收波动，定时器只做新版内部比较。 */ ()=>{
  const series=observations.filter(/** 只比较同一页面反复切换后的稳定场景。 */ item=>item.name==='repeated-project-switch'),prior=baselineData.observations.filter(/** 安静旧版基线采用相同页面切换流程。 */ item=>item.name==='repeated-project-switch');assert.equal(series.length,3);assert.equal(prior.length,3)
  /** 取首末差值，避免将累计构造数量误当存活数量。 */ const growth=(rows,read)=>read(rows.at(-1))-read(rows[0])
  const limits={objectURLs:Math.max(2,growth(prior,/** 对象地址未撤销数量。 */ row=>row.resources.objects)+2),workers:Math.max(1,growth(prior,/** 显式终止后的工作线程代理数。 */ row=>row.resources.workers-row.resources.terminated)+1),listeners:Math.max(32,growth(prior,/** CDP监听器数可能包括等待回收对象。 */ row=>row.metrics.JSEventListeners)+8),heapBytes:Math.max(32*1048576,growth(prior,/** 采用有界余量容忍非强制垃圾回收的堆波动。 */ row=>row.metrics.JSHeapUsedSize)+16*1048576),timeouts:4,intervals:2,liveDomNodes:20}
  const actual={objectURLs:growth(series,/** 实际对象URL净增长。 */ row=>row.resources.objects),workers:growth(series,/** 实际显式工作线程净增长。 */ row=>row.resources.workers-row.resources.terminated),listeners:growth(series,/** 实际CDP监听器增长。 */ row=>row.metrics.JSEventListeners),heapBytes:growth(series,/** 实际堆增长，不宣称全部对象已GC。 */ row=>row.metrics.JSHeapUsedSize),timeouts:growth(series,/** 未到期且未取消的一次性定时器增长。 */ row=>row.resources.timeouts),intervals:growth(series,/** 未清除周期定时器增长。 */ row=>row.resources.intervals),liveDomNodes:growth(series,/** 实际挂载DOM数量增长。 */ row=>row.resources.liveDomNodes)}
  for(const name of Object.keys(limits))assert.ok(actual[name]<=limits[name],name+' growth '+actual[name]+' exceeds '+limits[name])
  const resumed=observations.find(/** 读取实际前后台恢复后的导航时间。 */ item=>item.name==='resume-input-navigation');assert.ok(resumed&&resumed.wallMs<15000,'Resume navigation under15s including transport/settle')
  observations.push({name:'bounded-lifecycle-assertions',actual,limits,basis:'Same source project and same-page cycles; old measured growth plus small tolerance, heap minimum32MiB for GC churn. Timer bounds are after-only. After switch samples force GC; old raw samples did not. This is a bounded regression check, not absence-of-all-leaks proof.'});checks.push({name:'same-page resource growth and responsive resume',status:'passed'})
 })
 await a.client.send('Page.navigate',{url:origin+'/game/index.html'});await a.until("!!document.querySelector('.player-root .render-canvas')",30000);await wait(3000);await measure('exported-player-playing',smoke?2000:15000);await a.capture('exported-player');assert.equal(await a.evaluate("!!document.querySelector('.fault-overlay,.player-error')"),false)
 if(phase==='after')await a.check('Foreground idle live resources remain bounded for the measured duration',/** 不加速计时，只检查已有十个实际空闲窗口的挂载资源和原始监听器波动。 */ ()=>{const rows=observations.filter(/** 选择实际前台空闲窗口。 */ item=>item.name.startsWith('foreground-design-idle-'));for(const [name,read,budget] of [['live DOM',/** DOM节点不应随空闲窗口持续累积。 */ row=>row.resources.liveDomNodes,32],['listeners',/** 硬断言使用窗口结束后明确GC的保留监听器，原始数据仍完整保存。 */ row=>row.retainedAfterGC.JSEventListeners,100],['object URLs',/** 空闲不应累积未撤销地址。 */ row=>row.resources.objectURLs,2],['timeouts',/** 有界异步操作允许少量临时任务。 */ row=>row.resources.timeouts,4],['intervals',/** 定时轮询所有者应保持稳定。 */ row=>row.resources.intervals,2]]){const values=rows.map(read);assert.ok(Math.max(...values)-Math.min(...values)<=budget,name+' idle growth');}checks.push({name:'foreground idle live resource bounds',status:'passed',actualWallMs:rows.reduce(/** 累加已发生的真实测量窗口。 */ (sum,row)=>sum+row.durationMs,0)})})
 a.observations.push(...observations);await a.check('Measured real editor/player and lifecycle windows completed',/** 要求实际五分钟前台观测及前后台、项目切换、导出游戏测量齐全。 */ ()=>{assert.equal(observations.filter(/** 统计实际前台等待窗口；安静短基线不重复五分钟。 */ item=>item.name.startsWith('foreground-design-idle-')).length,quiet?0:10);assert.ok(observations.find(/** 找到实际播放器样本。 */ item=>item.name==='exported-player-playing').callbackCpuMs.samples>0);checks.push({name:'actual editor/player timing windows and lifecycle sequence completed',status:'passed'})})
})
/** 生成同场景实测差值；短开发窗口或画布差异必须明确标记不可直接比较。 */
function comparisons(){if(!baselineData)return null;assert.equal(sceneHash,baselineData.sceneHash,'Comparison uses identical project bytes');assert.equal(cpus()[0]?.model,baselineData.hardware.cpu,'Comparison CPU identity');assert.equal(platform(),baselineData.hardware.platform,'Comparison host platform');return ['editor-playing','script-stopped-idle','exported-player-playing'].map(/** 将旧版安静窗口和新版窗口逐项配对，保留绝对值与变化百分比。 */ name=>{const before=baselineData.observations.find(/** 选取同名旧版实测窗口。 */ item=>item.name===name),after=observations.find(/** 选取同名新版实测窗口。 */ item=>item.name===name);const sameCanvas=JSON.stringify(before.canvas)===JSON.stringify(after.canvas),sameDuration=Math.abs(after.durationMs-before.durationMs)<=Math.max(1000,before.durationMs*.1);return{name,comparable:!smoke&&sameCanvas&&sameDuration,sameDuration,beforeDurationMs:before.durationMs,afterDurationMs:after.durationMs,sameCanvas,taskCpuMsPerSecond:{before:before.taskCpuMs/before.durationMs*1000,after:after.taskCpuMs/after.durationMs*1000,percentChange:(after.taskCpuMs/after.durationMs/(before.taskCpuMs/before.durationMs)-1)*100},callbackCpuMs:{before:before.callbackCpuMs,after:after.callbackCpuMs},deliveredFrameIntervalMs:{before:before.deliveredFrameIntervalMs,after:after.deliveredFrameIntervalMs},limitations:smoke?'Development smoke duration differs; not a performance conclusion.':'Same fixed project, viewport and host; timer instrumentation exists only after; software renderer and OS scheduling remain limitations.'}})}
/** 配对实际首次和暖返回导航时间；包含固定自动化等待，不当作物理输入延迟。 */
function transitionComparisons(){if(!baselineData)return null;return observations.filter(/** 只选择实际项目首次打开和Design返回记录。 */ item=>['cold-project-design-open','first-design-return','warm-design-return'].includes(item.name)).map(/** 按窗口名称和重复序号配对旧版记录。 */ after=>{const before=baselineData.observations.find(/** 保持首次与每次暖返回顺序一致。 */ item=>item.name===after.name&&item.pass===after.pass);return{name:after.name,pass:after.pass??null,beforeMs:before?.wallMs??null,afterMs:after.wallMs,deltaMs:before?after.wallMs-before.wallMs:null,scope:'UI automation wall time including transport and350ms settle; not physical input latency.'}})}
await mkdir('release-audits',{recursive:true});await writeFile(result,JSON.stringify({format:'nova-v26.27-performance-measurement',version:1,release,status:'passed',checks,comparison:comparisons(),designTransitions:transitionComparisons(),phase,runLabel,smoke,baseline:baselineData?{sha256:createHash('sha256').update(baselineBytes).digest('hex'),report:baselineData}:null,engineVersion:release+'.0',generatedAt:new Date().toISOString(),sceneHash,viewport:{width:1400,height:900,dpr:1},hardware:{platform:platform(),arch:arch(),cpu:cpus()[0]?.model,logicalCPUs:cpus().length,totalMemory:totalmem()},scope:'Actual Edge headless SwiftShader on this Windows host. rAF callback CPU and delivered frame intervals exclude GPU/presentation completion; CDP task CPU includes browser tasks. No physicalGPU/oldPC claim. Full runs: five real foreground minutes plus30sScript/30sbackground. --smoke uses explicitly shorter windows; baseline-quiet omits repeated idle/background and measures active/Script/player and in-place switches. Transition wallMs includes350ms automation settle, not physical input-to-pixel latency. After switch samples and outside each foreground idle timing window explicitly collect garbage; raw end counters remain preserved; baseline counters are raw and include pending GC. After-only timer instrumentation adds small scheduling-bookkeeping overhead; baseline has no timer counts. Initial baseline was partly concurrent with development tools; quiet baseline is uncontended and uses in-place switches. Fixed scene bytes and quality; lifecycle counters are observations not proof all allocations collected.',build,observations},null,2)+'\n',{flag:'wx'});console.log('Saved immutable '+result)
