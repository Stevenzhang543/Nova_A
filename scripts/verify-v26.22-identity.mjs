/** 功能回归脚本：执行 verify-v26.22-identity.mjs 对应场景，保留断言和证据输出。 */
import assert from 'node:assert/strict'
import {readFile} from 'node:fs/promises'
import vm from 'node:vm'
import ts from 'typescript'
import {webcrypto} from 'node:crypto'
import {propertyAudit22} from './lib/propertyAudit22.mjs'
const audit=await propertyAudit22('identity'),checks=[],source=await readFile('src/world/identity.ts','utf8'),code=ts.transpileModule(source,{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2020}}).outputText;
const pattern=/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/;
/** 结构说明（自动提取）：load；输入 crypto、random；直接调用 Object.assign、Object.create、vm.runInNewContext；返回路径包含 context.exports。 */ function load(crypto,random){const context={exports:{},...(crypto===undefined?{}:{crypto}),...(random?{Math:Object.assign(Object.create(Math),{random})}:{})};vm.runInNewContext(code,context,{filename:'src/world/identity.ts',timeout:1000});return context.exports}
/** 结构说明（自动提取）：test；输入 name、covers、fn；直接调用 fn、checks.push、covers.map、console.log。 */ function test(name,covers,fn){fn();checks.push({name,status:'passed',covers:covers.map(/* 计算表达式 'src/world/identity.ts#'+name 并返回结果，沿用操作数的原有类型规则。 */ name=>'src/world/identity.ts#'+name)});console.log('PASS '+name)}
test('Native crypto UUID creation emits valid distinct identifiers in a bounded sample',['createUuid'],/** 结构说明（自动提取）：test 回调；无显式参数；直接调用 load、Array.from、assert.match、assert.equal、Set；包含循环处理。 */ ()=>{const api=load(webcrypto),ids=Array.from({length:1000},/* 调用 api.createUuid() 并返回调用结果。 */ ()=>api.createUuid());for(const id of ids)assert.match(id,pattern);assert.equal(new Set(ids).size,ids.length)});
test('UUID normalization preserves all currently supported version and variant combinations',['normalizeUuid'],/** 结构说明（自动提取）：test 回调；无显式参数；直接调用 load、assert.equal、api.normalizeUuid、id.toUpperCase；包含循环处理。 */ ()=>{let created=0;const api=load({/** 结构说明（自动提取）：randomUUID；无显式参数；直接调用 Error；包含显式抛错路径。 */ randomUUID(){created++;throw Error('Accepted identity must not be regenerated')}});for(const version of [1,2,3,4,5])for(const variant of ['8','9','a','b']){const id='abcdef01-2345-'+version+'678-'+variant+'abc-def012345678';assert.equal(api.normalizeUuid(id),id);assert.equal(api.normalizeUuid(id.toUpperCase()),id)}assert.equal(created,0)});
test('Malformed or unsupported identities use a fresh generator without coercing objects',['normalizeUuid','createUuid'],/** 结构说明（自动提取）：test 回调；无显式参数；直接调用 load、assert.equal、api.normalizeUuid；包含循环处理。 */ ()=>{let created=0;const fallback='00000001-0000-4000-8000-000000000000',api=load({/** 结构说明（自动提取）：randomUUID；无显式参数；返回路径包含 fallback。 */ randomUUID(){created++;return fallback}});const hostile={/** 结构说明（自动提取）：toString；无显式参数；直接调用 Error；包含显式抛错路径。 */ toString(){throw Error('Object must not be coerced')}};const invalid=[undefined,null,0,true,{},[],hostile,'','not-a-guid','00000000-0000-0000-0000-000000000000','00000001-0000-6000-8000-000000000000','00000001-0000-4000-c000-000000000000',' '+fallback,fallback+' ','{'+fallback+'}'];for(const value of invalid)assert.equal(api.normalizeUuid(value),fallback);assert.equal(created,invalid.length)});
test('Missing randomUUID uses random bytes with explicit UUID version and variant bits',['createUuid'],/** 结构说明（自动提取）：test 回调；无显式参数；直接调用 load、api.createUuid、assert.match、assert.ok、assert.equal；包含循环处理。 */ ()=>{for(const [fill,variant] of [[0,'8'],[0x55,'9'],[0xaa,'a'],[0xff,'b']]){let calls=0;const api=load({/** 结构说明（自动提取）：getRandomValues；输入 bytes；直接调用 bytes.fill；返回路径包含 bytes。 */ getRandomValues(bytes){calls++;bytes.fill(fill);return bytes}}),id=api.createUuid();assert.match(id,pattern);assert.ok(calls>0);assert.equal(id[14],'4');assert.equal(id[19],variant)}});
test('Legacy hosts without crypto still produce correctly shaped UUIDs',['createUuid'],/** 结构说明（自动提取）：test 回调；无显式参数；直接调用 load、assert.match、api.createUuid、assert.ok。 */ ()=>{let calls=0;const api=load(undefined,/** 结构说明（自动提取）：load 回调；无显式参数。 */ ()=>{calls++;return .5});assert.match(api.createUuid(),pattern);assert.ok(calls>0)});
await audit.write(checks,'Executed actual identity source in isolated host contexts. Normalization intentionally follows the current project contract (UUID versions1–5). Bounded distinctness is not a collision or cryptographic-security guarantee; the legacy Math.random fallback is tested only for shape and dispatch.');
