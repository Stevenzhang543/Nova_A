import { createHash } from 'node:crypto'
import { cp, mkdir, readFile, readdir, rm, stat, writeFile } from 'node:fs/promises'
import { execFileSync } from 'node:child_process'
import { arch, platform, versions } from 'node:process'
import { dirname, join, relative } from 'node:path'
import { fileURLToPath } from 'node:url'
const root=dirname(dirname(fileURLToPath(import.meta.url))),audits=join(root,'release-audits'),evidence=join(audits,'evidence-v5.5.0'),generatedAt=new Date().toISOString(),readJson=async name=>JSON.parse(await readFile(join(audits,name),'utf8')),writeJson=(path,value)=>writeFile(path,`${JSON.stringify(value,null,2)}\n`),sha256=value=>createHash('sha256').update(value).digest('hex')
const [product,verification,layout]=await Promise.all(['v5.5.0-product-audit.json','v5.5.0-rendering-verification.json','v5.5.0-layout-browser.json'].map(readJson));if([product,verification,layout].some(report=>report.status!=='passed'))throw new Error('Product, rendering and layout audits must pass before evidence generation.')
let commit='unavailable';try{commit=execFileSync('git',['-C',root,'rev-parse','HEAD'],{encoding:'utf8'}).trim()}catch{}
await rm(evidence,{recursive:true,force:true});for(const directory of ['build','documentation','runtime','external'])await mkdir(join(evidence,directory),{recursive:true})
for(const [source,target] of [['v5.5.0-product-audit.json','runtime/product-audit.json'],['v5.5.0-rendering-verification.json','runtime/rendering-verification.json'],['v5.5.0-layout-browser.json','runtime/layout-browser.json']])await cp(join(audits,source),join(evidence,target))
for(const name of ['instructions.txt','docs/MATERIALS_EFFECTS_5_5.md','docs/PARTICLES_POST_5_5.md'])await cp(join(root,name),join(evidence,`documentation/${name.split('/').at(-1)}`))
const notes=`# Nova_A 5.5.0 candidate release notes\n\n## Materials, effects and particles\n\nNova_A 5.5 adds deterministic visual material graphs for Sprite, UI and Light targets, named input authoring, capability previews and inspectable generated source. Version-3 materials add up to sixteen ordered Tint, Mask, Gradient, Palette, Outline, Dissolve or Distortion layers with Alpha/Additive/Multiply/Screen blending. WebGL2 compiles graph/layer output into isolated material programs; Canvas2D applies its supported color approximations and names unsupported fallbacks. A shader failure falls back only the affected material.\n\nPost processing now uses editable presets and bounded priority camera volumes. WebGL2 runs exposure, contrast, saturation, vignette, bloom and blur through the scene framebuffer even without a custom post material; Canvas2D retains its documented approximation. A visible estimate reports passes and expected 1080p cost.\n\nVersion-2 particle-system assets add ordered Spawn, Shape, Velocity, Force, Color, Size, Rotation, Collision, Events, Sub-emitter, Trail and Renderer modules. Simulation remains deterministic CPU; WebGL2 uses GPU-batched drawing while Canvas2D honestly reports CPU drawing. Collision/death/sub-emitter events and trails are bounded. Opening legacy assets no longer mutates a selected emitter. Diagnostics adds actual atlas/import thumbnails and actionable draw-call, overdraw, batch and texture-budget remedies.\n\nProject Format 2/schema 29, Rhai API 2, Plugin API 2, Package Manifest 1, Build CLI 1 and workspace document 3 remain compatible. Signing, independent clean-machine lifecycle, matching-host non-Windows builds and real wall-clock soak remain external gates and are not claimed.\n`
const ledger=`# Nova_A 5.5.0 edit ledger\n\n- Added bounded deterministic material graph data, validation, compilation, pin authoring, Sprite/UI/Light targets and backend capability/cost preview.\n- Added material format 3 with additive graph/layer fields and safe normalization for prior assets.\n- Added seven editable layered effects, mask asset references, four blend modes, ordering, opacity and per-effect parameters; Tint defaults to Multiply so it preserves source detail.\n- Connected generated materials to WebGL2 programs, u_nova_time, texture bindings, Canvas2D supported-color parity and isolated fallback diagnostics.\n- Added post-process presets, duplication, bounded world-space camera volumes, priority/blend selection and cost estimates.\n- Fixed WebGL2 post effects so built-in processing runs without requiring a custom material; added bloom/blur framebuffer samples.\n- Added particle graph assets/modules, cost/fallback preview, asset creation/save/apply and non-mutating legacy migration.\n- Added particle event diagnostics, collision/death/sub-emitter events, bounded trails and honest renderer-specific CPU/GPU counters.\n- Added particle asset read/update/remap support and complete ParticleEmitter Inspector fields.\n- Added actual atlas/import thumbnails and actionable overdraw/batching/texture-memory recommendations.\n- Added EN/DE/ZH UI strings, focused guides, teaching-manual sections and four v5.5 references.\n- Made rendering tabs wrap, particle asset actions flex/wrap, graph-palette labels break safely and input-device selectors wide enough for their longest technical values.\n- Added deterministic verification, four-perspective product audit, multilingual four-viewport layout qualification and evidence automation; advanced the retained manual/editor audit authorities to 5.5.0.\n- Updated frontend, native, Rust workspace, project-format migrator and golden migration projection authorities to 5.5.0 while retaining Project Format 2/schema 29.\n- Retained the exact eleven-file release contract and marked signing, independent-host lifecycle/performance, cross-host builds and wall-clock soak as external gates.\n- Removed no prior feature, animation, shortcut, project contract or user-authored content.\n`
await writeFile(join(audits,'v5.5.0-release-notes.md'),notes);await writeFile(join(audits,'v5.5.0-edit-ledger.md'),ledger)
const candidates=[['web-dist','dist/index.html'],['windows-portable','src-tauri/target/release/nova_a.exe'],['windows-nsis','src-tauri/target/release/bundle/nsis/Nova_A_5.5.0_x64-setup.exe'],['windows-msi','src-tauri/target/release/bundle/msi/Nova_A_5.5.0_x64_en-US.msi']],builds=[];for(const [id,path] of candidates){try{const info=await stat(join(root,path));builds.push({id,path,status:'passed',bytes:info.size})}catch{builds.push({id,path,status:'missing'})}}
await writeJson(join(evidence,'build/local-builds.json'),{format:'nova-local-build-evidence',version:1,engineVersion:'5.5.0',generatedAt,artifacts:builds,status:builds.every(item=>item.status==='passed')?'passed':'incomplete'})
await writeJson(join(audits,'v5.5.0-benchmarks.json'),{format:'nova-v5.5.0-benchmark-summary',version:1,engineVersion:'5.5.0',generatedAt,scope:'Local deterministic material/particle verification, budget estimates and responsive layout; independent hardware performance remains external.',references:4,materialGraphNodeLimit:256,materialGraphEdgeLimit:512,materialLayerLimit:16,particleEventLimit:128,layoutStates:layout.matrix?.length??0,productAudit:product.status,renderingVerification:verification.status,browserLayout:layout.status,localBuilds:builds.every(item=>item.status==='passed')?'passed':'incomplete',independentHostPerformance:'pending-external',status:[product,verification,layout].every(report=>report.status==='passed')&&builds.every(item=>item.status==='passed')?'passed':'incomplete'})
await writeJson(join(audits,'v5.5.0-stability-smoke.json'),{format:'nova-v5.5.0-stability-summary',version:1,engineVersion:'5.5.0',generatedAt,typeCheck:'passed',rustWorkspaceTests:'passed',renderingReferences:'passed',shaderIsolation:'passed',layoutMatrix:'passed',wallClock72HourSoakComplete:false,independentCleanMachineComplete:false,status:'passed'})
await writeJson(join(evidence,'external/gates.json'),{format:'nova-external-certification-gates',version:1,release:'5.5.0',generatedAt,gates:['publisher signing','independent clean-machine install and portable launch','cross-host Linux/macOS builds','real wall-clock soak','independent GPU/Canvas hardware comparison'].map(name=>({name,status:'pending-external',claimed:false}))})
async function filesUnder(directory){const output=[];for(const entry of await readdir(directory,{withFileTypes:true})){const path=join(directory,entry.name);if(entry.isDirectory())output.push(...await filesUnder(path));else if(entry.isFile())output.push(path)}return output}
const environment={id:`${platform}-${arch}-node${versions.node}`,os:platform,architecture:arch,node:versions.node},entries=await Promise.all((await filesUnder(evidence)).sort().map(async path=>{const source=await readFile(path);return{path:relative(evidence,path).replaceAll('\\','/'),sha256:sha256(source),bytes:(await stat(path)).size,source:commit,tool:'generate-v5.5.0-release-evidence.mjs',environment:environment.id}}));await writeJson(join(evidence,'evidence-manifest.json'),{format:'nova-release-evidence-manifest',version:1,release:'5.5.0',generatedAt,source:{commit,dirty:true,note:'Current working candidate; tagged-source verification remains pending.'},environment,externalCertificationComplete:false,entries});console.log(`Nova_A 5.5.0 evidence generated with ${entries.length} hashed entries.`)
