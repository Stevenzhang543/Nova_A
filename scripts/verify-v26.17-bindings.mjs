/** 功能回归脚本：执行 verify-v26.17-bindings.mjs 对应场景，保留断言和证据输出。 */
import assert from 'node:assert/strict'
import {reactive} from 'vue'
import {cpus,platform,release} from 'node:os'
import {createHash} from 'node:crypto'
import {mkdir,writeFile} from 'node:fs/promises'
import {dirname} from 'node:path'
import {resolveMilestoneAuditContext} from './lib/milestoneAuditContext.mjs'
import {openMediaAuditModules,mediaUuid as uuid} from './lib/mediaAudit16.mjs'
const context=resolveMilestoneAuditContext(import.meta.url,{release:'26.17',reportName:'bindings'}),checks=[],observations=[]
const opened=await openMediaAuditModules(context,{physics:'store/physics',world:'world/World',box:'world/BoxEntity',components:'world/components',db:'assets/AssetDatabase',ai:'runtime/aiTools',stream:'runtime/worldStreaming',leases:'runtime/runtimeSceneStreaming',lifecycle:'runtime/streamLifecycle',lifetimes:'runtime/entityLifetimes',scenes:'world/SceneManager',transition:'runtime/runtimeSceneTransition',gameplay:'runtime/worldGameplay',controllers:'runtime/gameplayComponents',geometry:'runtime/physicsGeometry',connections:'world/Connection'})
const m=opened.modules,wasm=await opened.wasm(),runtimes=[],clone=/* 调用 JSON.parse(JSON.stringify(value)) 并返回调用结果。 */ value=>JSON.parse(JSON.stringify(value)),box=/** 按编号、位置及固定尺寸创建物理绑定测试盒形实体。 */ (n,x=0,y=0)=>new m.box.BoxEntity(n,{x,y},{x:2,y:2},uuid(n))
const world=/** 建立真实 WASM 运行时世界并注入测试实体，记录运行时用于后续释放。 */ entities=>{const w=new m.world.World();Object.defineProperty(w,'__v_skip',{value:true});w.runtime=new wasm.module.WasmRuntimeWorld();w.wasmLoaded=true;w.entities=entities;w.setNextId(10000);m.physics.physicsState.world=w;runtimes.push(w.runtime);return w}
const sync=/* 调用 w.queryPhysics({kind:'Point',origin:{x:999,y:999}}) 并返回调用结果。 */ w=>w.queryPhysics({kind:'Point',origin:{x:999,y:999}}),record=/* 调用 w.bodyRecords.get(w.bodyHandles.get(e.id)) 并返回调用结果。 */ (w,e)=>w.bodyRecords.get(w.bodyHandles.get(e.id)),settings=/** 复制全局物理设置并禁用重力和空气摩擦，设置固定帧率与时间缩放。 */ ()=>({...clone(m.physics.physicsState.globalSettings),gravity:0,airFriction:0,tickRate:60,timeScale:1})
const check=/** 重置 AI 和流送状态后执行具名绑定检查，记录结果并输出通过或失败日志。 */ async(name,fn)=>{m.ai.resetAi();m.lifecycle.setStreamRuntimeHooks(null);m.stream.resetWorldStreaming();try{await fn();checks.push({name,status:'passed'});console.log('PASS '+name)}catch(error){checks.push({name,status:'failed',error:error.stack??String(error)});console.error('FAIL '+name+': '+error.message)}}
const node=/** 创建默认 Wait 行为树节点，并允许指定类型和扩展字段覆盖。 */ (id,type='Wait',extra={})=>({id,type,name:id,children:[],condition:'',action:'',seconds:10,...extra})
try{
await check('Every scalar body/material/primary collider ABI binding accepts edits after body creation with the same native handle',/** 逐项编辑刚体、材质和碰撞体 ABI 字段，验证原生记录实时更新且句柄不变。 */ ()=>{const e=box(1),w=world([e]);e.rigidBody.massMode='Manual';sync(w);const handle=w.bodyHandles.get(e.id),edits=[['mass',8,3],['velocity.x',4,2],['velocity.y',5,-3],['acceleration.x',6,4],['acceleration.y',7,-5],['angularVelocity',15,.7],['torque',16,3],['gravityScale',17,.5],['linearDamping',18,.1],['angularDamping',19,.2],['force.x',21,6],['force.y',22,7],['localGravity',23,2],['autoInertia',25,false],['inertia',26,9],['freezeRotation',46,true],['sleepingAllowed',48,false],['sleeping',49,true],['sleepTimer',50,.4]];for(const[path,index,value]of edits){const parts=path.split('.');let target=e.rigidBody;for(const key of parts.slice(0,-1))target=target[key];target[parts.at(-1)]=value;sync(w);assert.equal(record(w,e)[index],typeof value==='boolean'?Number(value):value,path);assert.equal(w.bodyHandles.get(e.id),handle,path)}for(const[path,index,value]of [['restitution',10,.6],['dynamicFriction',11,.3],['staticFriction',20,.4],['restitutionThreshold',27,.2]]){e.collider.material[path]=value;sync(w);assert.equal(record(w,e)[index],value,path)}for(const[path,index,value]of [['sensor',28,true],['physicsLayer',33,2],['collisionMask',42,4],['rotation',45,.5],['oneWay',51,true]]){e.collider[path]=value;sync(w);assert.equal(record(w,e)[index],typeof value==='boolean'?Number(value):value,path)}for(const[type,value]of [['Dynamic',0],['Kinematic',1],['Static',0]]){e.rigidBody.bodyType=type;sync(w);assert.equal(record(w,e)[24],value);assert.equal(record(w,e)[9],Number(type==='Static'))}e.rigidBody.continuousCollision='Continuous';sync(w);assert.equal(record(w,e)[47],1);observations.push({bodyAbiEdits:edits.map(/** 提取属性路径与 ABI 槽位索引，供编辑覆盖报告使用。 */ ([path,index])=>({path,index})),nativeHandleRetained:true})})
await check('Joint and rope property edits reach retained connection records without stale rope data overwriting joint limits',/** 验证绳索和关节属性编辑更新原生连接槽位且保留句柄，检查类型切换不残留绳段数据。 */ ()=>{const a=box(500),b=box(501,4),w=world([a,b]),connection=m.connections.createConnection(1,w.entities,[a.id,b.id],['local','local']);w.connections=[connection];connection.bendingToleranceMass=1e10;connection.stretchingToleranceMass=1e10;sync(w);const key=connection.uuid+':0',handle=w.connectionHandles.get(key),get=/* 调用 w.connectionRecords.get(handle) 并返回调用结果。 */ ()=>w.connectionRecords.get(handle);for(const[field,index,value]of[['stiffness',10,777],['damping',11,23],['maxStretchRatio',12,1.5],['bendingToleranceMass',13,15],['stretchingToleranceMass',14,25],['collisionRadius',25,.12],['linearDensity',26,.8]]){connection[field]=value;sync(w);assert.equal(get()[index],value,field);assert.equal(w.connectionHandles.get(key),handle)}connection.collisionEnabled=true;connection.segmentCount=7;m.connections.initializeRopeNodes(connection,w.entities);sync(w);assert.equal(get()[27],7);connection.componentType='PrismaticJoint2D';connection.jointAxis={x:0,y:1};connection.limitsEnabled=true;connection.lowerLimit=-2;connection.upperLimit=3;connection.motorEnabled=true;connection.motorSpeed=2;connection.maxMotorForce=5;connection.breakForce=90;connection.breakTorque=70;sync(w);for(const[index,value]of[[18,4],[27,0],[29,0],[30,1],[31,1],[32,-2],[33,3],[35,1],[36,2],[37,5],[38,90],[39,70]])assert.equal(get()[index],value,'joint slot '+index);assert.equal(w.connectionHandles.get(key),handle);connection.anchors[0].localPoint={x:.25,y:-.5};sync(w);assert.equal(get()[3],.25);assert.equal(get()[4],-.5)})
await check('Animation ownership prevents a second physics integration and retains velocity for returning to Physics',/** 验证动画拥有变换时保留作者位置与速度，切回物理所有权后恢复模拟位移。 */ ()=>{const e=box(2),w=world([e]);Object.assign(e.rigidBody,{massMode:'Manual',velocity:{x:6,y:0},angularVelocity:2,transformOwnership:'Animation'});w.update(1/60,true,settings());assert.equal(e.transform.position.x,0);assert.equal(e.velocity.x,6);assert.equal(e.angularVelocity,2);e.transform.position.x=5;w.update(1/60,true,settings());assert.equal(e.transform.position.x,5);e.rigidBody.transformOwnership='Physics';w.update(1/60,true,settings());assert.ok(w.stateBuffer[2]>5);w.update(1/60,true,settings());assert.ok(e.transform.position.x>5)})
await check('Parent scale determines automatic mass and material asset edits preserve inline authoring values',/** 验证父级缩放、材质密度和资源重导入影响原生质量参数，不覆写内联材质，损坏资源被拒绝。 */ ()=>{const parent=box(3),e=box(4),w=world([parent,e]);parent.removeComponent('RigidBody2D');parent.removeComponent('BoxCollider2D');parent.transform.scale={x:2,y:3};e.parentUuid=parent.uuid;e.rigidBody.density=2;sync(w);assert.equal(record(w,e)[8],48);const inline=clone(e.collider.material),density=e.density,material={format:'nova-physics-material',version:1,name:'Binding',density:3,staticFriction:.7,dynamicFriction:.5,restitution:.8,restitutionThreshold:.3,frictionCombine:'Multiply',restitutionCombine:'Minimum'},asset=m.db.createTextAsset('Binding material','material',JSON.stringify(material),'Assets');e.collider.materialAsset='asset://'+asset.uuid;sync(w);assert.equal(record(w,e)[8],72);assert.equal(record(w,e)[10],.8);assert.equal(record(w,e)[54],2);assert.equal(record(w,e)[55],1);assert.deepEqual(e.collider.material,inline);assert.equal(e.density,density);material.restitution=.2;m.db.updateTextAsset(asset.uuid,JSON.stringify(material));sync(w);assert.equal(record(w,e)[10],.2);m.db.updateTextAsset(asset.uuid,'{');assert.throws(/* 调用 sync(w) 并返回调用结果。 */ ()=>sync(w),/PHYSICS_MATERIAL_INPUT/);assert.deepEqual(e.collider.material,inline)})
await check('Mirrored shape offsets and one-way normals survive normalization; concave geometry remains unchanged on repeated queries',/** 验证负缩放正确作用于碰撞偏移、旋转和单向法线，凹分解不修改原顶点且错误凸形被拦截。 */ ()=>{const e=box(5),w=world([e]);e.isStatic=true;e.transform.scale={x:-2,y:3};e.collider.offset={x:2,y:1};e.collider.rotation=.5;e.collider.oneWayNormal={x:1,y:1};sync(w);assert.deepEqual(e.transform.scale,{x:-2,y:3});assert.equal(record(w,e)[43],-4);assert.equal(record(w,e)[44],3);assert.equal(record(w,e)[45],-.5);assert.equal(record(w,e)[52],-.5);assert.equal(record(w,e)[53],1/3);e.collider.shapeModel='ConcavePolygon';e.collider.vertices=[{x:0,y:0},{x:3,y:0},{x:3,y:1},{x:1,y:1},{x:1,y:3},{x:0,y:3}];const before=clone(e.collider.vertices);sync(w);sync(w);assert.deepEqual(e.collider.vertices,before);assert.equal(m.geometry.prepareColliderSet(e.collider,false).shapes.length,4);e.collider.shapeModel='ConvexPolygon';e.collider.vertices=[{x:0,y:0},{x:2,y:0},{x:.5,y:.5},{x:0,y:2}];assert.ok(m.geometry.prepareColliderSet(e.collider,false).blockedReason)})
await check('Area triggers work without effectors, honor scale and target monitorable, and emit exits on removal',/** 验证区域缩放、目标可监测性和区域删除均触发正确进入退出事件。 */ ()=>{const area=box(6),target=box(7,5),w=world([area,target]);area.isStatic=true;target.isStatic=true;area.collider.sensor=true;area.addComponent(new m.components.Area2D());area.transform.scale={x:3,y:1};m.gameplay.worldGameplayState.originShiftThreshold=1e9;const events=[],emit=/* 调用 events.push(args) 并返回调用结果。 */ (...args)=>events.push(args);m.gameplay.beforeWorldPhysicsStep(.01,0,0,emit);assert.ok(events.some(/* 先计算 e[0]==='area.entered'；仅当其为真值时求右侧 e[2]===target.uuid，返回短路求值结果。 */ e=>e[0]==='area.entered'&&e[2]===target.uuid));target.addComponent(new m.components.Area2D()).monitorable=false;events.length=0;m.gameplay.beforeWorldPhysicsStep(.01,.01,1,emit);assert.ok(events.some(/* 先计算 e[0]==='area.exited'；仅当其为真值时求右侧 e[2]===target.uuid，返回短路求值结果。 */ e=>e[0]==='area.exited'&&e[2]===target.uuid));target.removeComponent('Area2D');m.gameplay.beforeWorldPhysicsStep(.01,.02,2,emit);events.length=0;w.entities.splice(w.entities.indexOf(area),1);m.gameplay.beforeWorldPhysicsStep(.01,.03,3,emit);assert.ok(events.some(/* 先计算 e[0]==='area.exited'；仅当其为真值时求右侧 e[2]===target.uuid，返回短路求值结果。 */ e=>e[0]==='area.exited'&&e[2]===target.uuid))})
await check('AI blackboard writes persist across ticks and authored revision/retirement resets Wait and overrides',/** 验证 AI 黑板写入跨帧保留，作者覆盖修改和实体退役正确重置运行时状态。 */ ()=>{const e=box(8),b=e.addComponent(new m.components.BehaviorTree2D()),tree={version:2,root:'wait',nodes:[node('wait')],blackboard:{score:0}},asset=m.db.createTextAsset('AI state','behaviorTree',JSON.stringify(tree),'Assets');b.treeAsset='asset://'+asset.uuid;b.tickRate=60;b.blackboardOverrides={score:1};m.ai.updateAi([e],1/60,0);m.ai.setBlackboardValue(e.uuid,'score',7);m.ai.updateAi([e],1/60,1);assert.equal(m.ai.blackboardSnapshot(e.uuid).score,7);b.blackboardOverrides={score:3};m.ai.updateAi([e],1/60,2);assert.equal(m.ai.blackboardSnapshot(e.uuid).score,3);m.ai.retireAiEntity(e.uuid);assert.deepEqual(m.ai.blackboardSnapshot(e.uuid),{});m.ai.updateAi([e],1/60,3);assert.equal(m.ai.blackboardSnapshot(e.uuid).score,3)})
await check('Perception uses world units, parent rotation, tags, deterministic distance ties and bounded results',/** 验证感知使用世界距离、父级旋转和标签筛选，等距离结果稳定且遵守数量上限。 */ ()=>{const parent=box(9),e=box(10),near=box(11,0,3),tie=box(12,0,3),back=box(13,0,-1),far=box(14,0,9);parent.transform.rotation=Math.PI/2;parent.transform.scale={x:4,y:4};e.parentUuid=parent.uuid;for(const v of[near,tie,back,far])v.tags=['target'];const b=e.addComponent(new m.components.BehaviorTree2D()),tree={version:2,root:'wait',nodes:[node('wait')],perception:[{id:'eyes',tags:['target'],radius:5,fieldOfView:90,maximumResults:1,blackboardKey:'sight'}]},asset=m.db.createTextAsset('Perception','behaviorTree',JSON.stringify(tree),'Assets');b.treeAsset='asset://'+asset.uuid;b.tickRate=60;m.ai.updateAi([far,back,tie,near,e,parent],1/60,0);const board=m.ai.blackboardSnapshot(e.uuid);assert.equal(board['sight.count'],1);assert.equal(board['sight.uuid'],near.uuid);assert.equal(board['sight.distance'],3)})
await check('An acyclic shared-subtree amplification graph is bounded per tree and frame',/** 构造共享子节点的深层行为 DAG，验证多实体求值总量受预算限制并记录耗时。 */ ()=>{const nodes=[node('leaf','BlackboardCondition',{key:'never',operator:'truthy'})];for(let i=0;i<9;i++)nodes.push(node('n'+i,'Selector',{children:Array(256).fill(i?'n'+(i-1):'leaf')}));const tree={version:2,root:'n8',nodes},asset=m.db.createTextAsset('Bounded DAG','behaviorTree',JSON.stringify(tree),'Assets'),entities=Array.from({length:24},/** 创建绑定同一行为树且每秒六十次求值的测试实体。 */ (_,i)=>{const e=box(100+i),b=e.addComponent(new m.components.BehaviorTree2D());b.treeAsset='asset://'+asset.uuid;b.tickRate=60;return e});const started=performance.now();m.ai.updateAi(entities,1/60,0);assert.ok(m.ai.aiDebugState.nodeEvaluations<=65536);assert.ok(m.ai.aiDebugState.nodeEvaluations>=4096);observations.push({aiDAG:{entities:entities.length,nodeEvaluations:m.ai.aiDebugState.nodeEvaluations,milliseconds:performance.now()-started}})})
await check('Reactive scene hosts preserve shared leases, persistent descendants and lifetime generations across unload/reload',/** 验证响应式场景宿主共享租约、持久父子对象、生命周期代次和重载去重，场景切换解除适配器所有权。 */ ()=>{const w=world([]),host=reactive({entities:w.entities,connections:w.connections,allocateId:/* 调用 w.allocateId() 并返回调用结果。 */ ()=>w.allocateId(),allocateConnectionId:/* 调用 w.allocateConnectionId() 并返回调用结果。 */ ()=>w.allocateConnectionId()}),scenes=new m.scenes.SceneManager(),root=box(20),persistent=box(21,2),child=box(22,1);persistent.parentUuid=root.uuid;persistent.persistentAcrossScenes=true;child.parentUuid=persistent.uuid;const target=scenes.create('Persistent streamed');target.loaded=false;target.data={entities:[root,persistent,child].map(m.physics.serializeEntity),connections:[]};const calls=[];m.lifecycle.setStreamRuntimeHooks({beforeDeactivate:/** 记录停用实体 UUID 及原世界实体数量，供流送生命周期断言。 */ (values,original)=>calls.push({kind:'out',ids:values.map(/* 返回 e.uuid 的当前值。 */ e=>e.uuid),world:original.length}),afterActivate:/** 记录激活实体 UUID 及新建对象数量，供流送生命周期断言。 */ (values,fresh)=>calls.push({kind:'in',ids:values.map(/* 返回 e.uuid 的当前值。 */ e=>e.uuid),fresh:fresh.length})});const adapter=new m.leases.RuntimeSceneStreaming({world:host,scenes,origin:/** 返回零坐标原点，供流送或场景测试使用。 */ ()=>({x:0,y:0}),createEntity:m.physics.createEntityFromData,serializeEntity:m.physics.serializeEntity,readHandoff:m.stream.peekStreamedSaveState,writeHandoff:m.stream.handoffStreamedSaveState});adapter.setOwner('a',target.uuid,true,true);adapter.setOwner('b',target.uuid,true,true);assert.equal(calls.length,1);const live=host.entities.find(/* 比较 e.uuid 与 persistent.uuid，返回严格相等的判断结果。 */ e=>e.uuid===persistent.uuid),generation=m.lifetimes.entityLifetimeGeneration(live),oldRoot=host.entities.find(/* 比较 e.uuid 与 root.uuid，返回严格相等的判断结果。 */ e=>e.uuid===root.uuid);adapter.releaseOwner('a');adapter.releaseOwner('b');assert.equal(host.entities.length,2);assert.equal(live.parentUuid,null);assert.equal(live.transform.position.x,2);assert.equal(m.lifetimes.entityLifetimeGeneration(live),generation);assert.equal(m.lifetimes.entityLifetimeActive(oldRoot),false);adapter.setOwner('a',target.uuid,true,true);assert.equal(host.entities.length,3);assert.equal(host.entities.filter(/* 比较 e.uuid 与 persistent.uuid，返回严格相等的判断结果。 */ e=>e.uuid===persistent.uuid).length,1);assert.equal(m.lifetimes.entityLifetimeGeneration(live),generation);adapter.detachAfterSceneTransition();assert.equal(host.entities.length,3);assert.equal(adapter.inspect().length,0)})
await check('Malformed streamed handoff imports and cyclic/nonfinite saves preserve the previous saved state atomically',/** 验证重复键、结构错误、循环和非有限值的流送交接输入被原子拒绝，旧状态保持可读。 */ ()=>{m.stream.handoffStreamedSaveState('good',{value:3});const before=m.stream.exportWorldStreamingHandoffs();for(const source of ['[["a",{}],["a",{}]]','[["b",{}],42]','{}'])assert.throws(/* 调用 m.stream.importWorldStreamingHandoffs(source) 并返回调用结果。 */ ()=>m.stream.importWorldStreamingHandoffs(source));assert.equal(m.stream.exportWorldStreamingHandoffs(),before);const cycle={};cycle.self=cycle;assert.throws(/* 调用 m.stream.handoffStreamedSaveState('cycle',cycle) 并返回调用结果。 */ ()=>m.stream.handoffStreamedSaveState('cycle',cycle));assert.throws(/* 调用 m.stream.handoffStreamedSaveState('bad',{value:NaN}) 并返回调用结果。 */ ()=>m.stream.handoffStreamedSaveState('bad',{value:NaN}));assert.deepEqual(m.stream.peekStreamedSaveState('good'),{value:3});assert.equal(m.stream.importWorldStreamingHandoffs(before),1)})
await check('Scene transition aligns new entities with shifted persistent objects and preserves origin-relative connections',/** 验证场景切换保留持久对象，并将新实体和导航目标转换到已偏移原点坐标。 */ ()=>{const keeper=box(30,3),w=world([keeper]),scenes=new m.scenes.SceneManager();keeper.persistentAcrossScenes=true;const next=box(31,10006),agent=next.addComponent(new m.components.NavigationAgent2D());agent.targetPosition={x:10009,y:0};const target=scenes.create('Shifted next');target.data={entities:[m.physics.serializeEntity(next)],connections:[]};const transaction=m.transition.prepareRuntimeSceneTransition({world:w,scenes,origin:/** 返回横向偏移一万单位的测试世界原点。 */ ()=>({x:10000,y:0}),createEntity:m.physics.createEntityFromData,captureView:/* 返回固定值 null。 */ ()=>null,/** 为无需真实视图的场景切换测试提供恢复视图占位方法。 */ restoreView(){},/** 为无需真实视图的场景切换测试提供应用视图占位方法。 */ applyView(){}},target.uuid);assert.equal(transaction.commit(),true);assert.equal(w.entities.find(/* 比较 e.uuid 与 keeper.uuid，返回严格相等的判断结果。 */ e=>e.uuid===keeper.uuid),keeper);assert.equal(w.entities.find(/* 比较 e.uuid 与 next.uuid，返回严格相等的判断结果。 */ e=>e.uuid===next.uuid).transform.position.x,6);assert.equal(w.entities.find(/* 比较 e.uuid 与 next.uuid，返回严格相等的判断结果。 */ e=>e.uuid===next.uuid).getComponent('NavigationAgent2D').targetPosition.x,9)})
await check('Seeded malformed geometry is bounded and never publishes nonfinite solver shapes',/** 对六百个随机多边形输入检查分解数量上限和有限顶点，包含 NaN 时必须阻止构建。 */ ()=>{let seed=17;const random=/** 更新固定种子的线性同余状态，返回零至一的确定性伪随机数。 */ ()=>((seed=Math.imul(seed,1664525)+1013904223>>>0)/2**32);for(let i=0;i<600;i++){const e=box(200+i);e.collider.shapeModel=i%2?'ConcavePolygon':'ConvexPolygon';e.collider.vertices=Array.from({length:3+i%21},/** 生成两个坐标均位于零至十之间的随机顶点。 */ ()=>({x:random()*10,y:random()*10}));if(i%7===0)e.collider.vertices[0].x=NaN;const prepared=m.geometry.prepareColliderSet(e.collider,false);assert.ok(prepared.shapes.length<=128);for(const shape of prepared.shapes)for(const p of shape.points)assert.ok(Number.isFinite(p.x)&&Number.isFinite(p.y));if(i%7===0)assert.ok(prepared.blockedReason)}})
await check('Retained large-world physics/query replay preserves finite state without configuration rebuilds on unchanged bodies',/** 测量千刚体多帧更新和最近查询，验证无重复配置重建且重复查询结果一致。 */ ()=>{const entities=Array.from({length:1000},/** 按网格位置创建静态盒形实体，供千刚体性能样本使用。 */ (_,i)=>{const e=box(1000+i,(i%50)*4,Math.floor(i/50)*4);e.isStatic=true;return e}),w=world(entities),configuration=settings();w.update(1/60,true,configuration);const rebuilds=w.diagnostics.configurationRebuilds,started=performance.now();for(let i=0;i<30;i++)w.update(1/60,true,configuration);const physicsMs=performance.now()-started,queryStarted=performance.now(),results=[];for(let i=0;i<100;i++)results.push(w.queryPhysics({kind:'Nearest',origin:{x:(i%50)*4,y:4},distance:10})[0]?.entityUuid);const queryMs=performance.now()-queryStarted;assert.equal(w.diagnostics.configurationRebuilds,rebuilds);assert.ok(results.every(Boolean));assert.deepEqual(results.slice(0,50),results.slice(50));observations.push({workload:{bodies:1000,physicsFrames:30,queries:100,physicsMs,queryMs,configurationRebuilds:rebuilds}})})
await check('A broken component joint stays broken until explicitly disabled and re-enabled',/** 验证关节断裂后退出活动连接，显式禁用再启用可重新建立连接。 */ ()=>{const a=box(600),b=box(601,10),w=world([a,b]);a.isStatic=true;const joint=a.addComponent(new m.components.Joint2D('DistanceJoint2D'));joint.targetEntityUuid=b.uuid;joint.distance=1;joint.breakForce=.00001;w.update(1/60,true,settings());const first=w.activeConnectionRecords.find(/* 比较 r.connection.uuid 与 joint.uuid，返回严格相等的判断结果。 */ r=>r.connection.uuid===joint.uuid);assert.ok(first);assert.notEqual(first.connection.breakState,'intact');w.update(1/60,true,settings());assert.equal(w.activeConnectionRecords.some(/* 比较 r.connection.uuid 与 joint.uuid，返回严格相等的判断结果。 */ r=>r.connection.uuid===joint.uuid),false);joint.enabled=false;sync(w);joint.enabled=true;sync(w);assert.ok(w.activeConnectionRecords.some(/* 比较 r.connection.uuid 与 joint.uuid，返回严格相等的判断结果。 */ r=>r.connection.uuid===joint.uuid))})
await check('A persistent streamed rope is not translated again when its released scene is reopened',/** 验证持久绳索随原点偏移后再次取得场景租约时不会被旧节点数据覆盖。 */ ()=>{const w=world([]),scenes=new m.scenes.SceneManager(),a=box(602,10004),b=box(603,10008),origin={x:0,y:0};a.persistentAcrossScenes=b.persistentAcrossScenes=true;const rope=m.connections.createConnection(1,[a,b],[a.id,b.id],['center','center']);rope.collisionEnabled=true;rope.anchors.forEach(/** 为绳索锚点写入对应端点实体的 UUID。 */ (anchor,i)=>anchor.entityUuid=[a,b][i].uuid);m.connections.initializeRopeNodes(rope,[a,b]);const scene=scenes.create('Persistent rope');scene.loaded=false;scene.data={entities:[a,b].map(m.physics.serializeEntity),connections:[clone(rope)]};const leases=new m.leases.RuntimeSceneStreaming({world:w,scenes,origin:/* 返回 origin 的当前值。 */ ()=>origin,createEntity:m.physics.createEntityFromData,serializeEntity:m.physics.serializeEntity,readHandoff:m.stream.peekStreamedSaveState,writeHandoff:m.stream.handoffStreamedSaveState});leases.setOwner('a',scene.uuid,true,true);leases.releaseOwner('a');w.shiftOrigin({x:10000,y:0});origin.x=10000;const before=clone(w.connections[0].ropeNodes);leases.setOwner('a',scene.uuid,true,true);assert.deepEqual(w.connections[0].ropeNodes,before);leases.reset()})
await check('Deferred AI machines consume a broadcast once when their budgeted tick arrives',/** 验证大批状态机因预算延期更新后仍能消费信号并全部进入目标状态。 */ ()=>{const doc={version:2,initialState:'idle',states:[{id:'idle',name:'Idle',onEnter:'',onUpdate:'',onExit:''},{id:'done',name:'Done',onEnter:'',onUpdate:'',onExit:''}],transitions:[{from:'idle',to:'done',signal:'advance',priority:0}]},asset=m.db.createTextAsset('Deferred state','stateMachine',JSON.stringify(doc),'Assets'),entities=Array.from({length:2600},/** 创建绑定测试状态机资源的实体样本。 */ (_,i)=>{const e=box(700+i),machine=e.addComponent(new m.components.StateMachine2D());machine.machineAsset='asset://'+asset.uuid;return e});m.ai.notifyAiSignal('advance');for(let i=0;i<8;i++)m.ai.updateAi(entities,1/60,i);assert.ok(entities.every(/* 比较 e.getComponent('StateMachine2D').currentState 与 'done'，返回严格相等的判断结果。 */ e=>e.getComponent('StateMachine2D').currentState==='done'))})
await check('Top-down component input accelerates once per fixed tick, stops after release and respects collisions',/** 验证俯视控制器每物理步只加速一次、松开后停止且不会穿墙，并检查玩法重置清除旧错误。 */ ()=>{const e=box(3400),wall=box(3401,5),w=world([e,wall]);e.rigidBody.bodyType='Kinematic';wall.isStatic=true;const character=e.addComponent(new m.components.CharacterBody2D());character.floorSnap=0;character.stepHeight=0;character.applyPlatformVelocity=false;const controller=e.addComponent(new m.components.TopDownController2D());controller.speed=4;controller.acceleration=16;controller.rotateToMovement=false;const input={vectors:{Move:[1,0]},axes:{},pressed:{},performed:{},down:{},viewportSize:[1,1],mousePosition:[0,0]};const tick=/** 执行一次固定物理步，并在步进回调中更新控制器与世界玩法。 */ ()=>w.update(1/60,true,settings(),/** 用当前输入更新玩法组件，再执行物理前世界玩法逻辑。 */ dt=>{m.controllers.updateGameplayComponents(w.entities,input,dt,/** 提供不执行额外操作的空回调，用于测试接口占位。 */ ()=>{},/* 返回固定值 null。 */ ()=>null,/** 提供不执行额外操作的空回调，用于测试接口占位。 */ ()=>{});m.gameplay.beforeWorldPhysicsStep(dt,0,0,/** 提供不执行额外操作的空回调，用于测试接口占位。 */ ()=>{})});tick();assert.ok(Math.abs(e.transform.position.x-16/3600)<1e-8);for(let i=0;i<30;i++)tick();assert.ok(e.transform.position.x>1.5&&e.transform.position.x<2);input.vectors.Move=[0,0];for(let i=0;i<30;i++)tick();const stopped=e.transform.position.x;for(let i=0;i<10;i++)tick();assert.ok(Math.abs(e.transform.position.x-stopped)<1e-8);input.vectors.Move=[1,0];for(let i=0;i<60;i++)tick();assert.ok(e.transform.position.x<=3.001);assert.ok(character.onWall);m.gameplay.worldGameplayState.lastError='Old portal failure';m.gameplay.resetWorldGameplay();assert.equal(m.gameplay.worldGameplayState.lastError,'')})

}finally{m.lifecycle.setStreamRuntimeHooks(null);m.ai.resetAi();m.stream.resetWorldStreaming();for(const runtime of runtimes)runtime.free();await opened.close()}
const report={...context.metadata(),status:checks.every(/* 比较 v.status 与 'passed'，返回严格相等的判断结果。 */ v=>v.status==='passed')?'passed':'failed',scope:'Programmer fixtures invoking real World and compiled WASM, public lifecycle and AI modules; no browser interaction is claimed.',host:{platform:platform(),release:release(),cpu:cpus()[0]?.model,node:process.version},wasm:{bytes:wasm.bytes.length,sha256:createHash('sha256').update(wasm.bytes).digest('hex')},checks,observations};await mkdir(dirname(context.reportPath),{recursive:true});await writeFile(context.reportPath,JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));if(report.status!=='passed')process.exitCode=1
