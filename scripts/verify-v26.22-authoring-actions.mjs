/** 功能回归脚本：执行 verify-v26.22-authoring-actions.mjs 对应场景，保留断言和证据输出。 */
import assert from 'node:assert/strict'
import {propertyAudit22} from './lib/propertyAudit22.mjs'
import {openMediaAuditModules} from './lib/mediaAudit16.mjs'
const audit=await propertyAudit22('authoring-actions'),opened=await openMediaAuditModules({repository:process.cwd(),bases:[process.cwd()]},{p:'store/physics',a:'editor/authoring2d',g:'editor/gizmo',h:'world/hierarchy',e:'store/editor',templates:'projects/templates',connections:'world/Connection',prefabs:'runtime/prefabs',blueprints:'editor/objectBlueprintAuthoring',scenes:'runtime/sceneInstances'}),checks=[]
/** 结构说明（自动提取）：check；输入 name、run；直接调用 run、checks.push、console.log；等待异步结果。 */ async function check(name,run){await run();checks.push({name,status:'passed'});console.log('PASS '+name)}
try{
 const {p,a,g,h,e,templates,connections:c,blueprints,scenes,prefabs}=opened.modules,wasm=await opened.wasm();assert.ok(p.loadProject(wasm.module.migrate_project_json(templates.createTemplateProjectJson('empty','Authoring actions'))));
 for(const [i,position] of [{x:0,y:1},{x:2,y:5},{x:10,y:13}].entries()){const entity=a.createAuthoringObject('Box',position,false);entity.name='Edit '+i}
 const baseline=p.getSceneJSON(),ids=p.physicsState.world.entities.filter(/* 调用 x.name.startsWith('Edit ') 并返回调用结果。 */ x=>x.name.startsWith('Edit ')).map(/* 返回 x.uuid 的当前值。 */ x=>x.uuid),current=/** 结构说明（自动提取）：current；无显式参数；直接调用 ids.map；返回表达式求值结果。 */ ()=>ids.map(/** 结构说明（自动提取）：ids.map 回调；输入 id；直接调用 p.physicsState.world.entities.find；返回表达式求值结果。 */ id=>p.physicsState.world.entities.find(/* 比较 x.uuid 与 id，返回严格相等的判断结果。 */ x=>x.uuid===id)),world=/* 返回 p.physicsState.world.entities 的当前值。 */ ()=>p.physicsState.world.entities,positions=/** 结构说明（自动提取）：positions；无显式参数；直接调用 map、current；返回表达式求值结果。 */ ()=>current().map(/* 返回 h.worldTransform(x,world()).position 的当前值。 */ x=>h.worldTransform(x,world()).position);
 const reset=/** 结构说明（自动提取）：reset；无显式参数；直接调用 assert.ok、p.loadProject、p.selectEntities、map、current 等。 */ ()=>{assert.ok(p.loadProject(baseline));p.selectEntities(current().map(/* 返回 x.id 的当前值。 */ x=>x.id),'replace',current()[0].id);p.clearEditorHistory('Action baseline')};
 /** 结构说明（自动提取）：transaction；输入 name、action、verify、group；直接调用 reset、p.getSceneJSON、assert.equal、p.beginHistoryTransaction、action 等。 */ async function transaction(name,action,verify,group=false){reset();const before=p.getSceneJSON();if(group)assert.equal(p.beginHistoryTransaction(name),true);action();verify();if(group)assert.equal(p.commitHistoryTransaction(),true);p.pushHistory(name);const after=p.getSceneJSON();assert.notEqual(after,before);p.undo();assert.equal(p.getSceneJSON(),before,'Exact Undo '+name);p.redo();assert.equal(p.getSceneJSON(),after,'Exact Redo '+name);const migrated=wasm.module.migrate_project_json(after);assert.ok(p.loadProject(migrated));assert.equal(p.getSceneJSON(),after,'Native reopen '+name)}
 for(const [axis,coordinate,target] of [['left','x',0],['center-x','x',5],['right','x',10],['bottom','y',1],['center-y','y',7],['top','y',13]])await check('Alignment '+axis+' applies world coordinates and retains exact Undo Redo reopen',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Align '+axis,/* 调用 assert.equal(a.alignSelection(axis),true) 并返回调用结果。 */ ()=>assert.equal(a.alignSelection(axis),true),/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assert.ok、every、positions；返回表达式求值结果。 */ ()=>assert.ok(positions().every(/* 比较 p[coordinate] 与 target，返回严格相等的判断结果。 */ p=>p[coordinate]===target))));
 for(const axis of ['x','y'])await check('Distribution '+axis+' preserves endpoints and spaces the middle entity',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Distribute '+axis,/* 调用 assert.equal(a.distributeSelection(axis),true) 并返回调用结果。 */ ()=>assert.equal(a.distributeSelection(axis),true),/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 map、positions、assert.equal。 */ ()=>{const values=positions().map(/* 返回 p[axis] 的当前值。 */ p=>p[axis]);assert.equal(values[1],(values[0]+values[2])/2)}));
 for(const axis of ['x','y'])await check('Mirroring '+axis+' is reversible for all selected entities',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Mirror '+axis,/* 调用 assert.equal(a.mirrorSelection(axis),true) 并返回调用结果。 */ ()=>assert.equal(a.mirrorSelection(axis),true),/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 map、positions、assert.ok。 */ ()=>{const values=positions().map(/* 返回 p[axis] 的当前值。 */ p=>p[axis]);assert.ok(values[0]>values[2])}));
 for(const clockwise of [false,true])await check('Quarter-turn '+clockwise+' changes rotation and round trips',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Rotate',/* 调用 assert.equal(a.rotateSelection90(clockwise),true) 并返回调用结果。 */ ()=>assert.equal(a.rotateSelection90(clockwise),true),/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 current、assert.ok、Math.abs、h.worldTransform、world；包含循环处理。 */ ()=>{for(const entity of current())assert.ok(Math.abs(h.worldTransform(entity,world()).rotation-(clockwise?-1:1)*Math.PI/2)<1e-9)}));
 await check('Grouping preserves child world positions and component identity across Undo and reopen',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction。 */ ()=>{let before;return transaction('Group',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 positions、assert.ok、a.groupSelection；写入 before。 */ ()=>{before=positions();assert.ok(a.groupSelection())},/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assert.deepEqual、positions、assert.ok、every、current 等。 */ ()=>{assert.deepEqual(positions(),before);assert.ok(current().every(/* 返回 x.parentUuid 的当前值。 */ x=>x.parentUuid));assert.equal(new Set(current().map(/* 返回 x.parentUuid 的当前值。 */ x=>x.parentUuid)).size,1)})});
 for(const [name,action,verify] of [
 ['Translate',/* 调用 g.applyTranslation(snap,{x:3,y:-2},world()) 并返回调用结果。 */ snap=>g.applyTranslation(snap,{x:3,y:-2},world()),/* 调用 assert.deepEqual(positions(),[{x:3,y:-1},{x:5,y:3},{x:13,y:11}]) 并返回调用结果。 */ ()=>assert.deepEqual(positions(),[{x:3,y:-1},{x:5,y:3},{x:13,y:11}])],
 ['Rotate',/* 调用 g.applyRotation(snap,{x:0,y:0},Math.PI/2,world()) 并返回调用结果。 */ snap=>g.applyRotation(snap,{x:0,y:0},Math.PI/2,world()),/** 结构说明（自动提取）：匿名回调；无显式参数；直接调用 positions、assert.ok、Math.abs。 */ ()=>{const p=positions()[0];assert.ok(Math.abs(p.x+1)<1e-9&&Math.abs(p.y)<1e-9)}],
 ['Scale',/* 调用 g.applyScale(snap,{x:0,y:0},{x:2,y:3},world()) 并返回调用结果。 */ snap=>g.applyScale(snap,{x:0,y:0},{x:2,y:3},world()),/* 调用 assert.deepEqual(positions(),[{x:0,y:3},{x:4,y:15},{x:20,y:39}]) 并返回调用结果。 */ ()=>assert.deepEqual(positions(),[{x:0,y:3},{x:4,y:15},{x:20,y:39}])]
 ])await check('Gizmo '+name+' groups continuous edits into one reversible document transaction',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction(name,/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assert.equal、p.beginHistoryTransaction、action、g.captureTransforms、map 等。 */ ()=>{assert.equal(p.beginHistoryTransaction('Gizmo '+name,'gizmo:'+name),true);action(g.captureTransforms(current().map(/* 返回 x.id 的当前值。 */ x=>x.id),world()));assert.equal(p.commitHistoryTransaction(),true)},verify));
 await check('Clipboard paste remaps identities and retains all selected source entities',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Paste',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assert.equal、p.copySelectedEntities、p.pasteEntities。 */ ()=>{assert.equal(p.copySelectedEntities(),3);assert.equal(p.pasteEntities({x:3,y:4}).length,3)},/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assert.equal、filter、world、Set、map。 */ ()=>{assert.equal(world().filter(/* 调用 x.name.startsWith('Edit ') 并返回调用结果。 */ x=>x.name.startsWith('Edit ')).length,6);assert.equal(new Set(world().map(/* 返回 x.uuid 的当前值。 */ x=>x.uuid)).size,world().length)}));
 await check('Duplicate selection uses independent identities and reversible document history',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Duplicate selection',/* 调用 assert.equal(p.duplicateSelectedEntities().length,3) 并返回调用结果。 */ ()=>assert.equal(p.duplicateSelectedEntities().length,3),/* 调用 assert.equal(world().filter(x=>x.name.startsWith('Edit ')).length,6) 并返回调用结果。 */ ()=>assert.equal(world().filter(/* 调用 x.name.startsWith('Edit ') 并返回调用结果。 */ x=>x.name.startsWith('Edit ')).length,6)));
 await check('Single duplication and entity deletion honor explicit command transaction boundaries',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Duplicate/delete',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 p.duplicateEntity、current、assert.ok、p.deleteEntity。 */ ()=>{const copy=p.duplicateEntity(current()[0].id);assert.ok(copy);p.deleteEntity(current()[1].id)},/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assert.ok、some、world、assert.equal、filter。 */ ()=>{assert.ok(!world().some(/* 比较 x.uuid 与 ids[1]，返回严格相等的判断结果。 */ x=>x.uuid===ids[1]));assert.equal(world().filter(/* 调用 x.name.startsWith('Edit ') 并返回调用结果。 */ x=>x.name.startsWith('Edit ')).length,3)}));
 await check('Front/back entity ordering is serialized and restored by document history',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Entity order',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 p.moveToFront、current、p.moveToBack。 */ ()=>{p.moveToFront(current()[0].id);p.moveToBack(current()[2].id)},/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assert.equal、at、world。 */ ()=>{assert.equal(world().at(-1).uuid,ids[0]);assert.equal(world()[0].uuid,ids[2])}));
 await check('Empty and locked selections do not create alignment changes',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 reset、p.selectEntities、p.getSceneJSON、assert.equal、a.alignSelection 等；写入 entity.editorLocked；包含循环处理。 */ ()=>{reset();p.selectEntities([]);const before=p.getSceneJSON();assert.equal(a.alignSelection('left'),false);assert.equal(a.groupSelection(),null);assert.equal(a.rotateSelection90(),false);assert.equal(p.getSceneJSON(),before);p.selectEntities(current().map(/* 返回 x.id 的当前值。 */ x=>x.id));for(const entity of current())entity.editorLocked=true;const locked=p.getSceneJSON();assert.equal(a.distributeSelection('x'),false);assert.equal(p.getSceneJSON(),locked)});
 await check('Isolation can restore prior visibility without modifying transforms',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 reset、p.getSceneJSON、p.selectEntities、current、a.toggleIsolateSelection 等。 */ ()=>{reset();const before=p.getSceneJSON();p.selectEntities([current()[0].id]);a.toggleIsolateSelection();assert.equal(current()[0].editorVisible,true);assert.equal(current()[1].editorVisible,false);a.toggleIsolateSelection();assert.equal(p.getSceneJSON(),before)});
 for(const kind of ['Canvas','Panel','Image','Text','Button','Slider','ProgressBar','Checkbox','TextInput'])await check('Create '+kind+' retains UI component and reversible parent ownership',/* 调用 transaction('Create '+kind,()=>p.createUiEntity(kind),()=>assert.ok(world().some(x=>x.hasComponent(kind)))) 并返回调用结果。 */ ()=>transaction('Create '+kind,/* 调用 p.createUiEntity(kind) 并返回调用结果。 */ ()=>p.createUiEntity(kind),/* 调用 assert.ok(world().some(x=>x.hasComponent(kind))) 并返回调用结果。 */ ()=>assert.ok(world().some(/* 调用 x.hasComponent(kind) 并返回调用结果。 */ x=>x.hasComponent(kind)))));
 await check('Create TileMap retains its concrete component through history and native reopen',/* 调用 transaction('Create TileMap',()=>p.createTileMapEntity(),()=>assert.ok(world().some(x=>x.hasComponent('TileMap2D')))) 并返回调用结果。 */ ()=>transaction('Create TileMap',/* 调用 p.createTileMapEntity() 并返回调用结果。 */ ()=>p.createTileMapEntity(),/* 调用 assert.ok(world().some(x=>x.hasComponent('TileMap2D'))) 并返回调用结果。 */ ()=>assert.ok(world().some(/* 调用 x.hasComponent('TileMap2D') 并返回调用结果。 */ x=>x.hasComponent('TileMap2D')))));
 await check('Layer add and duplicate preserve named layers and remap entity identities',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Layers',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 current、e.addLayer、e.duplicateLayer。 */ ()=>{const source=current()[0].layer;e.addLayer();e.duplicateLayer(source)},/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assert.ok、assert.equal、Set、map、world。 */ ()=>{assert.ok(e.editorState.layers.length>=3);assert.equal(new Set(world().map(/* 返回 x.uuid 的当前值。 */ x=>x.uuid)).size,world().length)},true));
 await check('Layer front/back ordering is reversible without changing entity transforms',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Layer order',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 e.addLayer、current、e.moveLayerToBack、e.moveLayerToFront；写入 […].layer。 */ ()=>{e.addLayer();current()[0].layer=e.editorState.activeLayer;e.moveLayerToBack(e.editorState.activeLayer);e.moveLayerToFront(e.editorState.activeLayer)},/* 调用 assert.equal(world().at(-1).uuid,ids[0]) 并返回调用结果。 */ ()=>assert.equal(world().at(-1).uuid,ids[0]),true));
 await check('Layer deletion removes only its entities and Undo restores the document',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Layer delete',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 e.addLayer、current、e.deleteLayer；写入 […].layer。 */ ()=>{e.addLayer();current()[0].layer=e.editorState.activeLayer;e.deleteLayer(e.editorState.activeLayer)},/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assert.ok、some、world；返回表达式求值结果。 */ ()=>assert.ok(!world().some(/* 比较 x.uuid 与 ids[0]，返回严格相等的判断结果。 */ x=>x.uuid===ids[0])),true));
 for(const kind of ['Circle','Triangle'])await check('Create '+kind+' preserves geometry through history and native reopen',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Create '+kind,/* 调用 a.createAuthoringObject(kind,{x:6,y:8},false) 并返回调用结果。 */ ()=>a.createAuthoringObject(kind,{x:6,y:8},false),/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 at、world、assert.equal。 */ ()=>{const entity=world().at(-1);assert.equal(entity.entityType,kind);if(kind==='Triangle')assert.equal(entity.vertices.length,3)}));
 await check('Manual connection route and repatch retain endpoints through history and native reopen',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Manual route',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 p.addConnection、map、slice、current、c.setManualRoute 等。 */ ()=>{const link=p.addConnection(current().slice(0,2).map(/* 返回 x.id 的当前值。 */ x=>x.id));c.setManualRoute(link,[{x:0,y:1},{x:1,y:4},{x:2,y:5}],world());c.repatchConnection(link,world())},/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 p.physicsState.world.connections.at、assert.ok、assert.deepEqual、link.manualSegments[…].at。 */ ()=>{const link=p.physicsState.world.connections.at(-1);assert.ok(link.restLengths[0]>0);assert.deepEqual(link.manualSegments[0][0],{x:0,y:0});assert.deepEqual(link.manualSegments[0].at(-1),{x:1,y:0})},true));
 await check('Binding translates only the connected compound and preserves relative positions',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Bind compound',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 p.addConnection、map、slice、current、assert.equal 等。 */ ()=>{const link=p.addConnection(current().slice(0,2).map(/* 返回 x.id 的当前值。 */ x=>x.id));assert.equal(c.configureBinding(link,world()),true);assert.equal(c.boundCompoundEntityIds(current()[0].id,p.physicsState.world.connections,world()).size,2);c.translateBoundCompound(current()[0].id,{x:3,y:-2},p.physicsState.world.connections,world())},/* 调用 assert.deepEqual(positions(),[{x:3,y:-1},{x:5,y:3},{x:10,y:13}]) 并返回调用结果。 */ ()=>assert.deepEqual(positions(),[{x:3,y:-1},{x:5,y:3},{x:10,y:13}]),true));
 await check('Connection repair and delete preserve unrelated links',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Repair connection',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 p.addConnection、map、slice、current、p.repairConnection 等；写入 link.breakState、link.breakLink。 */ ()=>{const link=p.addConnection(current().slice(0,2).map(/* 返回 x.id 的当前值。 */ x=>x.id));link.breakState='broken';link.breakLink=0;p.repairConnection(link.id);assert.equal(link.breakState,'intact');assert.equal(link.breakLink,-1);const extra=p.addConnection(current().slice(1).map(/* 返回 x.id 的当前值。 */ x=>x.id));p.deleteConnection(extra.id)},/* 调用 assert.equal(p.physicsState.world.connections.length,1) 并返回调用结果。 */ ()=>assert.equal(p.physicsState.world.connections.length,1),true));
 await check('Blueprint derived asset and instance keep source identities across document history',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Blueprint authoring',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 blueprints.authorBlueprintFromEntity、current、blueprints.authorDerivedBlueprint、blueprints.authorBlueprintInstance、assert.ok 等。 */ ()=>{const base=blueprints.authorBlueprintFromEntity(current()[0]),derived=blueprints.authorDerivedBlueprint(base,'Derived audit');const instances=blueprints.authorBlueprintInstance(derived);assert.ok(instances.length);assert.notEqual(instances[0].uuid,ids[0])},/* 调用 assert.ok(world().length>3) 并返回调用结果。 */ ()=>assert.ok(world().length>3),true));
 await check('Scene asset instantiation and unpack preserve independent entity ownership',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Scene instance',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 scenes.createSceneAssetFromEntities、current、assert.ok、scenes.instantiateSceneAsset、assert.equal 等。 */ ()=>{const reference=scenes.createSceneAssetFromEntities([current()[0].id],'Scene audit');assert.ok(reference);const instances=scenes.instantiateSceneAsset(reference,{x:20,y:20});assert.equal(instances.length,1);assert.equal(scenes.sceneInstanceEntities(instances[0]).length,1);assert.equal(scenes.unpackSceneInstance(instances[0]),true);assert.equal(scenes.unpackSceneInstance(instances[0]),false)},/* 调用 assert.ok(world().length>3) 并返回调用结果。 */ ()=>assert.ok(world().length>3),true));
 await check('Delete selected entities removes only the selection and restores it through history',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Delete selected',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 p.selectEntities、current、p.deleteSelected。 */ ()=>{p.selectEntities([current()[0].id]);p.deleteSelected()},/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assert.ok、some、world。 */ ()=>{assert.ok(!world().some(/* 比较 x.uuid 与 ids[0]，返回严格相等的判断结果。 */ x=>x.uuid===ids[0]));assert.ok(world().some(/* 比较 x.uuid 与 ids[1]，返回严格相等的判断结果。 */ x=>x.uuid===ids[1]))},true));
 await check('Clear scene preserves a reversible empty world',/* 调用 transaction('Clear scene',()=>p.clearScene(),()=>assert.equal(world().length,0),true) 并返回调用结果。 */ ()=>transaction('Clear scene',/* 调用 p.clearScene() 并返回调用结果。 */ ()=>p.clearScene(),/* 调用 assert.equal(world().length,0) 并返回调用结果。 */ ()=>assert.equal(world().length,0),true));
 await check('Prefab replacement refresh and unpack retain entity identities and reversible ownership',/** 结构说明（自动提取）：check 回调；无显式参数；直接调用 transaction；返回表达式求值结果。 */ ()=>transaction('Prefab operations',/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 prefabs.createPrefabFromEntities、current、assert.ok、prefabs.replaceEntitiesWithPrefab、prefabs.refreshPrefabAssetInstances 等。 */ ()=>{const reference=prefabs.createPrefabFromEntities([current()[0].id],'Audit prefab');assert.ok(reference);const replacements=prefabs.replaceEntitiesWithPrefab([current()[1].id],reference);assert.ok(replacements.length);assert.ok(prefabs.refreshPrefabAssetInstances(reference)>=0);const live=world().find(/* 比较 x.uuid 与 replacements[0].uuid，返回严格相等的判断结果。 */ x=>x.uuid===replacements[0].uuid);assert.ok(live);assert.equal(prefabs.resetPrefabOverride(live,'missing.field'),false);assert.equal(prefabs.unpackPrefabInstance(live),true);assert.equal(live.prefabAsset,null)},/** 结构说明（自动提取）：transaction 回调；无显式参数；直接调用 assert.ok、some、world；返回表达式求值结果。 */ ()=>assert.ok(!world().some(/* 比较 x.uuid 与 ids[1]，返回严格相等的判断结果。 */ x=>x.uuid===ids[1])),true));
}finally{await opened.close()}
await audit.write(checks,'Actual authoring/gizmo/clipboard/UI/layer operations with named transaction history and native reopen; viewport gestures, export pixels and all malformed API domains remain separate.')

