**语言：** 中文 | [English](./README.md)

# Nova_A 2D 游戏引擎与编辑器

[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](./LICENSE.md)
[![Tier 1](https://img.shields.io/badge/Tier%201-Windows%20%7C%20Web-63c6ff)](./docs/PLATFORM_BUILD_MATRIX_5_0.md)
[![Release](https://img.shields.io/badge/release-26.04-63c6ff)]()

Nova_A 是一款开源 2D 游戏引擎与桌面编辑器，使用 Rust、WebAssembly、Vue 3 与 Tauri 构建。

版本 **26.04** 完成资产、2D 内容与可复用资源库生态，同时保持 Project Format 2/schema 29 与全部冻结契约。双向依赖视图、循环及缺失引用诊断、确定性缩略图、统一导入生产配置、命名资源变体、本地化导入验证和可信离线内容发现，与已有工作线程／缓存管线及全部创作动画功能共同工作。

**手册：** [中英德三语交互网页](./manual/index.html) · [中文 Markdown](./manual/MANUAL.zh-CN.md) · [English](./manual/MANUAL.en.md) · [Deutsch](./manual/MANUAL.de.md)

## 26.04 更新内容

- **完整路线：** [26.01–26.10 实施手册](docs/ROADMAP_26_01_TO_26_10.md)与[竞品审查](docs/COMPETITIVE_REVIEW_26_01.md)。
- **功能事实清单：** [完整功能与绑定清单](docs/FEATURE_INVENTORY_26_04.md)逐项记录绑定、验证、撤销、持久化、运行时／导出、文档与测试。
- **资产与资源库生态：** [26.04 实现契约](docs/ASSET_CONTENT_LIBRARY_26_04.md)记录依赖可视化、确定性导入／重新导入、缩略图、生产配置、资源变体、可信离线发现、修复与导出闭包。
- **26.04 布局审计：** [本地化资产工作区审计](docs/UI_LAYOUT_AUDIT_26_04.md)覆盖依赖栏、生产配置、变体、缩略图、主题、缩放、键盘焦点、滚动及窄布局。
- **语言与调试深度：** [26.03 实现契约](docs/LANGUAGE_DEBUGGING_26_03.md)说明可选类型／数据、模块、语句映射、任务取消、监视、断点、LSP 3.17、代码↔图表覆盖率、逃生积木、差异与合并。
- **26.03 布局审计：** [本地化脚本工作室与图表审计](docs/UI_LAYOUT_AUDIT_26_03.md)覆盖类型／语句窗格、调试任务、实时值、覆盖率卡片、“执行 Rhai”源码、主题、缩放与紧凑布局。
- **对象事件工作流：** [事件表与对象蓝图指南](docs/OBJECT_EVENT_AUTHORING_26_02.md)记录运行时绑定、继承、验证、可复用组合和引导创建。
- **流畅图表创作：** [可视化图表性能与交互契约](docs/VISUAL_GRAPH_PERFORMANCE_26_02.md)记录焦点缩放、直接连线、索引布局、逐帧合并、裁剪及保留的视觉反馈。
- **26.02 布局审计：** [面板与本地化审计](docs/UI_LAYOUT_AUDIT_26_02.md)覆盖新的三列事件表工作室及保留的全局布局约束。
- **20 个已验证模板：** 场景、测试和预制玩法支持搜索、难度、预计配置时间、能力与标签筛选；详见[模板库](docs/TEMPLATE_LIBRARY_26_01.md)。
- **真正双向的可视化脚本：** 选择现有 `.rhai` 后切换会打开其准确关联图；变量、自定义积木、`if/else`、有界循环、运算符及 API 值／命令都会结构化转换；详见[可视化脚本契约](docs/VISUAL_SCRIPTING_26_01.md)。
- **所有面板的布局约束：** 字段不会溢出卡片，翻译文本会换行，标签栏具有可达滚动路径，窄宽度下密集工作室会转为单列；详见[布局审计](docs/UI_LAYOUT_AUDIT_26_01.md)。
- **版本兼容：** [2026 版本策略](docs/VERSIONING_2026.md)说明公开版本与机器版本映射；旧项目仅将兼容上限封存为 `<27.0.0`，不重写 schema。

## 从 7.0.0 继承的稳定平台

- **平台就绪情况：**“管理 → 学习中心 → 平台就绪情况”对全部 358 项公开功能执行七维审计，并明确区分不适用与仍需外部证据的项目。
- **稳定契约：**Project Format 2/schema 29、Rhai API 2、Graph Format 1、Plugin API 2、Package Manifest 1、Build CLI 1 与 workspace document 3 保持兼容；下次破坏性契约决策等待证据后再进行。
- **安全 6.x 迁移封存：**干运行预览、语义差异、备份、确定性规范应用和回滚只把旧引擎上限更新为 `<8.0.0`；未来 schema 仍安全阻断。
- **完整教学：**每项公开功能均有中／英／德分类、前提、准确流程、预期结果、持久化／导出、撤销／恢复、常见错误、无障碍、示例与 Rhai／可视化图等价说明。
- **诚实的平台支持：**Windows 与 Web 为本地验证 Tier 1；Linux／macOS 需要匹配主机，Android 仍为工具链门禁实验目标，iOS／主机平台延期，3D 不在范围内。
- **发布资格：**稳定平台／迁移参考、历史黄金夹具、正常用户交互／本地化布局审计、保留的运行时／安全／性能检查、干净源码构建与精确十一文件组成本地证据。独立观察、签名、匹配主机构建和真实长测仍为外部工作。

请阅读[稳定创作平台契约](./docs/STABLE_CREATOR_PLATFORM_7_0.md)、[API 参考](./docs/API_REFERENCE_7_0.md)、[迁移指南](./docs/MIGRATION_7_0.md)和[故障排除](./docs/TROUBLESHOOTING_7_0.md)。

## 保留的 v6.9.0 生态与发布基线

- `pnpm package:publisher` 在本地验证、打包、复现并镜像有界 Package Manifest 1 压缩包；私钥与网络发布保持外部且必须明确执行。
- 依赖锁新增可读的排序求解轨迹；签名公告的序列／指纹检查会执行吊销，并默认阻止高危／严重漏洞。
- 应用签名更新为选择启用且只生成计划：不会隐式获取或替换；旧序列、重放、降级与错误哈希都会阻止，提交版本保留回滚身份。
- 团队工作流新增确定性变更列表、CODEOWNERS 匹配及项目／设置／场景／资源／可视化图表三方语义合并；每个真实冲突都必须明确选择。
- 生态系统工作室的“发布交付”页用中英德显示发布命令、信任状态、求解决策、更新状态、目标主机流水线、签名钩子和完整生命周期说明。
- 确定性软件包／安全／更新／合并夹具、两个发布参考、程序员／用户审计、Windows／Web 冒烟、证据哈希与精确十一文件打包提供本地证据；签名身份、干净机器生命周期、第二台机器复现、非 Windows 目标主机、独立审查与真实长测仍为外部工作。

详见 [6.9 生态、协作与发布交付指南](./docs/ECOSYSTEM_COLLABORATION_SHIPPING_6_9.md)。

## 保留的 v6.8.0 性能基线

数据导向组件索引、预备式层级／空间查询、有界 Worker／后台／流式路径、实时延迟／1% Low／启动证据、自适应仅显示质量以及 1 万／5 万／10 万对象夹具全部保持不变。

## 保留的 v6.7.0 设备基线

- 无需代码即可创作遵循安全区的虚拟摇杆、方向键与按钮；所有操作通过同一 Input Map 与键盘、鼠标和手柄输入汇合。
- 触摸事件会与浏览器兼容鼠标事件去重，且平移、缩放、旋转、双指平移、轻触、双击、长按和滑动均可确定性绑定到操作。
- 手柄支持引导式重映射、死区／反向／曲线校准与设备感知提示。运动传感器需要用户手势与明确授权；不支持触觉反馈或方向锁定时会显示清楚的降级状态。
- 设备预览可旋转常用尺寸并显示安全区、DPI 与最小触控目标。运行时无障碍可导出角色／名称／状态／数值／焦点语义证据，并支持 200–400% 文字检查。
- 可选 Android 流程会发现本地 JDK／SDK／NDK／平台工具与已审核 Gradle 模板，验证最小权限及用途，生成 Manifest，并使用非明文环境变量完成签名，同时提供有界安装与日志读取。缺少任意门禁时会返回精确阻断原因，而不会伪造 APK。
- 两个无代码参考项目分别教学触控平台玩法和同一项目的 Android 门禁交付；中英德手册均包含完整流程。

详见 [6.7 设备、移动端与无障碍指南](./docs/DEVICE_MOBILE_ACCESSIBILITY_6_7.md)。

## 保留的 v6.6.0 多人游戏基线

- 可选传输适配器与身份验证提供器必须先通过有界身份、权限、文档与安全元数据审核，才能使用。
- Protocol 2 的附加安全信封包含 epoch／nonce／时间／证明；畸形、超限、限速、过期、未来、重放、Schema 错误和认证失败数据包会在玩法分发前被拒绝。
- 加密提示会区分本地／WSS／已审核加密适配器与未加密 UDP／WS；项目要求加密时会阻止不安全构建。
- 服务器／所有者权威可安全转移，断线后返回主机／服务器；按场景／半径的兴趣过滤及主机／服务器场景交接均有界且可检查。
- 网络工作室新增显式本地大厅创建／发现、确定性 2–8 节点计划、已构建 Windows 播放器启动、独立日志／检查器身份、已验证节点、所有权、复制差异、回滚时间线及弱网控制。
- 双人协作与无头权威参考项目以及中英德教学流程覆盖运行、重连、后加入、保存／回放、撤销权限、客户端／服务器导出与不变的离线运行。

详见 [6.6 生产级多人游戏指南](./docs/MULTIPLAYER_PRODUCTION_6_6.md)。

## 保留的 v6.5.0 物理与渲染基线

- 组合体保留每个子碰撞器的精确几何、稳定接触／传感器身份、层／掩码策略及按面积与平行轴定理计算的质量和惯量，不再用凸包近似。
- 静态／运动学 Chain 与简单 Concave 会确定性分解为精确求解片段；不安全的动态 Chain／Concave 会给出改用凸子碰撞器的明确修复提示。
- CCD 同时考虑旋转与组合体最远半径；稳定流形会热启动；接触／约束岛整体睡眠和唤醒；关节电机、限位与断裂力使用正确的线性／角向轴。
- Rope2D 排除两个所属物体，但会与符合层掩码的第三方子碰撞器碰撞，并通过锚点传递冲量和保留拉伸／弯曲断裂。
- 物理调试显示真实求解几何；渲染诊断覆盖 Pass、Shader、灯光、粒子、纹理、上传与上下文恢复；质量体积确定性选择相机局部预算且不会删除特效。
- 发布门禁覆盖解析物理、单位／比例、流形、组合体、Chain／Concave、CCD、关节、绳索、确定性、模糊输入、渲染回退／预算、中英德用户流程、原生／Web 构建与精确十一发布文件。

详见 [6.5 生产物理与渲染指南](./docs/PHYSICS_RENDERER_6_5.md)。

## 保留的 v6.4.0 内容与动画基线

- 可导入 Aseprite JSON、TexturePacker／通用图集以及 Tiled TMX／JSON／TSX，并保留稳定帧 ID、轴心、碰撞器、标签、时长、引用和可见诊断；失败的重新导入会保留最后有效产物。
- 资源上下文页显示来源与切片，并可编辑六类 Resource：材质、动画库、输入映射、物理材质、主题和数据表。共享父资源与仅含局部字段的覆盖会确定性解析，循环引用会阻止构建。
- 动画新增蒙皮权重热图、有界自动权重、骨骼约束上下文、使用运行时采样的洋葱皮、保留的曲线编辑、重定向诊断和精确根运动预览。
- 128 KiB 以上元数据使用有超时限制的 Worker，规范化导入使用有界缓存；前后性能证据与大型图集／时间线测试启动了将在 6.8.0 完成的性能计划。
- 程序员与用户门禁覆盖正常／恶意导入、重新导入身份、精度、Resource 继承／循环／导出、动画播放、中英德布局、原生／Web 构建及精确十一发布文件。

详见 [6.4 内容与动画指南](./docs/CONTENT_ANIMATION_6_4.md)。

## 保留的 v6.3.0 自动化与积木基线

- 保存任意 Rhai 脚本会自动创建或更新关联 `.nova-graph`；保存图表也会创建或更新关联 `.rhai`。可识别的生命周期／API 代码会成为可编辑类型积木，其余源码会完整保存在可见的“代码”积木中。
- 可视化编辑器默认提供类 Scratch 分类、彩色堆叠／帽子／值／布尔积木及顺序放置；高级“节点”视图继续保留类型连线、重构、诊断和调试。
- “管理 → 自动化”提供本地 Rhai 模板、明确权限、试运行差异、有界执行、取消、单事务应用、回滚与轨迹；不能访问文件系统、进程、网络、时钟或随机数。
- WASM 插件在初始化前检查架构、签名、导入及导出。用户逐项审核权限后才启用；命令、选择、检查器／操纵器、资源导入／编辑和构建贡献都跟随真实插件生命周期。
- 程序员与用户门禁覆盖拒绝测试、过期句柄、恶意导入、1000 命令上限、异常回滚、热卸载／重载、代码图表自动往返、中英德布局、原生／Web 构建及精确十一发布文件。

## 保留的 v6.2.0 脚本基线

- Script Studio 的“行为契约”页会解释依赖、预算、API 模块、线程规则、确定性、权限和精确诊断。
- 新脚本模板会主动使用版本化注释契约；没有契约头的旧脚本保持原有行为与全局沙箱上限。
- 运行时、项目健康和构建共用同一分析器，检查组件、输入、资源、软件包、确定性 API 与回调预算。

## 保留的 v6.1.0 改进

- 最大化启动优先于旧窗口尺寸，同时保留边框、还原／缩放、显示器位置恢复和可选的真正全屏命令。
- 活跃构建／导出路径均与仓库相对；pnpm 覆盖项已迁移到当前工作区配置；移动后可用冻结锁文件重建依赖链接。
- 系统优先字体、语义化明暗材质、统一圆角、舒适对比度与自然动画遵循 Apple HIG 的层级、颜色、字体、布局、无障碍和动效原则，不捆绑 Apple 专有字体。
- 高频鼠标事件按显示帧合并；选择叠加层不再计算所有对象边界或重排场景；连接签名、渲染图和性能记录避免重复工作。
- 高级工作区与对话框按需缓存并在空闲时预热；检查器首次使用后保留；大型停靠面板不再实时模糊动态画布。
- 类型、Rust、模板／游戏、交互、英德中布局、本地浏览器、迁移、构建、原生打包和精确发布文件门禁覆盖 6.1.0。

## 保留的 v6.0.4 修正

- Windows 原生导出先在同目录完整写入并验证暂存程序。若旧输出仍在运行并被锁定，Nova_A 会保留该进程并生成带构建 ID 的确定性 `.exe`，不会再报“拒绝访问（os error 5）”。
- 可视化图表可创建关联 Rhai 资源。保存图表会重新生成关联代码；保存关联代码会更新图表变量、字面量、API 节点输入，并把无法对应标准节点的源码保存在明确可见的“代码”节点中。
- 图表编译不再跟随每次指针移动重复执行；节点／端口查询与小地图边界使用索引，大型图表拖动不会反复扫描全部节点。
- 启动器与播放器按需加载完整工作区；渲染排序不再进行平方级 `indexOf` 查询；编辑状态下自适应采样性能数据；“低配置”模式只限制空闲场景画布，工具、相机动画和游戏运行仍保持完整更新。
- 6.0.4 门禁覆盖类型、Rust 文件锁回归、图表往返、浏览器交互／布局、模板、原生构建／游戏导出与精确发布文件。

## 保留的 v6.0.3 修正

- 全部 12 个启动模板在未编辑构建设置时都会解析到真实的 Windows x64 播放器模板。
- 旧版 `windows-x86_64-v1` 等自动拼接 ID 会迁移到兼容的已登记模板，同时保留自定义 ID。
- `RectTransform` 默认不参与焦点；按钮、滑块、复选框和文本输入会自动获得焦点角色与名称。
- 朗读顺序 `0` 表示自动场景顺序；只有冲突的正数明确顺序才会产生重复警告。
- 6.0.3 门禁覆盖模板、无障碍、类型、Rust、渲染、交互、本地化、原生导出和发布文件。

## v6.0.2 更新内容

- **所有支持缩放均可操作：**外壳行高跟随无障碍缩放，密集导航切换为图标优先，长文本在面板边界内换行，浮动界面不会跑出窗口。
- **真实用户式交互审计：**自动遍历中／英／德界面，登记控件，打开各工作区与面板，修改并还原安全设置，检查缩放条与重新排序，并明确记录有破坏性或受上下文限制的控件。
- **更安全的后端导出：**限制网页负载数量／单项／总大小，严格校验相对路径，防止覆盖源程序或当前编辑器，并以可回滚原子替换写入输出；Android 能力与架构判断也已修正。
- **真正的便携游戏：**CLI 与原生路径共用带 SHA-256 校验的内嵌 `.nova-pak` 尾部；发布冒烟测试会单独启动导出的“鼠标击退”游戏 `.exe`。
- **发布文件完整：**继续强制精确十一文件和十条非循环 SHA-256 校验。

## 保留的 v6.0.1 可玩基线

- **语言归属修复：**网页手册会在生成结束后动态查找全部文章，只显示所选语言；搜索、目录、主题提示与书签均按语言工作；生成内容位于有效 HTML 文档内部。
- **清晰的首个游戏教程：**中、英、德三种手册都准确讲解“选择模板 → 播放 → 绘制对象 → 物理 → 脚本 → UI → 项目健康 → 便携 `.exe`”完整流程。
- **可玩的鼠标击退模板：**用鼠标移动蓝色运动学方块，把八个动态预制体目标撞出摄像机；每个目标加一分，8 / 8 时显示祝贺条。
- **准确的鼠标物理坐标：**Rhai API v2 新增 `mouse_world_x/y`、`view_min/max_x/y` 与视口尺寸；回放与网络回放保存这些数据，并兼容旧记录。
- **发布完整性：**没有修改项目架构，也没有移除任何稳定合约、现有模板、功能、动画、快捷键或渲染路径。

## 保留的 v6.0.0 创作者基线

- **做中学：**首次引导及“管理 → 学习”用准确步骤、持久化、恢复、无障碍、示例及 Rhai／可视化图 API 教授每项公开功能。
- **十二个完整项目：**贪吃蛇、平台跳跃、俯视角、物理谜题、本地化菜单、过场动画、TileMap 世界、检查点、软件包／插件、网络、Windows 便携版和 Web 部署。
- **稳定合约：**项目、Rhai、图、插件、软件包、构建与工作区合约均公开兼容及失败关闭迁移规则。
- **大型项目：**基于映射的一万对象层级查找、五万资源及一千节点认证、有界内存与低配置创作模式。
- **完整手册：**可搜索／书签化 HTML 与三个 Markdown 手册由编辑器相同的中英德教学目录生成。
- **诚实证据：**签名、独立干净机器、第二机器复现、匹配宿主构建、独立浏览器／硬件／无障碍及真实 72 小时浸泡仍为外部门槛。

进一步阅读：[扩展与平台交付](./docs/EXTENSIONS_PLATFORM_DELIVERY_5_9.md)。5.8 的[网络与回放](./docs/NETWORKING_REPLAY_5_8.md)、5.7 的[世界、导航与 AI](./docs/WORLDS_NAVIGATION_AI_5_7.md)、5.6 的[动画、音频与电影](./docs/ANIMATION_AUDIO_CINEMATICS_5_6.md)、5.5 的[材质与特效](./docs/MATERIALS_EFFECTS_5_5.md)、5.4 的[游戏玩法框架](./docs/GAMEPLAY_FRAMEWORK_5_4.md)与 5.3 的[可视化脚本](./docs/VISUAL_SCRIPTING_5_3.md)继续有效。

### 保留的 v5.8.0 网络基线

需授权的可靠／不可靠通道、RPC 合约、有界复制、预测／回滚、弱网模拟、回放／存档、脱敏诊断与权威无头验证均保持完整。

### 保留的 v5.7.0 世界基线

分层／网格导航、一万个智能体硬边界、行为树黑板／感知／效用、流式依赖／状态交接，以及确定性的场景／预制体 TileMap 烘焙均保持完整。

### 保留的 v5.6.0 动画／音频基线

混合树、同步动画图层／过渡、运行时录制、嵌套／分支时间线、安全区域字幕、波形循环区域、混音快照／闪避及响度／设备诊断均保持完整。

### 保留的 v5.5.0 渲染基线

确定性材质图表／图层、隔离着色器、后处理预设／体积、模块化粒子、诚实的 Canvas2D／WebGL2 回退、真实预览及渲染预算建议均保持完整。

### 保留的 v5.4.0 游戏玩法基线

稳定动态对象句柄、有界查询、十一类玩法组件、游戏流程、生产级输入交互与共享 AI 诊断保持不变并完全兼容。

### 保留的 v5.1.0 可玩／导出基线

- Windows／Linux 同主机项目仍默认生成经 SHA-256 校验的单文件便携播放器，并保留显式外置数据包选择。
- 独立游戏仍只加载播放器服务；贪吃蛇模板继续作为键盘／手柄输入、信号、触发器与导出的参考。

### 保留的 v5.0.1 体验基线

- **清晰的控件层次：**工作区、变换、历史与模拟保持直接可见；布局、命令／搜索、创作、绘制、吸附、参考线、摄像机与排列命令收纳在有名称的键盘可访问组中。
- **场景工具栏无水平滚动条：**界面会自适应，不再将命令藏在类似浏览器的滑动条中。
- **可读的多语界面：**12 px 说明字下限、13 px 紧凑控件、更大正文／标题、两行上下文栏与更宽弹出层保护英文、德文和中文布局。
- **精炼主题与动效：**更深的深色表面、更柔和的浅色表面、更明确边框、角色色彩、柔和层次、统一圆角，并保留减少动效选项。
- **保持契约：**Project Format 2 仍为架构 29；Rhai API v2、Plugin API 2、Package Manifest 1、Build CLI 1、工作区文档 3、快捷键、功能与十一文件发布契约均不变。

请参阅权威的 [v5.0.1 体验规格](./instructions.txt) 与[体验指南](./docs/UX_GUIDE_5_0_1.md)。

### 保留的 v5.0.0 冻结基线

- **分级且可复现的构建：**Windows／网页 Tier-1 预设、明确的目标可用性、内容裁剪／压缩、符号／崩溃文件、来源证明、CycloneDX SBOM、安全网页标头、发布通道，以及绝不隐式执行的签名／公证钩子。
- **失败关闭的软件包：**可信注册表、固定锁文件、校验和／签名、依赖约束、离线镜像、回滚、缓存验证、许可证／漏洞策略，以及安装前发布者／权限／来源审查。
- **本地优先协作：**Git 指南、场景／资源语义比较、外部变更、CODEOWNERS、所有权、任务／变更说明、共享预设与二进制锁提示；无强制云服务或隐藏网络操作。
- **发布就绪上手：**三语离线手册、首个游戏教程、软件包／插件 SDK、构建、源代码管理、迁移与发布工程指南、直接修复链接、五个发布参考工程与自动文档校验。
- **冻结的公开契约：**新工程仅创作 Rhai API v2 脚本；4.x 工作区布局安全迁移至文档 3；API v1 只为导入的旧资源保留只读迁移兼容层。
- **清晰的发布默认项：**默认构建选择器只显示证据支持的 Windows x86-64 与 Web Tier-1 目标；Linux／macOS 保留为匹配主机的实验记录，Android 不可用。
- **可操作诊断：**启动器、Project Health、软件包、脚本与构建警告都连接到稳定的离线帮助锚点。
- **5.0 候选纪律：**功能／架构／API／产物格式于 2026 年 8 月 25 日冻结。至少 14 天观察期及外部全新环境／双机／签名矩阵会保持待处理，直到附上真实证据；不会把本机候选结果伪报成最终认证。

从[首个游戏教程](./docs/FIRST_GAME_5_0.md)、[平台／构建矩阵](./docs/PLATFORM_BUILD_MATRIX_5_0.md)、[构建／导出指南](./docs/BUILD_EXPORT_5_0.md)、[软件包／插件 SDK](./docs/PACKAGE_PLUGIN_SDK_5_0.md)、[源代码管理指南](./docs/SOURCE_CONTROL_5_0.md)、[迁移指南](./docs/MIGRATION_5_0.md)及[发布工程指南](./docs/RELEASE_ENGINEERING_5_0.md)开始。

### 保留的 v4.7 动画与运行时 UI

生产级动画、二维骨骼、响应式运行时 UI、主题、本地化、RTL、无障碍、字幕及自动输入提示均完整保留。

### 保留的 v4.6 编程工作流

Rhai API v2、语义脚本工作室、调试器、事务热重载、脚本测试／覆盖率、无界面 CI 与外部工具协议均完整保留。

### 保留的 v4.5 生产级物理

精确／平衡／快速／自定义求解配置、结构化空间／接触查询、CharacterBody2D、Area2D、生产级碰撞器／关节／Rope2D、确定性碰撞事件、物理监视器、调试覆盖和测量证据均保持不变。

### 保留的 v4.4 内容生产基础

- **确定性资源数据库：**导入器／版本／预设／设置／哈希来源、依赖循环与修复、重复源、构建闭包、内容组、标签、收藏、集合、保存搜索、网格／列表、20,000 资源增量浏览、源代码状态、外部冲突、事务文件夹移动、比较、还原与批量重新导入。
- **生产级精灵与图集：**过滤、Mipmap、色彩／透明／压缩、PPU、轴心、裁剪、网格／自动切片、九宫格、多边形／碰撞、动画提取、SVG、确定性图集报告与平台覆盖。
- **图块地图与构图：**可搜索 TileSet、完整绘制／选择工具、逐层物理／导航／遮挡、流式边界、确定性存储／历史、多摄像机、视差重复／镜像／深度，以及可复用切线路径与跟随对象。
- **运行时字体：**塑形、OpenType、微调、过采样、位图／SDF／MSDF、回退链、平台设置、编辑器／游戏所有权，以及中日韩／RTL／组合字符／Emoji 字形报告。
- **界面改进：**内置圆润 Nunito／Noto 字体、统一控件字号、更柔和的材质与圆角、有目的的变换／透明动画、清晰焦点和完整减少动态支持。

参阅[资源管线契约](./docs/ASSET_PIPELINE_4_4.md)、[精灵／图块地图／字体指南](./docs/SPRITE_TILEMAP_FONT_4_4.md)和[已知问题](./docs/KNOWN_ISSUES_4_4.md)。

### 保留的 v4.2 数据完整性基础

- **原子项目事务：**保存前完整校验和规范化，为场景、资源、脚本、动画、UI、设置、锁文件与构建数据记录日志、临时写入和校验和；任何中断都不会破坏上次手动存档。
- **确定性数据：**固定字段顺序、UTF-8/LF、有限最短往返数值、小写 UUID 引用、创作／生成数据分离、完整校验、确定性重存与按身份语义差异。
- **统一撤销与恢复：**命名／分组／嵌套历史记录资源、时间和内存预算；恢复点与手动存档分离，恢复浏览器明确预览、比较、恢复、丢弃或作为新 UUID 副本打开。
- **安全迁移与修复：**模式 5–29 使用预演、兼容／影响报告、任务日志、备份、完整验证、确定性重跑和回滚；项目健康统一只读修复、缺失引用映射、缓存重建、事务／恢复／迁移和可恢复项目回收站。
- **外部与团队安全：**引导式打开／添加／迁移／归档导入、项目锁与只读打开、自产更改抑制、分支／大量变更分类，以及比较／重载／保留编辑器／保留磁盘选择。

参阅[序列化规范](./docs/SERIALIZATION_SPECIFICATION_4_2.md)、[模式兼容矩阵](./docs/SCHEMA_COMPATIBILITY_MATRIX_4_2.md)、[项目事务](./docs/PROJECT_TRANSACTIONS_4_2.md)、[撤销覆盖](./docs/UNDO_COVERAGE_4_2.md)、[恢复指南](./docs/RECOVERY_4_2.md)与[迁移／回滚](./docs/MIGRATION_AND_ROLLBACK_4_2.md)。

### 保留的 v4.0 生产稳定基线

- **升级助手：**所有外部项目打开前都会显示格式／引擎／内容／软件包预检。受支持的 3.x 升级必须完整备份并保留回滚数据，在内存中迁移、完整验证和规范化；失败时不会替换当前会话。模式 29 保持冻结。
- **稳定支持契约：**Studio Status 提供 Stable／Beta／Development 渠道、离线已知问题、补丁政策、存档引擎指南、发布健康状态，以及需要隐私审查和明确选择加入的本地崩溃报告；不会自动上传。
- **生产文档：**三语完整手册连接 API、迁移、构建／CI、软件包、性能、安全、无障碍、故障排除、支持、许可证及小游戏创建／导出指南。
- **稳定默认值：**实验性联网继续作为可选软件包和参考工程提供，但不再属于默认项目模板。
- **最终验证：**v4 证据记录迁移、Tier-1／浏览器／布局／参考项目矩阵、软件包安全、异常输入、性能、无障碍、文档、SBOM／许可证、来源、发布健康、安装程序门槛与签字状态，绝不伪报未执行的外部门槛。

### 保留的 v3.9 产品化能力

- **构建自动化：** 验证／导入／测试／构建／导出／打包／版本命令、无界面模式、预设、清洁／增量／验证缓存、包含排除、资源剔除、压缩、大小／依赖报告、符号、平台清单、CI 与 JSONL。
- **真实的平台等级：** Windows x86-64 编辑器／运行时和 Web 运行时为 Tier 1；Linux、macOS 在同平台矩阵通过前保持实验级；移动与主机平台在 4.0 后再考虑。
- **安全软件包：** Stable 要求语义版本、引擎/API 范围、依赖哈希、入口类型、归档身份与可验证签名；权限在安装及变更前显示，失败项目进入隔离区，并支持确定性锁、离线缓存、回滚与安全模式。
- **团队协作：** 场景／预制体／资源／设置／锁文件差异、外部重新加载／比较、无变化保存、共享与本机设置、Git 初始化、可选锁、提交前／CI 模板与操作摘要。
- **支持与冻结：** 离线手册可搜索；Studio Status 提供迁移、已知问题及隐私审查后的诊断包；Schema 29、Runtime API 1、Plugin API 2、Package Manifest 1 与 Build CLI 1 已为 4.0 稳定周期冻结。
- **实验网络门槛：** 传输、RPC、权限、同步、预测、诊断和无界面接口保持可选实验状态，不阻塞 4.0 核心稳定性。

参阅[世界数据架构](./docs/WORLD_DATA_3_8.md)、[Schema 28 迁移说明](./docs/PROJECT_FORMAT_2_SCHEMA_28.md)、[视觉／音频管线](./docs/VISUAL_AUDIO_PIPELINE_3_7.md)和[Rhai API v1](./docs/RHAI_API_V1.md)。

### 保留的 v3.3 创作基础

- **通用创建：** 按 Shift+A 或“创建对象”打开可搜索的 Core／2D／Physics／UI／Audio／Camera／Navigation／Script／Packages 面板，显示兼容性、依赖、收藏与最近使用；每次创建只产生一个撤销步骤。
- **完整视口编辑：** 移动、旋转、缩放、枢轴、矩形、路径、多边形、碰撞体与标尺工具，配合对齐／分布／镜像／旋转 90°／聚焦／隔离／分组、框选和七类吸附。
- **专业检查器：** 搜索、分类、多选混合值、重置、恢复预制体覆盖、复制／粘贴值、复制路径、关键帧、帮助、固定与仅显示修改项均已连接；组件支持启停、复制／粘贴、排序、重置和移除。
- **精灵与相机：** 支持枢轴、区域、翻转、调制、九宫格边框、排序与可见性；资源导入支持透明裁剪、精灵表切片、过滤／压缩／色彩／像素画预览；相机支持限制、缩放、平滑、拖动边距、跟随、预览与像素对齐。
- **高效层级：** 重命名、重复、重设父级、排序、分组和删除均可撤销。普通拖动保留世界变换，Alt 保留局部变换，Shift 调整顺序；面包屑、组件／标签搜索、状态标记和性能模式适用于大型场景。
- **发布证据：** 新增像素画、分辨率无关、视差、多相机、嵌套预制体和 5,000 对象六个参考工程，并记录创建／往返、检查器事务、像素对齐、视口性能与平台跳跃流程结果。

### 保留的 v3.2 项目数据基础

- **权威项目清单：** 明确项目 UUID、引擎范围、Schema、包锁文件、构建预设以及源码／共享／生成／缓存／用户目录。
- **确定性项目文本：** 统一键顺序、集合数组顺序、有限数值、两空格缩进与 LF 行尾；未修改项目的重复保存字节完全一致。
- **场景与预制体：** 支持场景实例、嵌套场景、嵌套预制体层、覆盖项比较／应用／恢复／单项重置／解包、组件依赖校验与场景依赖图。
- **完整资源工作流：** 源与产物哈希、导入器版本、正反依赖、后台进度／取消／重试／日志、外部更改冲突选择、导入预设、收藏、保存筛选、缺失引用修复与删除依赖预览。
- **安全迁移与修复：** Project Manager 显示引擎／包兼容性和迁移计划；完整备份与回滚保护现有项目；未来 Schema 只允许只读检查。
- **默认窗口：** 桌面编辑器以带正常边框、可恢复和可调整大小的最大化窗口启动；F11 才进入真正全屏。
- **发布证据：** 提供确定性与无改动报告、迁移矩阵、移动／重命名引用报告、五万资源基准、参考项目、源代码／二进制包和校验和。

### 保留的 v3.1 编辑器安全基础

- **冻结公开契约：**“帮助 → 工作室状态”显示项目、运行时、插件、软件包与 CLI 版本；[契约](./docs/STABLE_CONTRACTS.md)和[兼容策略](./docs/COMPATIBILITY.md)定义 3.x 承诺。
- **覆盖全部公开迁移：**Schema 5–22 均通过预览、备份、内存校验、原子替换、未知字段保留以及 Rust 金样／损坏输入测试。
- **源码参考项目：**六个生成的 `.nova` 工程和一个无权限 Plugin API 2 示例涵盖平台跳跃、俯视角、UI、动画、音频、灯光、图块、存档、插件、测试、联网与构建流程。
- **不伪造发布证据：**基准、稳定性、干净平台与 24 小时验证工具只发布实测值；未执行项会明确标为待验证并给出修正计划。
- **更安全的错误处理：**Vue、动画帧、Promise、可选包、外链和图集错误会进入有上限的应用内诊断／恢复界面，并支持安全模式。
- **更友好的文字：**多语言圆润系统字体使用 16 px 基础与 11 px 绝对下限，同时保留紧凑模式、对比度与减少动态效果。
- **范围透明：**参阅[基准策略](./docs/BENCHMARKS.md)、[稳定性门槛](./docs/STABILITY.md)、[平台矩阵](./docs/PLATFORM_VERIFICATION.md)和[已知限制](./docs/KNOWN_LIMITATIONS.md)。

### 保留的 v2.9 发布与协作基础

- **清晰的发布工作区：**构建设置按“概览／平台／交付／团队”分页；校验结果与构建报告无需在一张超长表单中寻找。底部空间狭窄时自动切换成紧凑选择器。
- **平台交付：**Windows、Linux、macOS、Web 与条件启用的 Android 支持 Debug／Release、x86_64／aarch64、标识符、版本、图标、启动图、方向、权限及签名／公证指引。Android 仅在官方包、SDK、JDK 与本地模板齐备时可用。
- **可复现导出：**稳定项目文本、排序资源、确定性 `.nova-pak` 时间、SHA-256 构建记录、增量写入、缓存指标、补丁清单与 `pnpm export -- …` 无界面 CLI 便于 CI 审查。
- **运行与隐私：**结构化日志、崩溃记录和符号映射均为显式选项。遥测默认关闭，只允许有界标量事件，要求 HTTPS，并在启用前说明隐私约束。
- **团队协作：**UUID 状态标记、忽略规则生成器、有界原生 Diff／Merge、三方冲突检查、稳定 Scene／Prefab JSON 与到期锁文件支持安全审查。
- **软件包安全：**浏览页显示已验证发布者、权限、评分、文档与安全链接；浏览绝不执行软件包，安装必须显式触发，并支持离线／本地镜像。
- **安全升级与示例：**旧项目先预览 Schema／包影响，可下载完整备份；验证成功后才替换会话，并可下载回滚副本。空白、平台跳跃、俯视角、物理沙盒、UI 展示与可选联网模板均在创建时审计。

### 保留的 v2.8 Production Lab

- **Production Lab：**逐帧关联输入、脚本、动画、物理、音频、渲染、资源、分配与 GPU Pass；内存／生命周期快照保持有界并可比较。
- **确定性回归：**记录输入，以固定种子重放，比较物理校验和并导出 Replay 资源；Rhai 的 `random()`／`random_range()` 使用运行时种子。
- **自动测试：**Unit、Scene、Integration、Headless 测试支持断言、超时、可选截图和 JSON／JUnit CI 报告。
- **数据资源：**Schema／Table 资源校验类型字段、导入 CSV／JSON／数据库结果、生成 TypeScript 访问器，并迁移带版本的存档封装。
- **有界任务：**Worker 数、队列、取消与串行回退避免无 Worker 时阻塞或淹没界面线程。
- **可选联网：**安装官方软件包后可使用 WebSocket／原生 UDP、RPC、快照、插值、预测／回滚助手、复制预算与诊断；默认禁用且不会进入构建。
- **无头服务器：**原生构建可选择权威 Headless 模式；平台或联网软件包不满足时会在导出前阻止错误构建。
- **抗锯齿：**WebGL 多重采样、高质量 Canvas 插值、圆角线段连接和字体光学整形改善界面与游戏画面，同时保留显式最近邻像素画。

- 独立的“呈现”面板集中 UI、本地化、音频与运行时无障碍编辑，可从“界面”工作区和命令面板打开，避免继续堆叠对象检查器。
- Canvas／RectTransform／Panel 实际应用锚点、固定／填充／内容尺寸策略、安全区域、宽高比、响应式断点、水平／垂直／网格容器、裁剪、圆角遮罩、滚轮滚动视图和可复用 UI 预制体。
- `.nova-theme` 资源支持父主题继承、变量、普通／悬停／按下／禁用／聚焦状态、实时预览、样式类和控件级覆盖。
- 运行时 UI 支持可见键盘／手柄焦点、显式或空间导航、屏幕阅读器元数据以及输入重映射控件；运行时无障碍与编辑器偏好保持分离。
- 本地化表支持源／预览语言、回退链、复数／选择规则、变量、数字／日期格式、伪本地化、字体回退、RTL、实时预览和按构建语言裁剪。
- 音频图支持自定义总线、父级路由、Send、静音／独奏、带干湿比的效果、表头、快照、闪避、空间衰减曲线、流式覆盖和有界总／分总线声部。
- 音频资源新增解码波形预览、循环标记、峰值标准化、流式选择以及活动／流式／缓冲／受限声部诊断。
- 内置手册改为同源应用内覆盖层，不再让 Tauri 打开 `http://tauri.localhost/manual/index.html`，从而修复用户报告的致命 URL 权限错误。
- 每次发布包含 Web、源码、便携 EXE、MSI、NSIS、发布说明、许可证和 SHA-256 校验文件。

### 保留的 v2.6 世界与玩法基础

- CharacterBody2D 支持斜坡、台阶、地面／墙壁／天花板状态、地面吸附、移动平台速度、单向平台、土狼时间辅助和 Rhai 精确单位移动。
- Area2D 的重力、风、阻力和浮力只在权威 Rust 固定步中累加一次；伤害和自定义信号使用有界运行时事件队列。
- 导航区域／代理支持有界网格、多边形、障碍、A*／流场、局部避障、平滑、动态重烘焙和场景调试叠加层；导航与行为工具仍是独立延迟加载的软件包。
- 行为树和分层状态机资源连接运行时信号，不会进入纯物理项目。
- 图块地图新增调色板、笔刷／地形资源、图层、流式控制以及碰撞／导航／遮挡体烘焙诊断。
- 世界分块按优先级和内存预算异步流式加载场景，支持大坐标原点移动与运行时传送门。
- 对象池预热有界预制体实例，并发送生成／回收生命周期信号；平台跳跃模板已演示 CharacterBody2D。
- 独立“世界工具”底部面板集中角色、区域、导航、AI、流式加载和对象池编辑，避免对象检查器继续堆叠。

### 保留的 v2.5 资源与软件包基础

- 内容寻址导入会哈希源文件字节、导入器版本、平台和规范化设置；相同输入直接复用缓存产物。
- 导入使用有上限的后台队列，并显示进度和取消操作；文件监视经过防抖，缓存先暂存再提交，重导入失败时继续使用上次有效产物。
- 资源工具提供依赖／反向依赖查询、引用和构建包含原因、未使用报告、缺失引用报告，以及移动／重命名时的路径修复。
- 导入器为图片、图集、字体、音频、图块数据、脚本、着色器、动画和本地化数据提供专用设置与预览。
- 软件包管理器支持本地／Git／注册表清单、语义版本兼容、锁定文件、离线清单缓存、更新预览、禁用／不兼容视图与卸载影响检查。
- Plugin API 2 新增编辑器／运行时扩展点、项目级启用、能力权限、SHA-256／签名校验、WASM 内存与调用时间上限、故障隔离和安全模式；原生扩展绝不会被自动下载或执行。
- Play／Pause 时会滑出物理监视器，显示位置、方向、速度、加速度、力、动能和接触数；碰撞时间线显示冲击力、冲量、碰撞点和碰撞前后相对运动，并保持有界。
- ResizeObserver 的平台通知不再作为致命错误；尺寸变化合并到每个动画帧一次，修复用户报告的渲染误报崩溃。
- Project Format 2 Schema 18 校验软件包状态和 Plugin API 1／2 清单，并完整保留 2.4 动画资源。
### 保留的 v2.3 渲染基础

- 底部“渲染”工具可编辑环境光、阴影质量、颜色空间、调试视图、后处理、用户后处理材质、安全着色器和实时预览。
- 点光、聚光、方向光和区域光遵守渲染层／遮罩；ShadowCaster2D 支持硬／柔和／超高质量，SpriteRenderer2D 法线贴图会改变光照响应。
- 材质保存经校验的着色器、纹理、有限统一变量、混合、采样、颜色空间和颜色写入状态。无效着色器安全回退，不会中断帧。
- 多 Camera2D 支持优先级／堆叠、归一化视口、剔除遮罩、像素完美定位和命名渲染纹理捕获。
- 颜色调整、暗角、泛光、模糊和自定义材质通道均为可选；世界精灵和 UI 图片均支持九宫格。
- 图片导入支持 sRGB／线性元数据和平台压缩变体，同时保留图集、采样和精灵区域设置。
- 渲染诊断显示通道耗时、Draw Call、三角形、纹理、过度绘制、渲染目标及受支持时的 GPU 时间，并可下载 PNG 帧捕获。
- 场景工具栏进入正常文档流，不再覆盖工作区栏和 Canvas 标签；完整清理覆盖层与切换场景时重置选择，消除随机白色控制点残影。

### 保留的 v2.2 脚本基础

- 脚本现为全宽专用工作区，不再挤在资源导入器的狭小文本框中。
- `use "…"` 项目模块具有缺失/循环依赖诊断；包依赖为只读元数据。
- Entity、Component、Animator 与 AudioSource 句柄统一返回 `valid`、`kind`、`id` 和 `error`。
- 自定义、脚本、物理、UI、动画与场景生命周期信号共享安全事件队列。
- `task_wait` / `on_task` 任务跟随对象和场景生命周期自动取消。
- 开发运行支持断点、继续/单步、调用栈、局部变量和安全监视；非开发发布包会移除调试元数据。
- 模板保持真实世界单位，但编辑相机现在默认每世界单位 40 像素，默认对象清晰可见。
- 底部工具使用更大的默认字体和自适应比例布局。

### 保留的 v2.1 编辑器工作区基础

- 五种一键工作区提供专注布局，不删除任何编辑器面板或功能。
- 层级、检查器和底部抽屉可独立显示或隐藏；专注模式临时隐藏界面，不会清除已保存布局。
- 默认设计工作区折叠底部抽屉；本地布局恢复会校验损坏或过时的数据。
- `Ctrl/Cmd+K` 或 `Ctrl/Cmd+Shift+P` 打开支持键盘操作的命令面板，覆盖工作区、视图、设置、所有底部工具、面板开关与布局重置。
- 检查器搜索和常规／变换／渲染／物理／玩法／界面分类消除长距离滚动查找。
- “添加组件”改为始终可见、可搜索、已分类的选择器，仍调用原来的组件创建与撤销路径。
- “视图”菜单的布局命令与 900 × 600 响应式规则在中文、英文和德文下保持可用。

### 保留的 v2.0 引擎基础

- 启动时显示项目管理器，提供新建、打开、导入、继续和本地最近项目。
- 包含四个自审计模板：空白 2D、平台跳跃、俯视角、物理沙盒；教程所需系统缺失时会明确拒绝加载。
- **Nova_A Project Format 2**（当前 Schema 28）保存明确兼容信息和项目身份，并迁移从 Schema 5 开始的所有受支持格式。
- 稳定组件 API 2.0 文档覆盖全部内置组件，并继续分离渲染、碰撞、编辑器与运行时职责。
- Rhai 脚本可在按项目和槽位隔离的存档中保存有限数字、布尔、字符串、数组与 Map。
- WASM Plugin API 1 校验清单／模块，只开放日志／事件，不提供文件系统、网络或进程权限。
- 随附的中／英／德手册解释全部编辑器按钮、组件、物理属性、运行时 API、资源流程、构建选项、迁移步骤和三个完整教程。
- CI 在原有代码／WASM／前端检查之外，加入三种桌面操作系统和手册完整性审计。

### 保留的 v1.9 分发与调试基础

- Nova Player 是独立的纯运行时应用模式，仅包含运行时、物理、渲染、音频、脚本、输入和打包资源，不会加载检查器、层级、资源浏览器、编辑网格、菜单或 Gizmo。
- “构建设置”可配置游戏名称、Windows／Linux／macOS／Web 目标、x86_64 架构、场景顺序、启动场景、开发元数据、输出目录和可选的可执行文件内嵌游戏数据。
- `game.nova-pak` 是版本化索引归档，记录每个条目的路径、偏移、MIME／类型、原始大小、可选 gzip 数据块和 SHA-256 校验。项目 JSON 与导入资源只打包一次，不会散落成数百个内容文件。
- Windows 与 Linux 主机原生构建默认输出 Nova Player 和 `game.nova-pak` 两个文件，也可把数据附加到可执行文件并通过校验页脚读取。macOS 保留 `.app` 结构并把数据放在内部播放器旁。Web 构建输出 `index.html`、生产版 Nova Player 模块和 `game.nova-pak`。
- 控制台支持 Trace、Debug、Info、Warning、Error、Fatal，提供分类／搜索过滤、来源信息、最多 2,000 条记录和脚本资源跳转。
- 性能分析器采样真实帧、物理、渲染、脚本、动画与音频耗时，并显示 FPS、可用时的内存、Draw Call、精灵、刚体、接触与脚本实例；历史图可冻结或清除。
- 物理调试器可叠加碰撞器、接触点、法线、休眠刚体、AABB、关节约束和绳索节点。
- 前端异常与 Rust panic 会把版本化诊断日志写入用户 Nova_A 应用数据目录的 `Logs` 文件夹。CI 会验证 Rust 测试／clippy、TypeScript、WASM、生产前端和 Tauri 后端。
- 游戏 UI 现在会显示并可在场景视图中选择；需要时自动创建 Canvas；不同控件拥有可见的默认尺寸；缺失图片有占位图；检查器可直接导入图片；游戏视图 TextInput 使用原生输入层，支持选择、粘贴、密码、输入法、德语与中文。
- 底部标签会换行显示，不再退化为浏览器原生横向滚动条。资源、控制台、动画、性能、Tilemap、项目设置和构建设置分别管理自己的滚动；动画时间刻度和帧控件保持有界且清晰。
- 项目格式 13 保存构建设置；格式 12 与此前所有受支持格式继续自动迁移。

### 编辑器快捷键

| 快捷键 | 操作 |
| --- | --- |
| `Q` / `W` / `E` / `R` | 选择 / 移动 / 旋转 / 缩放 |
| `Ctrl/Cmd+Z`、`Ctrl/Cmd+Shift+Z` | 撤销 / 重做 |
| `Ctrl/Cmd+C`、`Ctrl/Cmd+V`、`Ctrl/Cmd+D` | 复制 / 粘贴 / 重复所选子树 |
| `Delete`、`F2` | 删除选择 / 重命名主选择 |
| `Ctrl/Cmd+S` | 保存项目 |

## 引擎工作区

```text
Vue 编辑器
  └─ nova_wasm        唯一 WebAssembly 边界
       ├─ nova_runtime  固定时间、事件、诊断、运行时骨架
       │    └─ nova_physics  刚体、碰撞、求解器、绳、持久化世界
       │         └─ nova_math  向量、变换、AABB、射线、矩形
       ├─ nova_script   沙箱化 Rhai 游戏脚本执行
       └─ nova_format   版本化格式、验证与迁移
```

物理、数学、运行时、脚本和格式 crate 不导入 Vue、DOM、JavaScript 或 Tauri。内部 crate 采用静态链接，因此桌面发布仍是一个完整应用，不需要 Nova_A DLL 插件。

## 支持的构建目标

- 浏览器预览：支持 WebAssembly 的当前 Chromium、Firefox 和 Safari。
- 桌面端：Tauri 2 及其系统 WebView 支持的 Windows、macOS 与 Linux。
- 建议在目标操作系统上构建对应安装包：Windows 生成 NSIS/MSI，macOS 生成 app/DMG，Linux 生成当前 Tauri 工具链支持的包格式。

Nova_A 当前是桌面/网页编辑器；仓库尚未配置 Android 与 iOS 应用目标。

## 通用环境要求

- Node.js 18 或更高版本（`.node-version` 记录了测试版本）。
- pnpm 9 或更高版本（`package.json` 固定了项目使用的包管理器版本）。
- Rust 1.88 或更高版本与 Cargo（独立核心 crate 仍保留 1.77 MSRV；安全修复后的桌面依赖图需要 1.88）。
- Rust 的 `wasm32-unknown-unknown` 目标。
- `wasm-pack` 0.14.0 或更高版本。
- Git 以及当前系统需要的原生开发依赖。

安装通用工具与项目依赖：

```sh
corepack enable
corepack prepare pnpm@10.30.0 --activate
rustup target add wasm32-unknown-unknown
cargo install wasm-pack --version 0.14.0 --locked
pnpm install --frozen-lockfile
```

如已安装 `wasm-pack` 0.14.0 或更高版本，可跳过对应命令。Rust 官方的 [`wasm32-unknown-unknown` 说明](https://doc.rust-lang.org/stable/rustc/platform-support/wasm32-unknown-unknown.html)包含目标安装方法。

把现有仓库移动到其他磁盘后，不要继续使用复制过来的依赖链接或原生构建元数据。只删除移动后副本中的 `node_modules`、`target` 与 `src-tauri/target`，再运行 `pnpm install --frozen-lockfile` 并重新构建；源码、项目、`pnpm-lock.yaml` 和 Cargo 锁文件必须保留。这样可避免 pnpm 链接与 Tauri 权限元数据继续引用旧仓库的绝对路径。

### 操作系统依赖

请以最新的 [Tauri 官方环境要求](https://v2.tauri.app/start/prerequisites/)为准。

- Windows：安装 Microsoft C++ Build Tools，并选择“使用 C++ 的桌面开发”。Tauri 使用 WebView2；当前 Windows 10/11 通常已自带。生成 MSI 时可能还需要启用 Windows 的 VBSCRIPT 可选功能。
- macOS 10.15 或更高版本：安装 Xcode；仅开发桌面应用时也可安装 Xcode Command Line Tools，并完成首次初始化。
- Debian/Ubuntu Linux：

```sh
sudo apt update
sudo apt install libwebkit2gtk-4.1-dev build-essential curl wget file libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev
```

Arch、Fedora、openSUSE、Alpine、NixOS 等系统请直接使用 Tauri 官方页面中的对应包名，不要套用 Debian 包名。

## 构建与运行

以下命令均在仓库根目录执行。

### 浏览器开发模式

```sh
pnpm dev
```

该命令会先重新构建开发版 WASM，再启动 Vite，避免浏览器加载过期的 Rust 模块。默认地址通常为 `http://localhost:1420`。

### 原生桌面开发模式

```sh
pnpm tauri dev
```

### 完整验证

```sh
pnpm test:core
cargo clippy --workspace --all-targets -- -D warnings
pnpm check
pnpm audit:manual
pnpm build
```

这些命令依次运行全部 workspace 测试（包括完整物理测试套件）、将警告视为错误的代码检查、Vue/TypeScript 类型检查、Release WASM 和优化后的 Vite 前端构建。

### 浏览器生产预览

```sh
pnpm build
pnpm preview
```

### 原生发布包

```sh
pnpm tauri build
```

Tauri 打包前会自动执行 `pnpm build`。不同系统的结果通常位于 `src-tauri/target/release/bundle/` 下。

原生构建成功后，使用以下命令整理完整的版本发布文件：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\package-release.ps1 -Version 4.2.0
```

该命令会在 `releases/v4.2.0/` 生成规定的十一项顶层产物：便携版 EXE、MSI、NSIS 安装程序、Web ZIP、源码 ZIP、参考项目 ZIP、发布证据 ZIP、发布说明、完整编辑清单、许可证和 `SHA256SUMS.txt`。发布证据 ZIP 保留环境、工具、源码、测试、覆盖率、视觉基线／差异、日志、截图、已知问题及签字门槛的机器可读层级。

### 无界面导出

```sh
pnpm export -- --project ./project.nova --target web --profile release --output ./Builds/MyGame
```

CLI 接受 `windows`、`linux`、`macos` 或 `web`；桌面目标需要对应宿主 Player。它生成构建报告、缓存清单以及可选补丁清单；其他参数请运行 `pnpm export -- --help` 查看。

### 使用 Nova Player 导出游戏

1. 打开 **项目 → 构建设置**。
2. 输入游戏名称；桌面目标请选择与当前主机系统相同的平台，也可选择 **Web**。
3. 排列参与构建的场景，并指定启动场景。
4. 输出目录留空时使用 `Documents/Nova_A Builds/<游戏名>`；也可以输入明确目录。
5. 点击 **构建** 或 **构建并运行**；“构建并运行”用于桌面目标。

默认桌面开发导出有意保持为两个干净文件：

```text
MyGame/
├─ MyGame.exe       # Linux 为 MyGame；macOS 为 MyGame.app
└─ game.nova-pak
```

Windows 与 Linux 可勾选“将游戏数据打包进可执行文件”，生成单个播放器可执行文件。macOS 会把包保留在应用包内部，以免破坏 `.app` 结构和签名。桌面目标采用主机原生构建：Windows 目标在 Windows 构建、Linux 目标在 Linux 构建、macOS 目标在 macOS 构建。Web 导出会生成可部署的 `index.html`、Nova Player 模块／样式和 `game.nova-pak`；请通过 HTTP 提供该目录，不要用 `file://` 直接打开。

## 物理属性绑定情况

配置变化会通过 Vue → `nova_wasm` 以明确的持久化世界命令传入。固定物理步保留在 Rust 内；复用的 Float64 状态缓冲区把运行时变换和绳状态返回渲染器，不进行隐藏单位换算，也不会为每个刚体创建 JavaScript 返回对象。一个配置世界单位严格等于一米；相机缩放只负责将世界坐标转换为像素。

## 项目兼容性

- 新存档使用 **Nova_A Project Format 2**、Schema 29 与引擎版本 `6.9.0`。
- 持久化场景、对象、组件和连接使用 UUID；运行时句柄绝不会写入磁盘。
- 格式迁移与验证集中在 `nova_format`，不会散落在各个编辑器组件中。
- v1.9 格式 13、v1.8 格式 12、v1.7 格式 11、v1.6 格式 10、v1.5 格式 9、v1.4 格式 8、v1.3 格式 7、v1.2 格式 6、v1.1.2 格式 5、更早的对象根节点和旧顶层实体数组仍能加载；只有用户再次保存时才会写成 Format 2。

| 属性 | 求解器/渲染行为 |
| --- | --- |
| 位置、旋转、尺寸 | 直接用于世界变换、碰撞几何、锚点和渲染。 |
| 线速度、角速度 | 每个子步积分并回传渲染器。 |
| 加速度 | 直接加入动态刚体的线加速度。 |
| 密度与质量 | 密度 × 精确形状面积得到质量；编辑质量会同步更新密度。 |
| 自动/手动转动惯量 | 默认使用精确椭圆或凸多边形公式；关闭自动计算后严格使用手动值。 |
| 全局/局部重力与重力倍率 | 每个动态刚体子步都会组合并应用。 |
| 力与扭矩 | 遵循 `F = ma` 与 `τ = Iα`；偏心冲量也会产生角冲量。 |
| 线性/角/空气阻尼 | 每个稳定子步执行指数衰减。 |
| 静态/运动学刚体 | 静态刚体保持固定；运动学刚体只积分配置速度，不受动态力影响。 |
| 恢复系数与阈值 | 只有闭合速度超过阈值时才产生反弹。 |
| 静摩擦/动摩擦 | 顺序冲量先尝试静摩擦，再限制为动摩擦。 |
| 传感器 | 刚体之间会报告重叠诊断但不施加碰撞冲量；物理绳会穿过仅传感器对象。 |
| 渲染排序层/顺序 | 只控制绘制顺序，绝不会改变碰撞行为。 |
| 物理层/掩码/矩阵 | 仅当双方碰撞掩码与项目碰撞矩阵都允许该层组合时才求解接触；零掩码会明确禁用全部接触。 |
| 碰撞器偏移/旋转/形状 | 碰撞几何与渲染几何独立，使用自己的局部变换、材质、传感器状态和形状。 |
| 离散／连续碰撞、休眠、锁定旋转 | 默认 Discrete 刚体使用固定步进；可按对象启用 Continuous 自适应防穿透子步。静止刚体无需接触即可休眠，并会在受力、冲量、碰撞、关节牵引或编辑变换时唤醒；锁定刚体拒绝扭矩和角冲量。 |
| 绳路径与锚点 | 直线和归一化手绘路径会在编辑变换后重新拼接；中心/表面/顶点/边上点锚点跟随当前几何。旧版自动曲线记录仍可读取。 |
| 绳的伸长、弯曲、刚度、阻尼 | 作用于两端约束或每个物理绳分段；每段常数按节点数缩放，因此改变采样数不会改变整条绳的配置刚度或阻尼。 |
| 绳半径、线密度、碰撞 | 半径控制连续线段碰撞和渲染直径；线密度控制精确总质量；排除源对象，并向同图层第三方对象传递大小相等、方向相反的冲量与摩擦。 |
| 绳断裂承重 | 配置的等效质量会使用标准重力与实际伸长/弯曲力比较；过载线段断开后，两段残绳继续模拟。 |
| 绑定 / FixedJoint2D | 使用合并质量和平行轴转动惯量生成同一个组合体运动状态；移除内部重叠边线与接触，外部冲量会带动整个组合体。 |
| TileMap 与 TileSet | 图集区域按格子坐标解析；可见块提交渲染批次，连续静态碰撞格被合并，并在编辑后只重建受影响块。 |
| 物理查询 | 射线、重叠与形状投射使用实际椭圆／凸多边形窄相检测、图层掩码和稳定场景 UUID；TileMap 内部合并碰撞体会去重为所属对象。 |
| 2D 关节 | Fixed、Distance、Revolute、Prismatic 和 Spring 约束在每个物理子步求解；锚点、轴、限位、刚度、阻尼和连接对象碰撞开关均传入 Rust。 |
| 粒子发射器 | 发射、寿命、运动与颜色曲线在固定运行时步长更新；图像粒子进入精灵批次，无纹理粒子使用椭圆绘制，且从不创建物理碰撞体。 |
| 颜色、纹理、透明度 | 仅影响渲染，不改变质量或碰撞几何。 |

## 数值范围

Nova_A 在前端、WASM 和后端完整路径中使用 64 位浮点数。非有限输入会被规范化。一般物理量限制为 ±`1e50`；正的几何尺寸、质量与密度下限为 `1e-6`。

多边形碰撞几何必须保持凸形。求解器至少使用 8 个自适应子步，高速运动时最多 128 个，并在每个子步进行 20 次顺序冲量迭代。这些措施可减少穿透和不稳定，但 Nova_A 仍是离散实时求解器，不是符号或无限精度物理系统。
<!-- Nova_A v3.1.0 current release summary -->

## Nova_A 3.1.0 编辑器基础更新

Nova_A 3.1.0 新增无边框全屏启动与 F11 窗口恢复、可停靠和共享的六类工作区、全局搜索与快捷键编辑、100 步事务式撤销／重做、独立自动保存与崩溃恢复，以及统一任务中心。Project Format 2 仍为 Schema 22；从 3.0.0 升级无需迁移项目。

- **工作区：**设计、脚本、动画、UI、调试与自定义布局支持折叠、缩放、左右停靠、用户／项目级存储、保存、复制、重命名、更新、导入导出、重置和安全布局启动。
- **导航：**命令面板搜索命令、设置、资源、场景、对象、组件和脚本；后退／前进与冲突检测快捷键编辑器让核心功能都可快速到达。
- **恢复：**带校验和的有界自动快照不覆盖手动保存；异常退出后可选择快照、跳过损坏项、只读打开或进入安全模式。
- **状态：**任务中心统一显示导入、构建、软件包、迁移与保存，支持进度、取消、重试、错误详情和复制诊断。
- **位置调整：**“呈现”迁移到 UI 工作区；Production Lab 更名为 Profiler；运行时／存档诊断移至调试；插件移至软件包；项目信息集中为项目健康。
- **发布证据：**包含多分辨率布局、首次全屏、键盘流程、崩溃恢复、100 步撤销／重做、构建／测试日志、基准、稳定性冒烟和 SHA-256。

完整说明见[中文手册](./manual/MANUAL.zh-CN.md)和[三语交互网页](./manual/index.html)。

## Nova_A 4.5 生产级物理

4.5 新增项目级“精确／平衡／快速／自定义”求解配置、明确的丢帧时间与休眠调节、结构化射线／点／重叠／扫掠／最近／接触查询、世界边界创作、可解析 UUID 的休眠／唤醒与关节断裂事件、可调 Rope2D 分段，以及重新设计的虚拟物理监视器（排序、固定、曲线、约束遥测、捕获导出与快照比较）。项目健康和分析器共享物理预算／不稳定警告；视口调试新增质心、速度和力向量。

请阅读 [4.5 物理契约](docs/PHYSICS_2D_4_5.md)、[角色控制器](docs/CHARACTER_CONTROLLERS_4_5.md) 和[物理诊断](docs/PHYSICS_DIAGNOSTICS_4_5.md)。数值容差、加速稳定性测试、压力捕获、平台对比及如实标注的外部测试门禁位于发布证据包中。

## Nova_A 6.7 设备输入、Android 交付与无障碍

6.7 将触控／多点手势、安全区域虚拟按钮／摇杆／方向键、手柄捕获与校准、触觉反馈、方向和明确授权的传感器统一接入原有可保存 Input Map。游戏视图显示可访问的虚拟控件；设置新增可旋转设备预览和实时测试。呈现工作区可导出角色、名称、状态、数值、实时区域、焦点和边界的语义证据，运行时文字／字幕支持到 400%。

Android 现为可发现的可选 Experimental 目标。构建设置逐项报告 JDK、SDK 35、Build Tools、NDK、adb 和模板门禁，验证最小权限及用途，生成清单／资源，离线运行已验证的本地 Gradle Wrapper，并仅在成功后返回真实 APK。安装和 logcat 必须分别点击，签名机密不会写入项目。iOS 仍需匹配主机，当前延期。请阅读 [6.7 设备／移动／无障碍指南](docs/DEVICE_MOBILE_ACCESSIBILITY_6_7.md)和[无代码触控参考项目](reference-projects/projects/creator-v670-touch-platformer/README.md)。
