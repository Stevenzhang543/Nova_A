# Nova_A 3.2.0 known-issue disposition

No S0 or S1 defect is open from the automated/local qualification performed for this source snapshot.

## Qualification limits, not claimed passes

- No signed Git tag or code-signing identity is available; source provenance is a dirty working-tree snapshot and Windows binaries are unsigned.
- MSI/NSIS installation and uninstallation on a newly provisioned Windows VM remain pending. The final portable executable's maximized/decorated/resizable startup was tested locally. The native F11 transition rerun is pending because WebView2's reused production process did not expose a stable automation endpoint; the global F11 route passed clean-profile browser E2E and native API wiring passed static audit.
- The bounded stability smoke is not a 24-hour or 100-hour soak.
- Firefox, WebKit, Linux, macOS, Android, discrete-GPU families, and operating-system scaling at 125/150/200 percent remain pending in their documented platform tiers.
- The migration corpus is generated and malformed-state tested locally; an independently archived real-world corpus is still desirable before claiming broad ecosystem adoption.

These limits do not hide a known data-loss, migration, startup, or common-workflow crash. Any failed final build/audit invalidates this disposition until corrected and rerun.
