# Nova_A 3.1.0 CI and test summary

Local qualification environment: Windows x64, Node 22.22.2, pnpm 10.30.0, Rust stable toolchain, Edge 147.0.3912.72.

| Gate | Result | Evidence |
| --- | --- | --- |
| `cargo fmt --all -- --check` | Pass | No diff |
| `cargo clippy --workspace --all-targets -- -D warnings` | Pass | Zero warnings |
| `cargo test --workspace --all-targets` | Pass | 109 tests, zero failures |
| `pnpm check` | Pass | Vue TypeScript no-emit check |
| `pnpm run audit` | Pass | Manuals, editor, script, renderer, animation, v2.5–v3.1, typography; 1,167 controls |
| `pnpm verify:v3.1` | Pass | `v3.1.0-editor-foundation.json`, `v3.1.0-crash-recovery-automation.json` |
| `pnpm qualify:v3.1:browser` | Pass | Fresh Edge keyboard flow, four layouts, no errors |
| Native window probe | Pass | `v3.1.0-native-window.json` |
| `pnpm build` | Pass | Release WASM and Vite production dist |
| `pnpm tauri build` | Pass with non-updater warning | Portable EXE, MSI, NSIS produced |
| `pnpm benchmark:v3` | Pass with four explicit unmeasured values | `v3.1.0-benchmarks.json` |
| `pnpm stability:v3` | 500-cycle smoke pass | Not a long-duration qualification |
| Seven reference Web exports | Pass | Each bundled `project.nova` produced `nova-build-report.json` and matched engine 3.1.0 / schema 22 |

Migration coverage: all public schemas 5–22 passed Rust golden projection; corruption fuzz cases did not panic or accept future schemas. Schema remains 22.

Pending external gates: signed tag, Authenticode, clean-OS installer lifecycle, Linux/macOS clean runner, Firefox/WebKit export browser matrix, discrete GPU families, 125/150/200% OS scaling, and the roadmap’s long-duration aggregate editor soak.
