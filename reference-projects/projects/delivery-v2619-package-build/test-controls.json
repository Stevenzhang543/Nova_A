{
  "format": "nova-reference-test-controls",
  "version": 1,
  "release": "26.19",
  "engineVersion": "26.19.0",
  "projectFormat": 2,
  "schema": 29,
  "reference": "delivery-v2619-package-build",
  "authoring": "code",
  "classification": [
    "delivery",
    "offline",
    "recovery",
    "save-reopen",
    "export"
  ],
  "actions": [
    {
      "action": "Play and use WASD or arrow keys to collect all six checkpoints in order; press R to restart.",
      "expected": "Score increases once per checkpoint, completion appears after six, and R resets the route."
    },
    {
      "action": "Edit movement speed in the matching Code, Blocks or Mixed workflow; Undo/Redo, save/reopen, then export Web.",
      "expected": "The same authored change affects the running editor and exported game; graph conversion preserves supported code."
    },
    {
      "action": "Open Manage → Build Settings; choose Web, then Windows.",
      "expected": "Web is available in the browser; desktop export requires the actual desktop host. Diagnostics give the missing prerequisite before export."
    },
    {
      "action": "Open Manage → Packages → Browse; review and install Nova Navigation 2D. Disable, enable and uninstall it, then undo and save/reopen.",
      "expected": "The offline bundled entry shows publisher, permissions, dependency and hash information. No registry download is needed. Package and plugin state follow successful lifecycle actions."
    },
    {
      "action": "For a separately reviewed signed update manifest, opt in and stage it; disable updates during verification.",
      "expected": "A cancelled check cannot stage an update. Staging downloads and installs nothing; native installer lifecycle needs separate disposable-host evidence."
    }
  ]
}
