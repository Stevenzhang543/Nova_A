# Nova 26.10 Mixed Code and Blocks

Public release **26.10** · Engine **26.10.0** · Project Format 2/schema 29.

This is the representative **linked Rhai and Visual Graph** project. It is copied from a previously qualified playable/reference project and advanced without deleting authored scenes, components, assets, scripts, graphs, animations, controls, or build settings.

Use `test-controls.json` for the repeatable user workflow and `expected-output.json` for its machine-readable behavior oracles. Automated local checks cover structure and supported output; signing, matching-host platforms, real assistive hardware, independent observation, and a real-duration soak remain external.
