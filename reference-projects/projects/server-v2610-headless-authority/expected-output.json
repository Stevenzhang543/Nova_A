{
  "format": "nova-reference-expected-output",
  "version": 1,
  "publicRelease": "26.10",
  "release": "26.10",
  "engineVersion": "26.10.0",
  "projectFormatMajor": 2,
  "projectFormat": 2,
  "schemaVersion": 29,
  "schema": 29,
  "referenceId": "server-v2610-headless-authority",
  "reference": "server-v2610-headless-authority",
  "authoring": "permission-gated headless server",
  "preservedScenes": 1,
  "preservedAssets": 2,
  "behaviors": [
    {
      "id": "server-v2610-headless-authority-behavior-1",
      "description": "retain the stable Network Protocol 2 server path",
      "expectedOutcome": "The permission-gated headless server path visibly demonstrates retain the stable Network Protocol 2 server path, survives save/reload, and fails with an actionable diagnostic when a required capability is unavailable."
    },
    {
      "id": "server-v2610-headless-authority-behavior-2",
      "description": "validate exact current output authority",
      "expectedOutcome": "The permission-gated headless server path visibly demonstrates validate exact current output authority, survives save/reload, and fails with an actionable diagnostic when a required capability is unavailable."
    },
    {
      "id": "server-v2610-headless-authority-behavior-3",
      "description": "serve and reconnect loopback clients",
      "expectedOutcome": "The permission-gated headless server path visibly demonstrates serve and reconnect loopback clients, survives save/reload, and fails with an actionable diagnostic when a required capability is unavailable."
    },
    {
      "id": "server-v2610-headless-authority-behavior-4",
      "description": "fail closed on malformed permissions and unsafe paths",
      "expectedOutcome": "The permission-gated headless server path visibly demonstrates fail closed on malformed permissions and unsafe paths, survives save/reload, and fails with an actionable diagnostic when a required capability is unavailable."
    },
    {
      "id": "server-v2610-headless-authority-behavior-5",
      "description": "record honest renderer-disabled WebView scope",
      "expectedOutcome": "The permission-gated headless server path visibly demonstrates record honest renderer-disabled WebView scope, survives save/reload, and fails with an actionable diagnostic when a required capability is unavailable."
    }
  ],
  "localStructuralVerification": "required",
  "webOutput": "required",
  "windowsOutput": "required",
  "independentUserObservation": "pending-external"
}
