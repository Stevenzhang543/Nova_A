{
  "version": 1,
  "engineVersion": "5.3.0",
  "actions": [
    {
      "action": "Open graph",
      "expected": "Three nodes, two wires, one comment and one exposed String variable appear"
    },
    {
      "action": "Change Inspector value",
      "expected": "Script2D stores a per-object typed override"
    },
    {
      "action": "Play",
      "expected": "The graph emits one Info message through the shared runtime"
    },
    {
      "action": "Edit invalid wire",
      "expected": "Pre-play validation blocks type or cycle errors"
    }
  ]
}
