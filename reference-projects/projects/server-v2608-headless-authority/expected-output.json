{
  "format": "nova-reference-expected-output",
  "version": 1,
  "publicRelease": "26.08",
  "release": "26.08",
  "engineVersion": "26.8.0",
  "projectFormatMajor": 2,
  "projectFormat": 2,
  "schemaVersion": 29,
  "schema": 29,
  "referenceId": "server-v2608-headless-authority",
  "reference": "server-v2608-headless-authority",
  "authoring": "permission-gated headless server",
  "preservedScenes": 1,
  "preservedAssets": 2,
  "behaviors": [
    {
      "id": "server-v2608-headless-authority-behavior-1",
      "description": "validate the retained Network Protocol 2 policy",
      "expectedOutcome": "The permission-gated headless server path visibly demonstrates validate the retained Network Protocol 2 policy, survives save/reload, and fails with an actionable diagnostic when a required capability is unavailable."
    },
    {
      "id": "server-v2608-headless-authority-behavior-2",
      "description": "export one embedded Windows authority",
      "expectedOutcome": "The permission-gated headless server path visibly demonstrates export one embedded Windows authority, survives save/reload, and fails with an actionable diagnostic when a required capability is unavailable."
    },
    {
      "id": "server-v2608-headless-authority-behavior-3",
      "description": "admit a loopback client and send snapshots",
      "expectedOutcome": "The permission-gated headless server path visibly demonstrates admit a loopback client and send snapshots, survives save/reload, and fails with an actionable diagnostic when a required capability is unavailable."
    },
    {
      "id": "server-v2608-headless-authority-behavior-4",
      "description": "reject missing packages, grants and unsafe players",
      "expectedOutcome": "The permission-gated headless server path visibly demonstrates reject missing packages, grants and unsafe players, survives save/reload, and fails with an actionable diagnostic when a required capability is unavailable."
    },
    {
      "id": "server-v2608-headless-authority-behavior-5",
      "description": "reconnect without stale peer state",
      "expectedOutcome": "The permission-gated headless server path visibly demonstrates reconnect without stale peer state, survives save/reload, and fails with an actionable diagnostic when a required capability is unavailable."
    }
  ],
  "localStructuralVerification": "required",
  "webOutput": "required",
  "windowsOutput": "required",
  "independentUserObservation": "pending-external"
}
