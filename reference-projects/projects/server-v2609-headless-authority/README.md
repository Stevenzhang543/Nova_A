# Nova 26.09 Headless Authority

Public release **26.09** · Engine **26.9.0** · Project Format 2/schema 29.

This is the representative **permission-gated headless server** project. It is copied from a previously qualified playable/reference project and advanced without deleting authored scenes, components, assets, scripts, graphs, animations, controls, or build settings.

Use `test-controls.json` for the repeatable user workflow and `expected-output.json` for its machine-readable behavior oracles. Automated local checks cover structure and supported output; signing, matching-host platforms, real assistive hardware, independent observation, and a real-duration soak remain external.
