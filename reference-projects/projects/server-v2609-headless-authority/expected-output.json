{
  "format": "nova-reference-expected-output",
  "version": 1,
  "publicRelease": "26.09",
  "release": "26.09",
  "engineVersion": "26.9.0",
  "projectFormatMajor": 2,
  "projectFormat": 2,
  "schemaVersion": 29,
  "schema": 29,
  "referenceId": "server-v2609-headless-authority",
  "reference": "server-v2609-headless-authority",
  "authoring": "permission-gated headless server",
  "preservedScenes": 1,
  "preservedAssets": 2,
  "behaviors": [
    {
      "id": "server-v2609-headless-authority-behavior-1",
      "description": "retain server output during performance work",
      "expectedOutcome": "The permission-gated headless server path visibly demonstrates retain server output during performance work, survives save/reload, and fails with an actionable diagnostic when a required capability is unavailable."
    },
    {
      "id": "server-v2609-headless-authority-behavior-2",
      "description": "validate the embedded package and local player hash",
      "expectedOutcome": "The permission-gated headless server path visibly demonstrates validate the embedded package and local player hash, survives save/reload, and fails with an actionable diagnostic when a required capability is unavailable."
    },
    {
      "id": "server-v2609-headless-authority-behavior-3",
      "description": "serve deterministic loopback snapshots",
      "expectedOutcome": "The permission-gated headless server path visibly demonstrates serve deterministic loopback snapshots, survives save/reload, and fails with an actionable diagnostic when a required capability is unavailable."
    },
    {
      "id": "server-v2609-headless-authority-behavior-4",
      "description": "reject every negative permission case",
      "expectedOutcome": "The permission-gated headless server path visibly demonstrates reject every negative permission case, survives save/reload, and fails with an actionable diagnostic when a required capability is unavailable."
    },
    {
      "id": "server-v2609-headless-authority-behavior-5",
      "description": "reconnect after complete cleanup",
      "expectedOutcome": "The permission-gated headless server path visibly demonstrates reconnect after complete cleanup, survives save/reload, and fails with an actionable diagnostic when a required capability is unavailable."
    }
  ],
  "localStructuralVerification": "required",
  "webOutput": "required",
  "windowsOutput": "required",
  "independentUserObservation": "pending-external"
}
