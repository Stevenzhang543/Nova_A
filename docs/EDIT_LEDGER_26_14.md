# Nova_A26.14 exhaustive edit ledger

Engine26.14.0. Baseline: finalized26.13 source digest `9170494e5f33de82367fe32c7bf057317b0d9156ba279ec12b4b1c0e8217554a`. This deterministic path-level manifest compares actual authored bytes against the immutable preceding release, including dirty and untracked sources. 92 paths changed or were added; 0 authored paths were removed. Runtime/UI journals retain every later refinement and its consequence, including observed failures.

## Consequences assessed before integration

Preflighted composition, exact draft ownership and pure reads preserve existing documents and reject partial invalid mutations. Runtime generation and callback changes intentionally reject stale work, duplicate dispatch and capacity escape; cleanup and legacy zero-argument compatibility are behavior-tested. Reload/scene transactions prepare before commit and retain old state on failure. New provenance views observe detached data and use explicit author navigation; layouts trade additional vertical scrolling for readable values. Rust format additions recognize twelve existing component kinds without changing schema29. Native and WASM builds must be recreated from these exact sources.

Copied stage Cargo/support workspaces, scratch helper and generated outputs were deliberately excluded. No finalized26.12 or26.13 artifact is overwritten. Release metadata identifies a candidate, while fresh executed gates and source/build hashes determine qualification. Physical assistive technology, installation lifecycle, other hardware/platforms and long soak remain separately visible.

## Files changed/added

- `Cargo.lock` — Set only the26.14/26.14.0 candidate release authority or corresponding migration expectation. Dependency versions and public schemas remain unchanged.
- `Cargo.toml` — Set only the26.14/26.14.0 candidate release authority or corresponding migration expectation. Dependency versions and public schemas remain unchanged.
- `README.md` — Present current26.14 behavior and link its release notes and exhaustive ledger, retaining historical feature descriptions.
- `crates/nova_format/src/lib.rs` — Actual open follow-up: adds the twelve existing gameplay kinds (GridMover2D, PlatformController2D, TopDownController2D, Health2D, DamageHitbox2D, Collectible2D, Projectile2D, Spawner2D, Cooldown2D, Lifetime2D, MouseFollower2D, CameraFollow2D) to the explicit native/WASM project allowlist. Adds a real migrate/serialize/deserialize/validate regression preserving component data and rejecting an unknown future kind. No schema/version authority change is intended; this leaf was refreshed from the current26.13 baseline before editing.
- `crates/nova_script/examples/nova_script_test.rs` — Native CLI compiles a suite once, runs before_all/after_all once, clones initialized properties for each case/retry, always attempts case teardown after setup/body failure and reports suite cleanup failures separately. Actual Rhai AST functions drive discovery; comment/string masking prevents false metadata attachment. Compile errors remain machine-readable failed entries. Adds actual callback-count suite evidence and reports only observed callback-entry coverage; retries still require explicit infrastructure metadata. The existing fixture=name field is a report label, not a filesystem fixture loader.
- `crates/nova_script/examples/runtime_bridge.rs` — Reads one JSON request from stdin and executes/validates it using the staged real Rust VM, emitting a JSON result/error. Enables actual bridge tests without rebuilding frozen WASM. Does not replace the shipped CLI.
- `crates/nova_script/src/lib.rs` — Additive defaulted context invocationId/entityGenerations/callbackKind and scene generation fields preserve legacy deserialization. Entity handles/query readers use supplied generations; legacy contexts retain hashed generations. Pending spawn IDs include callback invocation identity. Explicit event family dispatch passes task/timer/signal/update/contact arguments to custom callbacks while preserving existing zero-argument custom handlers. Adds actual compiler function_names discovery without executing module statements. Adds four native regressions and updates the old pending-ID expectation.
- `docs/EDIT_LEDGER_26_14.md` — Record exact26.14 consequences, every edited path, current behavior, actual development observations and production/external qualification boundaries.
- `docs/IMPLEMENTATION_TRACKER_26_12_TO_26_16.md` — Record independently finalized26.13 and reviewed/integrated26.14 implementation while leaving its production gate result pending until actually executed.
- `docs/OBJECT_FAMILY_LESSON_26_14.de.md` — Provide the complete localized enemy-family authoring/runtime/recovery lesson with concrete controls and expected results.
- `docs/OBJECT_FAMILY_LESSON_26_14.en.md` — Provide the complete localized enemy-family authoring/runtime/recovery lesson with concrete controls and expected results.
- `docs/OBJECT_FAMILY_LESSON_26_14.zh.md` — Provide the complete localized enemy-family authoring/runtime/recovery lesson with concrete controls and expected results.
- `docs/RELEASE_NOTES_26_14.md` — Record exact26.14 consequences, every edited path, current behavior, actual development observations and production/external qualification boundaries.
- `docs/RUNTIME_STAGE.md` — Record exact26.14 consequences, every edited path, current behavior, actual development observations and production/external qualification boundaries.
- `docs/VERSION_26_14_UI.md` — Record exact26.14 consequences, every edited path, current behavior, actual development observations and production/external qualification boundaries.
- `manual/MANUAL.de.md` — Add the localized26.14 enemy-family chapter/current manual identity and preserve historical chapters and anchors.
- `manual/MANUAL.en.md` — Add the localized26.14 enemy-family chapter/current manual identity and preserve historical chapters and anchors.
- `manual/MANUAL.zh-CN.md` — Add the localized26.14 enemy-family chapter/current manual identity and preserve historical chapters and anchors.
- `manual/index.html` — Add the localized26.14 enemy-family chapter/current manual identity and preserve historical chapters and anchors.
- `package.json` — Set only the26.14/26.14.0 candidate release authority or corresponding migration expectation. Dependency versions and public schemas remain unchanged.
- `reference-projects/README.md` — Add five distinct26.14 project links, supported format and user task/expected-output guidance.
- `reference-projects/projects/creator-v2614-blocks-game/README.md` — Add this deterministic26.14 reference README.md with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/creator-v2614-blocks-game/expected-output.json` — Add this deterministic26.14 reference expected-output.json with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/creator-v2614-blocks-game/project.nova` — Add this deterministic26.14 reference project.nova with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/creator-v2614-blocks-game/test-controls.json` — Add this deterministic26.14 reference test-controls.json with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/creator-v2614-code-game/README.md` — Add this deterministic26.14 reference README.md with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/creator-v2614-code-game/expected-output.json` — Add this deterministic26.14 reference expected-output.json with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/creator-v2614-code-game/project.nova` — Add this deterministic26.14 reference project.nova with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/creator-v2614-code-game/test-controls.json` — Add this deterministic26.14 reference test-controls.json with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/creator-v2614-enemy-family/README.md` — Add this deterministic26.14 reference README.md with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/creator-v2614-enemy-family/expected-output.json` — Add this deterministic26.14 reference expected-output.json with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/creator-v2614-enemy-family/project.nova` — Add this deterministic26.14 reference project.nova with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/creator-v2614-enemy-family/test-controls.json` — Add this deterministic26.14 reference test-controls.json with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/creator-v2614-mixed-game/README.md` — Add this deterministic26.14 reference README.md with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/creator-v2614-mixed-game/expected-output.json` — Add this deterministic26.14 reference expected-output.json with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/creator-v2614-mixed-game/project.nova` — Add this deterministic26.14 reference project.nova with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/creator-v2614-mixed-game/test-controls.json` — Add this deterministic26.14 reference test-controls.json with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/server-v2614-headless-authority/README.md` — Add this deterministic26.14 reference README.md with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/server-v2614-headless-authority/expected-output.json` — Add this deterministic26.14 reference expected-output.json with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/server-v2614-headless-authority/project.nova` — Add this deterministic26.14 reference project.nova with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `reference-projects/projects/server-v2614-headless-authority/test-controls.json` — Add this deterministic26.14 reference test-controls.json with stable identities, current engine metadata, actual project data or task/expected-output guidance; prior release directories are retained.
- `scripts/generate-v26.14-core-references.mjs` — Generate three deterministic26.14 code/blocks/mixed Coin Trail references and one exact-session authority, preserving cumulative graph tasks and validating all50 shipped Rhai projections.
- `scripts/generate-v26.14-reference-projects.mjs` — added deterministic Enemy Family game/project expectations/controls and localized lesson artifacts. It imports the real template, validates all four scripts with the actual WASM VM and checks project structure. Development metadata remains explicit.
- `scripts/generate-v26.14-release-references.mjs` — Run and independently verify both the cumulative reference quartet and dedicated enemy-family generator without mixing their ownership.
- `scripts/generate-v26.14-teaching.mjs` — Add complete EN/DE/ZH object-family tasks and dedicated HTML anchors, update current manual metadata and retain prior lessons.
- `scripts/lib/runtimeAudit14.mjs` — Resolve actual integrated/staged roots and metadata, select ignored report/compiler paths, and freshly build only allowlisted native examples with hidden Windows execution.
- `scripts/verify-v26.14-authoring.mjs` — Sequentially execute actual ownership creation/layout and enemy-family runtime workflows; embed fresh screenshots and downloaded projects in the26.14 authoring bundle.
- `scripts/verify-v26.14-blueprint-handlers.mjs` — compiles the five changed/new SFC surfaces and executes actual extracted draft/save/close handlers with explicit fixture dependencies.
- `scripts/verify-v26.14-enemy-family.mjs` — added real browser creation, invalid draft/Cancel, derive/instantiate, instance override and exact callback navigation workflows; includes collision/UI binding creation, 42 introduced-state geometry/readability checks, Undo/Redo and actual save/reopen.
- `scripts/verify-v26.14-family-runtime-user.mjs` — added actual reference file-picker loading, pointer collision, inherited runtime inspection, one UI activation/one callback, bounded pool reuse, paused source validation, and downloaded/reopened game checks. Unreached steps remain explicitly unverified until the runtime report passes.
- `scripts/verify-v26.14-focus.mjs` — Run fresh native/WASM/CLI/project/ownership/handler and affected language/draft suites; embed exact bounded native fixtures/results and current SFC inventory in a hash-bound focus report.
- `scripts/verify-v26.14-native-tests.mjs` — Executes the actual staged CLI against generated source fixtures, checking initialized-state isolation, cleanup failures, failed setup, skipped suites, actual compiler discovery, compile errors and explicit retry attempts. Writes per-case/report/coverage evidence only under stage/reports.
- `scripts/verify-v26.14-ownership.mjs` — added focused pure model/raw-field/cache/range/localization checks.
- `scripts/verify-v26.14-project-roundtrip.mjs` — Loads actual production authoring, derived-blueprint, prefab, asset, serializer, validator and hydrator modules through a canonical Vite SSR overlay; browser background history timers are deferred in the Node host. Runs the actual staged WASM format boundary. Five groups cover quick creation/derived instance/export override persistence, raw/percent/base64/malformed prefab bytes, all40 published templates, every stable component kind/unknown rejection, and removed-versus-active composition. This is programmer evidence; it does not replace visible file-picker/download checks.
- `scripts/verify-v26.14-runtime-stage.mjs` — Vite SSR overlay loads actual runtime methods and real asset/entity/component/parser/compiler modules, with unrelated host boundaries isolated. Covers inheritance, authored script dispatch, paused/invalid/disabled reload, signal dedup, callback discovery, suite teardown, visual tests, composition rollback, timer cancellation/bounds, lifetime rejection and pool expiry/reentrancy/failure cleanup. Canonical Windows module IDs prevent accidental duplicate singleton fixtures. Writes ignored reports with explicit native/WASM backend and host limitations.
- `src-tauri/Cargo.lock` — Set only the26.14/26.14.0 candidate release authority or corresponding migration expectation. Dependency versions and public schemas remain unchanged.
- `src-tauri/Cargo.toml` — Set only the26.14/26.14.0 candidate release authority or corresponding migration expectation. Dependency versions and public schemas remain unchanged.
- `src-tauri/tauri.conf.json` — Set only the26.14/26.14.0 candidate release authority or corresponding migration expectation. Dependency versions and public schemas remain unchanged.
- `src/components/ConfigPanel.vue` — added ownership and blueprint entry points, error reporting and synchronization of the explicit scene blueprint identity when its existing Script2D selector changes.
- `src/components/EventSheetEditor.vue` — added real blueprint edit/derive/instantiate entry points, inherited primary-logic navigation, task selectors, task/destroy labels and corresponding starter callbacks. Existing draft guards remain active.
- `src/components/ObjectBlueprintEditor.vue` — added labeled source/composition forms, validation, retained invalid drafts, exact saved-base conflict handling, Save/Discard/Cancel and saved-only derive/instantiate actions. The modal uses the existing focus trap.
- `src/components/ObjectOwnershipPanel.vue` — added expandable source, property, event, composition and runtime views, search, contextual author navigation and real Inspector property navigation. Runtime values are read-only and refresh only while the expanded view is active.
- `src/components/ScriptStudio.vue` — consumes callback navigation against the actual open draft and selects its precise declaration range, or shows a localized location problem.
- `src/editor/objectAuthorNavigation.ts` — added pending project/record-scoped author navigation requests.
- `src/editor/objectBlueprintAuthoring.ts` — added create/derive/instantiate transactions with canonical asset references and Edit-mode guards.
- `src/editor/objectBlueprintFields.ts` — added raw draft checks before normalization for names, references, lists and component composition. Invalid inputs remain visible.
- `src/editor/objectCallbackLocation.ts` — added UTF16 callback declaration lookup with ambiguity and malformed-source refusal.
- `src/editor/objectOwnershipCopy.ts` — added matching English/German/Chinese control labels, draft decisions and new task/destroy event labels.
- `src/editor/objectProvenance.ts` — added the actual asset/runtime adapter, bounded source-chain traversal, precise Event Sheet author identities, prefab field baselines and exported property defaults. Uses readEntityAuthoringData, never the mutating serializer.
- `src/editor/objectProvenanceModel.ts` — added pure property-level source comparison, declaration baselines, component ownership, authored/runtime separation and detached runtime rows. A whole components-array patch no longer labels every property overridden.
- `src/editor/studioDraftRetention.ts` — added the blueprint draft kind mapped to objectBlueprint assets; no dirty-draft eviction or truncation was introduced.
- `src/i18n.ts` — Set only the26.14/26.14.0 candidate release authority or corresponding migration expectation. Dependency versions and public schemas remain unchanged.
- `src/projects/projectData.ts` — Actual save/reopen follow-up: decode prefab source through the existing byte decoder and strict UTF-8 before JSON validation, accepting established raw, percent-encoded and base64 assets. Malformed encoding/JSON remains an error. Component identity and unknown-kind checks still visit every serialized record; composition conflicts/dependencies consider only records whose removed flag is not true, matching runtime tombstone semantics. Disabled but present components remain part of authored composition.
- `src/projects/projectFormat.ts` — Set only the26.14/26.14.0 candidate release authority or corresponding migration expectation. Dependency versions and public schemas remain unchanged.
- `src/runtime/GameplayRuntime.ts` — Runs inherited callbacks using their actual author's script and independent per-entity/per-script properties; deduplicates signal/event/connection callbacks by author and callback. Retains compiled source/raw graph/module document snapshots during Play so unqueued edits cannot bypass reload policy. Processes queued reloads while paused, uses exact drafts and pinned unchanged dependencies, checks project/dependent policy again at apply, and prepares one complete replacement VM plus all primary/inherited property transfers. A failed dependent compile leaves every live cache/instance intact and frees the candidate; success commits once and discards paused commands from replaced programs. Adds actual script/visual suite execution with shared teardown helper. Structural queues are drained as bounded snapshots so nested commands survive for the next boundary. Despawn is deferred; retiring entities cancel timers/tasks, pending targeted signals and debugger work. Pool release invokes destruction cleanup before reset; session setup provides simulation clock and lifetime generations to Rust.
- `src/runtime/creatorLearning.ts` — registered the stable enemy-family guide and dispatches its concrete localized copy. Existing guide IDs/progress remain unchanged.
- `src/runtime/dynamicObjects.ts` — Validate source/target lifetimes for direct and pending handles; defer structural actions on the shared runtime path and reject configured-pool exhaustion instead of allocating an unpooled escape.
- `src/runtime/entityLifetimes.ts` — Process-monotonic32-bit generations are assigned per entity lifetime and keyed by Vue's raw entity identity. Retirement disables handle resolution; pooled reuse receives a new generation. Exhaustion fails explicitly. The inspector uses a separate readonly lookup that does not create a generation.
- `src/runtime/eventSheets.ts` — Resolves inherited handlers with actual source sheet and logic asset provenance; distinguishes event kind, case-sensitive selector and callback; additive inheritance keeps distinct authors while explicit overrides replace only matching inherited callbacks. Missing/cyclic/deeper-than64 chains fail closed; handlers are bounded to10,000. Callback discovery uses real module-level parser declarations and the production use resolver, excluding comments, strings and receiver-only methods. Adds destroy/task event kinds and explicit callback-family metadata. Inherited-only sheets resolve their nearest valid logic author; attachment validates before creating/changing Script2D and rejects invalid local logic or missing modules. Existing ambiguous duplicate handlers may now produce explicit diagnostics instead of silently hiding another callback.
- `src/runtime/objectBlueprints.ts` — added a validated current-asset draft override to the existing bounded resolver without temporarily writing the asset. Backend review and real AssetDatabase checks cover identity, missing sources, cycles, composition and no writes.
- `src/runtime/objectComposition.ts` — Pure preflight resolves required component dependencies and exclusions before mutation; rejects Transform exclusion, contradictions, conflicting controllers and connection-only Rope creation. Creates fresh default components for required additions and applies an already validated plan. Existing child composition is preserved.
- `src/runtime/objectFamilyLesson.ts` — added a concrete English/German/Chinese task lesson, including actual pool command names, inherited callback authors, instance overrides, paused validation and save/reopen checks.
- `src/runtime/objectPool.ts` — Track raw owner/member identity, simulation expiry and lifetime generations; clean up consistently before reset, prevent reentrant release, prune removed owners/members and expose configured-pool ownership for enforced capacity.
- `src/runtime/prefabs.ts` — User-audit follow-up: separates pure readPrefabOverrides from explicit normalization/capture. Inspector comparison and conflict reporting use the detached authoring snapshot without assigning reactive prefabOverrides, preventing their computed views from invalidating each other indefinitely. Explicit editing capture keeps its original persistence behavior. The actual module regression performs25 repeated reads with an override-setter trap, preserving override identity, unnormalized NaN, world connections/selection and asset bytes.
- `src/runtime/runtimeSceneTransition.ts` — Detaches/decodes a candidate scene, checks entity/connection identities and remaps stored numeric anchors to newly allocated IDs. Preserves actual persistent Entity objects and their descendants, IDs and lifetime generations, detaching persistent roots from outgoing parents with world transform retained. Commit rejects changed live entity/connection identities or target scene documents and restores old world object references, scene identity/navigation and view settings after installation failure. Failed preflight may consume monotonic allocation IDs; it cannot reuse an old ID or hydrate project metadata/assets.
- `src/runtime/scriptHotReload.ts` — A rejected plan cannot be downgraded to restart-required by later export checks. Rollback stores the previous raw graph document when supplied, preserving graph serialization. Adds peek/complete rollback lifecycle so persistence failure, disabled apply or rejected compilation cannot consume the previous committed history entry.
- `src/runtime/scriptTestExecution.ts` — A suite compiles in an isolated VM, runs before_all/after_all once, runs before_each/after_each per case, preserves teardown after failures and frees the VM in finally, including compile failure. Skip-only suites allocate no VM. Tests receive cloned case contexts; limits are checked between callbacks alongside real VM operation limits.
- `src/runtime/time.ts` — Timer/task generations invalidate already prepared callbacks when canceled/replaced/paused or when their entity/session is cleared. Rejects invalid requests; bounds timer count10,000 and frame callbacks4,096/64 per timer. Long repeats carry remaining debt to a later frame. Adds detached timer/task inspection; GameplayRuntime now uses the same expiration dispatch for ordinary frames and paused single-step.
- `src/runtime/worldGameplay.ts` — Install reviewed pool signals and prewarm synchronously before optional-package awaits; guard both preparation and completion by the current session so Stop cannot install stale work.
- `src/store/physics.ts` — Runtime load/reload now use the prepared scene-only transaction, replacing the old whole-project reload plus serialized persistent-object recreation. Removes the now-unused persistence capture/restore helpers and hierarchy import. Adds a shared authoring-data serializer body and detached readEntityAuthoringData accessor; normal serializeEntity retains its original normalization behavior. Runtime view commit/rollback updates scene layers/settings/selection without reloading assets, save namespaces, plugins or project metadata. Serializes/hydrates entity-level blueprint identity, migrates missing legacy values from Script2D, honors explicit null and synchronizes its component mirror; live/stored asset counting, replacement and clearing include static blueprint references.
- `src/world/Entity.ts` — Adds nullable objectBlueprintAsset metadata independently of Script2D. A valid static object can retain blueprint provenance without receiving an excluded or unwanted script component.
- `tests/fixtures/migrations/public-schema-expected.json` — Set only the26.14/26.14.0 candidate release authority or corresponding migration expectation. Dependency versions and public schemas remain unchanged.

## Generated outputs and reproducibility

WASM wrappers/binaries, Vite dist, Rust/Tauri targets, isolated compiled checkouts, reports/captures/downloads and qualification folders are generated outputs outside this authored source manifest. Five deterministic reference directories and four manuals are authored deliverables listed above. Fresh raw native test fixtures and actual browser downloads/captures are embedded in the release evidence bundles. The eleven final release artifacts are published only after their source, executed gates, embedded versions and checksums agree.

## Qualification follow-up edits

`scripts/generate-v26.14-teaching.mjs` and the three `manual/MANUAL.{en,de,zh-CN}.md` editions use the canonical `Engine: **26.14.0**` metadata. The first candidate already contained the correct number, but its missing colon failed the established manual contract; that failure is retained. The generator now preserves the contract without changing lesson or runtime behavior. `docs/RELEASE_NOTES_26_14.md` records the observed retry history. These files were already listed in the path-level manifest above.


Packaging follow-up: the candidate2 runtime/user gates all passed, but packaging refused the enemy-family reference because its README omitted the canonical engine label and its expected-output/test-controls omitted schema, authoring and classification metadata. The generator and three generated metadata documents now carry the same existing Project Format2/schema29 and exact engine; project.nova and runtime behavior are unchanged. The release-reference wrapper now validates these records against the package boundary before its gate passes. The failed packaging log and candidate2 evidence are retained; final qualification is performed on candidate3.
