# Nova_A 26.20 development — consequences and acceptance

Baseline: qualified 26.19 candidate7, digest 2d3f56b4c6216746610ecb5a4f8395d26bc6523908f64693b4e3213edfed5041. Preserve prior uncommitted work and releases. The user-supplied app_color_palette_system.md is input, not an assistant edit.

Before implementation:
- Palette catalog/preferences/Settings/CSS: add five exact semantic palettes, migrate legacy light/dark choices to Cloud Blue/Midnight Blue, retain mode buttons and high contrast, remember a palette per mode. No scene colors or project data change. Keep motion durations and animations intact. Test exact six role values, contrast, persistence, reset and all languages/scales/palettes.
- WebGL batch path: replace quadratic batch vertex recount with a running count; reuse bounded typed upload storage and grow GPU storage only when needed. Preserve packet ordering, colors, indices, blend modes, cameras and geometry. Test exact upload equivalence, limits, empty frames, mixed batches and real rendered pixels; record before/after timings without promising hardware-independent FPS.
- Anti-aliasing: investigate offscreen post-processing, which currently bypasses default-framebuffer MSAA. Any multisample target must resolve before post-processing, respect device/memory bounds, release resources and visibly report actual support. Preserve pixel-art sampling and every post effect.
- Release qualification: refresh implemented feature/gap inventory, EN/DE/ZH teaching, all-template walkthroughs, migration/recovery, reference workflows, measured budgets and full regression/common gates. Package only frozen tested source into exactly eleven files in releases/v26.20.

External checks retain 26.19 boundaries: only this Windows host is available; no independent beginner/expert, assistive technology, low-end/mobile hardware, matching Linux/macOS/Android, production signing/disposable installer lifecycle or real-duration 72h soak certification is claimed.

Compiler review before editing: both Cargo workspaces currently inherit the default release profile. Add optimization level 3, thin link-time optimization and one codegen unit to optimize across crate boundaries, disable incremental release compilation and strip debug information only. Preserve unwind/error recovery; no panic=abort, fast-math, native-only CPU instructions or feature stripping. This increases release build time and requires fresh Rust/WASM/native behavior and benchmark evidence.

Renderer harness finding: the retained TypeScript baseline was served as a virtual JavaScript module without transpilation, so the new audit never loaded. The corrected harness transpiles that baseline and compares identical default anti-aliasing settings. The initial failed report/module-load error remain unqualified evidence.

Implemented files so far: src/store/colorPalettes.ts, src/store/preferences.ts, src/panels/SettingsPanel.vue, src/assets/main.css; src/renderer/WebGL2Renderer.ts, Canvas2DRenderer.ts, outputQuality20.ts, renderSettings.ts, types.ts; src/components/WorldCanvas.vue and RenderingPanel.vue; both Cargo.toml workspaces. Added scripts/verify-v26.20-renderer.mjs, scripts/fixtures/renderer20-fixture.ts, scripts/fixtures/rendering20/baseline-webgl-v26.19.ts and scripts/inventory-v26.20.mjs. The retained baseline fixture is the exact19 renderer, transpiled only by the test harness. Added this plan and the implementation tracker. No authored file or animation removed.

Development findings:36 retained renderer checks now pass, including the previously failed German mounted-label check. Five new GPU/CPU checks pass. In one isolated endFrame grouping run with an identical recording sink,16000 packets measured median1182.2ms before and1.4ms after; this is not total render FPS. Exact vertex/index data and default-rendered pixels match. Post-processing produced actual4-sample edges. Final qualification will rerun against frozen source.

Resolution review found the overlay allocated before renderer limits and used the new DPR to infer old logical size, moving the camera when quality changed. Track logical size independently and apply the same backing limits before allocation; preserve screen coordinates and all overlay drawings.

Upload-capacity review: cap retained index capacity by splitting index-heavy batches as well as vertex-heavy batches. Every packet is still rendered in order; only pathological index-heavy groups may require an extra draw call. This prevents a valid repeated-index mesh from making one enormous upload allocation. Add an actual split regression and keep ordinary batch boundaries equivalent to19.

- Follow-up renderer defect: explicit MSAA with post effects disabled attempted an illegal single-to-multisample default framebuffer blit. Added a cached exact texel-copy shader and disposal in WebGL2Renderer; existing effects remain untouched. Added real GPU presentation and index-heavy packet preservation regressions in renderer20-fixture.

- Added 26.20 focus/authoring gate aggregators and five-palette layout aggregator. The common gate selects the expanded matrix only for26.20. The milestone audit context accepts an explicit26.20 regression target, validates the actual integrated version and retains regressionOrigin; earlier default behavior is unchanged. This re-executes existing animation, physics, network and delivery fixtures without duplicating their implementations or relabeling old results.

- Completed the interrupted WorldCanvas resize edit: cached logical dimensions now update from positive CSS dimensions, bounded DPR is applied before overlay allocation, hidden panels preserve the last valid viewport, and quality changes no longer move the camera. Quality browser audit now asserts the actual render canvas and rejects1x1 diagnostic results. Added all36 retained renderer fixtures to the20 runner, source-digest validation for qualification and nested capture paths. Corrected palette-user aggregate filename.

- Extended the palette audit with computed body/muted/action contrast ratios, actual high-contrast and reduce-motion switches, all three bundled help anchors and reset defaults. These tests affect only disposable browser preferences. The prior release preservation recheck found releases/ empty in the current environment; earlier archives cannot be rehashed here and were not removed by26.20 work. Existing source snapshots and earlier evidence remain the baseline.

- Corrected the20 signed-update test manifest to propose26.21 rather than the current26.20; otherwise the genuine downgrade/replay guard correctly prevents reaching signature/cancellation checks. No updater behavior was relaxed.

- Renderer audit cleanup now closes HTTP connections and logs caught failures before shutdown; an interrupted browser left CloseWait sockets that held the preflight process open with no report. This is harness cleanup only. That stalled preflight is retained as unsuccessful and rerun.

- First all-panel sweep observed1172 states;519 had repeated large-text/icon overflow, while all27 required viewport/scale cases and shell containment passed. Added scale-aware icon boxes, growing learning cards and wrapped learning/render/build/automation tabs/actions; stacked data selectors and large-scale build fields in shared main.css. PanelMaximizeButton now uses the existing font-ui token and scales its dimensions. No actions/animations removed. The harness replaces its fixed480ms viewport sleep with two actual repaint frames plus40ms; the independent320ms animation-settle observation remains. Concurrent-build404 findings remain in failed preflight evidence; final sweeps run on an unchanged dist.

- Added26.20 full40-starter browser library execution and retained26.11 template/WASM behavior suite to the gates. The new library runner rejects subset/discovery-only flags during qualification. Added a clearly marked narrow150/200% preflight mode to the panel runner to validate layout repairs quickly; final wrapper still runs the complete matrix. Corrected the localization layout selector to the actual presentation table editor.

- Renderer capture-history archival now resolves nested capture filenames correctly. The20 runner explicitly rejects its inherited15-only native-pixel option instead of suggesting it can qualify20; Windows build/process gates remain separate and no native pixel comparison is claimed. Prepared the20 executed-gate plan with expanded checks and all current documentation.

- All40 template exports passed individually; the reused06 aggregate incorrectly expected20. Added a20-owned export runner that checks all40 and copies actual production Web files, retaining honest structural-only native-template scope. Added separate20 delivery/package user runners to keep20 downloads distinct from19. Menu navigation now observes and accepts only the beforeunload leave prompt after its saved/exported bytes are verified; other dialogs are not automatically accepted. The first menu preflight passed11 stages but navigation timed out and remains failed evidence.

- Reviewed actual context-attribute lifetime: Off could reuse a canvas whose default framebuffer still had anti-aliasing. WorldCanvas now watches the Off boundary and replaces the consumed canvas only when the context antialias attribute changes; asynchronous generations prevent stale reset completion. The AA control retains history but no longer performs a redundant reset on every sample-count step. Quality user audit explicitly waits for actual0 samples after Off before checking MSAA4/resolution/undo/export.

- Hardened full layout qualification against stale/subset reports: the wrapper disables preflight/smoke modes, validates each child timestamp, palette, full state count and all27 required cases, and rejects any failed result. Each measured surface now records and checks the actual active palette.

- Narrow-panel repair follow-up: Automation retained two-column named grid areas after the shared narrow rule switched to one column, collapsing its authoring column. Synchronized its areas at the same1100px breakpoint. Saved hierarchy selectors now span the full row; data creation buttons no longer flex-shrink below enlarged text; project-health help icons get scaled width. Favorite-star rules now target the actual body-teleported dialog. Only presentation changes; all actions and animations retained.

- Expanded the20 focus gate to execute retained12 language parsing/emission and60 randomized VM pairs, API overload/lifecycle execution, typed syntax slot mutation, and production script module resolution. Existing suites execute against the current source; retained-format reports keep their original scope and are collected in the20 execution bundle.

- Shared world browser helper now waits for the complete launcher and its visible Open Project action after reload, instead of the root element alone. The quality preflight6 failed before opening a project during concurrent browser work; no renderer failure was observed in that attempt. This audit-only wait preserves real chooser input and does not weaken product assertions.

- Expanded scripting regression detected a stale source hash in the generated Rhai API inventory. Regenerate from current Rust native/WASM metadata using the existing generator, then verify every overload; this may refresh advertised signatures and source locations but removes no runtime API.

- Exact-resolution user audit now turns off the existing Adaptive presentation quality option through real Profiler input before measuring scale. The previous test assumed adaptive scale1 while automatic performance tiers may legitimately select0.86/0.72 under software-rendered concurrent load. This changes the disposable test project only and persists its explicit fixed-quality choice through save/export; production adaptive behavior remains available and unchanged.

- Retained12 module fixture now loads the actual MediaClock dependency introduced after12; its old generic function stub could not be constructed. Language/API report files now include execution timestamps so20 bundling rejects stale reports. No product code changed. Refreshed API diff contains only source hash and192 host source locations;988 signatures are retained.

- Isolated delivery preflight exposed the precise launcher race: Page.reload can return while the old document still reports complete/visible, then replace it before the next click. The shared helper now observes a new main-frame navigation before checking launcher readiness. It uses actual browser lifecycle events and keeps assertions/input intact. Isolated quality5 and menu12 checks passed before this audit-only correction.

- Delivery gameplay audit replaces850ms-right/330ms-up assumptions with bounded screenshot-guided real key presses toward the visible blue player/green checkpoint. The exact merged position and downloaded bytes are still independently asserted; no game state is read or changed through application internals. The old path passed merge/save/export but missed collection on this host.
- Retained module fixture now separately asserts stale-source rejection and passes a matching rejected candidate document/bundle to exercise contract rejection. Both failures must retain the prior compiled source and debug registration count. This follows the current production guard without changing or weakening it.

- Renderer preflight6 failed to load its fixture during broad Vite discovery and then stalled while the optimizer shut down. Stopped only the verified owned audit process after its logged failure. The audit now specifies exactly its two fixture entry points, an isolated cache and no file watcher/HMR; dynamic import errors are surfaced and cold fixture loading is bounded at120s. Product development/build behavior and all43 assertions are unchanged.

- Final development preflight passed: narrow panel sweep261 states/27 required cases; quality5, animated menu12, delivery4, package4, renderer43 and script modules20 checks. Renderer7 isolated CPU grouping measured813.7ms before/0.9ms after for16000 packets; GPU samples4 and partial edge pixels192. These are development results, not frozen-release certification or universal FPS. Updated tracker, release notes and exhaustive ledger before freeze; all21 final gates and exact11-file package verification remain mandatory.

- Candidate1 passed11 gates, including30 subsystem reports and byte-identical254-file clean/moved Web builds. Its panel gate was intentionally interrupted because forcing software rendering made the large layout matrix unnecessarily slow. A read-only probe found working default WebGL2 on ANGLE/NVIDIA RTX5060 Laptop GPU/D3D11. The20 panel harness now uses the browser default GPU and records its capability per palette; geometry, cases and animations are unchanged. Exact renderer pixel comparisons remain software-rendered. Candidate1 evidence/snapshot are retained as incomplete, and candidate2 must rerun all21 gates; no result is relabeled.

- Candidate2 passed the complete five-palette layout gate:5860 results,135 required cases,125 captures and no browser exceptions. The following palette reset audit failed because the screenshot showed migration notifications covering the bottom-right Reset Settings button. The production handler already restores both palette defaults. The audit now dismisses those notifications through their visible close buttons, asserts reset-button hit visibility, clicks and waits for the observable reset. No product behavior, notifications or animations change. Candidate2 remains an incomplete preserved run. Candidate3 places user interactions before the lengthy full layout gate, retaining all21 gates and all cases.

- Packaging-contract review before finishing candidate3 found missing machine-version wording in release notes and the required ledger section/manifest wording. Added those documentation labels in RELEASE_NOTES_26_20 and the repeatable ledger generator; exhaustive entries are unchanged. Candidate3 is retained incomplete after its source-integrity check, with no relaxation of the package verifier. Candidate4 is the next complete qualification.
