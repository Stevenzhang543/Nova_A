# Nova_A 26.25 / 26.25.0 implementation and acceptance

Base: committed 26.24 (ccb660b). Preserve schema 29, authored effects and default animations. Previous releases are immutable.

## Required work
- Debugger: honest VM/host capability boundary, session/source revision identity, watch safety, callback lifecycle, synchronized source/graph selection and diagnostics.
- Events/blueprints: deterministic inheritance order, cycles/override provenance, instance disposal and transactions; readable validation links.
- Web/mobile: capability startup notice, landscape guard preserving mounted edits/menus, visual viewport/keyboard/safe areas, touch manipulation, import/download fallback.
- PWA: relative scope/start URL/icons, HTTPS installation guidance and explicit update/offline policy.
- Teaching, targeted programmer/user evidence, immutable source and eleven release files.

## Risk-based audits authorized by the user
Run real builds, TypeScript checking, focused debugger/event/mobile tests, actual affected browser workflows, desktop launch/export startup, release integrity and documentation checks. Keep direct WASM behavior and history checks where scripts/events need them. Avoid the unchanged 5855-state UI matrix and unrelated rendering, audio, asset, physics and multiplayer regressions.

Skipped categories must be recorded individually as not-run with reasons, never passed or borrowed from older builds. The scoped qualification policy still requires actual current-source executions, original command logs, report timestamps, full Web inventory and six build artifacts. All failed attempts remain available. Source-bound checkpoint recovery only follows the user's prior authorization for identical frozen source/builds.

## Environment boundaries
Windows is available. Firefox/WebKit availability must be detected; unavailable engines remain explicitly unqualified. Real iPhone 12, Android, Linux/macOS, signing, clean-machine installation, low-end hardware, assistive technology and long-duration soak are not certified by desktop emulation.

## Implemented scope and evidence
Debugger/event/mobile implementations and localized teaching are present. Development checks passed: debugger contracts8, stale language replies3, language lifecycle7, real WASM runtime48, inherited events5groups, event validation30, mobile contracts4; browser debugger3, event2, mobile4, static-host/export2. Browser-discovered authored/bundled breakpoint mapping and resource-URI preview defects were fixed before freeze. Saved-project history covers309projects/810structured fixtures. Changed JS/TS/Vue comments cover30files/957functions. Final acceptance is the independently executed frozen-source13-gate plan and packaged evidence, not these development counts.

Godot reference: editor/debugger/script_editor_debugger.cpp real paused-thread step/continue and stack transport; Nova exposes the narrower synchronous callback/command host explicitly. Real VM instruction suspension remains outside this host capability, not simulated. Event inheritance, imported invalid chains, destruction, queued tasks and stop disposal are covered by actual module/WASM checks.
