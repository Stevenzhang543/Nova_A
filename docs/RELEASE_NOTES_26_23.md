# Nova_A26.23 — dynamic scripting and reliable launch

Engine26.23.0; Project Format2/schema29 unchanged.

Dynamic exported defaults now accept nested constant arrays/maps and null. Native/WASM semantic cases cover dynamic values, closures, function pointers, overloads, method dispatch, exceptions and budgets. Compiled programs are shared immutably between live and candidate runtimes; only affected roots compile. Failed or stale reloads preserve the live generation, compatible declared state and independent pending requests. Histories retain bounded metadata and session-owned rollback sources.

Script Studio has wider wrapping detail panes, snapshot value types and explicit reload outcomes. Inspection is bounded and rejects getters/prototypes/executable paths. Project modules retain literal use with a shared namespace; this is not native import aliases or unrestricted eval. Ordinary helpers receive state explicitly. Cooperative tasks remain owner-bound callbacks.

New reveals the complete40-template creation workflow; More groups existing advanced actions. Drafts survive cancel. Top-menu transitions retain one interactive branch, with pointer grace/re-entry, focus and dismissal handling. Exact website permissions and caught native failures prevent the brand-link crash. Profiler history trimming avoids repeated front shifts through thousands of reactive entries; no default animation or output-quality feature is removed.

Web editor/game archives support static root/subdirectory hosting. A public player manifest avoids hosts that block dot-directories. Explicit Download Web ZIP remains available alongside directory export. HOSTING.md explains MIME/cache/HTTPS and optional service boundaries.

Seven versioned references include an inventory/helper/callback/runtime-prefab project; three localized lessons preserve earlier chapters. The final source-bound qualification reports determine release status. Physical mobile/macOS/Linux/old-PC, signing, independent security/accessibility and long-duration soak checks remain separate; no claim of zero possible defects or universal best FPS.
