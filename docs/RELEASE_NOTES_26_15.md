# Nova_A 26.15 — reliable assets, measured rendering and useful starters

Engine 26.15.0. Project Format 2/schema 29, Rhai API 2 and Graph Format 1 remain compatible. This release's executed-gate manifest identifies the actual qualified source and Windows/Web/WASM bytes. Historical development results below are rerun against the integrated candidate; a version label is not a substitute for that evidence.

Imports and reimports prepare metadata and dependency changes before committing. Async work checks source, settings, record identity and project lifetime; failures retain the last valid asset and cancellation preserves already completed items. Retry targets the original UUID. Weighted cache, decode, queue, watcher and source limits bound retained data, and project replacement disposes importer work, decoded images/fonts and parsed tile documents. Reference repair changes complete supported references rather than arbitrary matching substrings. Folder moves preflight every affected source/path and retain identity on rollback.

Derived sprites share original pixels through stable owner/frame references. Atlas reorder, source rename and reimport preserve frame identity; rotated/trimmed input resolves correctly before any local crop. Decoded previews, trim and automatic region detection use the actual original or derived pixels. Frame-animation creation is a separate undoable operation using source order and individual durations. Automatic region detection records regions/outline; it does not itself instantiate frame assets or attach animation to an entity.

Tiled import supports bounded finite orthogonal JSON maps and atlas tilesets, plus the documented simple CSV TMX/TSX subset. It preserves firstgid, all eight horizontal/vertical/diagonal orientations, world-up row mapping, multiple sources, variable tile-animation durations and supported box/convex collision geometry. Unsupported layouts, encodings and complex layer/collision forms fail with diagnostics. Typed source selectors repair missing images or external tilesets before scene creation. Importing and opening Tilemap no longer trigger a reactive mutation loop. Map-owned definitions require an explicit independent editable TileSet copy, so a definition Save cannot erase the source map's layers; copy editing and Undo/Redo preserve the original document.

Assets uses desktop browse/preview/details panes and full-width narrow details with a Browse return action. Batch progress and cancellation remain visible; completed job logs collapse into an inline disclosure instead of covering the grid. Missing-source and failed-reimport diagnostics are visible, with typed repair and dependency navigation. Shared resources validate parent kind/cycles, resolve inherited defaults and named variants against the exact asset set, and preserve partial override semantics. Full invalid resource drafts survive workspace/asset switches and participate in existing departure/conflict recovery. Duplicate variant names and non-object JSON are refused. Saved runtime values are explicitly distinct from unsaved drafts; creating an inherited override requires a saved base.

Renderer fixes preserve painter order, refresh mutable content and include real texture revisions in tint/normal/atlas caches. Nine-slice, mesh and textured-shape color/alpha paths, normal-map orientation, point/spot/area shadow contribution, disabled casters, camera smoothing and render-texture replacement are exercised through actual rendered pixels. Failed shader/context setup disposes incomplete resources; when a WebGL-owned canvas cannot acquire Canvas2D, WorldCanvas replaces it and guards stale async resets. GPU/CPU caches and surfaces have count/byte/dimension bounds. The effective GPU ceiling is512MiB, including the Ultra preset. The per-frame upload budget drives a fair FIFO queue; preload margin participates in culling and nearby requests. Rendering diagnostics report actual queue/byte/deferred counts, with unavailable GPU values shown explicitly for Canvas2D. Unsupported directional shadows have disabled, explained controls that preserve authored values when switching light type. Canvas mesh antialiasing can differ from WebGL at triangle seams.

All forty stable launcher entries have captured runtime previews, searchable localized requirements, control instructions, expected outcomes, a feature-manual link and a matching starter task. Eight reused foundations are identified honestly. Optional networking starts offline and makes no hidden server connection; top-down and Tile World W/S mappings now follow world-up. The shared UI starter uses a real localized Text component for its Play caption, current ProgressBar min/max fields for68%, and explicit absolute panel layout. Its three derived entries retain their IDs. Included sample audio and unbound demonstration actions remain disclosed rather than being presented as a complete second game scene.

Web export validates exact dependency closure before output and preserves authoritative asset selection in NovaPak. The browser fallback now downloads a complete deterministic ZIP with player, WASM, game package and manifest, rather than discarding player files. The ZIP writer bounds count/bytes/paths, rejects unsafe/colliding names, retains UTF-8 names and checks cancellation before publishing a download. Project → Build uses the actual shared workspace router. Folder/native export keeps its existing route. Serve extracted browser output over HTTP. The evidence retains the actual lit-template project and ZIP produced through visible controls, verifies125 declared payload hashes, and compares its actual exported player pixels with the editor. The native continuation uses that exact saved project and the qualified Windows PE, validates the embedded package/source lineage and observes the exported executable's own Player origin; it does not substitute a fixture page.

Three localized workflow lessons explain assets, Tiled limits, resource variants, lighting/budgets and complete export. Another120 localized task sections cover every starter using the same guide records as the launcher. Four separate current-version code/blocks/mixed/authority references retain cumulative language and workspace tasks; historical references remain available.

## Evidence and limits

Development asset checks passed43 core,28 sprite,14 Tiled,7 batch and14 resource-handler groups, plus the library catalog/factory checks. Actual asset authoring passed14 workflows with real file import, slicing, animation, map creation, move/rename, repair, cancellation, inherited variants and downloaded-project reopen. Another36 new layout surfaces cover EN/DE/ZH, light/dark and100/150/200% scaling at720px. Forty starter runs verify visible output, Console status and Stop, while focused input/template checks cover corrected semantic values. These counts describe recorded states and assertions, not every possible combination.

The renderer corpus contains36 actual browser programmer groups, including controlled shader/context failures, mounted fallback/streaming panels and pixel goldens. Guard/archive/attachment/child-process tests enforce evidence and ownership boundaries. Actual browser light editing, saved-project download and complete ZIP/player output are separate user observations. The final focus and authoring bundles rerun these against the source-bound build, include matching native renderer/packaged-player comparisons, and embed fresh screenshots/downloads with hashes. Development preparation alone is not a native pass.

Common gates rebuild and test Rust/WASM/Windows/editor/player, check history/templates/layout, export current games and exercise a loopback authority. The headless harness now preserves client startup phase, early exit and bounded stderr/stdout without weakening its15-second readiness or20-second report limits. Performance measurements identify this host and exact inputs; increasing budgets cannot promise universal maximum performance. Physical assistive technology, clean-machine lifecycle/signing, other-platform/mobile hardware, public network infrastructure, independent security/usability and long-duration soak remain explicit external checks. The authority remains a renderer-disabled WebView. Version26.16 continues animation/audio/interface production separately.


Native audit correction after the interrupted run: the actual Windows WebView2 host reproducibly timed out on Emulation.setDeviceMetricsOverride. An isolated exact-source/native-byte probe used the real1707×996 CSS viewport at1.5 device scale and proved35 rendering groups; the remaining mounted fallback assertion had incorrectly assumed a1× backing surface. The fixture now explicitly sets/restores the existing maxPixelRatio1 preference for this deterministic canvas comparison. All36 native groups plus the complete browser/native golden comparison then passed the isolated diagnosis. The production renderer runner uses the actual native viewport and records it; the packaged-player continuation scales CSS coordinates by actual screenshot/viewport dimensions. Browser emulation and all pixel tolerances remain unchanged. These are audit-adapter/fixture changes, not renderer behavior changes. Failed and diagnostic reports remain retained; candidate2 repeats the full21 gates.

Candidate3 audit-runner refinement: candidate2 full runs3d5eefa3 and ae3d1b90 passed the native renderer, actual saved/native consumer and Windows gates, then timed out at first-peer readiness (15seconds); the later reconnect passed. Unchanged isolated traffic and separate cold/default-cache phase probes passed, with measured first-peer module-to-transport startup0.786/0.491seconds. The exact cause of the full-run startup stalls is not proven. The peer now uses its own Vite cache and records bounded module/Vite/production/network/entity/transport phase timestamps in success/failure reports. Timeouts, admission, replica assertions and engine behavior remain unchanged. Earlier failed reports remain retained; complete qualification is rerun against this candidate.
