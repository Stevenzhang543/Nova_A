# Nova_A 26.21 change consequences and audit log

Baseline: clean commit cb73d58, machine26.20.0, qualified source2aef852de592e7c1ecc538c865243a6809f1f7dbe7f7bf058e79ebbe652dd951. Preserve releases/v26.20 and all original palette input. The new roadmap defines future work separately from implemented behavior.

Before changes:
- New inventories/review/gap/manual documents and source audit tooling read authored inputs and expose coverage limits. Catalogue counts do not certify runtime behavior. Existing26.20 documents remain historical.
- Editor readability styles apply only to the editor, not exported game UI. Text-relative container rules may increase vertical scrolling while preserving controls, values, animations and user layout. Audit label/field width, focus, docking and restore; retain compact preferences.
- A new global form-label preference defaults to Automatic, accepts only Automatic/Above controls, persists in editor preferences and resets with other preferences. It does not alter project schema or game rendering. Labels/help/search must work in EN/DE/ZH and all palettes.
- The deferred control router currently uses a shared fallback key and reads state after an asynchronous import without retaining project identity. Distinct DOM-control identity, project lifetime checks and disposal must not load gameplay eagerly, produce duplicate domain history, or commit detached callbacks into another document. Exercise these boundaries before claiming a repair.
- Release helpers must accept explicit new-series targets while checking the actual source version and retaining original regression identity. Repair published ledger grammar in the generator, not in the frozen source after qualification. Validate all packaging contracts before the long matrix.

Source review findings to reproduce: fixed-pixel form breakpoints ignore text scale; number/range and paired controls can be contained yet too narrow; catalogued binding/test routes are declarations; source-backed Rhai regions are not fully structural graphs; external platform/human/device acceptance remains unexecuted.

## Implemented and observed so far

- Added normalized, remembered Automatic/Above controls preferences, localized English/German/Chinese help and search, and editor-only responsive form styling.
- Corrected stale delayed history callbacks with session generations, per-control keys, eligibility checks and disposal. The same project reloaded counts as a new lifetime.
- Reproduced rapid X/Y inspector edits collapsing into one Undo through actual browser input. Corrected the inspector's entity-wide key to include the originating control. The same user test then passed.
- Screenshot review found unscaled Settings help and inspector labels, short search-field height, overlapping hierarchy actions, and hierarchy filters starving the entity list at large text. Added scaled help/labels/units, flexible header rows and a bounded scrollable hierarchy filter region. The focused actual-input checks pass after these changes.
- Structural audit indexes 81 Vue surfaces, 2,981 control declarations and 1,610 conditional/repeated sections. All 102 distinct declared route paths exist. This does not prove every field's semantic behavior.
- Focused module regressions: twelve passed. Vue and Node TypeScript checks and the production Web build passed. Retained typed-graph and language tests passed (the latter includes 34 VM fixtures and 60 randomized VM pairs).
- Actual Edge user checks passed for preference persistence and unchanged saved-project dirty state, rapid independent inspector Undo, localized Settings at 100/150/200% in a 720px window, and numeric field capacity at 200%. The final hierarchy change was exercised by the focused user test.
- A development geometry preflight visited 262 states in three locales with Midnight Blue and 25 captures. It is not the complete five-palette qualification. Field-capacity findings are recorded separately; geometry success does not close readability findings. An explicit computed canvas font is required for capacity measurement because the computed shorthand may be empty.

## Environment and compatibility

No dependencies or toolchain pins changed. Node 22.22.2, pnpm 10.30.0 and Rust 1.92.0 remain declared; Rust includes the WASM target, clippy and rustfmt. Both TypeScript checks and the Web build execute locally. Existing build warnings about shared static/dynamic imports and large chunks remain; no bundling-performance claim is made by this foundation change. No new native/WASM qualification is claimed for modified source. Project Format 2/schema 29 and all version authorities still identify the packaged 26.20 baseline while 26.21 development is in progress.

## Remaining before a 26.21 release

Complete the all-palette/locale/scale and conditional-state readability audit, close measured short-field findings, exercise remaining dock/maximize/floating and save/reopen/export checks, align actual version authorities, prepare 26.21 references and teaching, run full retained release gates on frozen source, and generate/independently verify the eleven release files. No 26.21 binary/archive has been published or relabelled. Preserve the original 26.20 release hashes recorded in release-audits/v26.21-preserved-v26.20.json.

## Additional reproduced migration finding

The original 5,001-entity stress reference contains only animation/UI performance budgets in its production settings. The Rust formatter inserted defaults only when the entire production object was missing, so importing that valid historical partial object failed on missing traceCapacity. A failing regression reproduced it. Migration now inserts only absent required leaves across performance/replay/testing/data/jobs, preserving authored values and retaining rejection of explicitly invalid values. All 48 nova_format tests, clippy with warnings denied and workspace formatting checks pass; optimized WASM and Web builds pass. The browser large-scene run subsequently exceeded its default 30-second command deadline. A separate diagnostic run records a longer explicit deadline and elapsed open time; it is not a performance qualification.

The 5,001-entity browser diagnostic also exceeded its explicit 120-second Runtime.evaluate deadline. Its audit browser was terminated after the recorded failure to avoid leaving a hung test process. This is an unresolved large-scene responsiveness finding, not a passing virtual-scroll test. Diagnostic overrides now apply only to Runtime.evaluate; screenshot/cleanup commands retain bounded normal deadlines.

## Broader binding and reference repairs

The direct rebuilt-WASM audit initially rejected 11 of 213 references. In addition to partial production defaults, migration now fills only missing Build settings, preserves authored scripting API 1/2 (and rejects unsupported versions), admits the already implemented sensor/pen input devices, and canonicalizes known pre-calendar bare script asset UUIDs. Three references had Player/Enemy Health2D components sharing one UUID; repository search proved there were no uses outside those definitions, so only each Enemy component identity was replaced with a deterministic UUID. All 213 references now pass actual WASM migration and idempotence; this does not claim their gameplay was executed. Rust format tests now total 48 passing cases.

The stable-control observer also repeated complete label work for every mutation of a select. Its targets are now deduplicated per observer batch; a 5,000-mutation regression preserves labels/IDs with at most four complete label reads. Twelve focused module regressions pass. This bounded optimization did not by itself resolve the 5,001-entity browser load timeout, so the full-load performance finding remains open pending profiling.

## Profile-guided viewport and control registry repairs

A captured CPU profile located repeated full-scene scans in unbound compound geometry. The renderer now uses singleton membership when no intact binding exists, preserving polygon resolution, boundary generation, disabled/non-renderer filtering and the original bound-group path. A 5,000-shape regression verifies the fast path and retained binding behavior. A second profile located repeated document-wide ID collision searches. Stable-control allocation now snapshots occupied IDs once per synchronous batch and tracks suffixes in memory. A 5,000-collision regression verifies uniqueness, preservation of existing IDs and one document index. No animation, shape or control is removed.

The resulting browser run opened the 5,001-entity reference in 47,388 ms, after previous 120-second timeouts. This is a single development observation under headless software rendering, not an FPS benchmark or acceptable final performance. The subsequent workspace-navigation check needed an explicit DOM readiness wait; the complete virtual-scroll result is recorded separately. All twelve focused regressions, both TypeScript checks and the production Web build pass.

## Final hierarchy interaction result

The failed last-row check exposed virtual spacer padding on the scroll container itself, enlarging its minimum box and defeating virtualization. Spacing now belongs to an inner rows wrapper; scroll ownership and drag/drop remain on the original container. The actual browser test now passes at 200% scale: it scrolls to and selects Stress Object 5000, renders 15 rows at 58px height/spacing, and retains a 127px list viewport. Latest project-open observation: 46595 ms. The virtual-scroll defect is fixed in this tested scenario; overall load/FPS performance remains open. The final wrapper change passes Vue TypeScript and the production build. The four focused Settings/Undo/readability browser checks also passed on the preceding control-registry build.

Current deliverable status: the ten-version manual and inventories are published, and 26.21 foundation coding has begun with 38 described authored paths. This is not completion of every semantic/conditional-state audit and not a packaged 26.21 release. All eleven existing 26.20 release files were rechecked against their preserved SHA-256 hashes and remain unchanged.

## 26.21 completion candidate

The source authorities now identify 26.21 / 26.21.0 with unchanged Project Format 2/schema 29. Separate references, localized lessons, forty-template walkthroughs, retained regression runners and full palette qualification runners are authored. Selected-value details preserve native controls, existing descriptions and motion; keyboard/pointer focus and Escape are exercised separately from the programmer layout traversal. A scrolling-focus finding led to repositioning the active tooltip rather than dismissing it.

The preliminary panel traversal passes its visited geometry/capacity/full-value checks. Final acceptance requires the complete frozen-source qualification, including all five palettes, three locales/scales/sizes, retained runtime tests, actual authoring and output workflows, and exact eleven-file package verification. Refer to IMPLEMENTATION_TRACKER_26_21.md and the release evidence for the final executed outcomes; earlier observations above remain historical evidence, not current qualification totals. External human/device/platform limits remain unchanged.

The actual-input completion check reproduced Ctrl+S being ignored while a SELECT/INPUT/TEXTAREA had focus. TopBar now handles Save before the text-editing guard, blurs the active field, awaits Vue updates and saves. Native text Undo/Copy/Paste remain scoped as before. The focused browser check passes without clicking away. Windows portable/setup/MSI preflight artifacts report 26.21.0; final qualification rebuilds them against the immutable source candidate.

Candidate 1 passed native build, WASM, Web, TypeScript, Rust tests/lint and native tests/lint, then correctly rejected a retained update-test fixture whose target version had become current. Only the fixture changed: its future version is derived from the actual engine version. All 27 delivery regressions then passed. Preserve candidate 1 evidence; candidate 2 must run the full qualification against its own source digest.
Candidate 2 was intentionally interrupted during the first full palette layout run after screenshot review found mid-word navigation labels. Its completed native, Web, WASM, TypeScript, Rust, focus, history, template and layout-contract gates remain historical evidence, not a complete qualification. Candidate 3 must rerun all required gates on a new frozen snapshot.

Pre-freeze navigation and floating-layout follow-up: context navigation now grows with caption text, wraps at normal word boundaries without a two-line clamp, and exposes scrolling without shrinking away actions. Existing compact icon navigation and its accessible names remain. The floating hierarchy follows the real rail width. A real-input 1024x640 test exposed floating panels extending below the editor because their height was based on the whole window; a scoped editor-area maximum now preserves internal scrolling and excludes maximized panels. Workspace Manager Escape now stops propagation after closing, preventing the global shortcut from also clearing selection and hiding the Inspector. No actions, content or animations were removed.

The three new layout-user checks passed against rebuilt 26.21 production Web output: focused numeric draft survives resize, exact downloaded Transform2D value and maximize/restore; visible hierarchy/inspector float and redock; normal/large navigation labels in EN/DE/ZH. The test first needed centered, settled scrolling before an actual click (nearest scrolling landed under the inspector header), the actual serialized Transform2D kind, and exclusion of intentionally clipped accessible compact labels. These were test corrections, not evidence of a resize data-loss defect. Full candidate qualification remains required. Both TypeScript checks passed.
Pre-freeze follow-up completed: Midnight Blue preflight passed 297 panel/window states plus the engine authority check (298 results), with 25 captures and zero recorded failures. All five foundation user checks passed on rebuilt 26.21, including fifteen locale/palette combinations, focused save, separate Undo, narrow localized settings and readable 200% Inspector fields. These scoped observations precede, and do not replace, candidate 3 full qualification.
Candidate 3 passed the native/WASM/Web/type/Rust/focus/history/template/static-layout gates, but its full Cloud Blue layout run failed fourteen states. Twelve states exposed four profiler budget fields with under six readable numeric characters at 1366x768 and 150/200% text; two German navigation states had 41px label boxes but 43px text height at 150%. Navigation widths were sufficient. The candidate remains incomplete and its evidence is retained.
Before candidate 4, paired profiler budget rows now stack their label and give their inputs a wrapping full-width row with text-relative minimum sizes. Navigation line height increases from 1.15 to 1.35 to accommodate multiline text without clipping. These are editor-only layout changes; values, handlers, animations and exported UI remain intact. The layout-user check now covers 100/150/200% navigation and the exact four numeric fields in all three locales.
Candidate 4 pre-freeze: all three expanded layout-user checks passed on rebuilt 26.21 Web output, including the exact paired budget fields and navigation in EN/DE/ZH at 100/150/200%. Compact bottom tabs are exercised using the visible native selector. Fresh full qualification remains required.
Candidate 4's two complete-run attempts stopped in the retained networking test during Vite module loading (different shared runtime modules, 60-second fetchModule timeout), after seven build/source gates passed. A standalone unchanged retry passed, but recurrence showed that the concurrent test setup needed repair. No release package was published. Candidate 5 changes only the retained networking harness initialization: eleven overlapping SSR roots now load sequentially, preserving all assertions, engine behavior and timeout limits. The first sequential run passed in under ten seconds. Previous candidate source snapshots, reports and all 26.20 release files remain preserved.
