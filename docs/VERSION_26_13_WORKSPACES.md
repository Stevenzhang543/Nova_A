# 26.13 workspace changes — isolated staging

Integration notice: this journal preserves the original staged findings, failure results and pending-at-the-time entries. The reviewed files are now integrated for the26.13 candidate after26.12 was finalized. Final scope/status is in [26.13 release notes](RELEASE_NOTES_26_13.md) and its executed-gate evidence. Historical measurements are not relabeled as production qualification.

These files are prepared outside the 26.12 source snapshot and must be integrated only after that release is packaged. This is an implementation record, not a qualification result.

Consequences assessed before editing: maximize must preserve mounted panel state, widths, heights and docking choices; resizing must retain the hierarchy's existing collapse/expand behavior; translated labels must remain reachable; switching workspace must restore the user's last dimensions rather than preset defaults. Resize cancellation must restore both the initial value and the document's previous pointer styles. Project-scoped layout caches must not leak into another project's namespace. Editor layout changes do not alter authored game state or exports.

| File | Every prepared change |
| --- | --- |
| `src/editor/panelLayout.ts` | New finite-size clamp, pointer-scale conversion and keyboard resize functions. The independent audit adds a visible-size origin so CSS-capped preferences do not cause a drag dead zone. |
| `src/components/PanelResizeHandle.vue` | New visible separators with pointer capture, touch support, keyboard arrows/Shift/Home/End, Escape/cancel rollback, double-click reset, localized labels and disposal cleanup. The independent audit adds accepted-drag events, visible-size pointer/keyboard origins, unchanged click preservation, disable rollback, teardown without stale commits, and propagation for unrelated shortcuts. |
| `src/components/PanelMaximizeButton.vue` | New localized maximize/restore action; stores transient presentation focus only. |
| `src/editor/workspaces.ts` | Adds panel focus/restore, remembers each built-in workspace's layout in additive version-3 data, restores dimensions during workspace navigation, clears focus when routes/panels change, isolates project-scoped caches, and clears caches on reset. The independent audit repairs corrupt-storage workspace identity, prevents project scopes inheriting/deleting global legacy layouts, makes reset work when storage fails, preserves unrelated custom selection on deletion, applies the active replacement layout, rejects create/duplicate/import above the persisted 24-custom-layout limit atomically, normalizes duplicate imported IDs, and gives null imports an explicit format error. |
| `src/components/SceneSideBar.vue` | Replaces mouse-only resizing with the shared separator; preserves collapse/expand; follows restored workspace widths; adds maximize. The independent audit reserves a 22px collapsed rail so paint containment cannot clip the expand button and uses accepted-drag events instead of every pointer press. |
| `src/components/ConfigPanel.vue` | Replaces mouse-only resizing, follows restored widths, and adds maximize outside runtime-disabled editing controls. |
| `src/components/EditorBottomPanel.vue` | Replaces mouse-only resizing and adds maximize; auto-hide cannot collapse a maximized dock; explicit close clears panel focus. |
| `src/layout/EditorLayout.vue` | Marks maximized presentation state, prevents focus entering covered workspace/dock controls, and restores normal layout if the focused panel becomes unavailable. The independent audit also marks covered SceneTabs/physics monitoring inert, lets a sole visible split-dock panel use the available height, ignores bubbling entity drags when starting a panel drag, and clears the panel drag state on drag end. |
| `src/assets/main.css` | Adds bounded panel overlays and floating-panel handling without changing saved sizes; wraps Inspector titles; adds narrow-container form labels and full-width controls. |
| `src/visual/graphSyntax.ts` | Replaces per-node full token scans with binary span lookups and comment-only indexing; replaces per-region full-range scans with an exact-span/kind map. Equal-span expression wrappers select their own nodes; emitted source semantics remain unchanged. |
| `src/visual/graphTypes.ts` | Preserves optional ordered wire waypoints in Graph Format 1; rejects malformed/non-finite/out-of-range coordinates, more than 64 points per wire or 100,000 per graph; preflights total node/edge counts before normalization allocates payloads. |
| `scripts/verify-v26.13-workspaces.mjs` | Exercises real workspace state and storage namespaces, saved dimensions, maximize/reset and pointer/keyboard size math; supports isolated source overlays without integrating staged files. The independent audit adds seven groups for visible origins, corrupt/project legacy storage, custom deletion/capacity/duplicate IDs and storage-denied reset; failures remain in the JSON report instead of discarding later results. |
| `src/components/WorkspaceManager.vue` | Catches create/duplicate capacity refusals into its visible status field. Adds a localized Apply workspace action: selecting an existing row alone previously never restored that saved layout. The action uses the new bounded `applyNamedWorkspace` state operation and closes the manager after success. |
| `scripts/verify-v26.13-panel-controls.mjs` | New production Vue component-handler verifier for pointer rejection, no-motion preservation, Escape/lost-capture/disable rollback, safe teardown, keyboard sizing/propagation and all edited SFC compilation. Uses a custom Vue renderer; explicitly does not claim observed browser geometry or focus. |
| `docs/WORKSPACE_REVIEW_26_13.md` | Records reproductions, assessed consequences, exact verification commands, remaining browser scenarios and audit boundaries. |
| `scripts/lib/browserUserAudit.mjs` | Adds a reusable disposable Edge harness with real mouse/keyboard/select actions, bounded DevTools calls, screenshots and explicit cleanup; no application-state injection. Software rendering and file-input/download fallback remain explicit limits. |
| `scripts/audit-v26.13-panels.mjs` | Parses every Vue SFC and template AST, records imports, individual conditional/list branches, disclosures and controls with source lines/hashes, and generates the complete source inventory. Its output explicitly does not certify observed interactions. |
| `scripts/verify-v26.13-panels.mjs` | Adds real control-driven workspace, management, rendering, presentation, dock and settings routes; records per-field geometry, clipped prose, the browser accessibility tree and screenshots across language/scale/theme states. Prepared checks must run against the promoted build and unresolved findings block their pass. |
| `scripts/verify-v26.13-workspace-authoring.mjs` | Prepares actual pointer/keyboard resize, rollback, preset restoration, collapsed expansion rail, docked/floating maximize/focus, viewport-capped bottom resizing and named custom-layout restoration workflows. Requires the promoted production build. |
| `scripts/generate-v26.13-teaching.mjs` | Prepares the English/German/Chinese nested-loop layout, reroute, keyboard, resize/maximize and saved-workspace lesson for all four offline manuals. Its example must retain Console result18 after layout and export. Not run against the frozen 26.12 manual. |
| `scripts/generate-v26.13-reference-projects.mjs` | Prepares separate deterministic 26.13 code/blocks/mixed Coin Trail projects using the new layout engine, plus the current authority fixture. Adds layout/reroute/restore controls and verifies unchanged generated Rhai after arrangement. Historical references remain untouched. |

The named-layout correction also adds `applyNamedWorkspace` to `src/editor/workspaces.ts` and a fourteenth group to `scripts/verify-v26.13-workspaces.mjs`. The executed state test restores two distinct saved widths and rejects a stale identifier without mutation. The new action changes editor presentation only; it does not change project entities, asset payloads or export data. The current staged workspace state suite passes 14 groups.

### Eight-panel control and authoring review

The full consequence assessment and per-change evidence are recorded in `docs/PANEL_ACCESSIBILITY_REVIEW_26_13.md`. This independently prepared review passes 40 focused groups and inventories 580 conditional native controls. It does not claim observed browser interactions.

| File | Every prepared change |
| --- | --- |
| `src/components/AnimationPanel.vue` | Names asset/tangent selectors, playback and icon actions, individual animation/controller/skeleton/timeline fields; separates nested timeline mute/selection controls; adds pressed states; wraps/locally reflows panels and restores narrow inspectors; yields shortcuts to editable fields without changing canvas shortcuts. |
| `src/components/RenderingPanel.vue` | Names lighting/post/streaming/shader/uniform/layer/volume/capture controls, localizes navigation, replaces viewport-based narrow rules with component/card container queries, wraps prose and retains the preview card. |
| `src/components/PresentationPanel.vue` | Names device/localization/table/translation/snapshot/audio/effect/automation/loop/ducking controls; supplies waveform bounds; applies local container reflow and wrapped prose without changing handlers. |
| `src/components/EventSheetEditor.vue` | Names search/event controls and reflows all details/handler fields; guards open/create/quick-object/Open logic/leave with save/discard/cancel, serializes pending decisions and rejects stale/invalid/failed transitions; returns safe boolean saves; restores persisted drafts on discard; defers entity logic binding until successful save; exposes awaited `requestLeave()`. |
| `src/components/AudioSystemPanel.vue` | Bounds and flexes its embedded PresentationPanel and hides the empty audio-only header. |
| `src/components/MaterialGraphEditor.vue` | Names search, translates the missing amount label, stacks narrow columns and wraps diagnostics while preserving the scrollable canvas and graph generation. |
| `src/components/ParticleGraphEditor.vue` | Separates nested module toggle/selection controls, names enabled state and exposes pressed selection, translates the missing emission-shape label, and adds local narrow column/form rules. |
| `src/components/GraphProductionPanel.vue` | Names routine/variable/interface/event/library/watch/breakpoint/refactor/merge controls; replaces incorrect tablist semantics with pressed buttons; wraps narrow forms and diagnostic/reference/watch prose. |
| `src/editor/panelControlCopy.ts` | Adds typed contextual control and Event Sheet confirmation/error strings in English, German and Chinese. |
| `src/editor/panelAuthoringGuards.ts` | Adds the editable-target keyboard predicate and serialized draft transition helper with failed-save, stale-snapshot and concurrent-decision protection. |
| `scripts/verify-v26.13-panel-accessibility.mjs` | Compiles all eight SFCs, inventories every conditional native control, compares field/value/limit/disabled bindings and six unchanged script bodies against the immutable baseline, checks registered translations, and executes actual shortcut/Event Sheet functions plus guard regressions. |
| `reports/panel-control-name-edits.json` | Records all 190 explicit annotation additions individually with original panel/line/model/action/name-expression. |
| `reports/panel-accessibility-verification.json` | Captures 40 passing focused groups and 580 final conditional control records with explicit unobserved provenance. |
| `docs/PANEL_ACCESSIBILITY_REVIEW_26_13.md` | Records full-source review, pre-edit consequences, exact file ledger, reproducible commands, behavior changes and remaining browser/user checks. |

Required verification after integration: pointer and keyboard resize, cancel/reset, maximize/restore across docked/floating panels, focus order and panel disappearance, cached dimensions across switching/reload/project scope, EN/DE/ZH and 100/150/200% scale, all panel states, source-comment/VM equivalence and measured graph projection cost. No staged test is a release pass.

- CSS follow-up: exclude the measured Script Studio grid from the inherited 1100px one-column override. Other grids retain their fallback. The previous override forced a second row and reduced the editor to 77px in the real 1024px browser audit; measured Studio layout now owns its columns. Browser recheck pending.

- Browser hit-target correction: SceneSideBar.vue, ConfigPanel.vue and EditorBottomPanel.vue place their full 8px resize grip inside the panel border, replacing inherited -4px insets. The hierarchy paint containment clipped the grip center; actual pointer input hit its draggable ancestor and floated the dock. Input handlers and size limits stay unchanged; browser rerun pending. The workspace audit now selects Main Camera before requesting the selection-dependent Inspector and records the actual hit target for each drag. The staged26.13 reference generator also inherits26.12's explicit authority sessionName correction.

- Project departure: added projectDepartureGuard.ts and localized projectDepartureCopy.ts; projectManager.ts checks the exact pending draft revision/project before replacement, serializes decisions, preserves cancelled upgrade previews, treats cancellation as cancellation, and restores matching draft bases after failed loading. A new/open project no longer changes the current save directory during preview; it commits that directory only after success. The unreachable direct-open tail was removed. App.vue now owns the one ConfirmDialog instance, removed from EditorLayout.vue, so launcher-initiated decisions remain visible. Draft Save still uses the normal editor validation; replacement offers explicit discard or cancel/return, and failed replacements do not consume the drafts.
- Readability: SettingsPanel.vue now gives each input binding a visible localized label and wraps device/code/vector/scale/gamepad/dead-zone fields into available-width columns, preserving their model bindings and limits. Its search/filter toolbar and advanced fields stack in narrow cards. ContentAssetInspector.vue uses container queries for dependency/variant forms and wraps tab text. EditorBottomPanel.vue prevents asset inspector tabs/actions shrinking text vertically. PresentationPanel.vue gives snapshot volume inputs a text-scale-aware width and stacks snapshot rows in narrow mixer cards. Browser verification remains pending the assembled rebuild.
- Browser audit helper: unmodified Enter/Space include native text/keypress data; panel audit waits for the selected workspace and its mounted panel, retains both surface identity and field label, and resolves unnamed accessibility nodes to actual DOM markup for actionable findings. Previous174 observed snapshots and1126 findings remain failed development evidence; outgoing transition snapshots are not claimed as distinct completed routes.

- Maximize follow-up: main.css disables the inherited width transition only while a panel is maximized and uses the opaque base background. The first real maximize measurement caught an intermediate1410px width within1440px and a ghosted covered Inspector. Restore keeps its normal260ms motion; the browser audit now waits for that motion before comparing restored geometry. Resize/cancel/keyboard, preset restoration and collapse-rail browser groups passed in development r4.

- Added scripts/verify-v26.13-project-departure.mjs:11actual project-manager/helper behavior checks with explicit loader/dialog/storage doubles, allpassed. Added scripts/verify-v26.13-project-authoring.mjs: real launcher/Event Sheet cancel/continue/save, actual .nova download/file-picker reopen and explicitdiscard checks, prepared and syntaxchecked; browser execution pending. User files are disposable audit artifacts.

- Actual panel pass2 findings: main.css no longer forces the newly labelled input-binding fields into the old two-column-plus38px icon grid at200% scale, and default form container naming now has zero specificity so each panel's named container queries can execute. SettingsPanel.vue wraps its search/navigation and names search/device selectors. ConfigPanel.vue names body type; ConsolePanel.vue names its level/category filters; DeviceInputPanel.vue names device preset selection; PackageManagerPanel.vue names registry selection; AutomationStudio.vue names its source template selector. Their values, event handlers and persistence stay unchanged. PresentationPanel.vue enlarges DPI and snapshot controls; EditorBottomPanel.vue uses100px minimum tab/action columns that wrap instead of splitting Chinese labels into clipped glyphs. Baseline2 retained342 findings across177 snapshots; final matrix remains pending and transition snapshots are being excluded correctly.
- CreatorOnboarding.vue: only the dialog background owns Enter/arrow page shortcuts; focused native controls retain activation, while Escape still closes from any descendant. Handled dialog keys stop propagation. The visible6.0 label now reads the canonical current release. Browser reproduction showed keyboard Skip previously advanced the onboarding page; a real keyboard regression is included in the next browser run.

- Independent rollback follow-up (UI agent): projectManager.ts now passes readTextAsset to both draft snapshots. The snapshot records the current saved content separately from the original draft base, preserving pre-existing conflicts after a failed load/rollback. verify-v26.13-project-departure.mjs adds that regression; all12manager/helper groups pass. Stable browser dist is unchanged until the next coordinated build.

### Final modal and panel audit repairs

- `src/editor/modalFocus.ts`: new shared modal directive keeps Tab/Shift+Tab within visible enabled controls, retains native control key handling, restores a surviving trigger on close and prioritizes nested overlays by their visible stacking level. Listeners exist only while a modal is mounted and are removed after the final close. Nonmodal history/status panels remain nonmodal.
- `src/components/CommandPalette.vue`: attaches the shared focus directive to its existing modal root; dialog actions, validation and visibility are retained.
- `src/components/ConfigPanel.vue`: attaches the shared focus directive to its existing modal root; dialog actions, validation and visibility are retained.
- `src/components/ConfirmDialog.vue`: attaches the shared focus directive to its existing modal root; dialog actions, validation and visibility are retained.
- `src/components/ConnectionBuilder.vue`: attaches the shared focus directive to its existing modal root; dialog actions, validation and visibility are retained.
- `src/components/CreateObjectPalette.vue`: attaches the shared focus directive to its existing modal root; dialog actions, validation and visibility are retained.
- `src/components/CreatorOnboarding.vue`: attaches the shared focus directive to its existing modal root; dialog actions, validation and visibility are retained.
- `src/components/ErrorRecovery.vue`: attaches the shared focus directive to its existing modal root; dialog actions, validation and visibility are retained.
- `src/components/ExternalChangeDialog.vue`: attaches the shared focus directive to its existing modal root; dialog actions, validation and visibility are retained.
- `src/components/ManualViewer.vue`: attaches the shared focus directive to its existing modal root; dialog actions, validation and visibility are retained.
- `src/components/ProjectManager.vue`: attaches the shared focus directive to its existing modal roots (upgrade and read-only review); dialog actions, validation and visibility are retained.
- `src/components/RecoveryCenter.vue`: attaches the shared focus directive to its existing modal root; dialog actions, validation and visibility are retained.
- `src/components/ShortcutEditor.vue`: attaches the shared focus directive to its existing modal root; dialog actions, validation and visibility are retained.
- `src/components/StudioStatusDialog.vue`: attaches the shared focus directive to its existing modal root; dialog actions, validation and visibility are retained.
- `src/components/WorkspaceManager.vue`: attaches the shared focus directive to its existing modal root; dialog actions, validation and visibility are retained.
- `src/components/PresentationPanel.vue`: snapshot inputs now fill their grid cell instead of inheriting the generic58% field width; the observed Volume control was only41.75px wide. Values and audio behavior are unchanged.
- `scripts/verify-v26.13-workspace-authoring.mjs`: opens the visible compact dock selector when responsive tabs are hidden, and explicitly expands a closed dock before testing maximize. This fixes the test’s mistaken tab-only assumption.
- `scripts/generate-v26.13-reference-projects.mjs`: uses the mandatory spaced Project Format2/schema29 metadata spelling (`Project Format 2/schema 29`) established by the independent release verifier.
