# Nova_A 26.29 / 26.29.0

Baseline: immutable 26.28 candidate2 source digest `4542bf06db50f3d9e77513be205fe0fbc91ebf3d3c9f71dbcc6b3bb2378f9e60`. Preserve previous working-tree changes and all existing releases. Project Format 2/schema 29 remains unchanged. Final release qualification is pending source freeze and executed evidence.

## Requirement-to-implementation map

| Requirement | Concrete owner and acceptance |
| --- | --- |
| World bindings, navigation target validity and unload ownership | WORLD_26_29.md; actual navigation and streaming modules, bounded membership churn, same-cell handoff identity, target disappear/reappear and retained runtime suites |
| Multiplayer ownership, reconnect, replay and packet impairment | NETWORK_26_29.md; handshake cancellation, existing authority/RPC/admission/protocol contracts, 28 retained module cases and three independent UDP processes |
| Complete deterministic rollback contract | Explicit state inventory and exclusions in NETWORK_26_29.md; existing transform-delta correction remains narrower. Full physics/VM/RNG/checkpoint/journal implementation is not claimed |
| Clearly presented networking diagnostics | NetworkStudioPanel contract and last authorized snapshot packet age/tick/sender, locale-reactive EN/DE/ZH and timer disposal; actual affected-panel user checks |
| Windows runtime requirements and missing-runtime recovery | PLATFORM_26_29.md; read-only external prerequisite probe, real current-host detection and explicitly simulated failure branches; no unsupported OS promise |
| Linux/macOS and architecture-aware build prerequisites | Build Settings target-specific prerequisite card, unknown-target rejection, matching-host recipes and manually dispatched unqualified CI candidates; actual other-host qualification remains unavailable |
| Truly windowless authority design | NATIVE_HEADLESS_26_29.md; separate nova_headless native physics JSONL utility, actual process/protocol/physics/query validation and explicit no-project/no-Rhai/no-network-server boundary; rendering-disabled game export still requires WebView |
| Static editor, downloadable Web game and PWA service boundaries | WEB_HOSTING_26_29.md; root/subpath startup, local editing without an application backend, downloadable-game behavior and explicit optional service/permission checks selected by the parent workflow |
| Separate immutable release with full instructions | Per-path ledger, EN/DE/ZH lessons/manuals, current native/WASM/Web builds, frozen source binding and independent exact eleven-artifact verification |

## Risk-based execution

Execute the changed-module suites and real end-user flows linked to world/network/platform/runtime changes. Include current builds, version authority, package/source hashes, required history/manual checks and downloaded output behavior. Avoid unrelated repeated 40-template walkthroughs, media/renderer microbenchmarks, global dependency audits and long idle campaigns where ownership did not change. A discovered affected owner adds its focused regression.

Development evidence is described in subsystem documents under its actual identity. Final `executed-gates.json`, immutable source digest and packaged evidence determine qualification. Failed or interrupted attempts remain in raw logs; neither CI recipes nor test fixtures certify external machines.

## External qualification and design limits

Only this Windows computer is available. The offered iPhone has not been used. Linux/macOS/Android builds, signing/notarization, clean installation and the old-PC missing-runtime report cannot be marked passed here. The new native utility proves a separate no-WebView physics command path; it does not replace the full game player's script/project/network runtime. Transform history, multiplayer saves and replay checksums each retain their explicitly documented state boundaries. No feature/default animation is removed to simplify these contracts.
