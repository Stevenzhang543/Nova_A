# Nova_A 26.13 — readable workspaces and measured graph editing

Engine 26.13.0. Project Format 2/schema 29, Rhai API 2 and Graph Format 1 remain in use. The executed-gate manifest in this version's evidence archive determines qualification and the exact source/build hashes. Development observations below do not substitute for that manifest.

Graphs use actual rendered node and pin bounds, nested control regions, deterministic component placement and orthogonal routes. Arrange all or selected nodes as one undo action; retain selection, viewport and unaffected positions. Existing manually positioned graphs stay in place until requested. A new code projection receives one measured initial arrangement, cancelled by user interaction. Wire selection, keyboard pin connections, disconnect, bounded reroute points, symbol search, compact code nodes and cancellation share existing document history. New comments open in free space at a readable zoom, with creation and viewport movement covered by one Undo.

Source projection indexes token spans and owner lookup instead of repeatedly rescanning the complete syntax document. Separate 100/1,000/10,000-node measurements and worker/fallback/cancellation checks are retained in the focused evidence bundle. Measurements depend on this host and graph shape. Returned clear routes avoid node rectangles; impossible or bounded-search routes produce a visible diagnostic. Arbitrary wire-to-wire crossing freedom is not promised.

Hierarchy, Inspector and bottom tools have visible resize handles with keyboard, reset and cancellation controls, plus maximize/restore. A collapsed hierarchy leaves an expansion rail. Presets remember their own dimensions; named custom workspaces can be applied, exported, imported and restored after project reopening. Code and graph studios budget their panes from measured container space, retain secondary commands in disclosures and keep editable fields reachable. Settings input bindings have visible labels; narrow rendering, audio, asset, material, particle, event and animation forms wrap with their own controls. Modal focus remains inside the active dialog and returns to a surviving trigger when closed. Text editing no longer invokes animation shortcuts.

Unsaved source tabs, graphs and Event Sheets survive in-session workspace unmounts. Recovery belongs to the exact live asset and project, preserves complete draft content and graph history, and detects changes to the saved base. Conflict previews distinguish saved and draft versions; keeping a draft acknowledges the new base before the next normal validated Save. Cancelling or failing a project replacement retains drafts and the previous save location. Explicit discard clears them. Event Sheet Save validates raw callback names, priorities and seeds without silently normalizing an invalid draft. This session cache is not browser-restart autosave.

The localized manuals add a nested-loop layout exercise with expected output18, selected arrangement, wire editing, save/reopen and workspace export/import. Three deterministic Coin Trail projects provide code, graph and mixed bindings; the separate authority reference retains its exact session configuration. Existing forty launcher templates and all fifty shipped Rhai scripts remain covered by template/conversion checks.

## Evidence and boundaries

Development browser evidence recorded ten workspace, sixteen Studio and six project-departure groups, plus177 panel states with no remaining findings in those states. The final production release reruns these against its own built bytes. Actual pointer/key input, native downloads/file-picker reopen, screenshots and raw measurements are embedded with hashes in the authoring evidence bundle. The source inventory parses every Vue SFC and lists conditional/disclosure/control states separately; it does not claim that all possible combinations were clicked.

The focus bundle retains measured layout/routing/worker checks, actual Rhai WASM differential execution, persistence, draft ownership, project failure recovery, Vue control handlers and the complete SFC inventory. Its performance and binding comparison baseline is the independently verified26.12 source archive, digest `0d824ccfc9a79c7800fdec95a4f41bd9282b937172becc31d543cc733b2888a4`. Reproduction requires that archive at `releases/v26.12/Nova_A-v26.12-source.zip` beside the checkout.

Common gates rebuild and verify Windows/WASM/Web, typecheck, test/lint Rust, exercise templates/history/layout, export and launch Windows games, exercise a loopback authority, and retain scoped benchmark/stability/dependency results. Windows launch liveness does not certify installation/uninstallation. The authority remains a renderer-disabled WebView, not a windowless server. Local browser focus/accessibility-tree observations do not certify physical assistive technology. Clean-machine lifecycle, signing, matching-platform/mobile hardware, independent usability/security and long-duration soak remain explicit external checks. Version26.14 continues object/event/runtime ownership separately.
