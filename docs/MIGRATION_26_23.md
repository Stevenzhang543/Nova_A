# Upgrade to26.23

Project Format2/schema29 and API2 remain unchanged. Back up existing projects and open normally; no bulk schema conversion is required. Earlier26.22 releases are preserved byte-for-byte.

New now opens a creation dialog; advanced project actions are under More. Cancel retains the creation draft. Script detail width defaults to400px where space allows; saved layout preferences still apply. Hot-reload metadata is bounded; Stop ends rollback ownership. Ordinary Rhai helper functions require explicitly passed local state. Use literal project modules; import aliases remain unsupported.

For website deployment extract every file from the Web ZIP and serve via HTTP(S). Follow HOSTING.md. Browser folder export remains; Download Web ZIP is explicit. Native website permissions allow only exact documented HTTPS links.
