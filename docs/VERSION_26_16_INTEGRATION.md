#26.16 integration and teaching journal

Integration notice26.16: this journal retains historical development paths, agent handoffs, intermediate failures and pending-at-the-time statements. Its reviewed source is integrated after26.15 was independently packaged and verified. The combined final development source passed TypeScript,12 actual menu/downloaded-player groups and the required current module/field checks;54 additional layout surfaces are recorded separately. Current behavior is in RELEASE_NOTES_26_16.md and the three task lessons. Only fresh source-bound integrated gate results establish production qualification; physical devices and long soak remain external.

Before edits: each release retains its separate source/artifact identity. The new task lesson is additive and preserves all120 starter tasks and historical anchors. Generation is permitted against actual16 metadata; development previews require a separate output root. The four cumulative reference projects remain deterministic and carry exact Project Format2/schema29 and engine identity. The actual animated-menu reference and its observed authoring history are provided separately; no generated fixture is presented as a user-authored action.

- `docs/ANIMATION_AUDIO_INTERFACE_LESSON_26_16.en.md` — Add complete English menu/animation/audio/localization/IME/save/reopen/export workflow and supported limits.
- `docs/ANIMATION_AUDIO_INTERFACE_LESSON_26_16.de.md` — Add matching German task instructions and evidence boundaries.
- `docs/ANIMATION_AUDIO_INTERFACE_LESSON_26_16.zh.md` — Add matching Chinese task instructions and evidence boundaries.
- `scripts/generate-v26.16-core-references.mjs` — Generate and byte-verify four distinct16 cumulative code/blocks/mixed/authority references from the retained production generator; no prior reference is rewritten.
- `scripts/generate-v26.16-teaching.mjs` — Add three localized current-version Markdown/HTML task sections with canonical engine metadata, preserving earlier lessons; refuse active-root development output.


Before evidence integration: raw16 reports keep qualifiedRelease null until aggregate release qualification exists. `scripts/lib/milestoneAuditBundle.mjs` accepts this new context only when exact release, engine/build engine, integrated source/build roots, expected release and target all agree; development overlays and stale reports remain refused. Existing reports keep their original checks. `scripts/verify-v26.16-audit-bundle.mjs` adds14 positive/negative boundary groups. `scripts/verify-v26.14-runtime-stage.mjs` keeps the affected48 real native/WASM scripting regressions and updates only its controlled audio host with new16 clock/begin/disposal methods plus the real MediaClock import. The report retains its historical suite identity and actual current engine. The copied unchanged runtimeAudit14 helper is development support and not an authored diff.


Observed integration gap and consequences: runtime Skip existed only behind editor transport; ordinary game buttons had no explicit timeline route. `src/runtime/timelineUiActions.ts` adds four opt-in @timeline:play/pause/skip/resume actions, bounded nearest-owner hierarchy resolution, disabled/missing/cycle diagnostics and existing evaluator dispatch. Hierarchy ownership avoids embedded entity IDs that break prefab copies; legacy callback names retain their path. `src/runtime/GameplayRuntime.ts` dispatches these actions for active live UI entities without requiring Script2D and stops reserved actions from duplicate script/event fallthrough. `src/components/TimelineButtonAction.vue` supplies a readable action selector and resolved owner, preserves the previous custom callback, and relies on the parent Inspector change transaction. `src/components/RuntimeComponentsInspector.vue` adds that component before the existing callback field, retaining final15 directional controls. `src/editor/timelineUiActionCopy.ts` supplies matching EN/DE/ZH actions and scope. No serialized field or public format changes; onPressed holds the explicit opt-in value. Behavioral/export/user checks follow before qualification.


Caption localization integration: `src/runtime/timeline.ts` now resolves explicit {key} caption bindings through clip locale, owner Canvas preview locale, then project preview locale. Literal caption text and payload text retain their path; current resolved text/locale reach both editor and exported WorldCanvas. `src/assets/localizationDependencies.ts` includes timeline clip locales and their normal fallback/font closure in packages, with a64MiB source-text discovery budget and explicit refusal. `scripts/verify-v26.16-timeline-actions.mjs` tests real play/pause/skip/resume, nearest ownership, disabled/stale/cyclic/missing cases, unskippable clips, localized captions and dependency inclusion. The same public formats and onPressed field are retained.

Before timing correction: the new partition audit found direct clip playback still accumulated floating seconds, giving 1.0000000000000013 versus 0.9999999999999999 after equivalent60/30Hz updates. `src/runtime/animation.ts` moves only this private transport onto the existing integer MediaClock, including seeks, with unchanged serialized formats, duration caps and dispatch policy. Seek sampling is quantized at the common clock resolution; crossed events and preview ownership must be rechecked. `scripts/verify-v26.16-media-performance.mjs` adds12 measured animation/timeline/UI/rig workloads, equivalent-partition assertions,100 evaluator reset cycles and the20000-entity layout refusal. Timings are observational host CPU samples, not portable frame or hardware guarantees.

Evidence naming correction: `scripts/verify-v26.16-interface-user.mjs` and `scripts/verify-v26.16-animation-authoring-user.mjs` now use their full user suffix for the browser report and captures, preventing collision with the separate programmer reports when both run in the integrated root. Coverage and application behavior are unchanged.

Actual menu input found `TimelineButtonAction.vue` closed its own details element when Custom callback was selected because open depended on the action value. Use an initially open native disclosure whose user-controlled open state remains stable across selection changes. This keeps the focused selector reachable; no callback behavior changes.

`scripts/generate-v26.16-menu-reference.mjs` adds the deterministic animated-menu reference foundation (three languages, PCM music, opacity clip, timeline and four explicit actions) without changing the40 template IDs. `scripts/verify-v26.16-menu-user.mjs` adds actual picker/Inspector/curve/timeline/localization/IME/keyboard/touch/resize/save/reopen/Web-download/player checks. Its first timing attempt pressed Enter inside the deliberate32ms composition-commit guard; the test now waits for a subsequent ordinary Enter. The application composition policy is unchanged.

`scripts/verify-v26.16-focus.mjs` aggregates11 current media/interface programmer suites plus affected native/WASM runtime, format, typed-graph, deterministic-reference and full Vue inventory regressions. `scripts/verify-v26.16-authoring.mjs` requires fresh interface, animation, combined saved/exported menu and54-surface layout input reports; minimum workflow counts cannot be satisfied by stale development reports. Both wrappers require exact integrated26.16.0 metadata.

Actual large-menu Save failure: browser localStorage quota was exceeded by the optional temporary/backup copy, preventing the file download. `src/runtime/projectTransactions.ts` now catches and records optional recovery-cache failures before/after the real sink, preserves atomic setItem last-good behavior, and cleans temporary copies independently. The16MB recovery cap counts UTF-16 bytes. Browser origin quota is a recovery warning, since it does not measure the chosen file/native/download destination; the actual sink still propagates disk/permission/cancellation failures and leaves dirty state unchanged on failure. Validation/checksum/phase guards and native atomic writes are unchanged. Tests must prove quota/denial cannot block a download or replace a real sink error, and successful cache snapshots still recover.

The real720px layout matrix found74px transport inputs and62px marker-time fields. `src/components/AnimationPanel.vue` reserves96px numeric transport fields and96px marker columns, retaining narrow-container stacking and toolbar wrapping. `scripts/verify-v26.16-media-layout.mjs` measures54 timeline/audio/responsive surfaces across EN/DE/ZH,100/150/200 percent and light/dark themes; it checks contextual names, clipped text, editable widths and scrolled hit targets in the exact user-saved menu. `scripts/verify-v26.16-save-recovery.mjs` adds9 actual-module positive/negative transaction groups, now required by the focus wrapper.

The next narrow matrix surface found a59px output-device selector and70px snapshot gain field. `src/components/PresentationPanel.vue` gives the output row a wrapping180px selector basis and snapshot gains a96px scaled minimum, retaining stacked phone layouts. Mixer behavior and saved values are unchanged.

Teaching audit corrected all three lessons: Root Motion Apply retains absolute authored sampling; only the explicit delta API accumulates movement. The lessons now identify exact Timeline Button actions/nearest owner, caption-language precedence,64MiB dependency discovery limit, the real12-group saved/exported menu changes and optional browser recovery-cache behavior. `src/runtime/controlRegistry.ts` also contains the earlier observed SVG key-control fix: accessible naming reads SVG text safely instead of assuming HTMLElement.innerText exists; real keyboard selection of curve keys is covered by the9-group animation authoring audit.

At150percent scale the responsive preview authored-scale input shrank to75px. `src/components/PresentationPanel.vue` now reserves a96px numeric input basis and wraps its label. The layout audit also waits for the actual asynchronously mounted panel and explicitly selects Animation after a remembered Audio tab; these harness readiness corrections do not change workspace restoration.

Actual150percent screenshot and hit testing showed stacked Interface cards were compressed to the viewport while overflow-visible descendants overlapped later cards, blocking the device selector. `src/components/PresentationPanel.vue` now uses a scrolling nonshrinking column for narrow responsive/accessibility/localization/audio cards; each card retains its content height. Desktop grids remain. The54-surface audit must verify reachable controls after this substantive layout correction.

Reviewing the real preview exposed misleading validation: centered reference-sized controls were told to choose Fixed mode even though they correctly scale with their Canvas; that change would disable scaling. `src/runtime/uiProduction.ts` now diagnoses the actual ignored-input case (Fixed mode with responsive breakpoints) and accepts centered responsive controls. `scripts/verify-v26.16-interface.mjs` adds the actual half-size geometry/diagnostic comparison, raising the suite to55 groups and current total to219. No layout evaluator or project value is changed by this diagnostic correction.


The nonshrinking flex attempt exposed zero-height descendants in inline-size containment during intrinsic sizing. The final narrow-card rule uses explicit max-content grid rows and start alignment; this reserves each card’s measured content height without compressing rows to the viewport. The layout audit now retains ancestor geometry and hit-target diagnostics on failures. Qualification still requires all54 actual surfaces.


The final grid rule passed all36 English/German surfaces. Chinese revealed79px master voice-limit and76px limiter-ceiling fields. PresentationPanel now reserves96px numeric fields in wrapping master-control rows; their saved values, ranges and mixer behavior are unchanged. Full54-surface verification is repeated after this source edit.


Final combined development layout result: all54 actual Timeline/Audio/Responsive surfaces passed with screenshots, across three locales, three scales and both themes. The grid-row correction and numeric-width corrections are retained. Development proof uses the actual15 engine plus the explicit16 overlay; production qualification reruns every workflow on exact16 source/build metadata.
