# Nova_A26.16 exhaustive edit ledger

Engine26.16.0. Baseline: finalized26.15 source digest `ea991793f21e0afd9d5331b3994aa1d538c99fe70204dd10aee09421b7b78b83`. This deterministic path-level manifest compares every actual authored input, including dirty/untracked files. 114 paths changed or were added; 0 authored paths were removed. The four implementation journals preserve every refinement, observed failure and its reviewed consequence.

## Consequences assessed before integration

Shared media clocks remove frame-partition drift; explicit preview/record ownership prevents old sessions from stopping later Play or losing a captured draft. Audio cache/voice limits and timeline/rig/UI work bounds can refuse excessive inputs with diagnostics. Root motion retains prior absolute semantics; delta motion remains explicit. Four reserved Button callbacks are opt-in and use bounded live ancestry; ordinary script callbacks keep their path. Caption locale discovery expands the actual package closure. Sparse themes/retained drafts prevent selection from silently saving defaults. Optional browser recovery must not block a requested external save; real sink failures still preserve dirty state. Pane widths/wrapping add readable space without dropping controls. Public project/graph formats and40 library IDs remain.

All finalized12–15 directories remain immutable. Each integrated gate and artifact must identify the exact16 snapshot; development overlay reports do not qualify production. Physical sound, devices, other hosts, installation/signing, independent accessibility/security and long soak remain separately measured work.

## Files changed/added

- `Cargo.lock` — Set only26.16/26.16.0 engine/release/migration authority; retain dependency pins and public schemas.
- `Cargo.toml` — Set only26.16/26.16.0 engine/release/migration authority; retain dependency pins and public schemas.
- `README.md` — Present the current16 media/interface behavior and link its separate notes/ledger; retain all historical feature material.
- `crates/nova_format/src/lib.rs` — Set only26.16/26.16.0 engine/release/migration authority; retain dependency pins and public schemas.
- `docs/ANIMATION_AUDIO_INTERFACE_LESSON_26_16.de.md` — Record the complete localized workflow, exact edits/consequences, measured limits, observed failures and qualification boundaries for16.
- `docs/ANIMATION_AUDIO_INTERFACE_LESSON_26_16.en.md` — Record the complete localized workflow, exact edits/consequences, measured limits, observed failures and qualification boundaries for16.
- `docs/ANIMATION_AUDIO_INTERFACE_LESSON_26_16.zh.md` — Record the complete localized workflow, exact edits/consequences, measured limits, observed failures and qualification boundaries for16.
- `docs/EDIT_LEDGER_26_16.md` — Record the complete localized workflow, exact edits/consequences, measured limits, observed failures and qualification boundaries for16.
- `docs/IMPLEMENTATION_TRACKER_26_12_TO_26_16.md` — Record finalized15 and implemented/audited16 roadmap work while leaving final integrated qualification pending until executed.
- `docs/RELEASE_NOTES_26_16.md` — Record the complete localized workflow, exact edits/consequences, measured limits, observed failures and qualification boundaries for16.
- `docs/VERSION_26_16_ANIMATION_UI.md` — Record the complete localized workflow, exact edits/consequences, measured limits, observed failures and qualification boundaries for16.
- `docs/VERSION_26_16_INTEGRATION.md` — Record the complete localized workflow, exact edits/consequences, measured limits, observed failures and qualification boundaries for16.
- `docs/VERSION_26_16_INTERFACE.md` — Record the complete localized workflow, exact edits/consequences, measured limits, observed failures and qualification boundaries for16.
- `docs/VERSION_26_16_MEDIA.md` — Record the complete localized workflow, exact edits/consequences, measured limits, observed failures and qualification boundaries for16.
- `manual/MANUAL.de.md` — Add the complete localized16 animation/audio/interface lesson and canonical engine metadata while retaining all120 starter tasks and historical anchors.
- `manual/MANUAL.en.md` — Add the complete localized16 animation/audio/interface lesson and canonical engine metadata while retaining all120 starter tasks and historical anchors.
- `manual/MANUAL.zh-CN.md` — Add the complete localized16 animation/audio/interface lesson and canonical engine metadata while retaining all120 starter tasks and historical anchors.
- `manual/index.html` — Add the complete localized16 animation/audio/interface lesson and canonical engine metadata while retaining all120 starter tasks and historical anchors.
- `package.json` — Set only26.16/26.16.0 engine/release/migration authority; retain dependency pins and public schemas.
- `reference-projects/README.md` — Link five separately versioned16 references, their complete lesson and actual saved/exported evidence boundary.
- `reference-projects/projects/creator-v2616-animated-menu/README.md` — Add this deterministic16 README.md with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/creator-v2616-animated-menu/expected-output.json` — Add this deterministic16 expected-output.json with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/creator-v2616-animated-menu/project.nova` — Add this deterministic16 project.nova with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/creator-v2616-animated-menu/test-controls.json` — Add this deterministic16 test-controls.json with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/creator-v2616-blocks-game/README.md` — Add this deterministic16 README.md with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/creator-v2616-blocks-game/expected-output.json` — Add this deterministic16 expected-output.json with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/creator-v2616-blocks-game/project.nova` — Add this deterministic16 project.nova with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/creator-v2616-blocks-game/test-controls.json` — Add this deterministic16 test-controls.json with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/creator-v2616-code-game/README.md` — Add this deterministic16 README.md with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/creator-v2616-code-game/expected-output.json` — Add this deterministic16 expected-output.json with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/creator-v2616-code-game/project.nova` — Add this deterministic16 project.nova with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/creator-v2616-code-game/test-controls.json` — Add this deterministic16 test-controls.json with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/creator-v2616-mixed-game/README.md` — Add this deterministic16 README.md with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/creator-v2616-mixed-game/expected-output.json` — Add this deterministic16 expected-output.json with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/creator-v2616-mixed-game/project.nova` — Add this deterministic16 project.nova with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/creator-v2616-mixed-game/test-controls.json` — Add this deterministic16 test-controls.json with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/server-v2616-headless-authority/README.md` — Add this deterministic16 README.md with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/server-v2616-headless-authority/expected-output.json` — Add this deterministic16 expected-output.json with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/server-v2616-headless-authority/project.nova` — Add this deterministic16 project.nova with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `reference-projects/projects/server-v2616-headless-authority/test-controls.json` — Add this deterministic16 test-controls.json with stable project/asset identity, exact engine/schema, controls, expected output and generation-versus-observation scope.
- `scripts/fixtures/v26.16-interface-components.json` — scripts/fixtures/v26.16-interface-components.json (new)   Explicit nondefault field values for every own authored field of Canvas, RectTransform, Panel, Image, Text, Button, Slider, ProgressBar, Checkbox and TextInput. Constructor-key comparisons fail if the fixture misses a newly added field.
- `scripts/fixtures/v26.16-media-browser.mjs` — scripts/fixtures/v26.16-media-browser.mjs (new)   Actual Edge fixture generating a known PCM WAV and using the authored decoder/transport and actual Web Audio effects, bus/master graph and UI voice path. Eleven groups compare rendered sample values, loop/end silence, reverse/high rates, seek, disposal, cache sharing, low/high-pass, compressor dynamic-range reduction, delay, deterministic reverb and UI routing. Expectations account for equal-power center panning and compressor makeup gain.
- `scripts/fixtures/v26.16-media-fields.mjs` — Define the retained complete field/enum or actual browser media fixture used by the corresponding source/WASM/PCM/pixel qualification; it is programmer evidence, not an authored user action.
- `scripts/generate-v26.16-core-references.mjs` — scripts/generate-v26.16-core-references.mjs — Generate and byte-verify four distinct16 cumulative code/blocks/mixed/authority references from the retained production generator; no prior reference is rewritten.
- `scripts/generate-v26.16-menu-reference.mjs` — scripts/generate-v26.16-menu-reference.mjs adds the deterministic animated-menu reference foundation (three languages, PCM music, opacity clip, timeline and four explicit actions) without changing the40 template IDs. scripts/verify-v26.16-menu-user.mjs adds actual picker/Inspector/curve/timeline/localization/IME/keyboard/touch/resize/save/reopen/Web-download/player checks. Its first timing attempt pressed Enter inside the deliberate32ms composition-commit guard; the test now waits for a subsequent ordinary Enter. The application composition policy is unchanged.
- `scripts/generate-v26.16-teaching.mjs` — scripts/generate-v26.16-teaching.mjs — Add three localized current-version Markdown/HTML task sections with canonical engine metadata, preserving earlier lessons; refuse active-root development output.
- `scripts/lib/mediaAudit16.mjs` — Bundle real selected media/interface modules with explicit inert Node browser surfaces, bounded disposable output and actual compiled WASM access; staging is never an integrated release authority.
- `scripts/lib/milestoneAuditBundle.mjs` — Accept the new raw audit context only with exact release/engine/build/source identity and no development fallback; retain fresh bounded hashed screenshots and authored downloads.
- `scripts/lib/milestoneAuditContext.mjs` — Require exact integrated engine/source/build identity for qualification; allow only explicit development overlays, preserve baseline/build metadata and fresh report timestamps, and annotate real browser reports without declaring whole-release qualification.
- `scripts/verify-v26.14-runtime-stage.mjs` — Rerun the affected48 native and48 WASM lifecycle/ownership/debug/command regressions with real MediaClock/Timeline actions and updated controlled audio host methods; preserve historical suite identity and actual current engine.
- `scripts/verify-v26.16-animation-audio.mjs` — scripts/verify-v26.16-animation-audio.mjs (new)   Builds actual staged runtime modules with 15/14/13 dependencies; instantiates actual entity/components; runs 46 behavior groups covering baseline failures, timeline/animation/rig/recording/mixer semantics, serialization and work refusal. Browser media elements are controlled I/O fakes in this Node suite; these checks alone are not audio-output proof. Accepts --source-root and --report. scripts/verify-v26.16-animation-audio.mjs: preserves the media agent's 46 checks and adds three real direct-evaluator seek/stop groups (49 passed). Actual browser PCM evidence is the separate media agent suite.
- `scripts/verify-v26.16-animation-authoring-user.mjs` — Evidence naming correction: scripts/verify-v26.16-interface-user.mjs and scripts/verify-v26.16-animation-authoring-user.mjs now use their full user suffix for the browser report and captures, preventing collision with the separate programmer reports when both run in the integrated root. Coverage and application behavior are unchanged.
- `scripts/verify-v26.16-animation-authoring.mjs` — scripts/verify-v26.16-animation-authoring.mjs: 15 actual bundled-module groups for six document types, exact invalid drafts, no record-time asset writes, workspace retention, stale saved bases, atomic limits, actual mixer routing/seek/isolation, and detached final capture. Browser Audio elements are explicit transport fixtures; this is not PCM evidence.
- `scripts/verify-v26.16-animation-ui.mjs` — scripts/verify-v26.16-animation-ui.mjs: 14 actual SFC/compiler and extracted production handler groups for Save ordering/failure, target rebinding, cancelled startup, rapid Stop→Play, capture identity and keyboard/pointer selection. Explicit dependency fixtures; no mounted geometry or complete user-workflow claim.
- `scripts/verify-v26.16-audio-pcm.mjs` — scripts/verify-v26.16-audio-pcm.mjs (new)   Builds the fixture, serves it only on loopback, launches a fresh hidden headless Edge profile and records actual OfflineAudioContext PCM checks via CDP. Owns server/profile/browser cleanup and safe path validation. Accepts source root/report options. A complete disposable source overlay is needed during staged development because Vite's worker URL resolver requires physical worker files.
- `scripts/verify-v26.16-audit-bundle.mjs` — Before evidence integration: raw16 reports keep qualifiedRelease null until aggregate release qualification exists. scripts/lib/milestoneAuditBundle.mjs accepts this new context only when exact release, engine/build engine, integrated source/build roots, expected release and target all agree; development overlays and stale reports remain refused. Existing reports keep their original checks. scripts/verify-v26.16-audit-bundle.mjs adds14 positive/negative boundary groups. scripts/verify-v26.14-runtime-stage.mjs keeps the affected48 real native/WASM scripting regressions and updates only its controlled audio host with new16 clock/begin/disposal methods plus the real MediaClock import. The report retains its historical suite identity and actual current engine. The copied unchanged runtimeAudit14 helper is development support and not an authored diff.
- `scripts/verify-v26.16-audit-context.mjs` — Execute the named current programmer/user audit and preserve fresh actual reports, failures and scope; assertions and observed corrections are recorded in VERSION_26_16_INTEGRATION.md.
- `scripts/verify-v26.16-authoring.mjs` — scripts/verify-v26.16-focus.mjs aggregates11 current media/interface programmer suites plus affected native/WASM runtime, format, typed-graph, deterministic-reference and full Vue inventory regressions. scripts/verify-v26.16-authoring.mjs requires fresh interface, animation, combined saved/exported menu and54-surface layout input reports; minimum workflow counts cannot be satisfied by stale development reports. Both wrappers require exact integrated26.16.0 metadata.
- `scripts/verify-v26.16-focus.mjs` — scripts/verify-v26.16-focus.mjs aggregates11 current media/interface programmer suites plus affected native/WASM runtime, format, typed-graph, deterministic-reference and full Vue inventory regressions. scripts/verify-v26.16-authoring.mjs requires fresh interface, animation, combined saved/exported menu and54-surface layout input reports; minimum workflow counts cannot be satisfied by stale development reports. Both wrappers require exact integrated26.16.0 metadata.
- `scripts/verify-v26.16-interface-pixels.mjs` — scripts/verify-v26.16-interface-pixels.mjs (new)   Seven explicitly isolated actual browser Canvas2D programmer checks: opaque/transparent/partial-alpha tint pixels, untouched scene pixels, tint invalidation/disposal and measured Unicode wrapping. Creates and removes only its own checked test directory inside the selected build output.
- `scripts/verify-v26.16-interface-user.mjs` — scripts/verify-v26.16-interface-user.mjs (new)   Actual Edge/CDP launcher/Play controls, IME, native selection, Tab, touch cancellation/activation, editor Pause focus isolation, Stop cleanup, theme/locale Save/Discard/Cancel, duplicate refusal, actual downloaded project bytes and real picker reopening. No application state/module injection. Evidence naming correction: scripts/verify-v26.16-interface-user.mjs and scripts/verify-v26.16-animation-authoring-user.mjs now use their full user suffix for the browser report and captures, preventing collision with the separate programmer reports when both run in the integrated root. Coverage and application behavior are unchanged.
- `scripts/verify-v26.16-interface.mjs` — scripts/verify-v26.16-interface.mjs (new)  54 programmer groups using actual staged modules: geometry, renderer calls, composition bridge, callbacks, Unicode, themes, translations, retained drafts, every-field scene serialization/hydration and deterministic NovaPak rehydration, recursive locale/font build closure and excluded-resource diagnostics. Controlled canvas/media I/O is labeled as such. Reviewing the real preview exposed misleading validation: centered reference-sized controls were told to choose Fixed mode even though they correctly scale with their Canvas; that change would disable scaling. src/runtime/uiProduction.ts now diagnoses the actual ignored-input case (Fixed mode with responsive breakpoints) and accepts centered responsive controls. scripts/verify-v26.16-interface.mjs adds the actual half-size geometry/diagnostic comparison, raising the suite to55 groups and current total to219. No layout evaluator or project value is changed by this diagnostic correction.
- `scripts/verify-v26.16-media-layout.mjs` — The real720px layout matrix found74px transport inputs and62px marker-time fields. src/components/AnimationPanel.vue reserves96px numeric transport fields and96px marker columns, retaining narrow-container stacking and toolbar wrapping. scripts/verify-v26.16-media-layout.mjs measures54 timeline/audio/responsive surfaces across EN/DE/ZH,100/150/200 percent and light/dark themes; it checks contextual names, clipped text, editable widths and scrolled hit targets in the exact user-saved menu. scripts/verify-v26.16-save-recovery.mjs adds9 actual-module positive/negative transaction groups, now required by the focus wrapper.
- `scripts/verify-v26.16-media-performance.mjs` — Before timing correction: the new partition audit found direct clip playback still accumulated floating seconds, giving 1.0000000000000013 versus 0.9999999999999999 after equivalent60/30Hz updates. src/runtime/animation.ts moves only this private transport onto the existing integer MediaClock, including seeks, with unchanged serialized formats, duration caps and dispatch policy. Seek sampling is quantized at the common clock resolution; crossed events and preview ownership must be rechecked. scripts/verify-v26.16-media-performance.mjs adds12 measured animation/timeline/UI/rig workloads, equivalent-partition assertions,100 evaluator reset cycles and the20000-entity layout refusal. Timings are observational host CPU samples, not portable frame or hardware guarantees.
- `scripts/verify-v26.16-media-roundtrip.mjs` — Execute the named current programmer/user audit and preserve fresh actual reports, failures and scope; assertions and observed corrections are recorded in VERSION_26_16_INTEGRATION.md.
- `scripts/verify-v26.16-menu-user.mjs` — scripts/generate-v26.16-menu-reference.mjs adds the deterministic animated-menu reference foundation (three languages, PCM music, opacity clip, timeline and four explicit actions) without changing the40 template IDs. scripts/verify-v26.16-menu-user.mjs adds actual picker/Inspector/curve/timeline/localization/IME/keyboard/touch/resize/save/reopen/Web-download/player checks. Its first timing attempt pressed Enter inside the deliberate32ms composition-commit guard; the test now waits for a subsequent ordinary Enter. The application composition policy is unchanged.
- `scripts/verify-v26.16-save-recovery.mjs` — The real720px layout matrix found74px transport inputs and62px marker-time fields. src/components/AnimationPanel.vue reserves96px numeric transport fields and96px marker columns, retaining narrow-container stacking and toolbar wrapping. scripts/verify-v26.16-media-layout.mjs measures54 timeline/audio/responsive surfaces across EN/DE/ZH,100/150/200 percent and light/dark themes; it checks contextual names, clipped text, editable widths and scrolled hit targets in the exact user-saved menu. scripts/verify-v26.16-save-recovery.mjs adds9 actual-module positive/negative transaction groups, now required by the focus wrapper.
- `scripts/verify-v26.16-timeline-actions.mjs` — Caption localization integration: src/runtime/timeline.ts now resolves explicit {key} caption bindings through clip locale, owner Canvas preview locale, then project preview locale. Literal caption text and payload text retain their path; current resolved text/locale reach both editor and exported WorldCanvas. src/assets/localizationDependencies.ts includes timeline clip locales and their normal fallback/font closure in packages, with a64MiB source-text discovery budget and explicit refusal. scripts/verify-v26.16-timeline-actions.mjs tests real play/pause/skip/resume, nearest ownership, disabled/stale/cyclic/missing cases, unskippable clips, localized captions and dependency inclusion. The same public formats and onPressed field are retained.
- `src-tauri/Cargo.lock` — Set only26.16/26.16.0 engine/release/migration authority; retain dependency pins and public schemas.
- `src-tauri/Cargo.toml` — Set only26.16/26.16.0 engine/release/migration authority; retain dependency pins and public schemas.
- `src-tauri/tauri.conf.json` — Set only26.16/26.16.0 engine/release/migration authority; retain dependency pins and public schemas.
- `src/assets/assetGraph.ts` — src/assets/assetGraph.ts   Adds implicit runtime localization table/fallback roots to final15 project dependency discovery; retains all15 reference parsing/repair/derived-source behavior.
- `src/assets/localizationDependencies.ts` — src/assets/localizationDependencies.ts (new)   Pure bounded snapshot dependency roots for selected/source/Canvas-preview locale tables and every recursive fallback. Uses actual source text/data URLs, then ordinary asset graph resolves declared font references. Caption localization integration: src/runtime/timeline.ts now resolves explicit {key} caption bindings through clip locale, owner Canvas preview locale, then project preview locale. Literal caption text and payload text retain their path; current resolved text/locale reach both editor and exported WorldCanvas. src/assets/localizationDependencies.ts includes timeline clip locales and their normal fallback/font closure in packages, with a64MiB source-text discovery budget and explicit refusal. scripts/verify-v26.16-timeline-actions.mjs tests real play/pause/skip/resume, nearest ownership, disabled/stale/cyclic/missing cases, unskippable clips, localized captions and dependency inclusion. The same public formats and onPressed field are retained.
- `src/components/AnimationPanel.vue` — src/components/AnimationPanel.vue: replaces the ruler-only RAF preview with the owned runtime session; adds actual evaluated seek/pause/restore controls outside an inert authored field surface; retains six media document kinds with original saved bases; validates raw fields before Save; reports conflicts and failed persistence without clearing recovery. Save preserves the selected Curve/Dope view. Recorded runtime poses transfer to a retained clip draft after scene restoration. New timeline frame-rate/zoom/rate/Boolean visibility/compatible-asset/removal controls write their actual serialized fields. All existing controller, mask, rig, skin, command, marker and sprite editors remain. Measured numeric row heights align key lanes; structure and property resizers retain visible controls at narrow widths. Key pointer selection no longer toggles twice, and scale-aware drag/box selection have capture, cancellation and unmount cleanup. The real720px layout matrix found74px transport inputs and62px marker-time fields. src/components/AnimationPanel.vue reserves96px numeric transport fields and96px marker columns, retaining narrow-container stacking and toolbar wrapping. scripts/verify-v26.16-media-layout.mjs measures54 timeline/audio/responsive surfaces across EN/DE/ZH,100/150/200 percent and light/dark themes; it checks contextual names, clipped text, editable widths and scrolled hit targets in the exact user-saved menu. scripts/verify-v26.16-save-recovery.mjs adds9 actual-module positive/negative transaction groups, now required by the focus wrapper.
- `src/components/PresentationPanel.vue` — src/components/PresentationPanel.vue audio sections: replaces an isolated HTML audio preview with a separately owned actual mixer runtime; adds bus selection, output routing and all supported filter/compressor/delay/reverb fields; commits effect/send toggles and gains plus loop edits through existing history. Shows real transport and voice sample clocks. Labels RMS and sample-peak estimates accurately, with a streaming-rate capability hint. Mixer audition stops/releases its own media on selection/unmount. Waveform analysis no longer changes asset metadata merely on selection; explicit normalization changes commit only if the same live asset/source/target remains current after decoding. Waveform drag supports capture, Escape and unmount cleanup. Bus creation uses collision-free IDs. Adds a property-pane resize handle and contained layouts. Coordinated exception: createLocale now creates successfully before discarding the old translation draft and catches refusal into the existing error surface; Interface/theme changes are owned and documented by the Interface agent. src/components/PresentationPanel.vue   Replaces illustrative fixed menu with actual scene preview; shared responsive validation and selected-ancestor Canvas application. Localized draft/conflict/error feedback, theme/locale switch and tab guards, stale asynchronous-import/save checks, strict CSV/PO errors, retained theme/locale drafts, sparse theme source editing with resolved display/preview values. Existing audio sections preserved and later modified by the separate audio UI owner. The next narrow matrix surface found a59px output-device selector and70px snapshot gain field. src/components/PresentationPanel.vue gives the output row a wrapping180px selector basis and snapshot gains a96px scaled minimum, retaining stacked phone layouts. Mixer behavior and saved values are unchanged. At150percent scale the responsive preview authored-scale input shrank to75px. src/components/PresentationPanel.vue now reserves a96px numeric input basis and wraps its label. The layout audit also waits for the actual asynchronously mounted panel and explicitly selects Animation after a remembered Audio tab; these harness readiness corrections do not change workspace restoration. Actual150percent screenshot and hit testing showed stacked Interface cards were compressed to the viewport while overflow-visible descendants overlapped later cards, blocking the device selector. src/components/PresentationPanel.vue now uses a scrolling nonshrinking column for narrow responsive/accessibility/localization/audio cards; each card retains its content height. Desktop grids remain. The54-surface audit must verify reachable controls after this substantive layout correction.
- `src/components/RuntimeComponentsInspector.vue` — Add Timeline Button action selection before the retained callback fields while preserving final15 directional-light capability controls.
- `src/components/TimelineButtonAction.vue` — Expose EN/DE/ZH Timeline action/owner guidance in the Button Inspector, preserve the previous custom callback, and keep the disclosure reachable while changing its selection.
- `src/components/UiScenePreview.vue` — src/components/UiScenePreview.vue (new)   Actual current-scene GameUiRuntime in a separate read-only instance, shared DPI/safe-area/RTL/text/shape renderer, parent-resolved theme draft preview, visible empty/limit/error feedback, dirty rendering capped at20fps and4million backing pixels, disposal.
- `src/components/WorldCanvas.vue` — src/components/WorldCanvas.vue   Preserves final15 renderer fallback/remount. Real native TextInput overlay, IME/selection handlers, game/editor keyboard isolation, touch/pen capture/cancel, accessible native activation/focus/value updates, locale direction and native style; timeline and ordinary captions respect timing, category toggles, scale/background and disposal.
- `src/editor/animationAuthoring.ts` — src/editor/animationAuthoring.ts: dispatches the real six document normalizers on detached data; reports exact fields/counts normalization would change rather than silently accepting them. Tagged recovery preserves NaN and signed infinities as invalid drafts. Validation has a 16 MiB serialized ceiling and a bounded traversal; it does not mutate input.
- `src/editor/animationAuthoringCopy.ts` — src/editor/animationAuthoringCopy.ts: matching English, German and Chinese draft, validation, preview, capture, recording-limit and resize copy.
- `src/editor/animationPreviewSession.ts` — src/editor/animationPreviewSession.ts: owns only the Play session it started from Edit mode, runs the same physics readiness/preflight and GameplayRuntime as global Play, resolves the target again after startup callbacks, and restores pending authoring drafts after the normal whole-project Stop restore. It compares the stable runtime session identity, so rapid external Stop→Play cannot be adopted or stopped by the old editor owner. It preserves a final recording reset snapshot and does not overwrite a newer retained draft while restoring older recovery.
- `src/editor/animationStudioState.ts` — src/editor/animationStudioState.ts: removes immediate updateTextAsset writes from record mode; binds a live record/project/base to a retained draft and preserves recording across workspace navigation. It preflights finite values, the actual 100-track and 10,000-key limits before adding keys, permits replacement at the snapped existing frame, and exposes a refusal code. Explicit discard stops the recording owner.
- `src/editor/audioAudition.ts` — src/editor/audioAudition.ts: detached source entity plus a separate instance of the actual AudioRuntime for mixer-routed playback, absolute-time seek, pause, diagnostics and disposal; no scene/entity/asset mutation.
- `src/editor/audioAuthoringCopy.ts` — src/editor/audioAuthoringCopy.ts: matching EN/DE/ZH meter, transport, effect, routing and failure copy.
- `src/editor/interfaceCopy.ts` — src/editor/interfaceCopy.ts (new)   EN/DE/ZH Interface preview, draft, conflict, fallback and limit feedback.
- `src/editor/studioDraftRetention.ts` — src/editor/studioDraftRetention.ts: six exact media-kind mappings were coordinated with the Interface agent, who also owns theme/localization additions on the final15 resource baseline. src/editor/studioDraftRetention.ts   Adds theme→uiTheme and localization→localization kinds to the UI owner's final15+16 animation-draft baseline. No existing kind removed.
- `src/editor/timelineUiActionCopy.ts` — Provide matching EN/DE/ZH labels, resolved-owner guidance and reserved-action scope.
- `src/i18n.ts` — Set only26.16/26.16.0 engine/release/migration authority; retain dependency pins and public schemas.
- `src/projects/projectFormat.ts` — Set only26.16/26.16.0 engine/release/migration authority; retain dependency pins and public schemas.
- `src/runtime/GameplayRuntime.ts` — src/runtime/GameplayRuntime.ts was copied from the final 26.14 overlay only after verifying SHA256 5bab7f6dd96c2d8a73be765b7518ad64a4aeb4bbcb2fcedae8e7ee973342dde0. Its lifecycle, ownership, scene transactions, script hot reload, debug and timer changes remain. Only reserved media imports, clock state, begin/frame/step/stop integration changed. The existing 26.15 setTileAnimationTime(seconds: number   null) API is consumed; no tilemap, renderer, importer, derived-sprite or asset data files are copied into the 16 authored overlay. src/runtime/GameplayRuntime.ts   Copies the verified final 14 baseline; adds fixed-step MediaClock and tile-clock imports/state. Session begin resets media and initializes mixer settings before first clip evaluation. Actual fixed physics steps advance the common media clock and set audio/tile time before animation/timeline evaluation. Frame audio receives that transport; paused frames remain paused. Single-step updates evaluated frame/sample positions and leaves sound paused. Stop releases voices/listeners/buffers immediately, closes the context asynchronously, resets the clock and restores editor tile preview's explicit wall-clock mode. src/runtime/GameplayRuntime.ts: adds a readonly stable Play session identity with a serial increment only on a new beginSession; existing scene-transition/callback generation logic remains separate. src/runtime/GameplayRuntime.ts   Retains media core and merges final14 callback-delivery and configured-pool exhaustion hunks; initializes/advances/disposes shared caption time with media time. UI owner separately adds stable active Play-session identity for preview ownership; see Animation UI ledger. Observed integration gap and consequences: runtime Skip existed only behind editor transport; ordinary game buttons had no explicit timeline route. src/runtime/timelineUiActions.ts adds four opt-in @timeline:play/pause/skip/resume actions, bounded nearest-owner hierarchy resolution, disabled/missing/cycle diagnostics and existing evaluator dispatch. Hierarchy ownership avoids embedded entity IDs that break prefab copies; legacy callback names retain their path. src/runtime/GameplayRuntime.ts dispatches these actions for active live UI entities without requiring Script2D and stops reserved actions from duplicate script/event fallthrough. src/components/TimelineButtonAction.vue supplies a readable action selector and resolved owner, preserves the previous custom callback, and relies on the parent Inspector change transaction. src/components/RuntimeComponentsInspector.vue adds that component before the existing callback field, retaining final15 directional controls. src/editor/timelineUiActionCopy.ts supplies matching EN/DE/ZH actions and scope. No serialized field or public format changes; onPressed holds the explicit opt-in value. Behavioral/export/user checks follow before qualification.
- `src/runtime/animation.ts` — src/runtime/animation.ts   Tracks underlying numeric and sprite poses independently of last animation output; restores external changes before evaluation and prunes inactive ownership. Additive/partial layers no longer accumulate each frame; unit scale/opacity is neutral for additive sampling. Samples explicit loop/clamp time and half-open sprite frames, preserving direct-play final frames. Adds timeline asset sampling with normalized joint overlap weights, envelopes, optional additive contributions and restoration during repeated paused seeks. Missing tracks in crossfades/blend trees use the underlying pose. Adds unwrapped integer phase clocks so looping exit-time conditions can become true, separate previous-state clocks and complete clip/state/parameter speed factors, normalized synchronized-layer timing without a second advance, zero-weight layer clocks, actual active-transition interruption policy, and trigger consumption after every layer evaluates. Step keys select the new interior key at its boundary; Auto tangents use adjacent-key slopes; nonfinite sample time returns null; nonlinear easing keys are not removed as collinear. Adds explicit root-motion delta extraction/application across forward/reverse loops. Slicing retains evaluated endpoints and curved interiors through bounded adaptive baking; retiming maps numeric tracks, sprites, commands/events/markers; finite inputs are required. Root preview uses bounded streaming accumulation; importer resampling refuses oversized expansion before asset/settings mutation. Media document caches are bounded and invalidated by generation. Runtime recording retains frame zero, samples every integer recording frame, interpolates between actual evaluated poses, reports the real serialized key capacity, and retains the draft until persistence succeeds. src/runtime/animation.ts: authorized direct clip inspect/seek/stop methods sample through the existing evaluator, preserving Play/Pause ownership and suppressing crossed seek callbacks. Adds a detached recording snapshot reader shared by the existing persistence method, plus scoped reset observers so a global Stop can transfer final samples before runtime disposal. Existing recording persistence and cancellation remain available. Before timing correction: the new partition audit found direct clip playback still accumulated floating seconds, giving 1.0000000000000013 versus 0.9999999999999999 after equivalent60/30Hz updates. src/runtime/animation.ts moves only this private transport onto the existing integer MediaClock, including seeks, with unchanged serialized formats, duration caps and dispatch policy. Seek sampling is quantized at the common clock resolution; crossed events and preview ownership must be rechecked. scripts/verify-v26.16-media-performance.mjs adds12 measured animation/timeline/UI/rig workloads, equivalent-partition assertions,100 evaluator reset cycles and the20000-entity layout refusal. Timings are observational host CPU samples, not portable frame or hardware guarantees.
- `src/runtime/audio.ts` — src/runtime/audio.ts   Adds explicit completed state and separates manual pause/resume/restart/stop; ended or trim-ended non-loop clips do not autoplay again. Starting offsets are bounded. Playlist advancement recognizes trim completion and respects Single mode; completed polyphonic tails are released. Buffered imports use the new decoded transport; streaming keeps media elements. Adds a shared transport input, source/polyphonic/UI integer-sample clocks, drift correction, and independent keyed timeline voices without changing AudioSource asset/offset fields. Timeline clip rate is authoritative and is not silently altered by entity random pitch. Global time scale affects all playback; paused single steps do not schedule timeline sound; global pause suspends the actual AudioContext and freezes effect tails. Unsupported media-element rates pause with a retained diagnostic instead of silently clamping. UI clips route through actual buses/effects/master/panning and obey voice admission and disposal. Idle/paused/completed allocations no longer count as sounding voices; the obsolete allocation-count helper is removed; manual and polyphonic starts check admission. Bus sends are validated after all parent edges exist; ancestor/descendant solo routing is preserved; fallback gains include parents, snapshot/mute/solo/automation; ducking uses audible hierarchical bus counts, composes simultaneous reductions, and retains automation. Automation reads transport seconds and only repeats for an explicit loop duration. Reverb impulses are deterministic. Adds full session disposal, context/cache teardown on sample-rate replacement, removable device-change callback, voice-clock cleanup, audible diagnostic counts and repeated-failure deduplication. src/runtime/audio.ts: export-only change for AudioRuntime; the audition and game use the same implementation with independently owned contexts/voices.
- `src/runtime/audioBuffers.ts` — src/runtime/audioBuffers.ts (new)   Adds the real decoded AudioBuffer transport and an explicit playback interface. Decodes source bytes once per cache entry; deduplicates concurrent acquisitions; reserves pending leases before cache admission so promised buffers cannot be evicted during asynchronous handoff; leases shared buffers; bounds resident bytes, per-clip bytes and concurrent decoding; rejects stale decode completion after disposal. Supports sample-addressed seeking, rate changes, signed rates through a charged reversed buffer, native buffer loop/trim boundaries, pause/resume, ended dispatch and immediate source disconnection. Cancels stale scheduled-node callbacks and frees buffer leases/listeners on disposal. Awaits an existing pending decode when another play request joins it.
- `src/runtime/controlRegistry.ts` — Read accessible text from SVG controls without assuming HTMLElement.innerText; actual keyboard selection of curve keys no longer throws.
- `src/runtime/gameUi.ts` — src/runtime/gameUi.ts   Exported instance class for read-only preview; integrates shared solver/text, theme lookup cache and visible layout diagnostics. Nonmutating preview rendering, safe clipping/hit/focus scope, modal navigation, positive/zero/negative tab-index semantics, authored screen-reader reading order, passive ProgressBar range metadata, freeform state descriptions, rounded-mask corner hit tests, changed-only focus/value callbacks, checkbox release/cancel, repeat-key guard, slider callbacks/RTL pointer mapping, native text focus/commit/placeholder/style/font fallback, live accessibility values including passive/disabled nodes and password masking, isolated image tint, unclamped glyph width for input drawing and explicit cleanup. Authored Text typography matches shared measurements.
- `src/runtime/input.ts` — src/runtime/input.ts   Generic gameplay InputManager now ignores already-consumed keys, IME and editor/native interactive controls; focus transfer clears held keyboard/modifier state and attach/stop owns the focus listener. Public action-snapshot regressions cover all paths.
- `src/runtime/localization.ts` — src/runtime/localization.ts   Canonical locale validation and duplicate ownership refusal; atomic table writes and metadata updates; bounded generation-aware table cache; recursive fallback including regional language/source/project chain, per-Canvas locale, full-chain font fallback and build selection. Literal pseudo-localization and legacy whole-label {key} lookup. Strict multiline CSV/PO import with quote/variant preservation and refusal of malformed/indexed-gettext imports. Live Map and typed Rhai key extraction avoids comments and reports incomplete bounded coverage.
- `src/runtime/mediaClock.ts` — src/runtime/mediaClock.ts (new)   Defines 14,112,000 integer ticks/second, residual sub-tick accumulation, finite and safe-integer validation, explicit seek/advance, frame and integer-sample conversion/snapshots. Supports exact common 24/30/60/120/240-frame partitions and 44.1/48/96 kHz sample counts.
- `src/runtime/novaPak.ts` — src/runtime/novaPak.ts   Uses identical locale closure when filtering optional table assets; authoritative final15 selected-assets policy remains authoritative.
- `src/runtime/presentation.ts` — src/runtime/presentation.ts   Caption time follows explicit simulation media seconds; half-open expiry, paused stability, monotonic IDs and clear-on-disposal. Existing category/accessibility toggles preserved.
- `src/runtime/projectTransactions.ts` — Treat local browser recovery as optional and bounded by UTF-16 bytes; preserve external Save under quota/cleanup failures, log unavailable recovery, retain actual sink errors/dirty state and previous atomic last-good values. Browser quota is not misreported as destination disk capacity.
- `src/runtime/rigging.ts` — src/runtime/rigging.ts   Topologically orders valid reordered parents, rejects parent cycles, exports evaluated BoneWorld data and bounds estimated pose work. Preserves whole valid triangle triplets; corrects fallback indices for a three-vertex skin. Applies hard rotation/position limits during IK and final limits after copy constraints; zero-weight constraints/IK remain inert; copy rotation uses weighted shortest-angle deltas. Uses indexed bone lookup during IK. Equivalent bare/URI rig identities compare by resolved UUID. Mirrored bind-scale sign is retained in skin deformation. Retargeting applies source-rest displacement/rotation/scale ratios to the target rest pose. Auto-weighting rejects excessive raw vertices before mutation. Rig/skin caches discard previous generations and have fixed entry bounds.
- `src/runtime/timeline.ts` — src/runtime/timeline.ts   Adds per-owner integer clocks and authored-frame quantization while retaining precise audio local time, explicit seek and external playhead detection, pause-preserving skip/resume, loop/end handling and half-open subtitle/audio spans. Command traversal is separate from final-pose sampling: every crossed nested interval is visited, local offsets/rates compose, reverse ordering is retained, repeated nested instances have unique voice keys, and branches consume remaining update time from their marker. Actual Animation asset samples and independent audio descriptors are reconciled together. Existing no-asset animation state callbacks remain. Visibility/camera state derives from the underlying scene on every seek/evaluation; hiding the owner cannot stop its own clock; reset restores flags. Bounded traversal rejects cyclic/deep/oversized command work, restores the affected playhead and removes partially collected commands. Retains bounded asset/rate/traversal diagnostics for presentation. Timeline documents and caches have aggregate/generation bounds. Caption localization integration: src/runtime/timeline.ts now resolves explicit {key} caption bindings through clip locale, owner Canvas preview locale, then project preview locale. Literal caption text and payload text retain their path; current resolved text/locale reach both editor and exported WorldCanvas. src/assets/localizationDependencies.ts includes timeline clip locales and their normal fallback/font closure in packages, with a64MiB source-text discovery budget and explicit refusal. scripts/verify-v26.16-timeline-actions.mjs tests real play/pause/skip/resume, nearest ownership, disabled/stale/cyclic/missing cases, unskippable clips, localized captions and dependency inclusion. The same public formats and onPressed field are retained.
- `src/runtime/timelineUiActions.ts` — Parse four explicit opt-in Button actions; resolve the nearest live enabled TimelinePlayer through bounded ancestry; preserve legacy callback routing and apply real play/pause/skip/resume semantics.
- `src/runtime/uiImageTint.ts` — src/runtime/uiImageTint.ts (new)  16MiB LRU tint-source surface cache, source/region/revision/color invalidation, isolated multiply/mask compositing, bounded allocation refusal and explicit reset. Nine-slice consumes the same tinted source region.
- `src/runtime/uiLayout.ts` — src/runtime/uiLayout.ts (new)   Shared pure geometry; independent stretch axes, presets/custom anchors, fixed/responsive modes, offsets, min/max/aspect, actual Content measurement width, preferred/Fill sizes, Row/Column/Grid/Flow/Split/Center/Aspect/Overlay/Margin containers, wrapping/alignment/justification, scrolling/clips, ancestor visibility, safe areas, locale/RTL and explicit RTL preview. Indexed bounded hierarchy/cycle checks and lexicographic canvas/layer/z/insertion ordering.
- `src/runtime/uiNativeInput.ts` — src/runtime/uiNativeInput.ts (new)   Native composition/commit/selection ownership, Unicode-safe bounded flush and short IME Enter ownership; editor input/button/link/role focus isolation.
- `src/runtime/uiProduction.ts` — src/runtime/uiProduction.ts   Runtime-identical responsive rectangles and content sizing; actual/estimated font clipping and text limits, target sizes after scaling, hierarchy/ancestor/clipping findings, reusable component metadata fallback, theme inheritance findings, explicit negative-inset and native/canvas text-limit feedback. Reviewing the real preview exposed misleading validation: centered reference-sized controls were told to choose Fixed mode even though they correctly scale with their Canvas; that change would disable scaling. src/runtime/uiProduction.ts now diagnoses the actual ignored-input case (Fixed mode with responsive breakpoints) and accepts centered responsive controls. scripts/verify-v26.16-interface.mjs adds the actual half-size geometry/diagnostic comparison, raising the suite to55 groups and current total to219. No layout evaluator or project value is changed by this diagnostic correction.
- `src/runtime/uiTextLayout.ts` — src/runtime/uiTextLayout.ts (new)   Measured Unicode word/character wrapping, multiline clipping/ellipsis, grapheme segmentation, explicit canvas text limit. Separate truncateUtf16 preserves the authored native-input limit without splitting surrogate pairs.
- `src/runtime/uiTextPresentation.ts` — src/runtime/uiTextPresentation.ts (new)   Shared actual font/locale/outline/size/direction/measurement/drawing; locale and asset fallback families; no fillText maxWidth compression for authored Text. Estimated metrics are distinguished when Canvas2D is unavailable.
- `src/runtime/uiTheme.ts` — src/runtime/uiTheme.ts   Explicit child-before-default inheritance merging, grouped/chained token resolution and cycle limits, numeric/style validation, parent reference validation before save; normalizeUiThemeSource/readUiThemeSource separate sparse authored documents from resolved runtime values. Child saves keep unspecified inherited values absent.
- `src/store/physics.ts` — src/store/physics.ts   Unicode-safe TextInput hydration at authored and maxLength boundaries; extended component envelope enabled/removed metadata applied after data paste so stale data.enabled cannot re-enable a removed component.
- `tests/fixtures/migrations/public-schema-expected.json` — Set only26.16/26.16.0 engine/release/migration authority; retain dependency pins and public schemas.

## Generated outputs and reproducibility

WASM, Vite dist, Rust/Tauri targets, temporary check trees, screenshots, raw reports, actual project/Web downloads and qualification directories are generated outputs outside this authored manifest. Five reference directories and four updated manuals are authored deliverables above. Evidence bundles embed bounded actual captures/downloads and hashes; the eleven release files are published only after exact versions, source/build/gate identity and all payload checksums agree.
